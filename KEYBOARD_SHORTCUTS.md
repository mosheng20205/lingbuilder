# LingBuilder 快捷键使用文档

本文档记录 LingBuilder 当前约定的常用快捷键。新增编辑器快捷键时，应同步更新本文档，并确认不会破坏中文新手模式、专业 Monaco 编辑器和原生预览之间的基础操作习惯。

## 新手模式：结构代码导航

这些快捷键用于新手模式的事件代码和功能代码编辑框，主要解决长 `如果 / 否则 / 如果结束` 结构中反复滚动的问题。

| 快捷键 | 作用 |
| --- | --- |
| `Alt+↑` | 跳到当前分支起点。光标在 `如果` 真分支中时跳到所属 `如果 (条件)`；光标在 `否则 / 否则如果` 分支中时跳到当前 `否则 / 否则如果`。 |
| `Alt+↓` | 跳到下一个结构边界。优先跳到下一个 `否则 / 否则如果`；没有下一个分支时跳到 `如果结束`。 |
| `Ctrl+Shift+\` | 在当前 `如果` 结构的关键点之间循环：`如果` -> `否则 / 否则如果` -> `如果结束` -> `如果`。 |
| `Alt+Home` | 跳到当前嵌套结构的最外层 `如果`。 |
| `Alt+End` | 跳到当前嵌套结构的结束行，通常是最近的 `如果结束`。 |

跳转后，新手代码编辑器会短暂高亮目标行号，帮助用户确认跳转位置。

## 新手模式：代码输入

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl+Space` | 打开新手代码补全。 |
| `Tab` / `Enter` | 在补全弹层打开时接受当前候选；没有补全弹层时，`Tab` 插入缩进。 |
| `↑` / `↓` | 在补全弹层中切换候选。 |
| `Esc` | 关闭补全弹层。 |
| `Ctrl+Enter` | 结束当前代码块编辑，触发回写。 |

## 编辑器字体

| 快捷键或入口 | 作用 |
| --- | --- |
| `Ctrl+鼠标滚轮↑` | 放大当前代码编辑器字体。 |
| `Ctrl+鼠标滚轮↓` | 缩小当前代码编辑器字体。 |
| `文件 -> 设置编辑器字体...` | 输入具体字号，实时应用到专业 Monaco 和新手代码编辑器。 |

## 维护规则

- 新增快捷键前，优先检查是否和系统、浏览器、Monaco 常用快捷键冲突。
- 新手模式快捷键应优先服务结构阅读和直接输入，不引入确认/取消式流程。
- 专业模式快捷键应尽量复用 Monaco 或 VS Code 用户熟悉的习惯。
- 涉及中文代码结构的快捷键，应优先基于 `.lcpp` AST、结构化行或同等可测试的结构分析，不依赖不可控 DOM hack。
