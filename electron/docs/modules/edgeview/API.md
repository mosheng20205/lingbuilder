<!-- 此文件由 electron/scripts/generate-edgeview-api-doc.ts 生成，请修改 edgeViewApiCatalog.ts 或 builtinModules.ts 后运行 npm run module:edgeview-api-docs。 -->
# EdgeView 浏览器模块完整 API 参考

EdgeView 模块版本 **1.5.0**，当前公开 **289 条中文命令**：237 条安全 API、52 条基础与实例命令（含独立顶层弹窗、弹窗独立代理、实例级会话/Cookie/UA、实例枚举与生命周期）。本文由模块清单和 binding 自动生成，命令签名、参数类型、返回类型和说明不得在本文件中手工维护。

## 使用约定

- 设计器控件参数必须使用裸 `controlRef`，例如 `EdgeView_导航控件(演示浏览器, "https://example.com")`；不要给控件名加引号。
- 所有处理器参数必须使用 `&处理器名`，例如 `EdgeView_绑定控件事件(演示浏览器, "导航完成", &导航完成)`。
- 异步命令返回任务 ID。完成处理器中使用 `EdgeView任务_取当前任务ID()`，再读取状态、结果或错误，最后调用 `EdgeView任务_释放(任务ID)`。
- 返回值为整数的命令通常以 1 表示成功、0 表示失败；失败时应结合任务错误、模块诊断或原始 HRESULT 排查。
- 标记为 Runtime 150（v2）的命令需要 WebView2 Runtime 150 或更高版本；Runtime 141 仅提供基础能力。
- 模块不向 `.lcpp` 暴露 COM、IStream、IUnknown、裸指针或内存地址；二进制结果通过文件路径、十六进制文本或受管句柄返回。

## 快速示例

```text
事件 _MainWindow_创建完毕()
    EdgeView_绑定控件事件(演示浏览器, "导航完成", &导航完成)
    EdgeView_导航控件(演示浏览器, "https://example.com")
结束

事件 导航完成()
    标题 = EdgeView导航_取标题(演示浏览器)
    调试输出(标题)
结束
```

资源请求拦截、响应正文读取、任务生命周期和多实例示例见 `examples/edgeview-response-demos/` 与 `examples/module-demos/lingbuilder.edgeview/`。

## 完整命令清单

| # | 功能族 | 命令 | 签名 | 返回类型 | Runtime | 参数 | 说明 |
|---:|---|---|---|---|---|---|---|
| 1 | 任务 | `EdgeView任务_取当前任务ID` | `EdgeView任务_取当前任务ID()` | 长整数型 | Runtime 141 起 | 无 | 取得当前 EdgeView 完成处理器正在消费的任务 ID。 |
| 2 | 任务 | `EdgeView任务_取状态` | `EdgeView任务_取状态(任务ID)` | 整数型 | Runtime 141 起 | **任务ID**：长整数型；EdgeView 异步命令返回的任务 ID；状态 0 等待、1 成功、2 失败、3 已取消、4 已超时，用 EdgeView任务_释放 回收。 | 取得任务状态：0等待、1成功、2失败、3已取消、4已超时。 |
| 3 | 任务 | `EdgeView任务_取结果` | `EdgeView任务_取结果(任务ID)` | 文本型（UTF-16） | Runtime 141 起 | **任务ID**：长整数型；EdgeView 异步命令返回的任务 ID；状态 0 等待、1 成功、2 失败、3 已取消、4 已超时，用 EdgeView任务_释放 回收。 | 取得任务 UTF-16 文本或 JSON 结果。 |
| 4 | 任务 | `EdgeView任务_取错误` | `EdgeView任务_取错误(任务ID)` | 文本型（UTF-16） | Runtime 141 起 | **任务ID**：长整数型；EdgeView 异步命令返回的任务 ID；状态 0 等待、1 成功、2 失败、3 已取消、4 已超时，用 EdgeView任务_释放 回收。 | 取得任务的中文错误信息和 HRESULT。 |
| 5 | 任务 | `EdgeView任务_取消` | `EdgeView任务_取消(任务ID)` | 整数型 | Runtime 141 起 | **任务ID**：长整数型；EdgeView 异步命令返回的任务 ID；状态 0 等待、1 成功、2 失败、3 已取消、4 已超时，用 EdgeView任务_释放 回收。 | 取消尚未完成的任务；迟到回调将被代际检查拒绝。 |
| 6 | 任务 | `EdgeView任务_释放` | `EdgeView任务_释放(任务ID)` | 整数型 | Runtime 141 起 | **任务ID**：长整数型；EdgeView 异步命令返回的任务 ID；状态 0 等待、1 成功、2 失败、3 已取消、4 已超时，用 EdgeView任务_释放 回收。 | 释放任务及结果；运行中的任务会先取消。 |
| 7 | 导航 | `EdgeView导航_HTML` | `EdgeView导航_HTML(控件名, HTML)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**HTML**：文本型（UTF-16）；要在控件中渲染的完整 HTML 文本。 | 让指定控件导航到内存 HTML。 |
| 8 | 导航 | `EdgeView导航_请求` | `EdgeView导航_请求(控件名, 地址, 方法, 请求头, 正文)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**地址**：文本型（UTF-16）；要导航到的完整 URL（含协议）。<br>**方法**：文本型（UTF-16）；HTTP 方法文本，如 "GET"、"POST"；空文本按 GET。<br>**请求头**：文本型（UTF-16）；附加请求头文本（"名称: 值" 对）；可传空。<br>**正文**：文本型（UTF-16）；请求正文文本，按 UTF-8 编码发送；可传空。 | 使用文本请求头和 UTF-8 正文导航，不暴露 IStream。 |
| 9 | 导航 | `EdgeView导航_停止` | `EdgeView导航_停止(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 停止指定控件当前导航。 |
| 10 | 导航 | `EdgeView导航_取地址` | `EdgeView导航_取地址(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得当前页面地址。 |
| 11 | 导航 | `EdgeView导航_取标题` | `EdgeView导航_取标题(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得当前文档标题。 |
| 12 | 导航 | `EdgeView导航_取状态JSON` | `EdgeView导航_取状态JSON(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得后退、前进、挂起、主 Frame、进程、用户数据和故障报告目录状态 JSON。 |
| 13 | 导航 | `EdgeView导航_取进程信息异步` | `EdgeView导航_取进程信息异步(控件名, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 异步取得进程 ID、类型和关联 Frame 数量 JSON；旧 Runtime 使用基础进程集合。 |
| 14 | 导航 | `EdgeView导航_挂起异步` | `EdgeView导航_挂起异步(控件名, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名 | 异步挂起 WebView，返回任务 ID。 |
| 15 | 导航 | `EdgeView导航_恢复` | `EdgeView导航_恢复(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 恢复已挂起的 WebView。 |
| 16 | 导航 | `EdgeView导航_设置虚拟主机` | `EdgeView导航_设置虚拟主机(控件名, 主机名, 目录, 访问模式)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**主机名**：文本型（UTF-16）；虚拟主机名，如 "app.local"；页面通过该主机名访问映射目录。<br>**目录**：文本型（UTF-16）；要映射的本地目录绝对路径。<br>**访问模式**：整数型；共享缓冲访问模式枚举：0 只读、1 读写。 | 把虚拟主机映射到明确目录。 |
| 17 | 导航 | `EdgeView导航_清除虚拟主机` | `EdgeView导航_清除虚拟主机(控件名, 主机名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**主机名**：文本型（UTF-16）；虚拟主机名，如 "app.local"；页面通过该主机名访问映射目录。 | 清除指定虚拟主机映射。 |
| 18 | 脚本 | `EdgeView脚本_文档预注入异步` | `EdgeView脚本_文档预注入异步(控件名, 脚本, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**脚本**：文本型（UTF-16）；要执行或注入的 JavaScript 源码文本。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 注册文档创建前执行脚本，任务结果为脚本 ID。 |
| 19 | 脚本 | `EdgeView脚本_移除文档预注入` | `EdgeView脚本_移除文档预注入(控件名, 脚本ID)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**脚本ID**：文本型（UTF-16）；文档预注入脚本的 ID（注册任务返回结果），用于移除脚本。 | 移除文档预注入脚本。 |
| 20 | 脚本 | `EdgeView脚本_执行异步` | `EdgeView脚本_执行异步(控件名, 脚本, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**脚本**：文本型（UTF-16）；要执行或注入的 JavaScript 源码文本。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 异步执行 JavaScript，任务结果为 JSON。 |
| 21 | 脚本 | `EdgeView脚本_执行详情异步` | `EdgeView脚本_执行详情异步(控件名, 脚本, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**脚本**：文本型（UTF-16）；要执行或注入的 JavaScript 源码文本。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 异步执行 JavaScript，返回成功、JSON、字符串结果和异常详情。 |
| 22 | 脚本 | `EdgeView脚本_发送字符串消息` | `EdgeView脚本_发送字符串消息(控件名, 消息)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**消息**：文本型（UTF-16）；要发送给网页的字符串消息内容。 | 向网页发送字符串消息。 |
| 23 | 脚本 | `EdgeView脚本_发送JSON消息` | `EdgeView脚本_发送JSON消息(控件名, JSON)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**JSON**：文本型（UTF-16）；要发送的 JSON 消息文本。 | 向网页发送 JSON 消息。 |
| 24 | 设置 | `EdgeView设置_置脚本执行` | `EdgeView设置_置脚本执行(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 脚本执行。 |
| 25 | 设置 | `EdgeView设置_取脚本执行` | `EdgeView设置_取脚本执行(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 脚本执行 状态。 |
| 26 | 设置 | `EdgeView设置_置网页消息` | `EdgeView设置_置网页消息(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 网页消息。 |
| 27 | 设置 | `EdgeView设置_取网页消息` | `EdgeView设置_取网页消息(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 网页消息 状态。 |
| 28 | 设置 | `EdgeView设置_置脚本对话框` | `EdgeView设置_置脚本对话框(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 脚本对话框。 |
| 29 | 设置 | `EdgeView设置_取脚本对话框` | `EdgeView设置_取脚本对话框(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 脚本对话框 状态。 |
| 30 | 设置 | `EdgeView设置_置状态栏` | `EdgeView设置_置状态栏(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 状态栏。 |
| 31 | 设置 | `EdgeView设置_取状态栏` | `EdgeView设置_取状态栏(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 状态栏 状态。 |
| 32 | 设置 | `EdgeView设置_置开发者工具` | `EdgeView设置_置开发者工具(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 开发者工具。 |
| 33 | 设置 | `EdgeView设置_取开发者工具` | `EdgeView设置_取开发者工具(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 开发者工具 状态。 |
| 34 | 设置 | `EdgeView设置_置右键菜单` | `EdgeView设置_置右键菜单(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 右键菜单。 |
| 35 | 设置 | `EdgeView设置_取右键菜单` | `EdgeView设置_取右键菜单(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 右键菜单 状态。 |
| 36 | 设置 | `EdgeView设置_置缩放控制` | `EdgeView设置_置缩放控制(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 缩放控制。 |
| 37 | 设置 | `EdgeView设置_取缩放控制` | `EdgeView设置_取缩放控制(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 缩放控制 状态。 |
| 38 | 设置 | `EdgeView设置_置内置错误页` | `EdgeView设置_置内置错误页(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 内置错误页。 |
| 39 | 设置 | `EdgeView设置_取内置错误页` | `EdgeView设置_取内置错误页(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 内置错误页 状态。 |
| 40 | 设置 | `EdgeView设置_置快捷键` | `EdgeView设置_置快捷键(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 快捷键。 |
| 41 | 设置 | `EdgeView设置_取快捷键` | `EdgeView设置_取快捷键(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 快捷键 状态。 |
| 42 | 设置 | `EdgeView设置_置密码自动保存` | `EdgeView设置_置密码自动保存(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 密码自动保存。 |
| 43 | 设置 | `EdgeView设置_取密码自动保存` | `EdgeView设置_取密码自动保存(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 密码自动保存 状态。 |
| 44 | 设置 | `EdgeView设置_置通用自动填充` | `EdgeView设置_置通用自动填充(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 通用自动填充。 |
| 45 | 设置 | `EdgeView设置_取通用自动填充` | `EdgeView设置_取通用自动填充(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 通用自动填充 状态。 |
| 46 | 设置 | `EdgeView设置_置捏合缩放` | `EdgeView设置_置捏合缩放(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 捏合缩放。 |
| 47 | 设置 | `EdgeView设置_取捏合缩放` | `EdgeView设置_取捏合缩放(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 捏合缩放 状态。 |
| 48 | 设置 | `EdgeView设置_置滑动导航` | `EdgeView设置_置滑动导航(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 滑动导航。 |
| 49 | 设置 | `EdgeView设置_取滑动导航` | `EdgeView设置_取滑动导航(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 滑动导航 状态。 |
| 50 | 设置 | `EdgeView设置_置用户代理` | `EdgeView设置_置用户代理(控件名, 用户代理)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**用户代理**：文本型（UTF-16）；要使用的 User-Agent 字符串。 | 设置控件 User-Agent。 |
| 51 | 设置 | `EdgeView设置_取用户代理` | `EdgeView设置_取用户代理(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得控件 User-Agent。 |
| 52 | 设置 | `EdgeView设置_置缩放` | `EdgeView设置_置缩放(控件名, 缩放倍数)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**缩放倍数**：双精度型；缩放倍数，1.0 表示 100%。 | 设置控制器缩放倍数。 |
| 53 | 设置 | `EdgeView设置_取缩放` | `EdgeView设置_取缩放(控件名)` | 双精度型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得控制器缩放倍数。 |
| 54 | 设置 | `EdgeView设置_置静音` | `EdgeView设置_置静音(控件名, 静音)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**静音**：逻辑型；真=静音。 | 设置网页静音状态。 |
| 55 | 设置 | `EdgeView设置_取静音` | `EdgeView设置_取静音(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得网页静音状态。 |
| 56 | 设置 | `EdgeView设置_置背景色` | `EdgeView设置_置背景色(控件名, ARGB)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**ARGB**：整数型；32 位背景色，0xAARRGGBB 布局。 | 设置控制器默认背景色。 |
| 57 | 设置 | `EdgeView设置_置可见` | `EdgeView设置_置可见(控件名, 可见)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**可见**：逻辑型；真=显示，假=隐藏。 | 设置控制器可见性。 |
| 58 | 设置 | `EdgeView设置_取可见` | `EdgeView设置_取可见(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得控制器可见性。 |
| 59 | 设置 | `EdgeView设置_置边界` | `EdgeView设置_置边界(控件名, 左, 顶, 宽, 高)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**左**：整数型；边界左上角 X 坐标（DIP）。<br>**顶**：整数型；边界左上角 Y 坐标（DIP）。<br>**宽**：整数型；边界宽度（DIP）。<br>**高**：整数型；边界高度（DIP）。 | 设置控制器边界。 |
| 60 | 设置 | `EdgeView设置_取边界JSON` | `EdgeView设置_取边界JSON(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得控制器边界 JSON。 |
| 61 | 设置 | `EdgeView设置_移动焦点` | `EdgeView设置_移动焦点(控件名, 原因)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**原因**：整数型；移动焦点原因枚举：0 程序、1 上一个、2 下一个。 | 按 WebView2 原因枚举移动焦点。 |
| 62 | 设置 | `EdgeView设置_置光栅化缩放` | `EdgeView设置_置光栅化缩放(控件名, 缩放)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**缩放**：双精度型；光栅化缩放因子。 | 设置控制器光栅化缩放。 |
| 63 | 设置 | `EdgeView设置_取光栅化缩放` | `EdgeView设置_取光栅化缩放(控件名)` | 双精度型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得控制器光栅化缩放。 |
| 64 | 设置 | `EdgeView设置_置自动检测显示器缩放` | `EdgeView设置_置自动检测显示器缩放(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置是否自动检测显示器缩放。 |
| 65 | 设置 | `EdgeView设置_取自动检测显示器缩放` | `EdgeView设置_取自动检测显示器缩放(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得自动检测显示器缩放状态。 |
| 66 | 设置 | `EdgeView设置_置边界模式` | `EdgeView设置_置边界模式(控件名, 模式)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**模式**：整数型；边界模式枚举：0 使用物理大小、1 使用逻辑大小。 | 设置控制器边界模式。 |
| 67 | 设置 | `EdgeView设置_取边界模式` | `EdgeView设置_取边界模式(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得控制器边界模式。 |
| 68 | 设置 | `EdgeView设置_置允许外部拖放` | `EdgeView设置_置允许外部拖放(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置是否允许外部拖放。 |
| 69 | 设置 | `EdgeView设置_取允许外部拖放` | `EdgeView设置_取允许外部拖放(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得是否允许外部拖放。 |
| 70 | 设置 | `EdgeView设置_置PDF工具栏隐藏项` | `EdgeView设置_置PDF工具栏隐藏项(控件名, 掩码)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**掩码**：整数型；PDF 工具栏隐藏项位掩码（COREWEBVIEW2_PDF_TOOLBAR_ITEMS 组合）。 | 设置 PDF 工具栏隐藏项掩码。 |
| 71 | 设置 | `EdgeView设置_取PDF工具栏隐藏项` | `EdgeView设置_取PDF工具栏隐藏项(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 PDF 工具栏隐藏项掩码。 |
| 72 | 设置 | `EdgeView设置_置信誉检查` | `EdgeView设置_置信誉检查(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置是否要求 SmartScreen 信誉检查。 |
| 73 | 设置 | `EdgeView设置_取信誉检查` | `EdgeView设置_取信誉检查(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得信誉检查状态。 |
| 74 | 设置 | `EdgeView设置_置内存目标级别` | `EdgeView设置_置内存目标级别(控件名, 级别)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**级别**：整数型；内存使用目标级别枚举：0 正常、1 低。 | 设置 WebView 内存使用目标级别。 |
| 75 | 设置 | `EdgeView设置_取内存目标级别` | `EdgeView设置_取内存目标级别(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 WebView 内存使用目标级别。 |
| 76 | 创建选项 | `EdgeView创建选项_置独占用户目录` | `EdgeView创建选项_置独占用户目录(控件名, 启用)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置创建期独占 UDF；修改后必须重建控件。 |
| 77 | 创建选项 | `EdgeView创建选项_取独占用户目录` | `EdgeView创建选项_取独占用户目录(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得创建期独占 UDF 设置。 |
| 78 | 创建选项 | `EdgeView创建选项_置自定义崩溃报告` | `EdgeView创建选项_置自定义崩溃报告(控件名, 启用)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置创建期自定义崩溃报告开关。 |
| 79 | 创建选项 | `EdgeView创建选项_取自定义崩溃报告` | `EdgeView创建选项_取自定义崩溃报告(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得创建期自定义崩溃报告开关。 |
| 80 | 创建选项 | `EdgeView创建选项_置跟踪保护` | `EdgeView创建选项_置跟踪保护(控件名, 启用)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置创建期跟踪保护开关。 |
| 81 | 创建选项 | `EdgeView创建选项_取跟踪保护` | `EdgeView创建选项_取跟踪保护(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得创建期跟踪保护开关。 |
| 82 | 创建选项 | `EdgeView创建选项_置浏览器扩展` | `EdgeView创建选项_置浏览器扩展(控件名, 启用)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置创建期浏览器扩展开关。 |
| 83 | 创建选项 | `EdgeView创建选项_取浏览器扩展` | `EdgeView创建选项_取浏览器扩展(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得创建期浏览器扩展开关。 |
| 84 | 创建选项 | `EdgeView创建选项_置通道搜索方式` | `EdgeView创建选项_置通道搜索方式(控件名, 方式)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**方式**：整数型；Runtime 通道搜索方式枚举（COREWEBVIEW2_CHANNEL_SEARCH_KIND）。 | 设置创建期 Runtime 通道搜索方式。 |
| 85 | 创建选项 | `EdgeView创建选项_取通道搜索方式` | `EdgeView创建选项_取通道搜索方式(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得创建期 Runtime 通道搜索方式。 |
| 86 | 创建选项 | `EdgeView创建选项_置发布通道` | `EdgeView创建选项_置发布通道(控件名, 通道掩码)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**通道掩码**：整数型；允许的 Runtime 发布通道位掩码（COREWEBVIEW2_RELEASE_CHANNELS 组合）。 | 设置创建期允许的 Runtime 发布通道掩码。 |
| 87 | 创建选项 | `EdgeView创建选项_取发布通道` | `EdgeView创建选项_取发布通道(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得创建期 Runtime 发布通道掩码。 |
| 88 | 创建选项 | `EdgeView创建选项_置滚动条样式` | `EdgeView创建选项_置滚动条样式(控件名, 样式)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**样式**：整数型；滚动条样式枚举：0 默认、1 Fluent。 | 设置创建期滚动条样式。 |
| 89 | 创建选项 | `EdgeView创建选项_取滚动条样式` | `EdgeView创建选项_取滚动条样式(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得创建期滚动条样式。 |
| 90 | 创建选项 | `EdgeView创建选项_置脚本区域` | `EdgeView创建选项_置脚本区域(控件名, 区域)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**区域**：文本型（UTF-16）；脚本语言区域标签（如 "zh-CN"）；修改后必须重建。 | 设置创建期脚本区域；修改后必须重建。 |
| 91 | 创建选项 | `EdgeView创建选项_取脚本区域` | `EdgeView创建选项_取脚本区域(控件名)` | 文本型（UTF-16） | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得创建期脚本区域。 |
| 92 | 创建选项 | `EdgeView创建选项_置默认背景色` | `EdgeView创建选项_置默认背景色(控件名, ARGB)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**ARGB**：整数型；32 位背景色，0xAARRGGBB 布局。 | 设置控制器创建期默认背景色。 |
| 93 | 创建选项 | `EdgeView创建选项_取默认背景色` | `EdgeView创建选项_取默认背景色(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得控制器创建期默认背景色。 |
| 94 | 创建选项 | `EdgeView创建选项_置宿主输入处理` | `EdgeView创建选项_置宿主输入处理(控件名, 启用)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置控制器创建期宿主输入处理开关。 |
| 95 | 创建选项 | `EdgeView创建选项_取宿主输入处理` | `EdgeView创建选项_取宿主输入处理(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得控制器创建期宿主输入处理开关。 |
| 96 | 创建选项 | `EdgeView创建选项_添加自定义协议` | `EdgeView创建选项_添加自定义协议(控件名, 协议名, 含权限部分, 视为安全, 允许来源)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**协议名**：文本型（UTF-16）；自定义协议名（不含 ://），如 "app"。<br>**含权限部分**：逻辑型；真=协议 URL 含权限（主机）部分。<br>**视为安全**：逻辑型；真=该协议视为安全上下文。<br>**允许来源**：文本型（UTF-16）；允许调用该协议的来源列表，分号分隔；传 * 不限制。 | 添加安全的自定义协议注册；允许来源以分号分隔，修改后必须重建。 |
| 97 | 创建选项 | `EdgeView创建选项_清除自定义协议` | `EdgeView创建选项_清除自定义协议(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 清除当前控件的创建期自定义协议注册。 |
| 98 | 创建选项 | `EdgeView创建选项_重建控件` | `EdgeView创建选项_重建控件(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 应用创建期选项并显式重建指定 Edge 控件。 |
| 99 | 会话 | `EdgeView会话_取ProfileJSON` | `EdgeView会话_取ProfileJSON(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 Profile 名称、路径、隐私模式和下载目录 JSON。 |
| 100 | 会话 | `EdgeView会话_取Cookie异步` | `EdgeView会话_取Cookie异步(控件名, 地址, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**地址**：文本型（UTF-16）；要导航到的完整 URL（含协议）。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 异步取得 Cookie 列表 JSON。 |
| 101 | 会话 | `EdgeView会话_置Cookie` | `EdgeView会话_置Cookie(控件名, 名称, 值, 域, 路径)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**名称**：文本型（UTF-16）；Cookie 名称。<br>**值**：文本型（UTF-16）；Cookie 内容文本。<br>**域**：文本型（UTF-16）；Cookie 归属域，如 "example.com"。<br>**路径**：文本型（UTF-16）；Cookie 生效路径，如 "/"。 | 创建或更新 Cookie。 |
| 102 | 会话 | `EdgeView会话_删除Cookie` | `EdgeView会话_删除Cookie(控件名, 名称, 域, 路径)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**名称**：文本型（UTF-16）；Cookie 名称。<br>**域**：文本型（UTF-16）；Cookie 归属域，如 "example.com"。<br>**路径**：文本型（UTF-16）；Cookie 生效路径，如 "/"。 | 按名称、域和路径删除 Cookie。 |
| 103 | 会话 | `EdgeView会话_删除全部Cookie` | `EdgeView会话_删除全部Cookie(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 删除当前 Profile 全部 Cookie。 |
| 104 | 会话 | `EdgeView会话_置Cookie带属性` | `EdgeView会话_置Cookie带属性(控件名, 名称, 值, 域, 路径, 过期时间, 安全, 仅HTTP, 同源策略)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**名称**：文本型（UTF-16）；Cookie 名称。<br>**值**：文本型（UTF-16）；Cookie 内容文本。<br>**域**：文本型（UTF-16）；Cookie 归属域，如 "example.com"。<br>**路径**：文本型（UTF-16）；Cookie 生效路径，如 "/"。<br>**过期时间**：双精度型；Cookie 过期的 UTC 秒数（双精度）；传 -1 为会话 Cookie；过去时间会被 WebView2 拒绝写入。<br>**安全**：逻辑型；真=Secure Cookie，仅通过 HTTPS 发送。<br>**仅HTTP**：逻辑型；真=HttpOnly，页面 JavaScript 不可见。<br>**同源策略**：整数型；SameSite 枚举（COREWEBVIEW2_COOKIE_SAME_SITE_KIND）：0 None、1 Lax、2 Strict；None 必须同时启用安全。 | 创建或更新 Cookie，并设置过期时间、Secure、HttpOnly 和 SameSite；成功返回 1。 |
| 105 | 会话 | `EdgeView会话_批量置Cookie` | `EdgeView会话_批量置Cookie(控件名, Cookie列表JSON)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**Cookie列表JSON**：文本型（UTF-16）；Cookie JSON 数组文本，每项为含 name/value/domain/path 的对象，可选 expires（UTC 秒数）、secure、httpOnly、sameSite（0 None、1 Lax、2 Strict）；与 EdgeView会话_取Cookie异步 输出格式兼容。 | 按 Cookie JSON 数组批量注入 Cookie（支持 expires/secure/httpOnly/sameSite 元数据），返回成功注入条数；格式与 EdgeView会话_取Cookie异步 输出兼容。 |
| 106 | 会话 | `EdgeView会话_清理浏览数据异步` | `EdgeView会话_清理浏览数据异步(控件名, 数据类型掩码, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**数据类型掩码**：长整数型；浏览数据类型位掩码（COREWEBVIEW2_BROWSING_DATA_KINDS 组合）。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 按 WebView2 数据类型掩码清理浏览数据。 |
| 107 | 会话 | `EdgeView会话_清理全部浏览数据异步` | `EdgeView会话_清理全部浏览数据异步(控件名, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 清理当前 Profile 全部浏览数据。 |
| 108 | 会话 | `EdgeView会话_按时间清理浏览数据异步` | `EdgeView会话_按时间清理浏览数据异步(控件名, 数据类型掩码, 开始时间, 结束时间, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**数据类型掩码**：长整数型；浏览数据类型位掩码（COREWEBVIEW2_BROWSING_DATA_KINDS 组合）。<br>**开始时间**：双精度型；起始 Unix 秒时间。<br>**结束时间**：双精度型；结束 Unix 秒时间。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 按 Unix 秒时间范围清理浏览数据。 |
| 109 | 会话 | `EdgeView会话_置下载目录` | `EdgeView会话_置下载目录(控件名, 目录)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**目录**：文本型（UTF-16）；Profile 默认下载目录的绝对路径。 | 设置 Profile 默认下载目录。 |
| 110 | 会话 | `EdgeView会话_取下载目录` | `EdgeView会话_取下载目录(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 Profile 默认下载目录。 |
| 111 | 会话 | `EdgeView会话_置配色方案` | `EdgeView会话_置配色方案(控件名, 方案)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**方案**：整数型；配色方案枚举：0 自动、1 浅色、2 深色。 | 设置 Profile 首选配色方案。 |
| 112 | 会话 | `EdgeView会话_取配色方案` | `EdgeView会话_取配色方案(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 Profile 首选配色方案。 |
| 113 | 会话 | `EdgeView会话_置跟踪保护` | `EdgeView会话_置跟踪保护(控件名, 级别)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**级别**：整数型；内存使用目标级别枚举：0 正常、1 低。 | 设置 Profile 跟踪保护级别。 |
| 114 | 会话 | `EdgeView会话_取跟踪保护` | `EdgeView会话_取跟踪保护(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 Profile 跟踪保护级别。 |
| 115 | 会话 | `EdgeView会话_置密码保存` | `EdgeView会话_置密码保存(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 Profile 密码自动保存。 |
| 116 | 会话 | `EdgeView会话_取密码保存` | `EdgeView会话_取密码保存(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 Profile 密码自动保存状态。 |
| 117 | 会话 | `EdgeView会话_置自动填充` | `EdgeView会话_置自动填充(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 Profile 通用自动填充。 |
| 118 | 会话 | `EdgeView会话_取自动填充` | `EdgeView会话_取自动填充(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 Profile 通用自动填充状态。 |
| 119 | 会话 | `EdgeView会话_删除Profile` | `EdgeView会话_删除Profile(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 删除当前 Profile；删除完成通过事件清单中的对应完成事件通知。 |
| 120 | 下载 | `EdgeView下载_取状态JSON` | `EdgeView下载_取状态JSON(控件名, 下载ID)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**下载ID**：长整数型；“下载开始”事件字段 downloadId 携带的下载编号；未知编号时查询返回空 JSON、操作返回 0。 | 取得受管下载状态、地址、MIME、进度、可恢复性、中断原因、本次请求的 pendingResultFilePath 与 put_ResultFilePath 的 resultFilePathError JSON。在「下载开始」处理器内读到的 path 是决策前的默认落点快照，要确认改路径是否生效必须在事件结束后再读。 |
| 121 | 下载 | `EdgeView下载_暂停` | `EdgeView下载_暂停(控件名, 下载ID)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**下载ID**：长整数型；“下载开始”事件字段 downloadId 携带的下载编号；未知编号时查询返回空 JSON、操作返回 0。 | 暂停受管下载。 |
| 122 | 下载 | `EdgeView下载_恢复` | `EdgeView下载_恢复(控件名, 下载ID)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**下载ID**：长整数型；“下载开始”事件字段 downloadId 携带的下载编号；未知编号时查询返回空 JSON、操作返回 0。 | 恢复受管下载。 |
| 123 | 下载 | `EdgeView下载_取消` | `EdgeView下载_取消(控件名, 下载ID)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**下载ID**：长整数型；“下载开始”事件字段 downloadId 携带的下载编号；未知编号时查询返回空 JSON、操作返回 0。 | 取消受管下载。 |
| 124 | 下载 | `EdgeView下载_显示默认窗口` | `EdgeView下载_显示默认窗口(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 打开默认下载窗口。 |
| 125 | 下载 | `EdgeView下载_关闭默认窗口` | `EdgeView下载_关闭默认窗口(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 关闭默认下载窗口。 |
| 126 | 下载 | `EdgeView下载_置窗口角对齐` | `EdgeView下载_置窗口角对齐(控件名, 对齐)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**对齐**：整数型；下载窗口角对齐枚举：0 左上、1 右上、2 左下、3 右下。 | 设置默认下载窗口角对齐。 |
| 127 | 下载 | `EdgeView下载_取窗口角对齐` | `EdgeView下载_取窗口角对齐(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得默认下载窗口角对齐。 |
| 128 | 下载 | `EdgeView下载_置窗口边距` | `EdgeView下载_置窗口边距(控件名, 横向, 纵向)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**横向**：整数型；下载窗口水平边距（像素）。<br>**纵向**：整数型；下载窗口垂直边距（像素）。 | 设置默认下载窗口边距。 |
| 129 | 下载 | `EdgeView下载_取窗口边距JSON` | `EdgeView下载_取窗口边距JSON(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得默认下载窗口边距 JSON。 |
| 130 | 查找 | `EdgeView查找_开始异步` | `EdgeView查找_开始异步(控件名, 文本, 选项JSON, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**文本**：文本型（UTF-16）；要查找或设置的文本内容。<br>**选项JSON**：文本型（UTF-16）；查找选项 JSON 文本（WebView2 驼峰键，如 isCaseSensitive）；传空对象使用默认。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 开始页内查找，结果为匹配状态 JSON。 |
| 131 | 查找 | `EdgeView查找_下一项` | `EdgeView查找_下一项(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 移动到下一匹配项。 |
| 132 | 查找 | `EdgeView查找_上一项` | `EdgeView查找_上一项(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 移动到上一匹配项。 |
| 133 | 查找 | `EdgeView查找_停止` | `EdgeView查找_停止(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 停止页内查找并清除高亮。 |
| 134 | 查找 | `EdgeView查找_取状态JSON` | `EdgeView查找_取状态JSON(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得匹配数量和当前索引 JSON。 |
| 135 | 打印 | `EdgeView打印_置方向` | `EdgeView打印_置方向(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：整数型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印方向。 |
| 136 | 打印 | `EdgeView打印_取方向` | `EdgeView打印_取方向(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印方向。 |
| 137 | 打印 | `EdgeView打印_置每面页数` | `EdgeView打印_置每面页数(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：整数型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印每面页数。 |
| 138 | 打印 | `EdgeView打印_取每面页数` | `EdgeView打印_取每面页数(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印每面页数。 |
| 139 | 打印 | `EdgeView打印_置份数` | `EdgeView打印_置份数(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：整数型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印份数。 |
| 140 | 打印 | `EdgeView打印_取份数` | `EdgeView打印_取份数(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印份数。 |
| 141 | 打印 | `EdgeView打印_置逐份打印` | `EdgeView打印_置逐份打印(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：整数型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印逐份打印。 |
| 142 | 打印 | `EdgeView打印_取逐份打印` | `EdgeView打印_取逐份打印(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印逐份打印。 |
| 143 | 打印 | `EdgeView打印_置颜色模式` | `EdgeView打印_置颜色模式(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：整数型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印颜色模式。 |
| 144 | 打印 | `EdgeView打印_取颜色模式` | `EdgeView打印_取颜色模式(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印颜色模式。 |
| 145 | 打印 | `EdgeView打印_置双面模式` | `EdgeView打印_置双面模式(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：整数型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印双面模式。 |
| 146 | 打印 | `EdgeView打印_取双面模式` | `EdgeView打印_取双面模式(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印双面模式。 |
| 147 | 打印 | `EdgeView打印_置纸张类型` | `EdgeView打印_置纸张类型(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：整数型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印纸张类型。 |
| 148 | 打印 | `EdgeView打印_取纸张类型` | `EdgeView打印_取纸张类型(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印纸张类型。 |
| 149 | 打印 | `EdgeView打印_置缩放倍数` | `EdgeView打印_置缩放倍数(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：双精度型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印缩放倍数。 |
| 150 | 打印 | `EdgeView打印_取缩放倍数` | `EdgeView打印_取缩放倍数(控件名)` | 双精度型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印缩放倍数。 |
| 151 | 打印 | `EdgeView打印_置纸张宽度` | `EdgeView打印_置纸张宽度(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：双精度型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印纸张宽度。 |
| 152 | 打印 | `EdgeView打印_取纸张宽度` | `EdgeView打印_取纸张宽度(控件名)` | 双精度型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印纸张宽度。 |
| 153 | 打印 | `EdgeView打印_置纸张高度` | `EdgeView打印_置纸张高度(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：双精度型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印纸张高度。 |
| 154 | 打印 | `EdgeView打印_取纸张高度` | `EdgeView打印_取纸张高度(控件名)` | 双精度型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印纸张高度。 |
| 155 | 打印 | `EdgeView打印_置上边距` | `EdgeView打印_置上边距(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：双精度型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印上边距。 |
| 156 | 打印 | `EdgeView打印_取上边距` | `EdgeView打印_取上边距(控件名)` | 双精度型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印上边距。 |
| 157 | 打印 | `EdgeView打印_置下边距` | `EdgeView打印_置下边距(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：双精度型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印下边距。 |
| 158 | 打印 | `EdgeView打印_取下边距` | `EdgeView打印_取下边距(控件名)` | 双精度型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印下边距。 |
| 159 | 打印 | `EdgeView打印_置左边距` | `EdgeView打印_置左边距(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：双精度型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印左边距。 |
| 160 | 打印 | `EdgeView打印_取左边距` | `EdgeView打印_取左边距(控件名)` | 双精度型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印左边距。 |
| 161 | 打印 | `EdgeView打印_置右边距` | `EdgeView打印_置右边距(控件名, 值)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**值**：双精度型；要设置的属性值；取值由对应 WebView2 枚举或数值定义。 | 设置打印右边距。 |
| 162 | 打印 | `EdgeView打印_取右边距` | `EdgeView打印_取右边距(控件名)` | 双精度型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印右边距。 |
| 163 | 打印 | `EdgeView打印_置打印背景` | `EdgeView打印_置打印背景(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置是否打印背景。 |
| 164 | 打印 | `EdgeView打印_取打印背景` | `EdgeView打印_取打印背景(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得是否打印背景。 |
| 165 | 打印 | `EdgeView打印_置仅打印选区` | `EdgeView打印_置仅打印选区(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置是否仅打印选区。 |
| 166 | 打印 | `EdgeView打印_取仅打印选区` | `EdgeView打印_取仅打印选区(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得是否仅打印选区。 |
| 167 | 打印 | `EdgeView打印_置打印页眉页脚` | `EdgeView打印_置打印页眉页脚(控件名, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置是否打印页眉页脚。 |
| 168 | 打印 | `EdgeView打印_取打印页眉页脚` | `EdgeView打印_取打印页眉页脚(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得是否打印页眉页脚。 |
| 169 | 打印 | `EdgeView打印_置页眉标题` | `EdgeView打印_置页眉标题(控件名, 文本)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**文本**：文本型（UTF-16）；要查找或设置的文本内容。 | 设置打印页眉标题。 |
| 170 | 打印 | `EdgeView打印_取页眉标题` | `EdgeView打印_取页眉标题(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印页眉标题。 |
| 171 | 打印 | `EdgeView打印_置页脚地址` | `EdgeView打印_置页脚地址(控件名, 文本)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**文本**：文本型（UTF-16）；要查找或设置的文本内容。 | 设置打印页脚地址。 |
| 172 | 打印 | `EdgeView打印_取页脚地址` | `EdgeView打印_取页脚地址(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印页脚地址。 |
| 173 | 打印 | `EdgeView打印_置页面范围` | `EdgeView打印_置页面范围(控件名, 文本)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**文本**：文本型（UTF-16）；要查找或设置的文本内容。 | 设置打印页面范围。 |
| 174 | 打印 | `EdgeView打印_取页面范围` | `EdgeView打印_取页面范围(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印页面范围。 |
| 175 | 打印 | `EdgeView打印_置打印机名称` | `EdgeView打印_置打印机名称(控件名, 文本)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**文本**：文本型（UTF-16）；要查找或设置的文本内容。 | 设置打印打印机名称。 |
| 176 | 打印 | `EdgeView打印_取打印机名称` | `EdgeView打印_取打印机名称(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得打印打印机名称。 |
| 177 | 打印 | `EdgeView打印_显示界面` | `EdgeView打印_显示界面(控件名, 界面类型)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**界面类型**：整数型；打印界面类型枚举：0 浏览器、1 系统。 | 显示系统或浏览器打印界面。 |
| 178 | 打印 | `EdgeView打印_PDF异步` | `EdgeView打印_PDF异步(控件名, 文件路径, 设置JSON, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**文件路径**：文本型（UTF-16）；目标文件路径；相对路径按当前工作目录解析为绝对路径。<br>**设置JSON**：文本型（UTF-16）；打印设置 JSON 文本（WebView2 驼峰键覆盖打印设置）；传空对象不改设置。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 把当前页面导出为 PDF 文件；相对路径先按当前工作目录解析成绝对路径再交给 WebView2，设置JSON 用 WebView2 驼峰键名覆盖本控件的打印设置，空对象表示不改设置。 |
| 179 | 打印 | `EdgeView打印_PDF流到文件异步` | `EdgeView打印_PDF流到文件异步(控件名, 文件路径, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**文件路径**：文本型（UTF-16）；目标文件路径；相对路径按当前工作目录解析为绝对路径。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 使用 PrintToPdfStream 后把二进制结果写入明确文件路径。 |
| 180 | 打印 | `EdgeView打印_打印异步` | `EdgeView打印_打印异步(控件名, 设置JSON, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**设置JSON**：文本型（UTF-16）；打印设置 JSON 文本（WebView2 驼峰键覆盖打印设置）；传空对象不改设置。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 异步打印当前页面；设置JSON 用 WebView2 驼峰键名覆盖本控件的打印设置，空对象表示不改设置。 |
| 181 | 媒体 | `EdgeView媒体_截图异步` | `EdgeView媒体_截图异步(控件名, 文件路径, 格式, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**文件路径**：文本型（UTF-16）；目标文件路径；相对路径按当前工作目录解析为绝对路径。<br>**格式**：整数型；截图图像格式枚举：0 PNG、1 JPEG。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 把页面截图写入文件路径（相对路径按当前工作目录解析），任务结果返回落盘后的绝对路径，不暴露 IStream。 |
| 182 | 媒体 | `EdgeView媒体_取Favicon异步` | `EdgeView媒体_取Favicon异步(控件名, 文件路径, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**文件路径**：文本型（UTF-16）；目标文件路径；相对路径按当前工作目录解析为绝对路径。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 把页面 Favicon 写入文件路径（相对路径按当前工作目录解析），任务结果返回落盘后的绝对路径。 |
| 183 | 媒体 | `EdgeView媒体_取全屏状态` | `EdgeView媒体_取全屏状态(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得页面是否含全屏元素。 |
| 184 | 媒体 | `EdgeView媒体_取音频状态` | `EdgeView媒体_取音频状态(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得页面是否正在播放音频。 |
| 185 | 开发者工具 | `EdgeView开发者工具_打开` | `EdgeView开发者工具_打开(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 打开开发者工具窗口。 |
| 186 | 开发者工具 | `EdgeView开发者工具_打开任务管理器` | `EdgeView开发者工具_打开任务管理器(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 打开 WebView2 任务管理器。 |
| 187 | 开发者工具 | `EdgeView开发者工具_调用异步` | `EdgeView开发者工具_调用异步(控件名, 方法, 参数JSON, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**方法**：文本型（UTF-16）；HTTP 方法文本，如 "GET"、"POST"；空文本按 GET。<br>**参数JSON**：文本型（UTF-16）；方法参数 JSON 对象文本；无参数传 "{}"。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 调用 DevTools Protocol 方法，结果为 JSON。 |
| 188 | 开发者工具 | `EdgeView开发者工具_调用会话异步` | `EdgeView开发者工具_调用会话异步(控件名, 会话ID, 方法, 参数JSON, 完成处理器)` | 长整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**会话ID**：文本型（UTF-16）；DevTools 会话 ID，取自协议事件字段 sessionId。<br>**方法**：文本型（UTF-16）；HTTP 方法文本，如 "GET"、"POST"；空文本按 GET。<br>**参数JSON**：文本型（UTF-16）；方法参数 JSON 对象文本；无参数传 "{}"。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 在指定 DevTools 会话调用协议方法。 |
| 189 | 资源 | `EdgeView资源_添加过滤器` | `EdgeView资源_添加过滤器(控件名, URI模式, 上下文, 来源类型)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**URI模式**：文本型（UTF-16）；资源 URI 通配符匹配文本，如 "https://example.com/*"。<br>**上下文**：整数型；资源请求上下文位掩码（WEB_RESOURCE_CONTEXT_* 组合）。<br>**来源类型**：整数型；请求来源类型位掩码（WEB_RESOURCE_REQUEST_SOURCE_* 组合）。 | 添加带请求来源类型的资源过滤器。 |
| 190 | 资源 | `EdgeView资源_移除过滤器` | `EdgeView资源_移除过滤器(控件名, URI模式, 上下文)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**URI模式**：文本型（UTF-16）；资源 URI 通配符匹配文本，如 "https://example.com/*"。<br>**上下文**：整数型；资源请求上下文位掩码（WEB_RESOURCE_CONTEXT_* 组合）。 | 移除资源过滤器。 |
| 191 | 资源 | `EdgeView资源_移除来源过滤器` | `EdgeView资源_移除来源过滤器(控件名, URI模式, 上下文, 来源类型)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**URI模式**：文本型（UTF-16）；资源 URI 通配符匹配文本，如 "https://example.com/*"。<br>**上下文**：整数型；资源请求上下文位掩码（WEB_RESOURCE_CONTEXT_* 组合）。<br>**来源类型**：整数型；请求来源类型位掩码（WEB_RESOURCE_REQUEST_SOURCE_* 组合）。 | 移除带请求来源类型的资源过滤器。 |
| 192 | 资源 | `EdgeView资源_设置事件响应文本` | `EdgeView资源_设置事件响应文本(控件名, 状态码, 原因, 响应头, 正文)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**状态码**：整数型；HTTP 状态码 100~599；仅在同步事件处理期有效。<br>**原因**：文本型（UTF-16）；移动焦点原因枚举：0 程序、1 上一个、2 下一个。<br>**响应头**：文本型（UTF-16）；响应头文本（"名称: 值" 对）；可传空。<br>**正文**：文本型（UTF-16）；请求正文文本，按 UTF-8 编码发送；可传空。 | 在 Web资源请求同步事件中以限长 UTF-8 正文替换响应。 |
| 193 | 资源 | `EdgeView资源_读响应正文异步` | `EdgeView资源_读响应正文异步(控件名, 响应句柄, 最大字节数, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**响应句柄**：受管句柄；“Web资源响应收到”事件字段 responseHandle 携带的响应句柄；仅事件处理期内可读正文。<br>**最大字节数**：长整数型；单次读取的最大字节数，超出部分截断。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 把响应流限长读取为十六进制 JSON，不暴露 IStream；必须在 Web资源响应收到 处理器执行期间交给本命令，处理器返回后 WebView2 会释放该响应的正文，稍后读取一律失败。 |
| 194 | 事件 | `EdgeView事件_取字段` | `EdgeView事件_取字段(控件名, 字段名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**字段名**：文本型（UTF-16）；当前同步事件的字段名，取值见事件说明。 | 取得当前同步事件字段；右键菜单目标等复合参数会预先转换为字段。 |
| 195 | 事件 | `EdgeView事件_设置动作` | `EdgeView事件_设置动作(控件名, 动作)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**动作**：整数型；同步事件动作码；1 表示按事件语义取消默认行为，未设置时保持 WebView2 默认。 | 设置当前同步事件动作；未设置时保持 WebView2 默认行为。 |
| 196 | 事件 | `EdgeView事件_设置返回文本` | `EdgeView事件_设置返回文本(控件名, 文本)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**文本**：文本型（UTF-16）；要查找或设置的文本内容。 | 设置脚本对话框、认证或下载等事件返回文本。 |
| 197 | 事件 | `EdgeView事件_设置认证` | `EdgeView事件_设置认证(控件名, 用户名, 密码)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**用户名**：文本型（UTF-16）；基本身份验证用户名。<br>**密码**：文本型（UTF-16）；基本身份验证密码。 | 设置当前基本身份验证请求凭据。 |
| 198 | 事件 | `EdgeView事件_设置下载路径` | `EdgeView事件_设置下载路径(控件名, 文件路径)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**文件路径**：文本型（UTF-16）；目标文件路径；相对路径按当前工作目录解析为绝对路径。 | 设置当前下载开始事件的目标文件路径。只能在「下载开始」同步处理器执行期间调用；相对路径会按当前工作目录补全成绝对路径并自动创建父目录；实际落点必须在事件结束后用下载状态查询接口再读一次确认。 |
| 199 | 事件 | `EdgeView事件_取菜单项JSON` | `EdgeView事件_取菜单项JSON(控件名, 菜单项句柄)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**菜单项句柄**：受管句柄；“右键菜单请求”事件菜单树携带的受管菜单项句柄。 | 取得受管 WebView2 菜单项名称、标签、命令 ID、类型、状态、快捷键、图标存在性和子项句柄。 |
| 200 | 事件 | `EdgeView事件_置菜单项勾选` | `EdgeView事件_置菜单项勾选(控件名, 菜单项句柄, 勾选)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**菜单项句柄**：受管句柄；“右键菜单请求”事件菜单树携带的受管菜单项句柄。<br>**勾选**：逻辑型；真=勾选。 | 设置受管菜单项勾选状态。 |
| 201 | 事件 | `EdgeView事件_置菜单项启用` | `EdgeView事件_置菜单项启用(控件名, 菜单项句柄, 启用)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**菜单项句柄**：受管句柄；“右键菜单请求”事件菜单树携带的受管菜单项句柄。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置受管菜单项启用状态。 |
| 202 | 对象 | `EdgeView对象_取状态JSON` | `EdgeView对象_取状态JSON(对象句柄)` | 文本型（UTF-16） | Runtime 150（v2） | **对象句柄**：受管句柄；受管对象句柄（Frame、Worker、响应等），来自对应事件字段 handle 或创建命令返回；释放后不复用。 | 取得受管对象类型、所属控件、generation、线程和生命周期状态。 |
| 203 | 对象 | `EdgeView对象_释放` | `EdgeView对象_释放(对象句柄)` | 整数型 | Runtime 150（v2） | **对象句柄**：受管句柄；受管对象句柄（Frame、Worker、响应等），来自对应事件字段 handle 或创建命令返回；释放后不复用。 | 显式释放受管对象；句柄不会被复用。 |
| 204 | 框架 | `EdgeView框架_枚举JSON` | `EdgeView框架_枚举JSON(控件名)` | 文本型（UTF-16） | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 枚举当前控件已发现的 Frame 受管句柄和层级信息。 |
| 205 | 框架 | `EdgeView框架_取信息JSON` | `EdgeView框架_取信息JSON(控件名, 框架句柄)` | 文本型（UTF-16） | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**框架句柄**：受管句柄；“框架创建”等事件字段 handle 携带的 Frame 受管句柄，或 EdgeView框架_枚举JSON 返回的句柄。 | 取得 Frame ID、名称、来源和生命周期信息。 |
| 206 | 框架 | `EdgeView框架_执行脚本异步` | `EdgeView框架_执行脚本异步(控件名, 框架句柄, 脚本, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**框架句柄**：受管句柄；“框架创建”等事件字段 handle 携带的 Frame 受管句柄，或 EdgeView框架_枚举JSON 返回的句柄。<br>**脚本**：文本型（UTF-16）；要执行或注入的 JavaScript 源码文本。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 在受管 Frame 中异步执行 JavaScript。 |
| 207 | 框架 | `EdgeView框架_发送字符串消息` | `EdgeView框架_发送字符串消息(控件名, 框架句柄, 消息)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**框架句柄**：受管句柄；“框架创建”等事件字段 handle 携带的 Frame 受管句柄，或 EdgeView框架_枚举JSON 返回的句柄。<br>**消息**：文本型（UTF-16）；要发送给网页的字符串消息内容。 | 向受管 Frame 发送字符串消息。 |
| 208 | 框架 | `EdgeView框架_发送JSON消息` | `EdgeView框架_发送JSON消息(控件名, 框架句柄, JSON)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**框架句柄**：受管句柄；“框架创建”等事件字段 handle 携带的 Frame 受管句柄，或 EdgeView框架_枚举JSON 返回的句柄。<br>**JSON**：文本型（UTF-16）；要发送的 JSON 消息文本。 | 向受管 Frame 发送 JSON 消息。 |
| 209 | 框架 | `EdgeView框架_发送共享缓冲` | `EdgeView框架_发送共享缓冲(控件名, 框架句柄, 缓冲句柄, 访问模式, 附加JSON)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**框架句柄**：受管句柄；“框架创建”等事件字段 handle 携带的 Frame 受管句柄，或 EdgeView框架_枚举JSON 返回的句柄。<br>**缓冲句柄**：受管句柄；EdgeView缓冲_创建 返回的共享缓冲受管句柄。<br>**访问模式**：整数型；共享缓冲访问模式枚举：0 只读、1 读写。<br>**附加JSON**：文本型（UTF-16）；随共享缓冲发送的附加数据 JSON 文本。 | 把受管共享缓冲发送到指定 Frame。 |
| 210 | 工作线程 | `EdgeView工作线程_枚举异步` | `EdgeView工作线程_枚举异步(控件名, 类型, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**类型**：整数型；要枚举的工作线程类型枚举值。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 枚举 Shared Worker 或 Service Worker；结果为受管句柄 JSON。 |
| 211 | 工作线程 | `EdgeView工作线程_取信息JSON` | `EdgeView工作线程_取信息JSON(控件名, 工作线程句柄)` | 文本型（UTF-16） | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**工作线程句柄**：受管句柄；工作线程枚举结果中的受管句柄。 | 取得 Dedicated、Shared 或 Service Worker 类型及 URI 信息。 |
| 212 | 工作线程 | `EdgeView工作线程_发送字符串消息` | `EdgeView工作线程_发送字符串消息(控件名, 工作线程句柄, 消息)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**工作线程句柄**：受管句柄；工作线程枚举结果中的受管句柄。<br>**消息**：文本型（UTF-16）；要发送给网页的字符串消息内容。 | 向支持消息的 Dedicated 或 Service Worker 发送字符串消息。 |
| 213 | 工作线程 | `EdgeView工作线程_发送JSON消息` | `EdgeView工作线程_发送JSON消息(控件名, 工作线程句柄, JSON)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**工作线程句柄**：受管句柄；工作线程枚举结果中的受管句柄。<br>**JSON**：文本型（UTF-16）；要发送的 JSON 消息文本。 | 向支持消息的 Dedicated 或 Service Worker 发送 JSON 消息。 |
| 214 | 工作线程 | `EdgeView工作线程_置ServiceWorker脚本API` | `EdgeView工作线程_置ServiceWorker脚本API(控件名, 启用)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**启用**：逻辑型；真=开启，假=关闭。 | 设置 Service Worker 是否允许 WebView 脚本 API。 |
| 215 | 工作线程 | `EdgeView工作线程_取ServiceWorker脚本API` | `EdgeView工作线程_取ServiceWorker脚本API(控件名)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 取得 Service Worker 的 WebView 脚本 API 开关。 |
| 216 | 扩展 | `EdgeView扩展_安装异步` | `EdgeView扩展_安装异步(控件名, 扩展目录, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**扩展目录**：文本型（UTF-16）；包含 manifest.json 的扩展目录绝对路径。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 从显式目录安装浏览器扩展，结果为受管扩展句柄。 |
| 217 | 扩展 | `EdgeView扩展_枚举异步` | `EdgeView扩展_枚举异步(控件名, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 枚举当前 Profile 的浏览器扩展。 |
| 218 | 扩展 | `EdgeView扩展_置启用异步` | `EdgeView扩展_置启用异步(控件名, 扩展句柄, 启用, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**扩展句柄**：受管句柄；扩展枚举或安装结果中的受管扩展句柄。<br>**启用**：逻辑型；真=开启，假=关闭。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 异步启用或禁用受管浏览器扩展。 |
| 219 | 扩展 | `EdgeView扩展_删除异步` | `EdgeView扩展_删除异步(控件名, 扩展句柄, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**扩展句柄**：受管句柄；扩展枚举或安装结果中的受管扩展句柄。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 异步删除受管浏览器扩展。 |
| 220 | 权限 | `EdgeView权限_枚举异步` | `EdgeView权限_枚举异步(控件名, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 枚举当前 Profile 的非默认权限设置。 |
| 221 | 权限 | `EdgeView权限_设置异步` | `EdgeView权限_设置异步(控件名, 权限类型, 来源, 状态, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**权限类型**：整数型；权限类型枚举（COREWEBVIEW2_PERMISSION_KIND）。<br>**来源**：文本型（UTF-16）；权限目标来源，如 "https://example.com"。<br>**状态**：整数型；权限状态枚举：0 默认、1 允许、2 拒绝。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 异步设置指定来源的权限状态。 |
| 222 | 通知 | `EdgeView通知_取信息JSON` | `EdgeView通知_取信息JSON(控件名, 通知句柄)` | 文本型（UTF-16） | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**通知句柄**：受管句柄；“网页通知收到”事件字段 handle 携带的通知受管句柄。 | 取得受管通知的标题、正文、语言和状态；来源由“通知收到”事件提供。 |
| 223 | 通知 | `EdgeView通知_报告已显示` | `EdgeView通知_报告已显示(控件名, 通知句柄)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**通知句柄**：受管句柄；“网页通知收到”事件字段 handle 携带的通知受管句柄。 | 向 WebView2 报告自定义通知已显示。 |
| 224 | 通知 | `EdgeView通知_报告单击` | `EdgeView通知_报告单击(控件名, 通知句柄)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**通知句柄**：受管句柄；“网页通知收到”事件字段 handle 携带的通知受管句柄。 | 向 WebView2 报告通知被单击。 |
| 225 | 通知 | `EdgeView通知_报告关闭` | `EdgeView通知_报告关闭(控件名, 通知句柄)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**通知句柄**：受管句柄；“网页通知收到”事件字段 handle 携带的通知受管句柄。 | 向 WebView2 报告通知已关闭。 |
| 226 | 缓冲 | `EdgeView缓冲_创建` | `EdgeView缓冲_创建(控件名, 字节数)` | 受管句柄 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**字节数**：长整数型；要创建的共享缓冲字节数，不超过 16 MiB。 | 创建不超过 16 MiB 的受管共享缓冲。 |
| 227 | 缓冲 | `EdgeView缓冲_取大小` | `EdgeView缓冲_取大小(控件名, 缓冲句柄)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**缓冲句柄**：受管句柄；EdgeView缓冲_创建 返回的共享缓冲受管句柄。 | 取得受管共享缓冲大小。 |
| 228 | 缓冲 | `EdgeView缓冲_写十六进制` | `EdgeView缓冲_写十六进制(控件名, 缓冲句柄, 十六进制)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**缓冲句柄**：受管句柄；EdgeView缓冲_创建 返回的共享缓冲受管句柄。<br>**十六进制**：文本型（UTF-16）；偶数长度十六进制文本。 | 把限长十六进制数据写入受管共享缓冲。 |
| 229 | 缓冲 | `EdgeView缓冲_读十六进制` | `EdgeView缓冲_读十六进制(控件名, 缓冲句柄, 最大字节数)` | 文本型（UTF-16） | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**缓冲句柄**：受管句柄；EdgeView缓冲_创建 返回的共享缓冲受管句柄。<br>**最大字节数**：长整数型；单次读取的最大字节数，超出部分截断。 | 从受管共享缓冲读取限长十六进制。 |
| 230 | 缓冲 | `EdgeView缓冲_发送到网页` | `EdgeView缓冲_发送到网页(控件名, 缓冲句柄, 访问模式, 附加JSON)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**缓冲句柄**：受管句柄；EdgeView缓冲_创建 返回的共享缓冲受管句柄。<br>**访问模式**：整数型；共享缓冲访问模式枚举：0 只读、1 读写。<br>**附加JSON**：文本型（UTF-16）；随共享缓冲发送的附加数据 JSON 文本。 | 把共享缓冲以只读或读写模式发送到网页。 |
| 231 | 安全 | `EdgeView安全_取证书JSON` | `EdgeView安全_取证书JSON(控件名, 证书句柄)` | 文本型（UTF-16） | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**证书句柄**：受管句柄；客户端证书决策事件携带的受管证书句柄。 | 取得受管客户端或服务器证书的主题、签发者、有效期、序列号、显示名和 PEM。 |
| 232 | 安全 | `EdgeView安全_选择客户端证书` | `EdgeView安全_选择客户端证书(控件名, 证书句柄)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**证书句柄**：受管句柄；客户端证书决策事件携带的受管证书句柄。 | 在客户端证书同步决策事件中选择受管证书。 |
| 233 | 安全 | `EdgeView安全_清除证书错误决策异步` | `EdgeView安全_清除证书错误决策异步(控件名, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 清除当前 WebView 的服务器证书错误持久决策。 |
| 234 | 安全 | `EdgeView安全_显示另存为界面异步` | `EdgeView安全_显示另存为界面异步(控件名, 完成处理器)` | 长整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**完成处理器**：处理器引用（&名称）；必须使用 &处理器名；任务完成时被调用，结果用 EdgeView任务_取结果 读取。 | 显示 WebView2 另存为界面并返回状态。 |
| 235 | 对象 | `EdgeView对象_取文件路径` | `EdgeView对象_取文件路径(控件名, 对象句柄)` | 文本型（UTF-16） | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**对象句柄**：受管句柄；受管对象句柄（Frame、Worker、响应等），来自对应事件字段 handle 或创建命令返回；释放后不复用。 | 从受管 File 对象取得安全文件路径。 |
| 236 | 对象 | `EdgeView对象_创建文件系统句柄` | `EdgeView对象_创建文件系统句柄(控件名, 路径, 目录, 权限)` | 受管句柄 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**路径**：文本型（UTF-16）；Cookie 生效路径，如 "/"。<br>**目录**：逻辑型；要映射的本地目录绝对路径。<br>**权限**：整数型；文件系统句柄权限枚举：0 只读、1 读写。 | 从绝对路径创建受管文件或目录句柄；拒绝相对路径和不存在路径。 |
| 237 | 脚本 | `EdgeView脚本_发送附加对象JSON` | `EdgeView脚本_发送附加对象JSON(控件名, JSON, 对象句柄)` | 整数型 | Runtime 150（v2） | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**JSON**：文本型（UTF-16）；要发送的 JSON 消息文本。<br>**对象句柄**：受管句柄；受管对象句柄（Frame、Worker、响应等），来自对应事件字段 handle 或创建命令返回；释放后不复用。 | 向网页发送 JSON 与一个受管文件系统附加对象。 |
| 238 | 兼容基础接口 | `EdgeView_创建` | `EdgeView_创建(父组件句柄, 地址)` | 整数型 | Runtime 141 起 | **父组件句柄**：长整数型；承载 EdgeView 的父窗口或组件 HWND；传 0 嵌入当前窗口。<br>**地址**：文本型（UTF-16）；完整 URL 或本地文件路径。 | 在指定 HWND 组件客户区内创建 EdgeView；传 0 时嵌入当前窗口。成功返回 1。 |
| 239 | 兼容基础接口 | `EdgeView_创建实例` | `EdgeView_创建实例(实例编号, 父组件句柄, 地址, 独立缓存目录)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**父组件句柄**：长整数型；承载 EdgeView 的父窗口或组件 HWND；传 0 嵌入当前窗口。<br>**地址**：文本型（UTF-16）；完整 URL 或本地文件路径。<br>**独立缓存目录**：文本型（UTF-16）；实例专用缓存目录（相对工作目录或绝对路径）；相同目录共享会话数据。 | 创建具名 EdgeView 实例；不同缓存目录拥有独立 Cookie、存储和会话。 |
| 240 | 兼容基础接口 | `EdgeView_创建实例代理` | `EdgeView_创建实例代理(实例编号, 父组件句柄, 地址, 独立缓存目录, 代理地址)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**父组件句柄**：长整数型；承载 EdgeView 的父窗口或组件 HWND；传 0 嵌入当前窗口。<br>**地址**：文本型（UTF-16）；完整 URL 或本地文件路径。<br>**独立缓存目录**：文本型（UTF-16）；实例专用缓存目录（相对工作目录或绝对路径）；相同目录共享会话数据。<br>**代理地址**：文本型（UTF-16）；代理地址，如 "http://127.0.0.1:7890" 或 "socks5://127.0.0.1:1080"。 | 创建使用独立代理的 EdgeView 实例；该代理覆盖全局代理。 |
| 241 | 兼容基础接口 | `EdgeView_创建区域` | `EdgeView_创建区域(实例编号, 左, 顶, 宽, 高, 地址, 独立缓存目录)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**左**：整数型；左边界 X 坐标（像素或 DIP，视命令而定）。<br>**顶**：整数型；顶边界 Y 坐标（像素或 DIP，视命令而定）。<br>**宽**：整数型；宽度（像素或 DIP，视命令而定）。<br>**高**：整数型；高度（像素或 DIP，视命令而定）。<br>**地址**：文本型（UTF-16）；完整 URL 或本地文件路径。<br>**独立缓存目录**：文本型（UTF-16）；实例专用缓存目录（相对工作目录或绝对路径）；相同目录共享会话数据。 | 在当前窗口指定区域创建独立承载 HWND 和 EdgeView 实例。 |
| 242 | 兼容基础接口 | `EdgeView_创建区域代理` | `EdgeView_创建区域代理(实例编号, 左, 顶, 宽, 高, 地址, 独立缓存目录, 代理地址)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**左**：整数型；左边界 X 坐标（像素或 DIP，视命令而定）。<br>**顶**：整数型；顶边界 Y 坐标（像素或 DIP，视命令而定）。<br>**宽**：整数型；宽度（像素或 DIP，视命令而定）。<br>**高**：整数型；高度（像素或 DIP，视命令而定）。<br>**地址**：文本型（UTF-16）；完整 URL 或本地文件路径。<br>**独立缓存目录**：文本型（UTF-16）；实例专用缓存目录（相对工作目录或绝对路径）；相同目录共享会话数据。<br>**代理地址**：文本型（UTF-16）；代理地址，如 "http://127.0.0.1:7890" 或 "socks5://127.0.0.1:1080"。 | 在指定区域创建使用独立代理的 EdgeView 实例。 |
| 243 | 兼容基础接口 | `EdgeView_设置全局代理` | `EdgeView_设置全局代理(代理地址)` | 整数型 | Runtime 141 起 | **代理地址**：文本型（UTF-16）；代理地址，如 "http://127.0.0.1:7890" 或 "socks5://127.0.0.1:1080"。 | 设置后续新建 EdgeView 实例默认使用的 HTTP/HTTPS/SOCKS5 代理；现有实例不变。 |
| 244 | 兼容基础接口 | `EdgeView_清除全局代理` | `EdgeView_清除全局代理()` | 空 | Runtime 141 起 | 无 | 清除后续新建实例的全局代理，现有实例不变。 |
| 245 | 兼容基础接口 | `EdgeView_取全局代理` | `EdgeView_取全局代理()` | 文本型（UTF-16） | Runtime 141 起 | 无 | 返回当前 EdgeView 全局代理设置。 |
| 246 | 兼容基础接口 | `EdgeView_取实例代理` | `EdgeView_取实例代理(实例编号)` | 文本型（UTF-16） | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。 | 返回指定实例创建时实际采用的代理地址。 |
| 247 | 兼容基础接口 | `EdgeView_绑定事件` | `EdgeView_绑定事件(实例编号, 事件名, &处理器名)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**事件名**：文本型（UTF-16）；中文事件名，如 "导航完成"，取值见事件清单。<br>**处理器名**：处理器引用（&名称）；新代码必须使用 &处理器名；旧字符串写法仅兼容迁移。 | 绑定 WebView2 完整事件清单中的中文事件；当前清单共 71 项。旧字符串处理器仍兼容，但会产生迁移警告。 |
| 248 | 兼容基础接口 | `EdgeView_监听开发者工具事件` | `EdgeView_监听开发者工具事件(实例编号, 协议事件名)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**协议事件名**：文本型（UTF-16）；Chromium DevTools Protocol 事件名，如 "Console.messageAdded"。 | 监听指定 Chromium DevTools Protocol 事件，触发“开发者工具协议事件”。 |
| 249 | 兼容基础接口 | `EdgeView_等待事件` | `EdgeView_等待事件(实例编号, 事件名, 超时毫秒)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**事件名**：文本型（UTF-16）；中文事件名，如 "导航完成"，取值见事件清单。<br>**超时毫秒**：整数型；最长等待毫秒数，超时返回 0。 | 泵送窗口消息并等待指定浏览器事件，成功返回 1，超时返回 0。 |
| 250 | 兼容基础接口 | `EdgeView_等待事件控件` | `EdgeView_等待事件控件(控件名, 事件名, 超时毫秒)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**事件名**：文本型（UTF-16）；中文事件名，如 "导航完成"，取值见事件清单。<br>**超时毫秒**：整数型；最长等待毫秒数，超时返回 0。 | 在设计器 Edge 浏览器控件上泵送消息并等待指定中文事件，成功返回 1，超时或控件不存在返回 0。用于在“创建完毕”里等页面就绪后再调用同步命令；不能在其它浏览器事件处理器内调用，否则同样会等满超时。 |
| 251 | 兼容基础接口 | `EdgeView_导航` | `EdgeView_导航(地址)` | 整数型 | Runtime 141 起 | **地址**：文本型（UTF-16）；完整 URL 或本地文件路径。 | 导航到 HTTP/HTTPS 地址或本地文件地址。 |
| 252 | 兼容基础接口 | `EdgeView_导航实例` | `EdgeView_导航实例(实例编号, 地址)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**地址**：文本型（UTF-16）；完整 URL 或本地文件路径。 | 让指定 EdgeView 实例导航。 |
| 253 | 兼容基础接口 | `EdgeView_执行JS` | `EdgeView_执行JS(脚本)` | 文本型（UTF-16） | Runtime 141 起 | **脚本**：文本型（UTF-16）；JavaScript 源码文本。 | 执行 JavaScript 并等待异步回调，返回 WebView2 的 JSON 编码结果；失败返回空文本。 |
| 254 | 兼容基础接口 | `EdgeView_执行JS实例` | `EdgeView_执行JS实例(实例编号, 脚本)` | 文本型（UTF-16） | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**脚本**：文本型（UTF-16）；JavaScript 源码文本。 | 在指定实例执行 JavaScript，并返回 WebView2 JSON 编码结果。 |
| 255 | 兼容基础接口 | `EdgeView_取最近事件` | `EdgeView_取最近事件()` | 文本型（UTF-16） | Runtime 141 起 | 无 | 返回最近触发的 WebView2 中文事件名。 |
| 256 | 兼容基础接口 | `EdgeView_取事件数据` | `EdgeView_取事件数据()` | 文本型（UTF-16） | Runtime 141 起 | 无 | 返回最近事件携带的 UTF-16 JSON 对象文本。 |
| 257 | 兼容基础接口 | `EdgeView_取最近事件实例` | `EdgeView_取最近事件实例(实例编号)` | 文本型（UTF-16） | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。 | 返回指定浏览器实例最近事件名。 |
| 258 | 兼容基础接口 | `EdgeView_取事件数据实例` | `EdgeView_取事件数据实例(实例编号)` | 文本型（UTF-16） | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。 | 返回指定浏览器实例最近事件携带的数据。 |
| 259 | 兼容基础接口 | `EdgeView_后退` | `EdgeView_后退()` | 整数型 | Runtime 141 起 | 无 | 浏览器可以后退时返回上一页。 |
| 260 | 兼容基础接口 | `EdgeView_前进` | `EdgeView_前进()` | 整数型 | Runtime 141 起 | 无 | 浏览器可以前进时进入下一页。 |
| 261 | 兼容基础接口 | `EdgeView_刷新` | `EdgeView_刷新()` | 空 | Runtime 141 起 | 无 | 刷新当前网页。 |
| 262 | 兼容基础接口 | `EdgeView_关闭` | `EdgeView_关闭()` | 空 | Runtime 141 起 | 无 | 关闭浏览器控制器并释放 WebView2 资源。 |
| 263 | 兼容基础接口 | `EdgeView_关闭实例` | `EdgeView_关闭实例(实例编号)` | 空 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。 | 关闭指定 EdgeView 实例并释放其承载窗口。 |
| 264 | 兼容基础接口 | `EdgeView_创建控件` | `EdgeView_创建控件(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 使用设计器属性重新创建指定 Edge 浏览器控件；空文本创建当前窗口全部 Edge 浏览器控件。 |
| 265 | 兼容基础接口 | `EdgeView_导航控件` | `EdgeView_导航控件(控件名, 地址)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**地址**：文本型（UTF-16）；完整 URL 或本地文件路径。 | 让指定设计器 Edge 浏览器控件导航到新地址。 |
| 266 | 兼容基础接口 | `EdgeView_执行JS控件` | `EdgeView_执行JS控件(控件名, 脚本)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**脚本**：文本型（UTF-16）；JavaScript 源码文本。 | 在指定设计器 Edge 浏览器控件中执行 JavaScript 并返回 JSON 编码结果。 |
| 267 | 兼容基础接口 | `EdgeView_取最近事件控件` | `EdgeView_取最近事件控件(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 读取指定设计器 Edge 浏览器控件最近触发的事件名。 |
| 268 | 兼容基础接口 | `EdgeView_取事件数据控件` | `EdgeView_取事件数据控件(控件名)` | 文本型（UTF-16） | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 读取指定设计器 Edge 浏览器控件最近事件的数据。 |
| 269 | 兼容基础接口 | `EdgeView_后退控件` | `EdgeView_后退控件(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 让指定设计器 Edge 浏览器控件后退。 |
| 270 | 兼容基础接口 | `EdgeView_前进控件` | `EdgeView_前进控件(控件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 让指定设计器 Edge 浏览器控件前进。 |
| 271 | 兼容基础接口 | `EdgeView_刷新控件` | `EdgeView_刷新控件(控件名)` | 空 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 刷新指定设计器 Edge 浏览器控件。 |
| 272 | 兼容基础接口 | `EdgeView_关闭控件` | `EdgeView_关闭控件(控件名)` | 空 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。 | 关闭指定设计器 Edge 浏览器控件并保留设计器宿主占位。 |
| 273 | 兼容基础接口 | `EdgeView_绑定控件事件` | `EdgeView_绑定控件事件(控件名, 事件名, &处理器名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**事件名**：文本型（UTF-16）；中文事件名，如 "导航完成"，取值见事件清单。<br>**处理器名**：处理器引用（&名称）；新代码必须使用 &处理器名；旧字符串写法仅兼容迁移。 | 按控件名绑定 WebView2 完整事件清单中的中文事件；当前清单共 71 项。旧字符串处理器仍兼容，但会产生迁移警告。 |
| 274 | 兼容基础接口 | `EdgeView_监听开发者工具事件控件` | `EdgeView_监听开发者工具事件控件(控件名, 协议事件名)` | 整数型 | Runtime 141 起 | **控件名**：控件引用（裸名称）；裸写的可视控件名称（不能加引号），只在当前窗口内解析、且类型必须是 EdgeBrowser；生成 C++ 时按对象名定位。对象不存在、同名歧义或类型不兼容会给出中文诊断，按住 Ctrl 单击可跳转到设计器选中该对象。<br>**协议事件名**：文本型（UTF-16）；Chromium DevTools Protocol 事件名，如 "Console.messageAdded"。 | 按设计器控件名监听 Chromium DevTools Protocol 事件。 |
| 275 | 兼容基础接口 | `EdgeView_创建弹窗浏览器` | `EdgeView_创建弹窗浏览器(实例编号, 窗口标题, 宽, 高, 地址, 独立缓存目录, 用户代理)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**窗口标题**：文本型（UTF-16）；独立顶层弹窗窗口的标题栏文本，便于人工识别店铺。<br>**宽**：整数型；宽度（像素或 DIP，视命令而定）。<br>**高**：整数型；高度（像素或 DIP，视命令而定）。<br>**地址**：文本型（UTF-16）；完整 URL 或本地文件路径。<br>**独立缓存目录**：文本型（UTF-16）；实例专用缓存目录（相对工作目录或绝对路径）；相同目录共享会话数据。<br>**用户代理**：文本型（UTF-16）；要使用的 User-Agent 字符串；需在首次导航前生效，建议建弹窗时直接传入。 | 创建独立顶层浏览器弹窗（任务栏可见、可独立拖动缩放、页面随窗口自适应），用指定独立缓存目录实现店铺间 Cookie/存储隔离，并在首次导航前应用 User-Agent。实例编号供 导航实例/关闭实例/绑定事件/等待事件/执行JS实例/会话命令 复用；关闭主窗口会连带关闭全部弹窗。代理沿用 EdgeView_设置全局代理；要每个弹窗各走独立代理 IP 请用 EdgeView_创建弹窗浏览器代理。成功返回 1。 |
| 276 | 兼容基础接口 | `EdgeView_创建弹窗浏览器代理` | `EdgeView_创建弹窗浏览器代理(实例编号, 窗口标题, 宽, 高, 地址, 独立缓存目录, 用户代理, 代理地址)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**窗口标题**：文本型（UTF-16）；独立顶层弹窗窗口的标题栏文本，便于人工识别店铺。<br>**宽**：整数型；宽度（像素或 DIP，视命令而定）。<br>**高**：整数型；高度（像素或 DIP，视命令而定）。<br>**地址**：文本型（UTF-16）；完整 URL 或本地文件路径。<br>**独立缓存目录**：文本型（UTF-16）；实例专用缓存目录（相对工作目录或绝对路径）；相同目录共享会话数据。<br>**用户代理**：文本型（UTF-16）；要使用的 User-Agent 字符串；需在首次导航前生效，建议建弹窗时直接传入。<br>**代理地址**：文本型（UTF-16）；代理地址，如 "http://127.0.0.1:7890" 或 "socks5://127.0.0.1:1080"。 | 创建带独立代理的顶层浏览器弹窗：代理地址非空时该弹窗用专属 HTTP/HTTPS/SOCKS5 代理（配合不同独立缓存目录=独立浏览器进程，实现每店铺独立出口 IP）；代理地址为空时回落到 EdgeView_设置全局代理，与创建实例/创建实例代理同一语义。 |
| 277 | 兼容基础接口 | `EdgeView_关闭全部实例` | `EdgeView_关闭全部实例()` | 整数型 | Runtime 141 起 | 无 | 关闭当前全部 EdgeView 实例（弹窗、区域、控件实例一并释放）并返回关闭数量。 |
| 278 | 兼容基础接口 | `EdgeView_枚举实例JSON` | `EdgeView_枚举实例JSON()` | 文本型（UTF-16） | Runtime 141 起 | 无 | 返回当前全部 EdgeView 实例的 JSON 数组，每项含 实例编号/窗口标题/地址/缓存目录/代理/是否弹窗/是否有效，供调用方自省与列表展示。 |
| 279 | 兼容基础接口 | `EdgeView_置实例可见` | `EdgeView_置实例可见(实例编号, 可见)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**可见**：逻辑型；真=显示，假=隐藏。 | 按实例编号显示或隐藏：弹窗实例连带顶层窗口显隐，区域/控件实例调整控制器可见性。 |
| 280 | 兼容基础接口 | `EdgeView_置实例大小` | `EdgeView_置实例大小(实例编号, 宽, 高)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**宽**：整数型；宽度（像素或 DIP，视命令而定）。<br>**高**：整数型；高度（像素或 DIP，视命令而定）。 | 按实例编号设置浏览器尺寸；弹窗实例调整顶层窗口客户区并自适应，区域/控件实例设置控制器边界。 |
| 281 | 兼容基础接口 | `EdgeView_取实例大小JSON` | `EdgeView_取实例大小JSON(实例编号)` | 文本型（UTF-16） | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。 | 按实例编号返回当前客户区尺寸 JSON：{"宽":..,"高":..}。 |
| 282 | 兼容基础接口 | `EdgeView_置实例标题` | `EdgeView_置实例标题(实例编号, 标题)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**标题**：文本型（UTF-16）；对话框或窗口标题文本。 | 按实例编号设置弹窗顶层窗口标题，便于人工识别店铺。 |
| 283 | 设置 | `EdgeView设置_置用户代理实例` | `EdgeView设置_置用户代理实例(实例编号, 用户代理)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**用户代理**：文本型（UTF-16）；要使用的 User-Agent 字符串；需在首次导航前生效，建议建弹窗时直接传入。 | 按实例编号设置 User-Agent；建议用 EdgeView_创建弹窗浏览器 的 用户代理 参数在首次导航前设定，运行时再改首个请求已带旧 UA。 |
| 284 | 设置 | `EdgeView设置_取用户代理实例` | `EdgeView设置_取用户代理实例(实例编号)` | 文本型（UTF-16） | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。 | 按实例编号读取当前 User-Agent。 |
| 285 | 会话 | `EdgeView会话_批量置Cookie实例` | `EdgeView会话_批量置Cookie实例(实例编号, Cookie列表JSON)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**Cookie列表JSON**：文本型（UTF-16）；Cookie JSON 数组文本，每项为含 name/value/domain/path 的对象，可选 expires（UTC 秒）、secure、httpOnly、sameSite（0 None、1 Lax、2 Strict）；None 必须同时 secure=true。 | 按实例编号批量注入 Cookie，返回成功条数。Cookie列表JSON 为含 name/value/domain/path 的数组，可带 expires（UTC 秒，会话 Cookie 用 -1 或省略）、secure、httpOnly、sameSite（0 None/1 Lax/2 Strict，None 必须同时 secure）。这是注入 HttpOnly Cookie 的正确方式，禁止用 JavaScript document.cookie。 |
| 286 | 会话 | `EdgeView会话_置Cookie带属性实例` | `EdgeView会话_置Cookie带属性实例(实例编号, 名称, 值, 域, 路径, 过期时间, 安全, 仅HTTP, 同源策略)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**名称**：文本型（UTF-16）；实例显示名称。<br>**值**：文本型（UTF-16）；要写入或转换的值。<br>**域**：文本型（UTF-16）；Cookie 生效域名，如 mms.example.com；Cookie 管理器要求非空。<br>**路径**：文本型（UTF-16）；截图保存路径。<br>**过期时间**：双精度型；Cookie 过期时间（UTC 秒数）；会话 Cookie 传 -1，早于当前时间的过期值会被 WebView2 拒绝写入。<br>**安全**：逻辑型；真=Secure Cookie（仅 HTTPS 发送），传 1/0。<br>**仅HTTP**：逻辑型；真=HttpOnly Cookie（禁止 document.cookie 读写），传 1/0。<br>**同源策略**：整数型；Cookie SameSite 级别：0 None、1 Lax、2 Strict。 | 按实例编号创建或更新带完整属性的 Cookie；过期时间为 UTC 秒（会话 Cookie 传 -1），安全/仅HTTP 传 1/0，同源策略 0 None/1 Lax/2 Strict。 |
| 287 | 会话 | `EdgeView会话_删除全部Cookie实例` | `EdgeView会话_删除全部Cookie实例(实例编号)` | 整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。 | 按实例编号删除该实例 Profile 的全部 Cookie。 |
| 288 | 会话 | `EdgeView会话_取Cookie实例异步` | `EdgeView会话_取Cookie实例异步(实例编号, 地址, &完成处理器)` | 长整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**地址**：文本型（UTF-16）；完整 URL 或本地文件路径。<br>**完成处理器**：处理器引用（&名称）；新代码必须使用 &处理器名。 | 按实例编号异步读取指定地址的 Cookie 列表 JSON，完成后在 完成处理器 里用 EdgeView任务_取结果 取值，可校验 httpOnly 标志是否回读成功。 |
| 289 | 会话 | `EdgeView会话_清理全部浏览数据实例异步` | `EdgeView会话_清理全部浏览数据实例异步(实例编号, &完成处理器)` | 长整数型 | Runtime 141 起 | **实例编号**：整数型；EdgeView 实例的正整数编号；后续命令按它指定实例。<br>**完成处理器**：处理器引用（&名称）；新代码必须使用 &处理器名。 | 按实例编号异步清理该实例 Profile 的全部浏览数据。 |

## 相关文档

- [事件参考](./README.md)：71 项普通 HWND 事件、关键字段和不适用的 CompositionController 事件。
- [用户指南](/guide/user/modules/edgeview)：安装、设计器控件、平台依赖和常用操作。
- [完整演示清单](../../../../examples/module-demos/lingbuilder.edgeview/src/README.md)：可复制的 288 条命令示例签名。
