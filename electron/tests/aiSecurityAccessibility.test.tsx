import assert from 'node:assert/strict'; import fs from 'node:fs/promises'; import path from 'node:path'; import test from 'node:test';
test('AI credentials use Electron safeStorage and are removed from localStorage persistence', async () => { const main = await fs.readFile(path.resolve(import.meta.dirname, '../electron/main.ts'), 'utf8'); const preload = await fs.readFile(path.resolve(import.meta.dirname, '../electron/preload.ts'), 'utf8'); const assistant = await fs.readFile(path.resolve(import.meta.dirname, '../src/components/AiAssistant.tsx'), 'utf8'); assert.match(main, /safeStorage\.encryptString/u); assert.match(main, /safeStorage\.decryptString/u); assert.match(preload, /credentials:ai:get/u); assert.match(assistant, /apiKey: undefined/u); assert.match(assistant, /delete parsed\.apiKey/u); assert.match(assistant, /getAiApiKey/u); assert.doesNotMatch(assistant, /JSON\.stringify\(\{ \.\.\.aiConfig, modelName/u); });
test('FBro VIP Key is user-managed, encrypted and only injected into controlled runtime processes', async () => { const main = await fs.readFile(path.resolve(import.meta.dirname, '../electron/main.ts'), 'utf8'); const preload = await fs.readFile(path.resolve(import.meta.dirname, '../electron/preload.ts'), 'utf8'); const settings = await fs.readFile(path.resolve(import.meta.dirname, '../src/components/SettingsDialog.tsx'), 'utf8'); const server = await fs.readFile(path.resolve(import.meta.dirname, '../server.ts'), 'utf8'); assert.match(main, /fbro-vip-key\.bin/u); assert.match(main, /safeStorage\.encryptString\(value\)/u); assert.match(main, /credentials:fbro-vip:set/u); assert.match(preload, /getFbroVipKeyStatus/u); assert.doesNotMatch(preload, /getFbroVipKey:\s*\(/u); assert.match(settings, /浏览器凭据/u); assert.match(settings, /已保存的值不会明文回显/u); assert.match(settings, /不写入项目、源码、日志、AI 上下文或设置同步包/u); assert.match(server, /delete process\.env\.LINGBUILDER_FBRO_VIP_KEY/u); assert.match(server, /env: createFbroRuntimeEnvironment\(\)/u); });
test('AI index and chat expose cancellation, keyboard names and live accessible states', async () => { const panel = await fs.readFile(path.resolve(import.meta.dirname, '../src/components/AiIndexPanel.tsx'), 'utf8'); const assistant = await fs.readFile(path.resolve(import.meta.dirname, '../src/components/AiAssistant.tsx'), 'utf8'); assert.match(panel, /\/api\/ai-index\/rebuild/u); assert.match(panel, /\/api\/ai-index\/cancel/u); assert.match(panel, /aria-live="polite"/u); assert.match(panel, /htmlFor="ai-index-query"/u); assert.match(panel, /tabIndex=\{0\}/u); assert.match(assistant, /AbortController/u); assert.match(assistant, /停止 AI 请求/u); assert.match(assistant, /发送 AI 请求/u); });
