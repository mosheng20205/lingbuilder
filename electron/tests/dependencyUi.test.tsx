import test from 'node:test'; import assert from 'node:assert/strict'; import fs from 'node:fs/promises'; import path from 'node:path';
test('dependency panel exposes manifests, restore, offline cache and conflicts', async () => { const source = await fs.readFile(path.resolve(import.meta.dirname, '../src/components/DependencyPanel.tsx'), 'utf8'); assert.match(source, /C\+\+ 依赖管理/u); assert.match(source, /\/api\/dependencies/u); assert.match(source, /\/api\/dependencies\/restore/u); assert.match(source, /仅离线缓存/u); assert.match(source, /版本冲突/u); assert.match(source, /vcpkg \/ Conan \/ NuGet/u); assert.match(source, /role="alert"/u); });
