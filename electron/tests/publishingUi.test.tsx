import assert from 'node:assert/strict'; import fs from 'node:fs/promises'; import path from 'node:path'; import test from 'node:test';
test('publishing panel exposes configuration, empty, publish, signature and remote target states', async () => { const source = await fs.readFile(path.resolve(import.meta.dirname, '../src/components/PublishingPanel.tsx'), 'utf8'); assert.match(source, /\/api\/publishing\/configuration/u); assert.match(source, /\/api\/publishing\/run/u); assert.match(source, /\/api\/publishing\/verify-signature/u); assert.match(source, /尚未创建发布配置/u); assert.match(source, /发布 \/ 签名 \/ 远程目标/u); assert.match(source, /role="alert"/u); assert.match(source, /role="status"/u); });
