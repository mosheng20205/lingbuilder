import assert from 'node:assert/strict'; import fs from 'node:fs/promises'; import path from 'node:path'; import test from 'node:test';
test('performance UI exposes collection, cancellation, CPU profile import and empty state', async () => { const source = await fs.readFile(path.resolve(import.meta.dirname, '../src/components/PerformancePanel.tsx'), 'utf8'); assert.match(source, /\/api\/performance\/collect/u); assert.match(source, /\/api\/performance\/cancel/u); assert.match(source, /\/api\/performance\/import/u); assert.match(source, /CPU \/ 内存 \/ I\/O 性能分析/u); assert.match(source, /尚无性能报告/u); assert.match(source, /role="alert"/u); });
