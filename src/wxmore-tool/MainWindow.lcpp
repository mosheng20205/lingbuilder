包 微信4.1.10.27多开
使用 Win32窗口基础模块
使用 Win32高级控件模块
使用 微信4.1.10.27多开管理模块

类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置文本(状态栏, "正在连接本地微信管理服务...")
        如果 (微信多开_绑定监控界面(微信实例列表, 状态栏, 运行日志))
            调试输出("微信多开监控已启动。")
        否则
            控件_设置文本(状态栏, 微信多开_取错误())
            信息框(微信多开_取错误(), 0, "监控启动失败")
        如果结束
    结束

    事件 _启动微信_被单击()
        控件_设置文本(状态栏, "正在启动微信4.1.10.27...")
        如果 (微信多开_启动微信(控件_取文本(微信路径输入)))
            微信多开_立即刷新()
            调试输出("已提交一个微信启动请求。")
        否则
            控件_设置文本(状态栏, 微信多开_取错误())
            信息框(微信多开_取错误(), 0, "启动微信失败")
        如果结束
    结束

    事件 _立即刷新_被单击()
        如果 (微信多开_立即刷新())
            控件_设置文本(状态栏, "正在刷新全部微信实例...")
        否则
            信息框(微信多开_取错误(), 0, "刷新失败")
        如果结束
    结束
结束类
