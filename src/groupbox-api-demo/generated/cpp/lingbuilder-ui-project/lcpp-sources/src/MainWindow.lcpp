包 分组框接口完整演示
使用 Win32窗口基础模块
使用 Win32高级控件模块

类 MainWindow : 公开 窗体
    构造()
        调试输出("分组框演示窗口正在初始化", 真)
    结束

    事件 _MainWindow_创建完毕()
        控件_设置文本("状态栏", "已加载：分组框属性、容器、运行时接口和边界状态演示。")
        控件_设置选择项("演示选项卡", 0)
        调试输出("分组框标题：", 控件_取文本("运行时分组框"), 真)
    结束

    // 运行时接口 01：设置分组框标题。分组框没有专属 setter，使用通用控件接口。
    事件 _设置标题按钮_被单击()
        控件_设置文本("运行时分组框", "运行时标题已更新")
        控件_设置文本("运行结果", "已调用 控件_设置文本：运行时分组框")
        调试输出("控件_设置文本 返回值：", 控件_设置文本("运行时分组框", "运行时标题已更新"), 真)
    结束

    // 运行时接口 02：读取分组框当前标题。
    事件 _读取标题按钮_被单击()
        局部 文本型 当前标题 = ""
        当前标题 = 控件_取文本("运行时分组框")
        控件_设置文本("运行结果", "控件_取文本 返回：" + 当前标题)
        调试输出("控件_取文本 返回值：", 当前标题, 真)
    结束

    // 运行时接口 03：禁用分组框。分组框内的子控件随父容器进入禁用状态。
    事件 _禁用分组框按钮_被单击()
        控件_设置启用("运行时分组框", 假)
        控件_设置文本("运行结果", "已调用 控件_设置启用(运行时分组框, 假)")
    结束

    // 运行时接口 04：隐藏分组框。使用外部按钮恢复，避免隐藏后无法操作。
    事件 _隐藏分组框按钮_被单击()
        控件_设置可见("运行时分组框", 假)
        控件_设置文本("运行结果", "已调用 控件_设置可见(运行时分组框, 假)")
    结束

    // 运行时接口 05：按窗口客户区坐标调整分组框位置和大小。
    事件 _调整大小按钮_被单击()
        控件_设置位置大小("运行时分组框", 58, 178, 455, 285)
        控件_设置文本("运行结果", "已调用 控件_设置位置大小：58,178,455,285")
    结束

    // 运行时接口 06：恢复标题、可见/启用状态及初始布局。
    事件 _恢复状态按钮_被单击()
        控件_设置文本("运行时分组框", "可恢复的运行时分组框")
        控件_设置启用("运行时分组框", 真)
        控件_设置可见("运行时分组框", 真)
        控件_设置位置大小("运行时分组框", 58, 178, 455, 285)
        控件_设置文本("运行结果", "已恢复标题、启用、可见和布局状态。")
    结束

    事件 _显示隐藏示例按钮_被单击()
        控件_设置可见("初始隐藏分组框", 真)
        控件_设置文本("边界结果", "已用 控件_设置可见 显示初始隐藏分组框。")
    结束

    事件 _子按钮_被单击()
        控件_设置文本("容器结果", "分组框内的子按钮事件已触发；容器不会吞掉子控件事件。")
        调试输出("分组框容器子按钮被单击", 真)
    结束
结束类

