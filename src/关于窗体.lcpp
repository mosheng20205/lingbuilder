包 LingBuilder
使用 Win32窗口
使用 标准控件

类 关于窗体 : 公开 窗体
公开:
    文本型 关联设计文件 = "AboutWindow.xml"

    构造()
        调试输出("关于太空冒险客户端初始化完毕，Win32 渲染正常。")

    事件 _关于窗体_创建完毕()
        调试输出("已载入 AboutWindow.xml 关联布局。")

    事件 _确认关闭按钮_被单击()
        信息框("我知道了", 64, "事件触发")
        调试输出("确认关闭按钮被单击")
结束类