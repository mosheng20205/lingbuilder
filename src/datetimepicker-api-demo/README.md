# 日期时间选择器完整接口示例

这是 LingBuilder `new_emoji` 日期时间选择器的完整接口演示项目。界面使用 TabControl 分组，覆盖：

- 创建属性：年份、月份、日期、小时、分钟；
- 日期时间设置、范围、显示选项、打开/关闭和清空；
- 今天、现在、月份移动、滚动、默认时间；
- 快捷键、起止占位符、范围分隔符和范围选择；
- 日期、时间、范围、选项、滚动和打开状态读取；
- 选择变化、鼠标、焦点等全部设计器事件回调。

源码中的 `@ EU_*` 行是 native new_emoji 接口的逐项调用示例，方便复制到自己的 `.lcpp` 项目中。
