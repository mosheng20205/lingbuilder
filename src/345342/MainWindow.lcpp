类 MainWindow
    事件 创建完毕()
        调试输出("窗口创建完毕")
    结束

    事件 _MainWindow_按键被按下(整数型 键码，逻辑型 Ctrl键按下，逻辑型 Shift键按下，逻辑型 Alt键按下)
        调试输出("MainWindow按键被按下"，键码)
    结束

    事件 _按钮1_被单击()
        // 信息框("新按钮", 64, "事件触发")
        调试输出("按钮1被单击")
        局部 文本型 局部变量 = "123"
        如果 (局部变量="123")
            调试输出("1")
        否则
            调试输出("2")
        如果结束
        局部常量 整数型 局部常量 = 123
        调试输出("局部常量"，局部常量)

        局部 整数型 i

        计次循环首 (10, i)
            局部变量=到文本(到整数(局部变量)+10)
            调试输出(i,局部常量,局部变量)
        计次循环尾 ()
    结束
结束类