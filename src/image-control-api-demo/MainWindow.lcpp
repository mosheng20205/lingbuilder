包 图片框Image全接口演示
使用 Win32窗口基础模块
使用 通用控件模块

类 MainWindow : 公开 窗体
    构造()
    结束

    事件 _MainWindow_创建完毕()
        控件_设置文本(全局状态, "已就绪：请按选项卡逐项体验图片框 Image 的全部接口")
        控件_设置文本(事件日志, "等待鼠标移入、按下、单击或移出事件")
        调试输出("图片框 Image 全接口演示项目已启动")
    结束

    // 设计器属性页：通过图片源与填充方式展示四种显示模式。
    事件 _方法设置图片按钮_被单击()
        动态图片框.设置图片("assets/image-control-api-demo/示例图片.png")
        控件_设置文本(全局状态, "已调用：动态图片框.设置图片(图片路径)")
        调试输出("图片框方法设置图片", 真)
    结束

    事件 _清空图片按钮_被单击()
        动态图片框.设置图片("")
        控件_设置文本(全局状态, "已调用：动态图片框.设置图片(空路径)，图片已清空")
    结束

    事件 _通用命令设置图片按钮_被单击()
        控件_设置图片(动态图片框, "assets/image-control-api-demo/示例图片.png")
        控件_设置文本(全局状态, "已调用：控件_设置图片(控件名, 图片路径)")
        调试输出("控件_设置图片", 真)
    结束

    事件 _恢复图片按钮_被单击()
        控件_设置图片(动态图片框, "assets/image-control-api-demo/示例图片.png")
        控件_设置文本(全局状态, "图片已恢复；相对路径会随项目资源一起导出")
    结束

    // 通用控件接口页：图片框也可以被通用控件命令定位和操作。
    事件 _设置文本按钮_被单击()
        控件_设置文本(接口目标图片框, "图片框文本接口已调用")
        控件_设置文本(全局状态, "已调用：控件_设置文本")
    结束

    事件 _读取文本按钮_被单击()
        控件_设置文本(通用接口结果, "控件_取文本结果：" + 控件_取文本(接口目标图片框))
        调试输出("控件_取文本", 控件_取文本(接口目标图片框), 真)
    结束

    事件 _禁用图片按钮_被单击()
        控件_设置启用(接口目标图片框, 假)
        控件_设置文本(通用接口结果, "已调用：控件_设置启用(接口目标图片框, 假)")
    结束

    事件 _启用图片按钮_被单击()
        控件_设置启用(接口目标图片框, 真)
        控件_设置文本(通用接口结果, "已调用：控件_设置启用(接口目标图片框, 真)")
    结束

    事件 _隐藏图片按钮_被单击()
        控件_设置可见(接口目标图片框, 假)
        控件_设置文本(通用接口结果, "已调用：控件_设置可见(接口目标图片框, 假)")
    结束

    事件 _显示图片按钮_被单击()
        控件_设置可见(接口目标图片框, 真)
        控件_设置文本(通用接口结果, "已调用：控件_设置可见(接口目标图片框, 真)")
    结束

    事件 _调整位置按钮_被单击()
        控件_设置位置大小(接口目标图片框, 60, 205, 300, 250)
        控件_设置文本(通用接口结果, "已调用：控件_设置位置大小；图片框已移动并放大")
    结束

    事件 _恢复位置按钮_被单击()
        控件_设置位置大小(接口目标图片框, 40, 180, 260, 230)
        控件_设置文本(通用接口结果, "已恢复图片框位置和大小")
    结束

    // 图片框事件页：四个稳定可触发的事件都绑定到中文处理器。
    事件 _事件图片框_被单击()
        控件_设置文本(事件日志, "Click：事件图片框被单击")
        调试输出("Image Click", 真)
    结束

    事件 _事件图片框_鼠标被按下()
        控件_设置文本(事件日志, "MouseDown：事件图片框收到鼠标按下")
    结束

    事件 _事件图片框_鼠标移入()
        控件_设置文本(事件日志, "MouseEnter：鼠标已移入事件图片框")
    结束

    事件 _事件图片框_鼠标移出()
        控件_设置文本(事件日志, "MouseLeave：鼠标已移出事件图片框")
    结束

    事件 _清空事件日志按钮_被单击()
        控件_设置文本(事件日志, "事件日志已清空，移动鼠标到图片框上继续体验")
    结束

    事件 _接口分类选项卡_标签页被改变()
        控件_设置文本(全局状态, "已切换选项卡；当前页可逐项验证图片框能力")
    结束
结束类
