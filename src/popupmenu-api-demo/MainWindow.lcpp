包 PopupMenu 弹出菜单完整接口示例
使用 Win32窗口基础模块
使用 Win32高级控件模块

类 MainWindow : 公开 窗体
    事件 _MainWindow_创建完毕()
        控件_设置选择项(演示选项卡, 0)
        控件_设置文本(状态栏, "已加载：可按当前鼠标位置或指定屏幕坐标显示 PopupMenu。")
        调试输出("PopupMenu 示例已启动；资源名：", "演示弹出菜单", 真)
    结束

    // 01：在当前鼠标位置显示 PopupMenu。菜单取消时接口返回假。
    事件 _当前位置显示按钮_被单击()
        局部 逻辑型 显示成功 = 假
        控件_设置文本(状态栏, "正在调用 弹出菜单_显示；请选择菜单项或点击空白处取消。")
        显示成功 = 弹出菜单_显示(演示弹出菜单)
        如果 (显示成功)
            调试输出("弹出菜单_显示 返回真；最后项目：", 菜单_取最后项目(演示弹出菜单), 真)
        否则
            控件_设置文本(状态栏, "弹出菜单_显示 返回假：本次菜单被取消或未能显示。")
        如果结束
    结束

    // 02：指定屏幕坐标显示。这里以当前鼠标屏幕坐标为基准向右下偏移，便于观察差异。
    事件 _坐标显示按钮_被单击()
        局部 整数型 屏幕横坐标 = 0
        局部 整数型 屏幕纵坐标 = 0
        局部 逻辑型 显示成功 = 假
        屏幕横坐标 = 取鼠标水平位置() + 80
        屏幕纵坐标 = 取鼠标垂直位置() + 40
        控件_设置文本(状态栏, "正在调用 弹出菜单_在坐标显示；菜单将出现在鼠标右下方。")
        显示成功 = 弹出菜单_在坐标显示(演示弹出菜单, 屏幕横坐标, 屏幕纵坐标)
        如果 (显示成功)
            调试输出("弹出菜单_在坐标显示 返回真；最后项目：", 菜单_取最后项目(演示弹出菜单), 真)
        否则
            控件_设置文本(状态栏, "弹出菜单_在坐标显示 返回假：本次菜单被取消或未能显示。")
        如果结束
    结束

    // 03：读取最近一次成功选择的稳定项目 ID；修改菜单文字不会改变这个 ID。
    事件 _读取最后项目按钮_被单击()
        局部 文本型 项目ID = ""
        项目ID = 菜单_取最后项目(演示弹出菜单)
        如果 (项目ID == "")
            控件_设置文本(状态栏, "菜单_取最后项目 返回空文本：还没有成功选择菜单项。")
        否则
            控件_设置文本(状态栏, "菜单_取最后项目 返回稳定 ID：" + 项目ID)
        如果结束
        调试输出("菜单_取最后项目：", 项目ID, 真)
    结束

    事件 _演示选项卡_标签页被改变()
        控件_设置文本(状态栏, "已切换演示分组；PopupMenu 是非可视资源，不会占用页面布局。")
    结束

    // 04～09：每个可用菜单项分别绑定“菜单项被选择”处理器。
    事件 _演示弹出菜单_打开菜单项_被选择()
        控件_设置文本(状态栏, "已选择：打开（稳定 ID：open-item；文字带 Alt 快捷标记）。")
        调试输出("PopupMenu 项目事件：open-item", 真)
    结束

    事件 _演示弹出菜单_保存菜单项_被选择()
        控件_设置文本(状态栏, "已选择：保存（稳定 ID：save-item）。")
        调试输出("PopupMenu 项目事件：save-item", 真)
    结束

    事件 _演示弹出菜单_网格菜单项_被选择()
        控件_设置文本(状态栏, "已选择：对齐网格（稳定 ID：snap-grid；设计器中初始勾选）。")
        调试输出("PopupMenu 项目事件：snap-grid", 真)
    结束

    事件 _演示弹出菜单_深色菜单项_被选择()
        控件_设置文本(状态栏, "已选择：深色界面（稳定 ID：dark-theme；设计器中初始勾选）。")
        调试输出("PopupMenu 项目事件：dark-theme", 真)
    结束

    事件 _演示弹出菜单_说明菜单项_被选择()
        控件_设置文本(状态栏, "已选择：查看接口说明（稳定 ID：show-help）。")
        调试输出("PopupMenu 项目事件：show-help", 真)
    结束

    事件 _演示弹出菜单_关闭菜单项_被选择()
        控件_设置文本(状态栏, "已选择：关闭菜单演示（稳定 ID：close-demo）；示例窗口继续保留。")
        调试输出("PopupMenu 项目事件：close-demo", 真)
    结束
结束类
