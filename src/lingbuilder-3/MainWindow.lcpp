类 MainWindow
    事件 创建完毕(文本型 23)
        调试输出("窗口创建完毕")
    结束
    // 新手模式右键新增的子程序
    空 新子程序()
        调试输出("新子程序 已执行")
        局部 文本型 局部变量12 = ""
        // 34563265
        计次循环首(次数, 计次变量)
            调试输出()
        计次循环尾 ()
        如果 (条件)

        否则

        如果结束

        如果真 (条件)

        如果真结束

        如果真 (条件)
            计次循环首 (次数, 计次变量)
                调试输出("")
            计次循环尾 ()
        如果真结束


        计次循环首 (次数, 计次变量)
            如果真 (条件)
                调试输出("")
                信息框("提示内容", 64, "提示")
            如果真结束
        计次循环尾 ()
结束类
