包 月历完整接口示例
使用 Win32窗口基础模块
使用 Win32高级控件模块

类 MainWindow : 公开 窗体
    事件 _MainWindow_创建完毕()
        控件_设置文本("状态标签", "就绪：请切换选项卡并操作三个真实月历控件。")
        控件_设置可见("范围月历", 真)
        控件_设置启用("外观月历", 真)
        调试输出("月历演示", "当前日期、多选范围和日期改变事件已准备就绪。", 真)
    结束

    空 运行全部接口演示()
        控件_设置可见("单选月历", 真)
        控件_设置启用("范围月历", 真)
        控件_设置位置大小("外观月历", 520, 180, 360, 300)
        控件_设置文本("状态标签", "已覆盖：当前日期、允许范围选择、日期被改变，以及通用控件操作。")
        调试输出("接口 1", "当前日期：设计器 value 属性", 真)
        调试输出("接口 2", "允许范围选择：设计器 multiSelect 属性", 真)
        调试输出("接口 3", "日期被改变：MonthCalendar.ValueChanged 事件", 真)
    结束

    事件 _运行全部接口按钮_被单击()
        运行全部接口演示()
    结束

    事件 _切换单选月历按钮_被单击()
        控件_设置可见("单选月历", 假)
        控件_设置可见("范围月历", 真)
        控件_设置文本("状态标签", "已切换到范围选择月历：请拖动选择连续日期。")
    结束

    事件 _切换范围月历按钮_被单击()
        控件_设置可见("单选月历", 真)
        控件_设置可见("范围月历", 假)
        控件_设置文本("状态标签", "已切换到单日期月历：请单击一个日期触发改变事件。")
    结束

    事件 _启用禁用按钮_被单击()
        控件_设置启用("外观月历", 假)
        控件_设置文本("状态标签", "已禁用外观月历；再次运行全部接口演示可恢复启用。")
    结束

    事件 _调整位置按钮_被单击()
        控件_设置位置大小("外观月历", 540, 200, 380, 320)
        控件_设置文本("状态标签", "已调用控件_设置位置大小调整外观月历。")
    结束

    事件 _单选月历_日期被改变()
        控件_设置文本("状态标签", "单选月历触发了日期被改变事件。")
        调试输出("单选月历", "ValueChanged：用户选择了新的日期。", 真)
    结束

    事件 _范围月历_日期被改变()
        控件_设置文本("状态标签", "范围月历触发了日期被改变事件；multiSelect 已启用。")
        调试输出("范围月历", "ValueChanged：用户更新了日期范围。", 真)
    结束

    事件 _外观月历_日期被改变()
        控件_设置文本("状态标签", "外观月历触发了日期被改变事件。")
    结束

    事件 _事件月历_日期被改变()
        控件_设置文本("状态标签", "事件月历触发了日期被改变事件；请查看输出面板。")
        调试输出("事件月历", "ValueChanged：事件分组中的月历已改变日期。", 真)
    结束

    事件 _演示选项卡_页被改变()
        调试输出("选项卡", "已切换月历能力演示分组。", 真)
    结束
结束类
