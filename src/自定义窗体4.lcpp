包 LingBuilder
使用 Win32窗口
使用 标准控件

类 自定义窗体4 : 公开 窗体
公开:
    文本型 关联设计文件 = "Window4.xml"

    构造()
        调试输出("自定义窗口 4初始化完毕，Win32 渲染正常。")

    事件 _自定义窗体4_创建完毕()
        调试输出("已载入 Window4.xml 关联布局。")

    事件 _按钮4_被单击()
        信息框("确认", 64, "事件触发")
        调试输出("按钮4被单击")
结束类