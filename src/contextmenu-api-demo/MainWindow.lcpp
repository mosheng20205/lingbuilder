包 ContextMenu 上下文菜单完整接口示例
使用 Win32窗口基础模块
使用 Win32高级控件模块

类 MainWindow : 公开 窗体
    事件 _MainWindow_创建完毕()
        控件_设置文本("状态栏", "已加载：ContextMenu 自动右键、主动显示、最后项目读取和菜单项状态演示。")
        控件_设置选择项("演示选项卡", 0)
        调试输出("ContextMenu 示例已启动；右键目标：", "右键目标按钮", 真)
    结束

    // 01：ContextMenu 绑定目标的自动触发。右键选项卡页内的真实按钮即可弹出菜单。
    事件 _右键目标按钮_被单击()
        控件_设置文本("状态栏", "左键单击了右键目标按钮；请改用鼠标右键验证自动弹出。")
    结束

    // 02：ContextMenu 的主动显示接口。在当前鼠标位置显示绑定的上下文菜单。
    事件 _主动显示菜单按钮_被单击()
        局部 逻辑型 显示成功 = 假
        显示成功 = 上下文菜单_显示("演示上下文菜单")
        如果 (显示成功)
            控件_设置文本("状态栏", "上下文菜单_显示 已调用：菜单已在当前鼠标位置显示。")
        否则
            控件_设置文本("状态栏", "上下文菜单_显示 返回假：菜单未能显示。")
        如果结束
    结束

    // 03：读取最近一次选择的稳定菜单项 ID。
    事件 _读取最后项目按钮_被单击()
        局部 文本型 项目ID = ""
        项目ID = 菜单_取最后项目("演示上下文菜单")
        控件_设置文本("状态栏", "菜单_取最后项目 返回：" + 项目ID)
        调试输出("菜单_取最后项目 返回稳定 ID：", 项目ID, 真)
    结束

    // 04：TabControl 容器事件，用于确认菜单目标位于选项卡页面内。
    事件 _演示选项卡_标签页被改变()
        控件_设置文本("状态栏", "已切换演示分组；ContextMenu 仍绑定到选项卡页内的真实目标按钮。")
    结束

    // 05～08：菜单项逐项事件。事件绑定使用资源中稳定的 item ID，不依赖菜单文字。
    事件 _演示上下文菜单_复制菜单项_被选择()
        控件_设置文本("状态栏", "已选择菜单项：复制（稳定 ID：copy-item）。")
        调试输出("ContextMenu 菜单项事件：copy-item", 真)
    结束

    事件 _演示上下文菜单_切换高亮菜单项_被选择()
        控件_设置文本("状态栏", "已选择菜单项：切换高亮（稳定 ID：toggle-highlight）；该项初始为勾选。")
        调试输出("ContextMenu 菜单项事件：toggle-highlight", 真)
    结束

    事件 _演示上下文菜单_查看日志菜单项_被选择()
        控件_设置文本("状态栏", "已选择菜单项：查看日志（稳定 ID：view-log）。")
        调试输出("ContextMenu 菜单项事件：view-log", 真)
    结束

    事件 _演示上下文菜单_关闭菜单项_被选择()
        控件_设置文本("状态栏", "已选择菜单项：关闭（稳定 ID：close-item）。")
        调试输出("ContextMenu 菜单项事件：close-item", 真)
    结束
结束类
