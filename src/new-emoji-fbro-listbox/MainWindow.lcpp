包 new_emoji列表框多浏览器演示
使用 new_emoji 原生界面库
使用 FBro指纹浏览器模块

类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        应用浏览器选择("browser-baidu")
        调试输出("new_emoji ListBox 多浏览器演示已启动")
    结束

    空 应用浏览器选择(文本型 选中键列表)
        FBro_隐藏(百度浏览器)
        FBro_隐藏(必应浏览器)
        FBro_隐藏(代码托管浏览器)

        如果 (选中键列表 == "browser-baidu")
            FBro_显示(百度浏览器)
            控件_设置文本(当前浏览器标签, "当前浏览器：百度")
            调试输出("已切换到百度浏览器")
        否则如果 (选中键列表 == "browser-bing")
            FBro_显示(必应浏览器)
            控件_设置文本(当前浏览器标签, "当前浏览器：必应")
            调试输出("已切换到必应浏览器")
        否则如果 (选中键列表 == "browser-github")
            FBro_显示(代码托管浏览器)
            控件_设置文本(当前浏览器标签, "当前浏览器：GitHub")
            调试输出("已切换到 GitHub 浏览器")
        如果结束
    结束

    事件 _浏览器列表_选择变化(文本型 选中键列表)
        应用浏览器选择(选中键列表)
    结束
结束类
