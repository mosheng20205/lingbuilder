包 CEF3浏览器操作小项目
使用 Win32窗口基础模块
使用 CEF3浏览器模块

类 CEF3浏览器操作窗体 : 窗口
公开
  构造()
  结束

  事件 _CEF3浏览器操作窗体_创建完毕()
    // 将本地 pages/home.html 的绝对路径填入地址栏后调用导航。
    调试输出("CEF3 操作小项目已启动：", CEF3_取地址(浏览器1))
  结束

  事件 后退按钮_被单击()
    CEF3_后退(浏览器1)
  结束
  事件 前进按钮_被单击()
    CEF3_前进(浏览器1)
  结束
  事件 刷新按钮_被单击()
    CEF3_刷新(浏览器1)
  结束
  事件 停止按钮_被单击()
    CEF3_停止(浏览器1)
  结束
  事件 放大按钮_被单击()
    CEF3_执行缩放(浏览器1, 1)
  结束
  事件 缩小按钮_被单击()
    CEF3_执行缩放(浏览器1, 0)
  结束
  事件 重置缩放按钮_被单击()
    CEF3_设置缩放级别(浏览器1, 0.0)
  结束
  事件 地址栏_内容被改变()
    CEF3_导航(浏览器1, 控件_取文本(地址栏))
  结束
  事件 浏览器1_加载完成()
    控件_设置文本(标题标签, CEF3_取标题(浏览器1))
    调试输出("当前地址：", CEF3_取地址(浏览器1))
    调试输出("脚本标题：", CEF3_执行JS(浏览器1, "document.title"))
    调试输出("当前缩放：", CEF3_取缩放级别(浏览器1))
  结束
结束类
