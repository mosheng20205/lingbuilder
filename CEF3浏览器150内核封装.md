# CEF3 浏览器 150 内核封装

> 记录日期：2026-07-26
> 内核版本：CEF 150.0.14+g7c1aa68 / Chromium 150.0.7871.129（x64）
> 状态：`3.0.0-alpha.2` 核心 Bridge 闭环已验证，Alpha.3 正在实施；覆盖 v2 为 265 implemented / 8 internal / 185 notApplicable / 1119 planned，不得视为全部 CEF API 已完成

本文完整记录 `CEF3浏览器模块` 的封装过程：模块定义、控件工具箱接入、SDK 下载与依赖编译、构建链路改造、生成器 CEF 150 API 适配、CLI 示例生成与最终验证，以及排障过程中确诊的所有根因。

---

## 1. 模块概述

| 项目 | 内容 |
| --- | --- |
| 模块名称 | CEF3浏览器模块 |
| 模块 ID | `lingbuilder.cef3.browser` |
| 模块类型 | 内置界面模块（v2 manifest，`schemaVersion: 2`） |
| 内核 | Chromium Embedded Framework 3，CEF 150.0.14 |
| 定义位置 | `electron/src/services/modules/builtinModules.ts` |
| C++ 运行时生成 | `electron/src/services/windowDesigner/lingCppWin32Project.ts` |
| 原生依赖复制 | `electron/src/services/modules/nativeDependencyService.ts` |

模块按 v2 标准同时提供：

- `contributes.designerControls`：设计器可视控件贡献（`CefBrowser`，`nativeAdapter: cef3-browser`）。
- `contributes.commands`：22 条兼容核心 `CEF3_*` 中文命令的补全、提示、文档与诊断；高级能力由 3.0 子模块增量提供。
- `bindings.commands`：中文命令到 C++ 的确定性映射。
- `targets[]`：仅保留真实验证过的 `windows-msvc-x64` target。应用只链接 `LingBuilderCefBridge.lib` 并包含 Bridge 头；`libcef.lib`、wrapper 和 CEF C++ 头只属于 Bridge 构建，运行时继续部署 `libcef.dll`、`chrome_elf.dll` 和资源；Win32 明确不支持。

## 2. 控件工具箱支持

项目启用 `lingbuilder.cef3.browser` 后（保存到 `.lingbuilder/projects/<项目>/project-modules.json`），窗口设计器控件工具箱**自动新增** `CEF3浏览器 (CefBrowser)` 控件，不需要修改任何 React 组件——这是 v2 `contributes.designerControls` 设计器控件高级贡献的参考实现之一。

- 可在任意窗口添加多个实例。
- 属性面板可配置：

| 属性 | 说明 |
| --- | --- |
| `url` | 打开地址（创建后自动导航） |
| `cacheDir` | 实例缓存标识；稳定映射到全局 `root_cache_path` 下该控件独立的直接子目录 |
| `userAgent` | User-Agent（CEF 150 起为全局 `CefSettings.user_agent`，同 exe 共享） |
| JavaScript / 图片 / WebGL 开关 | 映射到 `CefBrowserSettings` 的 `javascript` / `image_loading` / `webgl` |
| 静音 | 浏览器创建完成后调用 `GetHost()->SetAudioMuted(true)` |
| 代理模式 / 代理服务器 | 创建前生效 |

- 控件序列化：`properties.url` 写入 `data`；`cacheDir`、`userAgent`、`proxyMode`、`proxyServer` 经 `encodeControlFields` 写入 `data2`。

## 3. 中文命令（22 条兼容核心 `CEF3_*`）与浏览器侧事件

运行时命令第一个参数均为设计器中文控件名：

原有 18 条命令保持不变，新增 `CEF3_取事件字段`、`CEF3_设置事件结果`、`CEF3_设置事件返回文本`。

事件目录已扩展为 92 项 CEF 150 浏览器侧回调，定义在 `electron/src/services/modules/cef3BrowserEvents.ts`，覆盖 `CefClient` 的生命周期、加载显示、菜单/对话框、输入焦点、下载、权限安全、请求/资源/Cookie、音频打印、框架和渲染进程状态。原有 5 个事件及设计器 ID 保持兼容，可通过设计器事件面板或 `CEF3_绑定事件` 回调到当前窗口的无参数中文事件/方法。

普通通知通过 `WM_LINGBUILDER_CEF_EVENT` 异步投递到所属 Win32 窗口线程；需要 CEF 当场返回结果的回调同步投递，并读取 `CEF3_设置事件结果`（`0=默认、1=允许、2=拒绝、3=已处理`）。音频包、下载进度和页面加载进度按受控频率采样，避免消息洪泛。当前控件采用 windowed CEF；仅适用于离屏渲染模式的 `CefRenderHandler` 像素缓冲与无障碍回调不伪装成当前控件事件。

语义约束（已写入《LingBuilder AI 规则手册.md》）：

- CEF3 同一 exe 内共享 Chromium 进程；每个控件使用独立 `CefRequestContext` 和缓存直接子目录，登录态/缓存不再默认共用。
- `CEF3_设置缓存目录` / `CEF3_设置代理` 必须在创建前调用，创建后调用无效。
- `CEF3_绑定事件` 的处理器必须写为 `&处理器名`；旧字符串形式只作为迁移输入。
- 编辑体验（Monaco 补全/诊断）与 C++ 生成结果消费同一份模块上下文，保持一致。

## 4. SDK 下载与准备

### 4.1 下载

从 CEF 官方构建（Spotify CDN，cef-builds.spotifycdn.com）手动下载了两个包：

| 包 | 用途 |
| --- | --- |
| `cef_binary_150.0.14+g7c1aa68+chromium-150.0.7871.129_windows64.tar.tar` | **64 位标准包（优先使用，本次实际采用）** |
| `cef_binary_150.0.14+g7c1aa68+chromium-150.0.7871.129_windows32_minimal.tar.tar` | 32 位 minimal 包（备用，尚未验证） |

64 位包解压后放置到两处（构建链路按顺序发现）：

1. 工作区 `.lingbuilder/cef3-sdk/`
2. `C:\cef3-sdk\`（用于规避中文路径在 cmd/GBK 链路下被破坏的问题）

发现顺序：`CEF3_SDK_ROOT` 环境变量 → 工作区 `.lingbuilder/cef3-sdk` → `C:\cef3-sdk`（判定标准：目录下存在 `include/cef_app.h`）。

### 4.2 官方包目录结构（isOfficialLayout）

```text
<cef3-sdk>/
  include/                 # cef_app.h 等头文件
  Release/                 # libcef.lib、libcef.dll、chrome_elf.dll、v8_context_snapshot.bin、
                           # d3dcompiler_47.dll、dxcompiler.dll、dxil.dll、libEGL.dll、libGLESv2.dll、
                           # vk_swiftshader.dll、vulkan-1.dll 等
  Resources/               # icudtl.dat、resources.pak、chrome_100/200_percent.pak、locales/
  libcef_dll/              # libcef_dll_wrapper 源码（需自行编译）
  cmake/                   # cef_variables.cmake 等
  CMakeLists.txt
```

与旧的预编译平铺布局（`lib/Win32`、`bin/Win32`）不同，官方包的 `.lib`/`.dll` 集中在 `Release/`，且 **wrapper 静态库必须用 CMake 现场编译**。`nativeDependencyService.ts` 两种布局都支持。

### 4.3 编译要求

- CEF 150 要求 **C++20**（构建链已使用 `/std:c++20`）。
- 本次采用 **x64** 架构（vcvars64 + `windows-msvc-x64` target）。
- 链接 `.lib` 需要 MSVC / Visual Studio Build Tools（本机为 VS 2022 Community）。

## 5. libcef_dll_wrapper 依赖编译（含排障根因）

这是本次封装最曲折的部分，共确诊三个根因：

### 5.1 根因一：本机 CMake 启用 C 语言时崩溃（0xC0000409）

**现象**：CMake configure CEF SDK 时静默崩溃（退出码 0xC0000409，零输出），Ninja / VS 生成器、bat / PowerShell 链路均复现。

**排除法定位过程**：

1. `cmake --version` 正常 → CMake 本体没坏。
2. PowerShell 直跑 VS 生成器 + 最小 CXX-only 项目 → **成功**。
3. CEF SDK 在输出 "The C compiler identification" 后崩溃。
4. `--trace-expand` 定位崩溃点在 `CMakeFindBinUtils.cmake` 的 `find_program(CMAKE_LINKER ...)`。
5. 最小 C+CXX 双语言项目复现崩溃 → **确诊：VS 2022 自带 CMake 3.31.6-msvc6 在项目启用 C 语言时，于 C 编译器工具查找阶段必崩；CXX-only 完全正常**。

**修复**：CEF 的 `CMakeLists.txt` 中 `project(cef)` 默认启用 C+CXX 两种语言，而 wrapper 是纯 C++。把它改为 `project(cef LANGUAGES CXX)` 后 configure + build 立即成功。该修复已代码化为 `nativeDependencyService.ts` 中的 `patchCefCMakeListsForCxxOnly()`，在每次 configure 前自动执行。

**附带教训**：

- CMake（VS 生成器）不需要 vcvars 环境，**直接在 PowerShell 中运行即可**；经 cmd.exe / bat 批处理链路调用反而会崩溃或让中文路径被 GBK 破坏。
- bat 中 `if errorlevel 1` 捕获不到负退出码（0xC0000409），应改用 `if not "%errorlevel%"=="0"`。

### 5.2 根因二：CRT 运行库不匹配（LNK2038）

**现象**：wrapper 编译成功后，构建示例时链接器报大量 `LNK2038: 检测到"RuntimeLibrary"的不匹配项: 值"MT_StaticRelease"不匹配值"MD_DynamicRelease"`。

**原因**：CEF 官方 CMake 默认 `CEF_RUNTIME_LIBRARY_FLAG` 为 `/MT`（静态 CRT），而 LingBuilder 编译链使用 `/MD`（动态 CRT）。

**修复**：configure 时追加 `-DCEF_RUNTIME_LIBRARY_FLAG=/MD` 重新编译 wrapper。产物目录改为 `build_wrapper_<架构>_md/`（带 `_md` 后缀），避免命中历史 `/MT` 产物。

### 5.3 最终编译命令（已验证）

```powershell
# configure（VS 自带 CMake，PowerShell 直接运行，不要经 cmd/bat）
& "C:\Program Files\Microsoft Visual Studio\2022\Community\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe" `
  -S C:\cef3-sdk -B C:\cef3-sdk\build_wrapper_x64_md `
  -G "Visual Studio 17 2022" -A x64 `
  -DCEF_RUNTIME_LIBRARY_FLAG=/MD

# build
& "...\cmake.exe" --build C:\cef3-sdk\build_wrapper_x64_md --config Release --target libcef_dll_wrapper
```

产物：`build_wrapper_x64_md\libcef_dll_wrapper\Release\libcef_dll_wrapper.lib`（/MD 版约 53.8 MB；早期 /MT 版约 102 MB，已废弃）。产物已同步复制到 `.lingbuilder/cef3-sdk/` 同结构路径。

> 注意：CEF 150 的 wrapper 源码目录名为 `libcef_dll/`，但 CMake 目标名仍是 `libcef_dll_wrapper`；产物路径探测同时覆盖 `libcef_dll_wrapper/Release/`、`libcef_dll/Release/` 等多个候选。

### 5.4 自动编译链路（代码化）

`nativeDependencyService.ts` 的 `ensureCefDllWrapper()` 完整实现了上述流程：

1. 先探测 `build_wrapper_<架构>_md/` 下的候选产物路径，命中则直接使用（本次实际路径）。
2. 未命中时查找 CMake（PATH 或 VS 2022/2019 各版本自带路径）。
3. `patchCefCMakeListsForCxxOnly()` 把 `project(cef)` patch 为 `project(cef LANGUAGES CXX)`。
4. `execFile` 直接调用 CMake configure（带 `-DCEF_RUNTIME_LIBRARY_FLAG=/MD`）+ build，约 1-3 分钟，仅首次。
5. 全部失败时输出中文诊断并优雅降级。

## 6. 构建链路运行时文件复制

### 6.1 根因三：浏览器白屏（缺 v8_context_snapshot.bin）

**现象**：编译链接全部通过、exe 启动、Chromium 多进程已拉起，但两个浏览器区域一直白屏。

**原因**：早期复制清单只有 `libcef.dll` 和 `chrome_elf.dll`。**`v8_context_snapshot.bin` 缺失时渲染进程无法启动，表现为白屏**。

**修复**：`nativeDependencyService.ts` 的复制清单拆分为必需 + 可选两组：

| 组 | 文件 | 缺失行为 |
| --- | --- | --- |
| 必需 | `libcef.dll`、`chrome_elf.dll`、`v8_context_snapshot.bin` | 输出中文诊断 |
| 可选（GPU/软渲染） | `d3dcompiler_47.dll`、`dxcompiler.dll`、`dxil.dll`、`libEGL.dll`、`libGLESv2.dll`、`vk_swiftshader.dll`、`vk_swiftshader_icd.json`、`vulkan-1.dll` | 静默跳过，不阻断构建 |
| 资源 | `Resources/` 整目录（`icudtl.dat`、`*.pak`、`locales/`） | 递归复制到 exe 目录 |

F5 / CLI 构建后 exe 输出目录（`.lingbuilder-build/<项目>/bin/`）最终包含全部 CEF 运行时文件，可整体复制迁移。

## 7. 生成器 CEF 150 API 适配

生成器模板（`lingCppWin32Project.ts`）经历两轮修复：

### 7.1 第一轮：转义与类型错误

| 问题 | 修复 |
| --- | --- |
| 生成的 C++ 出现 `\c` `\l` 非法转义（warning C4129），源于 TS 模板字符串中 `\\` 输出为单反斜杠 | **TS 模板中生成 C++ `\\` 字面量必须写四反斜杠 `\\\\`**（如 `L".cef3\\\\cache"`、`L"\\\\locales"`） |
| `std::wstring absoluteCache[MAX_PATH]` 被当作 `GetFullPathNameW` 第 3 参（C2664） | 改为 `wchar_t absoluteCache[MAX_PATH] = {};` |

### 7.2 第二轮：CEF 150 API 变化

| CEF 150 变化 | 生成器适配 |
| --- | --- |
| `CefWindowInfo::SetAsChild` 第二参从 `RECT` 改为 `const CefRect&` | 先构造 `CefRect cefBounds(l, t, w, h)` 再传入 |
| `CefBrowserSettings.mute_audio` 已删除 | 改为 `CEF3_通知已创建` 中 `browser->GetHost()->SetAudioMuted(true)` |
| `CefBrowserSettings.user_agent` 已删除（UA 只能全局） | `CEF3_初始化` 扫描第一个配置了 UA 的控件，写入全局 `CefSettings.user_agent` |
| `LingCefClient` 访问 `LingWindowBase::CEF3_通知*` protected 成员（C2248） | 类内加 `friend class LingCefClient;` + 前向声明 `class LingCefClient;` |

### 7.3 生命周期（沿用既有设计）

- `wWinMain` 入口最先执行 `CefExecuteProcess` 子进程分流（同一 exe 既是主进程又是渲染/GPU 子进程，`browser_subprocess_path` 指向自身）。
- `settings.multi_threaded_message_loop = true`、`no_sandbox = true`。
- 窗口 `WM_SIZE` 时 `CEF3_调整全部大小` 自适应；关闭时 `CEF3_关闭全部` + `CefShutdown`。

## 8. CLI 生成示例：cef3-cli-test

### 8.1 项目结构

| 文件 | 说明 |
| --- | --- |
| `examples/cef3-cli-test/cef3-build-request.json` | 构建请求：窗口 1180×640，两个 CefBrowser 控件 |
| `examples/cef3-cli-test/src/CEF3测试窗体.lcpp` | 中文源码（事件绑定 + 导航 + 回调） |
| `examples/cef3-cli-test/README.md` | 示例说明与验收标准 |
| `.lingbuilder/projects/cef3-cli-test/project-modules.json` | 启用 `lingbuilder.win32.basic` + `lingbuilder.cef3.browser` |
| `electron/scratch/cef3_build_run.cjs` | POST `/api/ai-bridge/build/run` 的测试脚本 |

窗口模型：

| 控件名 | 位置 | 打开地址 | 缓存目录 |
| --- | --- | --- | --- |
| 浏览器1 | (10, 10) 560×560 | https://www.baidu.com | `.cef3/cache-1` |
| 浏览器2 | (590, 10) 560×560 | https://www.bing.com | `.cef3/cache-2` |

### 8.2 中文源码要点

```text
包 CEF3CLI测试
使用 Win32窗口基础模块
使用 CEF3浏览器模块

类 CEF3测试窗体 : 窗口
公开
  事件 _CEF3测试窗体_创建完毕()
    CEF3_绑定事件("浏览器1", "加载完成", &浏览器1_加载完成)
    CEF3_绑定事件("浏览器2", "加载完成", &浏览器2_加载完成)
    CEF3_绑定事件("浏览器1", "标题被改变", &浏览器1_标题改变)
    CEF3_导航("浏览器1", "https://www.baidu.com")
    CEF3_导航("浏览器2", "https://www.bing.com")
    调试输出("两个 CEF3 浏览器已开始导航")
  结束

  事件 浏览器1_加载完成()
    @ 调试输出((L"事件回调：浏览器1加载完成 地址=" + CEF3_取地址(L"浏览器1")).c_str());
  结束
  ……
结束类
```

（`@` 前缀为原生 C++ 行；独立一行 `结束` 是块结束标记，不是退出命令。）

### 8.3 构建与运行步骤

```powershell
# 1. 启动 AI Bridge（preview 权限）
cd electron
node dist/cli.cjs ai-server --workspace .. --port 18459 --permission preview --token <token>

# 2. 提交构建（请求体含 project 窗口模型、activeWindowId、lingCppSourceCode、approved=true）
node scratch\cef3_build_run.cjs
```

构建产物：

- `.lingbuilder-build/cef3-cli-test/`：C++ 源码、`cef3-cli-test.sln`、`bin/LingBuilderPreview.exe` + 全套 CEF 运行时。
- `generated/cpp/cef3-cli-test/`：可复制迁移的 Visual Studio 工程。

## 9. 最终验证结果（2026-07-26）

| 验收项 | 结果 |
| --- | --- |
| MSVC (cl, x64, /MD, C++20) 编译链接 | ✅ 通过 |
| exe 同目录 CEF 运行时完整（含 `v8_context_snapshot.bin`、`locales/`） | ✅ |
| exe 启动 ≥3 秒仍在运行 | ✅（持续运行） |
| Chromium 多进程架构 | ✅ 启动后出现 10 个同名子进程（主进程 + 渲染/GPU/网络等） |
| 浏览器真实渲染网页 | ✅ 左侧百度、右侧必应真实加载（用户截图确认） |
| `test:lingcpp` 回归 | ✅ 124 通过 / 1 失败（失败项为 DiffViewer UI 断言，与 CEF3 改动无关） |

### 9.1 2026-07-31 安全覆盖 alpha 增量验证

- 覆盖 v2 扫描 287 个 SDK 头并登记 1577 项记录；当前 265 implemented、8 internal、185 notApplicable、1119 planned，169 个测试签名逐项排除，完成门禁会按预期拒绝发布“全覆盖”结论。
- `LingBuilderCefBridge` v3 已用 C++20/MSVC x64 Release 真实链接 CEF 150 与 wrapper，并通过句柄、任务、缓冲、路径边界、核心浏览器和导出符号检查；SDK 发布清单包含 Bridge 头文件、导入库、DLL、版本和 SHA-256。
- `lingbuilder.cef3.objects` 已提供 6 条真实受管缓冲命令。F5、AI Bridge 和 Visual Studio 导出生成代码会校验 Bridge ABI、链接 `LingBuilderCefBridge.lib`，并把 `LingBuilderCefBridge.dll` 复制到 exe 同目录。
- `cef3-cli-test` 双实例持续运行 15 秒，两个 profile 目录相互独立；页面加载后执行 `document.title` 返回真实 DevTools JSON。

## 10. 已知限制与后续事项

- **进程与会话语义**：CEF3 同 exe 共享 Chromium 进程；每控件使用独立 `CefRequestContext`，缓存映射到全局 `root_cache_path` 的直接子目录。
- **UA 全局**：CEF 150 起 User-Agent 只能全局设置，多控件不同 UA 不受支持。
- **wrapper 全自动首次编译未实测**：本次 wrapper 为手动预编译（构建时直接命中产物路径）；`ensureCefDllWrapper` 的 execFile 自动链路理论可行但未在干净环境验证。
- **仅支持 x64**：Win32 target 已删除；Windows MSVC x64 以外的平台必须明确报不支持。
- **优雅降级保留**：缺少 SDK 时 exe 仍可编译运行，浏览器区域为空白占位并输出中文诊断。
- `lingbuilder.cef3.objects` 现有 183 条命令并已清零模块内 planned：Value、Dictionary、List、Binary、Image、NavigationEntry、MenuModel、X509Certificate/Principal/SSLStatus 均使用类型化受管句柄；容器边界深复制，Image/Menu 自动投递 UI 线程，导航历史以任务 JSON 返回。错误类型返回 `-8`，重复释放返回 `-7`。`lingbuilder.cef3.session` 现有 19 条命令并已清零模块内 planned，真实接通独立 RequestContext、Preference、HTTP 缓存、Cookie CRUD/落盘、证书例外、HTTP 认证和连接清理。
- 下载、权限、新窗口等已进入 92 项兼容事件目录；Bridge 已真实接通 27 个上游事件签名，包括生命周期、加载、标题/地址、弹窗、显示、控制台、进度、焦点和键盘。同步事件现在从窗口处理器回传 action/返回文本，事件字段 JSON 会解析为 `CEF3_取事件字段` 可读取的顶层字段。其余 1119 个 planned 上游签名（其中事件 86 项）仍属于后续事项。

## 11. 分发方案：打包给其他用户能否直接运行？

需要区分两类用户：

### 11.1 用生成 exe 的最终用户 —— ✅ 开箱即用

exe 同目录已带全套 CEF 运行时（`libcef.dll`、`v8_context_snapshot.bin`、`*.pak`、`locales/` 等），整个 `bin/` 目录复制走即可运行；唯一系统依赖是 VC++ 2015-2022 运行库（因为 `/MD` 动态 CRT）。

### 11.2 用 LingBuilder 开发的用户（IDE 用户）—— 目前有 3 个前置条件

| 依赖 | 现状 | 说明 |
| --- | --- | --- |
| MSVC / VS Build Tools | 本来就需要 | LingBuilder F5 编译任何项目都要 cl，不算 CEF3 额外负担 |
| CEF SDK（解压后约 700MB） | ❌ 不随 IDE 分发 | 用户必须自己下载 `cef_binary_150...windows64.tar` 解压到 `.lingbuilder/cef3-sdk` 或 `C:\cef3-sdk` |
| CMake 编译 wrapper | ⚠️ 半自动 | 首次构建自动触发（VS 2022 自带 CMake 即可），但全自动链路尚未在干净机器实测 |

结论：**目前 IDE 用户不能完全开箱即用**，模块定义是内置的，但 SDK 不随 IDE 分发。

### 11.3 后期改进方案对比

| 方案 | 做法 | 优点 | 缺点 |
| --- | --- | --- | --- |
| **方案 A（✅ 2026-07-26 已实施）：预编译 SDK 包** | 把 `include/ + Release/ + Resources/ + 已编译的 /MD wrapper.lib` 打成离线 SDK 模块包（实测 184MB），安装模块时一并落盘 | 用户免下载、免 CMake、免编译；符合 AGENTS.md“模块安装要简单”的交付目标 | 包体大；需为每个架构/内核版本单独打包 |
| **方案 B：IDE 内下载向导** | 检测到 SDK 缺失时提供“一键下载 CEF3 SDK”（从官方 CDN 下载、解压、自动编译 wrapper） | IDE 安装包体积小 | 依赖网络与 CMake；首次体验慢 |

> 关键事实：wrapper 是静态库，只要 CEF 版本 + MSVC 工具链 + `/MD` CRT 一致就可以直接分发预编译产物，用户侧不需要 CMake。

### 11.4 方案 A 实施细节（2026-07-26 已落地验证）

**SDK 载体模块**：`lingbuilder.cef3.sdk`（CEF3 内核SDK包 (x64)），v2 manifest 纯资产模块 —— 不提供中文命令/控件，无需为项目启用，安装即生效。版本号自动从 `include/cef_version.h` 读取（`150.0.14+g7c1aa68+chromium-150.0.7871.129`）。

**模块包结构**（`cef3-sdk-x64.lbmod`，实测 184MB / 解压 447.6MB / 532 文件）：

```text
lingbuilder.module.json     # v2 manifest（schemaVersion 2，无 targets/commands）
README.md
sdk/
  include/                  # CEF 头文件
  Release/                  # libcef.lib + 全部运行时 DLL（含 v8_context_snapshot.bin）
  Resources/                # icudtl.dat、*.pak、locales/
  build_wrapper_x64_md/libcef_dll_wrapper/Release/libcef_dll_wrapper.lib  # 预编译 /MD wrapper
  README.txt                # CEF 官方说明/许可信息
```

**打包命令**：`cd electron && npm run module:cef3-sdk -- --install`（脚本 `electron/scripts/generate-cef3-sdk-module.cjs`；默认从 `C:\cef3-sdk` 读取，支持 `--source <路径>` 或 `CEF3_SDK_ROOT`；来源缺少预编译 wrapper 时报错并给出完整 CMake 编译指引）。产出：`.lingbuilder/module-packages/cef3-sdk-x64.lbmod`；`--install` 直接落盘到 `.lingbuilder/modules/lingbuilder.cef3.sdk/`。

**构建链路接入**：`findCef3SdkRoot()` 发现顺序扩展为：`CEF3_SDK_ROOT` → 工作区 `.lingbuilder/cef3-sdk` → **`.lingbuilder/modules/lingbuilder.cef3.sdk/sdk`**（新增） → `C:\cef3-sdk`；工作区根目录从 `layout.buildDir`（`<ws>/.lingbuilder-build/<项目>`）反推，不再依赖进程 cwd。预编译 wrapper 路径与 `ensureCefDllWrapper` 探测路径一致，命中后直接使用、不触发 CMake。

**其它配套改动**：`moduleService.ts` 的 `.lbmod` 包上限从 100MB 放宽到 1GB；SDK 缺失诊断文案更新为优先推荐安装离线模块包。

**发布完整性门禁**：Windows 发布使用 `electron/scripts/verify-cef3-release-sdk.cjs` 校验源 SDK 的模块 ID、schema、CEF 版本、关键文件大小、x64 PE 架构、最低文件数/体积，并为全部文件计算路径、大小和 CRC32。Electron Builder 的 `beforePack` 与 `afterPack` 分别检查源目录和 `win-unpacked`；`package:win` 完成 NSIS 后再通过 7-Zip 读取最终安装包归档并逐文件比对。任何 SDK 缺失、损坏、版本错配或漏复制都会中断发布。

**验证结果**（2026-07-26）：

| 验证项 | 结果 |
| --- | --- |
| `.lbmod` 走正式安装链路（previewPackageInstall → installPackage） | ✅ 532 文件，canInstall=true，安装成功 |
| 挡住 `.lingbuilder/cef3-sdk` 与 `C:\cef3-sdk` 后全新目录构建 | ✅ 仅凭模块 SDK 编译链接成功（未触发 CMake） |
| exe 启动后 Chromium 多进程持续运行 | ✅ 9 个同名进程 |
| 模块回归测试 `tests/modules.test.ts` | ✅ 34/34 通过 |

## 12. 内核升级流程（升级官方 Chromium 版本）

CEF 跟随 Chromium 每个大版本发布（150 → 151 → …），升级可行，但有四条硬约束：

1. **不能只换 DLL** —— `include/` 头文件、`libcef.lib`、`libcef_dll_wrapper.lib`、`libcef.dll`、资源文件必须**同版本整体替换**。CEF 运行时有 `cef_api_hash` 校验，版本不匹配会直接拒绝初始化。
2. **wrapper 必须重编译** —— 换版本后重跑一次 CMake；现有 `-DCEF_RUNTIME_LIBRARY_FLAG=/MD` + CXX-only patch 链路可复用（`ensureCefDllWrapper` 已代码化）。
3. **API 可能有 breaking change** —— 150 就移除了 `mute_audio`/`user_agent`、改了 `SetAsChild` 签名。好在生成器模板是集中式确定性规则，升级时只需适配 `lingCppWin32Project.ts` 一处。
4. **工具链要求会水涨船高** —— 150 已要求 C++20；未来版本可能提高 MSVC 最低版本。

### 12.1 标准升级步骤（每次换内核照此执行）

```text
1. 从官方 CDN 下载新版 cef_binary_<版本>_windows64
2. 整体替换 SDK 目录（.lingbuilder/cef3-sdk 或 C:\cef3-sdk）
3. 删除旧 build_wrapper_*_md/ 目录，触发 wrapper 重编译
4. 构建 cef3-cli-test 示例
5. 有编译错误 → 适配 lingCppWin32Project.ts 生成器模板（只改规则模块，不硬编码新分支）
6. 验证 exe 启动 ≥3 秒仍运行、多进程拉起、浏览器真实渲染网页
7. 更新本文档版本号与相关文档（规则手册、FUTURE_OPTIMIZATIONS、electron/README）
```

### 12.2 版本管理建议（待实施）

- 构建时读取 `include/cef_version.h` 把 CEF 版本写进诊断日志，避免用户混用不同版本的 SDK 与 wrapper 产物。
- wrapper 产物目录可追加版本标记，SDK 替换后自动判定需要重编译。

## 13. 相关文件索引

| 文件 | 角色 |
| --- | --- |
| `electron/src/services/modules/builtinModules.ts` | 模块 v2 manifest（控件、命令、binding、targets） |
| `electron/src/services/modules/nativeDependencyService.ts` | SDK 发现、wrapper 自动编译（CXX patch + /MD）、运行时文件复制 |
| `electron/src/services/windowDesigner/lingCppWin32Project.ts` | CEF3 C++ 运行时生成模板（CEF 150 API 适配） |
| `examples/cef3-cli-test/` | CLI 示例项目（请求、源码、README） |
| `electron/scratch/cef3_build_run.cjs` | 构建验证脚本 |
| `LingBuilder AI 规则手册.md` | CEF3 AI 侧使用规则（命令、事件、单实例语义） |
| `AI_BRIDGE_CLI_USAGE.md` §5.2 | CLI 测试项目验收标准 |
| `FUTURE_OPTIMIZATIONS.md` | 2026-07-26 落地记录与后续事项 |
| `electron/README.md` | 模块能力与构建链路摘要 |
