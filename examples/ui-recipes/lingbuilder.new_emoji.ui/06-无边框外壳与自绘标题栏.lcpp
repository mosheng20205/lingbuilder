// 配方：无边框外壳窗口（自绘标题栏 + 地址栏 + 标签页 + 按钮下拉菜单）
// 适用：浏览器外壳、应用商店壳、需要自绘标题栏的工具窗口。
// 项目模型要求：窗口 designerBackend="new-emoji"，窗口本身用无边框/自定义框架预设创建；
//              缩放边框与拖拽区必须在 创建完毕 里先设，再放标题栏控件。
// 要点：最小化/最大化/关闭不自己写消息，用 NE_设置元素窗口命令(窗口, 按钮, 命令编号) 交给原生窗口过程。
包 无边框外壳演示
使用 new_emoji原生界面库

类 演示窗口 : 窗口
公开
  事件 创建完毕()
    NE_设置窗口缩放边框(当前窗口, 6, 6, 6, 6)
    NE_设置窗口拖拽区(当前窗口, 0, 0, 900, 52, 真)
    局部 NE地址栏 地址栏 = 控件_创建NE地址栏(当前窗口, 120, 10, 560, 36, "地址栏", "地址栏", 1)
    NE地址栏_设置安全状态(地址栏, 1, "https://lingbuilder.com")
    NE地址栏_绑定提交回调(地址栏, &地址被提交)
    局部 NE图标按钮 最小化按钮 = 控件_创建NE图标按钮(当前窗口, 790, 10, 32, 32, "➖", "最小化按钮", 11)
    局部 NE图标按钮 关闭按钮 = 控件_创建NE图标按钮(当前窗口, 830, 10, 32, 32, "✕", "关闭按钮", 12)
    NE_设置元素窗口命令(当前窗口, 最小化按钮, 1)
    NE_设置元素窗口命令(当前窗口, 关闭按钮, 3)
    局部 NE标签页 页签 = 控件_创建NE标签页(当前窗口, 24, 64, 840, 60, "页签", "页签", 2)
    NE标签页_设置标签项(页签, "标签页 1|标签页 2|标签页 3")
    NE标签页_绑定选择变化(页签, &页签选择变化)
    局部 NE按钮 菜单按钮 = 控件_创建NE按钮(当前窗口, 24, 140, 160, 44, "打开菜单", "菜单按钮", 13)
    局部 NE菜单 弹出菜单 = 控件_创建NE菜单(当前窗口, 24, 200, 260, 300, "弹出菜单", "弹出菜单", 3)
    NE菜单_设置项目(弹出菜单, “🆕 新标签页\n📂 打开文件\n🔗 复制链接\n🖨 打印”)
    NE菜单_绑定菜单命令(弹出菜单, &菜单命令被选择)
    NE_EU_SetElementPopup(当前窗口, 菜单按钮, 弹出菜单, 1)
    NE按钮_绑定被点击(菜单按钮, &菜单按钮被点击)
  结束

  事件 地址被提交(文本型 地址)
    调试输出("提交地址 " + 地址)
  结束

  事件 页签选择变化(整数型 选中索引, 整数型 项目数量, 整数型 动作)
    调试输出("页签切换", 选中索引, 项目数量, 动作)
  结束

  事件 菜单按钮被点击()
    局部 NE菜单 弹出菜单 = 通过标记文本获取NE菜单("弹出菜单")
    NE_EU_SetPopupOpen(当前窗口, 弹出菜单, 1)
  结束

  事件 菜单命令被选择(整数型 项目索引, 文本型 菜单路径, 文本型 命令)
    调试输出("菜单 " + 菜单路径)
  结束
结束类
