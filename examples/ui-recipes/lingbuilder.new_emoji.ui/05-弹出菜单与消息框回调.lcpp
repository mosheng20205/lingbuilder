// 配方：弹出菜单（NE菜单）+ 消息框回调
// 适用：左侧导航、右键菜单、按钮下拉菜单，以及操作后的结果确认弹窗。
// 要点：菜单项用换行分隔，前缀 > 表示子菜单；处理器参数必须写显式中文类型；
//      消息框结果处理器签名为 (整数型 消息框编号, 整数型 结果)，结果值 1 确认、2 取消/关闭。
包 弹出菜单演示
使用 new_emoji原生界面库

类 演示窗口 : 窗口
公开
  事件 创建完毕()
    局部 NE菜单 主菜单 = 控件_创建NE菜单(当前窗口, 24, 60, 260, 420, "主菜单", "主菜单", 1)
    NE菜单_设置项目(主菜单, “🏠 首页\n📊 项目\n>📋 设计稿\n>🧪 测试报告\n⚙️ 设置\n❓ 帮助”)
    NE菜单_设置项目图标(主菜单, 0, "⭐")
    NE菜单_设置项目快捷键(主菜单, 5, "F1")
    NE菜单_绑定菜单命令(主菜单, &菜单命令被选择)
    NE菜单_绑定右键菜单(主菜单, &菜单右键被按下)
    局部 NE文本 状态 = 控件_创建NE文本(当前窗口, 320, 60, 500, 60, "选择左侧菜单项后看输出面板。", "状态", 3)
    局部 NE按钮 确认按钮 = 控件_创建NE按钮(当前窗口, 320, 130, 240, 44, "❓ 弹出确认框", "确认", 11)
    局部 NE按钮 投递按钮 = 控件_创建NE按钮(当前窗口, 320, 194, 240, 44, "投递：精简菜单", "投递", 12)
    NE按钮_绑定被点击(确认按钮, &确认被点击)
    NE按钮_绑定被点击(投递按钮, &投递被点击)
  结束

  事件 菜单命令被选择(整数型 项目索引, 文本型 菜单路径, 文本型 命令)
    调试输出("菜单选择", 项目索引, "路径=" + 菜单路径)
    局部 NE文本 状态 = 通过标记文本获取NE文本("状态")
    控件_设置文本(状态, "当前菜单：" + 菜单路径)
  结束

  事件 菜单右键被按下(整数型 项目索引, 整数型 附加值一, 整数型 附加值二)
    调试输出("菜单右键 索引=" + 到文本(项目索引))
  结束

  事件 确认被点击()
    NE_显示消息框(当前窗口, "清空列表", "确认要清空当前所有条目吗？", "知道了", &确认结果)
  结束

  事件 确认结果(整数型 消息框编号, 整数型 结果)
    局部 NE文本 状态 = 通过标记文本获取NE文本("状态")
    如果 (结果 == 1)
      控件_设置文本(状态, "✅ 已确认清空")
    否则
      控件_设置文本(状态, "✋ 已取消")
    如果结束
  结束

  事件 投递被点击()
    局部 NE菜单 主菜单 = 通过标记文本获取NE菜单("主菜单")
    NE菜单_投递项目(主菜单, “精简模式”)
    局部 NE文本 状态 = 通过标记文本获取NE文本("状态")
    控件_设置文本(状态, "已投递菜单项：精简模式")
  结束
结束类
