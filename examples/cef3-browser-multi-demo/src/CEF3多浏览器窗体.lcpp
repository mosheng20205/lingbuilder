包 CEF3 多浏览器示例
使用 Win32窗口基础模块
使用 CEF3浏览器模块

类 CEF3多浏览器窗体 : 窗口
    事件 _CEF3多浏览器窗体_创建完毕()
        CEF3_创建(浏览器1)
        CEF3_创建(浏览器2)
        CEF3_绑定事件(浏览器1, "加载完成", &浏览器1_加载完成)
        CEF3_绑定事件(浏览器2, "加载完成", &浏览器2_加载完成)
        CEF3_绑定事件(浏览器1, "加载状态改变", &浏览器1_加载状态改变)
        CEF3_绑定事件(浏览器2, "加载状态改变", &浏览器2_加载状态改变)
        调试输出("CEF3 多浏览器已创建：浏览器1、浏览器2")
    结束

    事件 浏览器1_加载完成()
        调试输出("浏览器1 加载完成：", CEF3_取地址(浏览器1))
    结束

    事件 浏览器2_加载完成()
        调试输出("浏览器2 加载完成：", CEF3_取地址(浏览器2))
    结束

    事件 浏览器1_加载状态改变()
        如果 (CEF3_是否加载中(浏览器1) == 0)
            调试输出("浏览器1 标题：", CEF3_执行JS(浏览器1, "document.title"))
        如果结束
    结束

    事件 浏览器2_加载状态改变()
        如果 (CEF3_是否加载中(浏览器2) == 0)
            调试输出("浏览器2 标题：", CEF3_执行JS(浏览器2, "document.title"))
        如果结束
    结束
结束类
