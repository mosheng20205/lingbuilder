包 FBro浏览器简单示例
使用 Win32窗口基础模块
使用 FBro指纹浏览器模块
使用 FBro事件模块

类 FBro浏览器简单窗体 : 窗口
公开
  事件 _FBro浏览器简单窗体_创建完毕()
    FBro_绑定事件(浏览器1, "加载状态改变", &浏览器1_加载状态改变)
    FBro_导航(浏览器1, "https://example.com")
    调试输出("FBro 浏览器已开始导航")
  结束

  事件 浏览器1_加载状态改变()
    如果 (FBro_是否加载中(浏览器1) == 0)
      调试输出("FBro 页面标题：", FBro_执行JS(浏览器1, "document.title"))
      调试输出("FBro 当前地址：", FBro_取地址(浏览器1))
    如果结束
  结束
结束类
