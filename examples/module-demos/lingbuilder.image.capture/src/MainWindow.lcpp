包 屏幕截图模块完整演示
使用 Win32窗口基础模块
使用 屏幕截图模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "屏幕截图模块：3 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.image.capture")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–3：截图_主屏到PNG 至 截图_窗口到PNG；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：截图_主屏到PNG
            // [1] 截图_主屏到PNG(目标路径)
            // 功能：截取主显示器并保存 PNG。
            // 参数：目标路径:wideString；返回：bool；可见性：default
            截图_主屏到PNG("module-demo-temp.txt")
            // 命令ID：截图_区域到PNG
            // [2] 截图_区域到PNG(目标路径, 横坐标, 纵坐标, 宽度, 高度)
            // 功能：截取指定屏幕区域。
            // 参数：目标路径:wideString，横坐标:int，纵坐标:int，宽度:int，高度:int；返回：bool；可见性：default
            截图_区域到PNG("module-demo-temp.txt", 1, 1, 1, 1)
            // 命令ID：截图_窗口到PNG
            // [3] 截图_窗口到PNG(目标路径, 窗口句柄)
            // 功能：截取窗口边界区域。
            // 参数：目标路径:wideString，窗口句柄:handle；返回：bool；可见性：default
            截图_窗口到PNG("module-demo-temp.txt", 0)
        如果结束
        调试输出("已查看 命令 1–3：截图_主屏到PNG 至 截图_窗口到PNG；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
