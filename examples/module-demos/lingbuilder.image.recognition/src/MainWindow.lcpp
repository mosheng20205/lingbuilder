包 基础识图模块完整演示
使用 Win32窗口基础模块
使用 基础识图模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "基础识图模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.image.recognition")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–5：识图_查找颜色 至 识图_颜色相似度；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：识图_查找颜色
            // [1] 识图_查找颜色(图片路径, ARGB, 容差)
            // 功能：从左到右、从上到下查找接近指定颜色的像素。
            // 参数：图片路径:wideString，ARGB:longLong，容差:int；返回：bool；可见性：default
            识图_查找颜色("module-demo-temp.txt", 1, 1)
            // 命令ID：识图_模板匹配
            // [2] 识图_模板匹配(图片路径, 模板路径, 容差)
            // 功能：执行朴素像素模板匹配，适合小图和测试。
            // 参数：图片路径:wideString，模板路径:wideString，容差:int；返回：bool；可见性：default
            识图_模板匹配("module-demo-temp.txt", "module-demo-temp.txt", 1)
            // 命令ID：识图_取结果横坐标
            // [3] 识图_取结果横坐标()
            // 功能：返回最近识图结果横坐标，失败为 -1。
            // 参数：无；返回：int；可见性：default
            识图_取结果横坐标()
            // 命令ID：识图_取结果纵坐标
            // [4] 识图_取结果纵坐标()
            // 功能：返回最近识图结果纵坐标，失败为 -1。
            // 参数：无；返回：int；可见性：default
            识图_取结果纵坐标()
            // 命令ID：识图_颜色相似度
            // [5] 识图_颜色相似度(ARGB一, ARGB二)
            // 功能：返回 RGB 最大通道差值，0 表示完全相同。
            // 参数：ARGB一:longLong，ARGB二:longLong；返回：int；可见性：default
            识图_颜色相似度(1, 1)
        如果结束
        调试输出("已查看 命令 1–5：识图_查找颜色 至 识图_颜色相似度；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
