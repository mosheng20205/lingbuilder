包 XML文本模块完整演示
使用 Win32窗口基础模块
使用 XML文本模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "XML 文本模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.data.xml")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–5：XML_转义文本 至 XML_取节点文本；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：XML_转义文本
            // [1] XML_转义文本(文本)
            // 功能：转义 XML 文本节点中的特殊字符。
            // 参数：文本:wideString；返回：wideString；可见性：default
            XML_转义文本("演示参数1")
            // 命令ID：XML_反转义文本
            // [2] XML_反转义文本(文本)
            // 功能：还原本模块支持的五种预定义 XML 实体。
            // 参数：文本:wideString；返回：wideString；可见性：default
            XML_反转义文本("演示参数1")
            // 命令ID：XML_生成节点
            // [3] XML_生成节点(节点名, 内容)
            // 功能：生成包含安全转义文本的简单 XML 节点；非法节点名返回空文本。
            // 参数：节点名:wideString，内容:wideString；返回：wideString；可见性：default
            XML_生成节点("演示参数1", "演示参数2")
            // 命令ID：XML_是否包含节点
            // [4] XML_是否包含节点(XML文本, 节点名)
            // 功能：判断 XML 文本是否包含指定简单节点。
            // 参数：XML文本:wideString，节点名:wideString；返回：bool；可见性：default
            XML_是否包含节点("演示参数1", "演示参数2")
            // 命令ID：XML_取节点文本
            // [5] XML_取节点文本(XML文本, 节点名)
            // 功能：读取第一个指定简单节点的文本内容并反转义。
            // 参数：XML文本:wideString，节点名:wideString；返回：wideString；可见性：default
            XML_取节点文本("演示参数1", "演示参数2")
        如果结束
        调试输出("已查看 命令 1–5：XML_转义文本 至 XML_取节点文本；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
