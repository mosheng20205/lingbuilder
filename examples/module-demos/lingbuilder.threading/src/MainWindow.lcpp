包 多线程模块完整演示
使用 Win32窗口基础模块
使用 多线程模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "多线程模块：9 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.threading")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–9：线程_启动延时输出 至 线程_批量启动；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：线程_启动延时输出
            // [1] 线程_启动延时输出(内容, 延时毫秒)
            // 功能：启动后台线程，等待指定毫秒后写入调试输出，并返回任务编号。
            // 参数：内容:wideString，延时毫秒:int；返回：int；可见性：default
            线程_启动延时输出("演示参数1", 1)
            // 命令ID：线程_等待全部
            // [2] 线程_等待全部()
            // 功能：等待当前窗口启动的全部后台任务结束并回收线程。
            // 参数：无；返回：void；可见性：default
            线程_等待全部()
            // 命令ID：线程_活动数量
            // [3] 线程_活动数量()
            // 功能：返回当前仍在执行的后台任务数量。
            // 参数：无；返回：int；可见性：default
            线程_活动数量()
            // 命令ID：线程_硬件并发数
            // [4] 线程_硬件并发数()
            // 功能：返回 C++ 运行时建议的并行线程数量。
            // 参数：无；返回：int；可见性：default
            线程_硬件并发数()
            // 命令ID：线程_休眠
            // [5] 线程_休眠(毫秒)
            // 功能：让当前线程休眠指定毫秒；界面线程中使用会暂停界面响应。
            // 参数：毫秒:int；返回：void；可见性：default
            线程_休眠(1)
            // 命令ID：线程_启动延时设置文本
            // [6] 线程_启动延时设置文本(控件名, 文本, 延时毫秒)
            // 功能：启动后台线程，延时后线程安全地设置指定控件的显示文本。
            // 参数：控件名:controlRef，文本:wideString，延时毫秒:int；返回：int；可见性：default
            线程_启动延时设置文本(演示状态, "演示参数2", 1)
            // 命令ID：线程_启动延时添加行
            // [7] 线程_启动延时添加行(控件名, Tab分隔单元格, 延时毫秒)
            // 功能：启动后台线程，延时后线程安全地向列表视图追加一行。
            // 参数：控件名:controlRef，Tab分隔单元格:wideString，延时毫秒:int；返回：int；可见性：default
            线程_启动延时添加行(演示ListView控件, "演示参数2", 1)
            // 命令ID：线程_启动延时添加项目
            // [8] 线程_启动延时添加项目(控件名, 文本, 延时毫秒)
            // 功能：启动后台线程，延时后线程安全地向列表框追加一条日志。
            // 参数：控件名:controlRef，文本:wideString，延时毫秒:int；返回：int；可见性：default
            线程_启动延时添加项目(演示ListBox控件, "演示参数2", 1)
            // 命令ID：线程_批量启动
            // [9] 线程_批量启动(任务数控件, 线程数控件, 列表视图, 日志列表, 状态标签)
            // 功能：读取输入框中的任务数和线程数，动态创建多线程并行执行任务，实时更新列表视图和日志。
            // 参数：任务数控件:controlRef，线程数控件:controlRef，列表视图:controlRef，日志列表:controlRef，状态标签:controlRef；返回：void；可见性：default
            线程_批量启动(演示TextBox控件, 演示TextBox控件, 演示ListView控件, 演示ListView控件, 演示状态)
        如果结束
        调试输出("已查看 命令 1–9：线程_启动延时输出 至 线程_批量启动；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
