包 FBroVIP指纹模块完整演示
使用 Win32窗口基础模块
使用 FBroVIP指纹模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "FBro VIP 指纹模块：3 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.fbro.vip")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–3：FBroVIP_应用指纹JSON 至 FBroVIP_清空调用次数；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：FBroVIP_应用指纹JSON
            // [1] FBroVIP_应用指纹JSON(控件名, JSON)
            // 功能：通过 UTF-16 JSON 应用受控 VIP 指纹配置；不会暴露 Key。
            // 参数：控件名:wideString，JSON:wideString；返回：int；可见性：advanced
            FBroVIP_应用指纹JSON("演示输出", "{}")
            // 命令ID：FBroVIP_取调用次数
            // [2] FBroVIP_取调用次数(控件名)
            // 功能：查询 VIP 指纹调用次数。
            // 参数：控件名:wideString；返回：wideString；可见性：advanced
            FBroVIP_取调用次数("演示输出")
            // 命令ID：FBroVIP_清空调用次数
            // [3] FBroVIP_清空调用次数(控件名)
            // 功能：清空 VIP 指纹调用次数。
            // 参数：控件名:wideString；返回：int；可见性：advanced
            FBroVIP_清空调用次数("演示输出")
        如果结束
        调试输出("已查看 命令 1–3：FBroVIP_应用指纹JSON 至 FBroVIP_清空调用次数；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
