包 CPU指令能力模块完整演示
使用 Win32窗口基础模块
使用 CPU指令能力模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "CPU指令能力模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.advanced.assembly")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–5：CPU_取厂商 至 位运算_循环左移；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：CPU_取厂商
            // [1] CPU_取厂商()
            // 功能：返回 CPUID 厂商标识。
            // 参数：无；返回：wideString；可见性：default
            CPU_取厂商()
            // 命令ID：CPU_是否支持SSE2
            // [2] CPU_是否支持SSE2()
            // 功能：检查 SSE2。
            // 参数：无；返回：bool；可见性：default
            CPU_是否支持SSE2()
            // 命令ID：CPU_是否支持AVX
            // [3] CPU_是否支持AVX()
            // 功能：检查 CPU 和操作系统是否启用 AVX。
            // 参数：无；返回：bool；可见性：default
            CPU_是否支持AVX()
            // 命令ID：位运算_统计一位数量
            // [4] 位运算_统计一位数量(数值)
            // 功能：统计 64 位数值中的 1 位数量。
            // 参数：数值:longLong；返回：int；可见性：default
            位运算_统计一位数量(1)
            // 命令ID：位运算_循环左移
            // [5] 位运算_循环左移(数值, 位数)
            // 功能：执行 64 位循环左移。
            // 参数：数值:longLong，位数:int；返回：longLong；可见性：default
            位运算_循环左移(1, 1)
        如果结束
        调试输出("已查看 命令 1–5：CPU_取厂商 至 位运算_循环左移；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
