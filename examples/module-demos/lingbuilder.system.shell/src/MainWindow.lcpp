包 系统外壳模块完整演示
使用 Win32窗口基础模块
使用 系统外壳模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "系统外壳模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.system.shell")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–5：系统_打开 至 系统_取文档目录；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：系统_打开
            // [1] 系统_打开(目标)
            // 功能：使用系统默认程序打开文件、目录或网址。
            // 参数：目标:wideString；返回：bool；可见性：default
            系统_打开("演示参数1")
            // 命令ID：系统_定位文件
            // [2] 系统_定位文件(路径)
            // 功能：在资源管理器中选中指定文件。
            // 参数：路径:wideString；返回：bool；可见性：default
            系统_定位文件("module-demo-temp.txt")
            // 命令ID：系统_取临时目录
            // [3] 系统_取临时目录()
            // 功能：返回当前用户临时目录。
            // 参数：无；返回：wideString；可见性：default
            系统_取临时目录()
            // 命令ID：系统_取桌面目录
            // [4] 系统_取桌面目录()
            // 功能：返回当前用户桌面目录。
            // 参数：无；返回：wideString；可见性：default
            系统_取桌面目录()
            // 命令ID：系统_取文档目录
            // [5] 系统_取文档目录()
            // 功能：返回当前用户文档目录。
            // 参数：无；返回：wideString；可见性：default
            系统_取文档目录()
        如果结束
        调试输出("已查看 命令 1–5：系统_打开 至 系统_取文档目录；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
