包 键盘输入模块完整演示
使用 Win32窗口基础模块
使用 键盘输入模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "键盘输入模块：31 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.input.keyboard")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–16：键盘_全局_键是否按下 至 键盘_前台_组合按键；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：键盘_全局_键是否按下
            // [1] 键盘_全局_键是否按下(虚拟键码)
            // 功能：[全局状态｜前后台均可调用｜不占用键盘] 读取当前桌面的异步键状态；不发送按键。
            // 参数：虚拟键码:int；返回：bool；可见性：default
            键盘_全局_键是否按下(1)
            // 命令ID：键盘_全局_切换状态
            // [2] 键盘_全局_切换状态(虚拟键码)
            // 功能：[全局状态｜前后台均可调用｜不占用键盘] 读取 Caps Lock、Num Lock、Scroll Lock 等切换键的开关位。
            // 参数：虚拟键码:int；返回：bool；可见性：default
            键盘_全局_切换状态(1)
            // 命令ID：键盘_全局_大小写锁定状态
            // [3] 键盘_全局_大小写锁定状态()
            // 功能：[全局状态｜前后台均可调用｜不占用键盘] 读取 Caps Lock 开关状态。
            // 参数：无；返回：bool；可见性：default
            键盘_全局_大小写锁定状态()
            // 命令ID：键盘_全局_数字锁定状态
            // [4] 键盘_全局_数字锁定状态()
            // 功能：[全局状态｜前后台均可调用｜不占用键盘] 读取 Num Lock 开关状态。
            // 参数：无；返回：bool；可见性：default
            键盘_全局_数字锁定状态()
            // 命令ID：键盘_全局_滚动锁定状态
            // [5] 键盘_全局_滚动锁定状态()
            // 功能：[全局状态｜前后台均可调用｜不占用键盘] 读取 Scroll Lock 开关状态。
            // 参数：无；返回：bool；可见性：default
            键盘_全局_滚动锁定状态()
            // 命令ID：键盘_全局_取前台窗口
            // [6] 键盘_全局_取前台窗口()
            // 功能：[全局状态｜前后台均可调用｜不占用键盘] 返回当前接收系统输入的前台顶层窗口 HWND，没有时返回 0。
            // 参数：无；返回：handle；可见性：default
            键盘_全局_取前台窗口()
            // 命令ID：键盘_全局_取前台焦点控件
            // [7] 键盘_全局_取前台焦点控件()
            // 功能：[全局状态｜前后台均可调用｜不占用键盘] 返回前台线程当前具有键盘焦点的子控件 HWND，无法取得时返回 0。
            // 参数：无；返回：handle；可见性：default
            键盘_全局_取前台焦点控件()
            // 命令ID：键盘_键名取键代码
            // [8] 键盘_键名取键代码(键名)
            // 功能：[本地转换｜不发送输入｜不占用键盘] 把常用键名或单字符转为 Win32 虚拟键码，无法识别时返回 0。
            // 参数：键名:wideString；返回：int；可见性：default
            键盘_键名取键代码("演示参数1")
            // 命令ID：键盘_键代码取键名
            // [9] 键盘_键代码取键名(虚拟键码)
            // 功能：[本地转换｜不发送输入｜不占用键盘] 使用当前 Windows 键盘布局返回键名，无法识别时返回空文本。
            // 参数：虚拟键码:int；返回：wideString；可见性：default
            键盘_键代码取键名(1)
            // 命令ID：键盘_键代码取扫描码
            // [10] 键盘_键代码取扫描码(虚拟键码)
            // 功能：[本地转换｜不发送输入｜不占用键盘] 使用当前键盘布局把虚拟键码转为扫描码。
            // 参数：虚拟键码:int；返回：int；可见性：default
            键盘_键代码取扫描码(1)
            // 命令ID：键盘_扫描码取键代码
            // [11] 键盘_扫描码取键代码(扫描码)
            // 功能：[本地转换｜不发送输入｜不占用键盘] 使用当前键盘布局把扫描码转为虚拟键码。
            // 参数：扫描码:int；返回：int；可见性：default
            键盘_扫描码取键代码(1)
            // 命令ID：键盘_键代码是否扩展键
            // [12] 键盘_键代码是否扩展键(虚拟键码)
            // 功能：[本地转换｜不发送输入｜不占用键盘] 判断虚拟键是否需要 KEYEVENTF_EXTENDEDKEY。
            // 参数：虚拟键码:int；返回：bool；可见性：default
            键盘_键代码是否扩展键(1)
            // 命令ID：键盘_前台_按下
            // [13] 键盘_前台_按下(虚拟键码)
            // 功能：[前台输入｜作用于当前焦点｜不独占实体键盘] 向系统输入流注入按下事件；必须与“放开”成对使用。
            // 参数：虚拟键码:int；返回：bool；可见性：default
            键盘_前台_按下(1)
            // 命令ID：键盘_前台_放开
            // [14] 键盘_前台_放开(虚拟键码)
            // 功能：[前台输入｜作用于当前焦点｜不独占实体键盘] 向系统输入流注入放开事件。
            // 参数：虚拟键码:int；返回：bool；可见性：default
            键盘_前台_放开(1)
            // 命令ID：键盘_前台_单击
            // [15] 键盘_前台_单击(虚拟键码)
            // 功能：[前台输入｜作用于当前焦点｜不独占实体键盘] 使用一个 SendInput 批次按下并放开指定虚拟键。
            // 参数：虚拟键码:int；返回：bool；可见性：default
            键盘_前台_单击(1)
            // 命令ID：键盘_前台_组合按键
            // [16] 键盘_前台_组合按键(主键码, 修饰键码1, 修饰键码2, 修饰键码3)
            // 功能：[前台输入｜作用于当前焦点｜不独占实体键盘] 批量按下最多三个修饰键和主键，再按相反顺序放开；未使用的修饰键传 0。
            // 参数：主键码:int，修饰键码1:int，修饰键码2:int，修饰键码3:int；返回：bool；可见性：default
            键盘_前台_组合按键(1, 1, 1, 1)
        如果结束
        调试输出("已查看 命令 1–16：键盘_全局_键是否按下 至 键盘_前台_组合按键；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 _运行第02组_被单击()
        控件_设置文本(演示状态, "命令 17–31：键盘_前台_输入文本 至 键盘_组合按键；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：键盘_前台_输入文本
            // [17] 键盘_前台_输入文本(文本)
            // 功能：[前台输入｜作用于当前焦点｜不独占实体键盘] 使用 KEYEVENTF_UNICODE 输入文本，不依赖当前键盘布局。
            // 参数：文本:wideString；返回：bool；可见性：default
            键盘_前台_输入文本("演示参数1")
            // 命令ID：键盘_前台_扫描码按下
            // [18] 键盘_前台_扫描码按下(扫描码, 扩展键)
            // 功能：[前台输入｜作用于当前焦点｜不独占实体键盘] 按物理扫描码注入按下事件，适用于与键盘布局无关的按键。
            // 参数：扫描码:int，扩展键:bool；返回：bool；可见性：default
            键盘_前台_扫描码按下(1, 真)
            // 命令ID：键盘_前台_扫描码放开
            // [19] 键盘_前台_扫描码放开(扫描码, 扩展键)
            // 功能：[前台输入｜作用于当前焦点｜不独占实体键盘] 按物理扫描码注入放开事件。
            // 参数：扫描码:int，扩展键:bool；返回：bool；可见性：default
            键盘_前台_扫描码放开(1, 真)
            // 命令ID：键盘_前台_扫描码单击
            // [20] 键盘_前台_扫描码单击(扫描码, 扩展键)
            // 功能：[前台输入｜作用于当前焦点｜不独占实体键盘] 按物理扫描码批量注入按下和放开事件。
            // 参数：扫描码:int，扩展键:bool；返回：bool；可见性：default
            键盘_前台_扫描码单击(1, 真)
            // 命令ID：键盘_窗口_取焦点控件
            // [21] 键盘_窗口_取焦点控件(窗口句柄)
            // 功能：[后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 读取指定顶层窗口线程的焦点子控件 HWND；不改变焦点。
            // 参数：窗口句柄:handle；返回：handle；可见性：default
            键盘_窗口_取焦点控件(0)
            // 命令ID：键盘_窗口_按下
            // [22] 键盘_窗口_按下(窗口句柄, 虚拟键码, 系统键)
            // 功能：[后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 向目标消息队列投递 WM_KEYDOWN 或 WM_SYSKEYDOWN；返回值只表示成功入队。
            // 参数：窗口句柄:handle，虚拟键码:int，系统键:bool；返回：bool；可见性：default
            键盘_窗口_按下(0, 1, 真)
            // 命令ID：键盘_窗口_放开
            // [23] 键盘_窗口_放开(窗口句柄, 虚拟键码, 系统键)
            // 功能：[后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 向目标消息队列投递 WM_KEYUP 或 WM_SYSKEYUP；不改变全局按键状态。
            // 参数：窗口句柄:handle，虚拟键码:int，系统键:bool；返回：bool；可见性：default
            键盘_窗口_放开(0, 1, 真)
            // 命令ID：键盘_窗口_单击
            // [24] 键盘_窗口_单击(窗口句柄, 虚拟键码, 系统键)
            // 功能：[后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 依次投递按下和放开消息；目标程序可能忽略或过滤后台消息。
            // 参数：窗口句柄:handle，虚拟键码:int，系统键:bool；返回：bool；可见性：default
            键盘_窗口_单击(0, 1, 真)
            // 命令ID：键盘_窗口_组合按键
            // [25] 键盘_窗口_组合按键(窗口句柄, 主键码, 修饰键码1, 修饰键码2, 修饰键码3, 系统键)
            // 功能：[后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 向同一目标按顺序投递最多三个修饰键和主键消息；不保证目标使用物理修饰键状态。
            // 参数：窗口句柄:handle，主键码:int，修饰键码1:int，修饰键码2:int，修饰键码3:int，系统键:bool；返回：bool；可见性：default
            键盘_窗口_组合按键(0, 1, 1, 1, 1, 真)
            // 命令ID：键盘_窗口_输入文本
            // [26] 键盘_窗口_输入文本(窗口句柄, 文本)
            // 功能：[后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 向指定 HWND 投递 WM_CHAR；适用于接收文本消息的控件，不等价于真实物理按键。
            // 参数：窗口句柄:handle，文本:wideString；返回：bool；可见性：default
            键盘_窗口_输入文本(0, "演示参数2")
            // 命令ID：键盘_键是否按下
            // [27] 键盘_键是否按下(虚拟键码)
            // 功能：[兼容入口] [全局状态｜前后台均可调用｜不占用键盘] 保留 1.x 源码兼容；等价于“键盘_全局_键是否按下”。
            // 参数：虚拟键码:int；返回：bool；可见性：default
            键盘_键是否按下(1)
            // 命令ID：键盘_大小写锁定状态
            // [28] 键盘_大小写锁定状态()
            // 功能：[兼容入口] [全局状态｜前后台均可调用｜不占用键盘] 保留 1.x 源码兼容；等价于全局 Caps Lock 状态查询。
            // 参数：无；返回：bool；可见性：default
            键盘_大小写锁定状态()
            // 命令ID：键盘_数字锁定状态
            // [29] 键盘_数字锁定状态()
            // 功能：[兼容入口] [全局状态｜前后台均可调用｜不占用键盘] 保留 1.x 源码兼容；等价于全局 Num Lock 状态查询。
            // 参数：无；返回：bool；可见性：default
            键盘_数字锁定状态()
            // 命令ID：键盘_单击
            // [30] 键盘_单击(虚拟键码)
            // 功能：[兼容入口] [前台输入｜作用于当前焦点｜不独占实体键盘] 保留 1.x 源码兼容；等价于“键盘_前台_单击”。
            // 参数：虚拟键码:int；返回：bool；可见性：default
            键盘_单击(1)
            // 命令ID：键盘_组合按键
            // [31] 键盘_组合按键(修饰键码, 主键码)
            // 功能：[兼容入口] [前台输入｜作用于当前焦点｜不独占实体键盘] 保留 1.x 源码兼容；仅支持一个修饰键，新代码应使用“键盘_前台_组合按键”。
            // 参数：修饰键码:int，主键码:int；返回：bool；可见性：default
            键盘_组合按键(1, 1)
        如果结束
        调试输出("已查看 命令 17–31：键盘_前台_输入文本 至 键盘_组合按键；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
