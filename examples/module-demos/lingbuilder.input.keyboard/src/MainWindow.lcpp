包 键盘输入模块完整演示
使用 Win32窗口基础模块
使用 键盘输入模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "键盘输入模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.input.keyboard")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–5：键盘_键是否按下 至 键盘_组合按键；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：键盘_键是否按下
            // [1] 键盘_键是否按下(虚拟键码)
            // 功能：判断指定 Win32 虚拟键当前是否按下。
            // 参数：虚拟键码:int；返回：bool；可见性：default
            键盘_键是否按下(1)
            // 命令ID：键盘_大小写锁定状态
            // [2] 键盘_大小写锁定状态()
            // 功能：读取 Caps Lock 切换状态。
            // 参数：无；返回：bool；可见性：default
            键盘_大小写锁定状态()
            // 命令ID：键盘_数字锁定状态
            // [3] 键盘_数字锁定状态()
            // 功能：读取 Num Lock 切换状态。
            // 参数：无；返回：bool；可见性：default
            键盘_数字锁定状态()
            // 命令ID：键盘_单击
            // [4] 键盘_单击(虚拟键码)
            // 功能：模拟一次按下并释放。
            // 参数：虚拟键码:int；返回：bool；可见性：default
            键盘_单击(1)
            // 命令ID：键盘_组合按键
            // [5] 键盘_组合按键(修饰键码, 主键码)
            // 功能：模拟修饰键与主键组合。
            // 参数：修饰键码:int，主键码:int；返回：bool；可见性：default
            键盘_组合按键(1, 1)
        如果结束
        调试输出("已查看 命令 1–5：键盘_键是否按下 至 键盘_组合按键；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
