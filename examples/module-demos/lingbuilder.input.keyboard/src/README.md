# 键盘输入模块完整演示

- 模块 ID：`lingbuilder.input.keyboard`
- 版本：`2.0.0`
- 类型：LingBuilder 内置模块
- 命令数：31
- 设计器控件数：0
- 分组数：2

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

分类提供全局状态、前台 SendInput 注入、指定 HWND 后台消息和键码转换能力。

## 命令清单

| # | 命令 | 签名 | 返回 | 说明 |
|---:|---|---|---|---|
| 1 | `键盘_全局_键是否按下` | `键盘_全局_键是否按下(虚拟键码)` | bool | [全局状态｜前后台均可调用｜不占用键盘] 读取当前桌面的异步键状态；不发送按键。 |
| 2 | `键盘_全局_切换状态` | `键盘_全局_切换状态(虚拟键码)` | bool | [全局状态｜前后台均可调用｜不占用键盘] 读取 Caps Lock、Num Lock、Scroll Lock 等切换键的开关位。 |
| 3 | `键盘_全局_大小写锁定状态` | `键盘_全局_大小写锁定状态()` | bool | [全局状态｜前后台均可调用｜不占用键盘] 读取 Caps Lock 开关状态。 |
| 4 | `键盘_全局_数字锁定状态` | `键盘_全局_数字锁定状态()` | bool | [全局状态｜前后台均可调用｜不占用键盘] 读取 Num Lock 开关状态。 |
| 5 | `键盘_全局_滚动锁定状态` | `键盘_全局_滚动锁定状态()` | bool | [全局状态｜前后台均可调用｜不占用键盘] 读取 Scroll Lock 开关状态。 |
| 6 | `键盘_全局_取前台窗口` | `键盘_全局_取前台窗口()` | handle | [全局状态｜前后台均可调用｜不占用键盘] 返回当前接收系统输入的前台顶层窗口 HWND，没有时返回 0。 |
| 7 | `键盘_全局_取前台焦点控件` | `键盘_全局_取前台焦点控件()` | handle | [全局状态｜前后台均可调用｜不占用键盘] 返回前台线程当前具有键盘焦点的子控件 HWND，无法取得时返回 0。 |
| 8 | `键盘_键名取键代码` | `键盘_键名取键代码(键名)` | int | [本地转换｜不发送输入｜不占用键盘] 把常用键名或单字符转为 Win32 虚拟键码，无法识别时返回 0。 |
| 9 | `键盘_键代码取键名` | `键盘_键代码取键名(虚拟键码)` | wideString | [本地转换｜不发送输入｜不占用键盘] 使用当前 Windows 键盘布局返回键名，无法识别时返回空文本。 |
| 10 | `键盘_键代码取扫描码` | `键盘_键代码取扫描码(虚拟键码)` | int | [本地转换｜不发送输入｜不占用键盘] 使用当前键盘布局把虚拟键码转为扫描码。 |
| 11 | `键盘_扫描码取键代码` | `键盘_扫描码取键代码(扫描码)` | int | [本地转换｜不发送输入｜不占用键盘] 使用当前键盘布局把扫描码转为虚拟键码。 |
| 12 | `键盘_键代码是否扩展键` | `键盘_键代码是否扩展键(虚拟键码)` | bool | [本地转换｜不发送输入｜不占用键盘] 判断虚拟键是否需要 KEYEVENTF_EXTENDEDKEY。 |
| 13 | `键盘_前台_按下` | `键盘_前台_按下(虚拟键码)` | bool | [前台输入｜作用于当前焦点｜不独占实体键盘] 向系统输入流注入按下事件；必须与“放开”成对使用。 |
| 14 | `键盘_前台_放开` | `键盘_前台_放开(虚拟键码)` | bool | [前台输入｜作用于当前焦点｜不独占实体键盘] 向系统输入流注入放开事件。 |
| 15 | `键盘_前台_单击` | `键盘_前台_单击(虚拟键码)` | bool | [前台输入｜作用于当前焦点｜不独占实体键盘] 使用一个 SendInput 批次按下并放开指定虚拟键。 |
| 16 | `键盘_前台_组合按键` | `键盘_前台_组合按键(主键码, 修饰键码1, 修饰键码2, 修饰键码3)` | bool | [前台输入｜作用于当前焦点｜不独占实体键盘] 批量按下最多三个修饰键和主键，再按相反顺序放开；未使用的修饰键传 0。 |
| 17 | `键盘_前台_输入文本` | `键盘_前台_输入文本(文本)` | bool | [前台输入｜作用于当前焦点｜不独占实体键盘] 使用 KEYEVENTF_UNICODE 输入文本，不依赖当前键盘布局。 |
| 18 | `键盘_前台_扫描码按下` | `键盘_前台_扫描码按下(扫描码, 扩展键)` | bool | [前台输入｜作用于当前焦点｜不独占实体键盘] 按物理扫描码注入按下事件，适用于与键盘布局无关的按键。 |
| 19 | `键盘_前台_扫描码放开` | `键盘_前台_扫描码放开(扫描码, 扩展键)` | bool | [前台输入｜作用于当前焦点｜不独占实体键盘] 按物理扫描码注入放开事件。 |
| 20 | `键盘_前台_扫描码单击` | `键盘_前台_扫描码单击(扫描码, 扩展键)` | bool | [前台输入｜作用于当前焦点｜不独占实体键盘] 按物理扫描码批量注入按下和放开事件。 |
| 21 | `键盘_窗口_取焦点控件` | `键盘_窗口_取焦点控件(窗口句柄)` | handle | [后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 读取指定顶层窗口线程的焦点子控件 HWND；不改变焦点。 |
| 22 | `键盘_窗口_按下` | `键盘_窗口_按下(窗口句柄, 虚拟键码, 系统键)` | bool | [后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 向目标消息队列投递 WM_KEYDOWN 或 WM_SYSKEYDOWN；返回值只表示成功入队。 |
| 23 | `键盘_窗口_放开` | `键盘_窗口_放开(窗口句柄, 虚拟键码, 系统键)` | bool | [后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 向目标消息队列投递 WM_KEYUP 或 WM_SYSKEYUP；不改变全局按键状态。 |
| 24 | `键盘_窗口_单击` | `键盘_窗口_单击(窗口句柄, 虚拟键码, 系统键)` | bool | [后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 依次投递按下和放开消息；目标程序可能忽略或过滤后台消息。 |
| 25 | `键盘_窗口_组合按键` | `键盘_窗口_组合按键(窗口句柄, 主键码, 修饰键码1, 修饰键码2, 修饰键码3, 系统键)` | bool | [后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 向同一目标按顺序投递最多三个修饰键和主键消息；不保证目标使用物理修饰键状态。 |
| 26 | `键盘_窗口_输入文本` | `键盘_窗口_输入文本(窗口句柄, 文本)` | bool | [后台窗口｜指定 HWND｜不抢焦点且不占用实体键盘] 向指定 HWND 投递 WM_CHAR；适用于接收文本消息的控件，不等价于真实物理按键。 |
| 27 | `键盘_键是否按下` | `键盘_键是否按下(虚拟键码)` | bool | [兼容入口] [全局状态｜前后台均可调用｜不占用键盘] 保留 1.x 源码兼容；等价于“键盘_全局_键是否按下”。 |
| 28 | `键盘_大小写锁定状态` | `键盘_大小写锁定状态()` | bool | [兼容入口] [全局状态｜前后台均可调用｜不占用键盘] 保留 1.x 源码兼容；等价于全局 Caps Lock 状态查询。 |
| 29 | `键盘_数字锁定状态` | `键盘_数字锁定状态()` | bool | [兼容入口] [全局状态｜前后台均可调用｜不占用键盘] 保留 1.x 源码兼容；等价于全局 Num Lock 状态查询。 |
| 30 | `键盘_单击` | `键盘_单击(虚拟键码)` | bool | [兼容入口] [前台输入｜作用于当前焦点｜不独占实体键盘] 保留 1.x 源码兼容；等价于“键盘_前台_单击”。 |
| 31 | `键盘_组合按键` | `键盘_组合按键(修饰键码, 主键码)` | bool | [兼容入口] [前台输入｜作用于当前焦点｜不独占实体键盘] 保留 1.x 源码兼容；仅支持一个修饰键，新代码应使用“键盘_前台_组合按键”。 |
