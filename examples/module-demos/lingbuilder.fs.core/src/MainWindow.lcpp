包 文件目录模块完整演示
使用 Win32窗口基础模块
使用 文件目录模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "文件目录模块：11 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.fs.core")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–11：文件_是否存在 至 目录_删除空目录；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：文件_是否存在
            // [1] 文件_是否存在(路径)
            // 功能：判断指定路径是否为普通文件。
            // 参数：路径:wideString；返回：bool；可见性：default
            文件_是否存在("module-demo-temp.txt")
            // 命令ID：目录_是否存在
            // [2] 目录_是否存在(路径)
            // 功能：判断指定路径是否为目录。
            // 参数：路径:wideString；返回：bool；可见性：default
            目录_是否存在("module-demo-temp.txt")
            // 命令ID：文件_读取文本
            // [3] 文件_读取文本(路径)
            // 功能：按 UTF-8 读取完整文本；失败返回空文本。
            // 参数：路径:wideString；返回：wideString；可见性：default
            文件_读取文本("module-demo-temp.txt")
            // 命令ID：文件_写入文本
            // [4] 文件_写入文本(路径, 内容)
            // 功能：按 UTF-8 覆盖写入文本文件。
            // 参数：路径:wideString，内容:wideString；返回：bool；可见性：default
            文件_写入文本("module-demo-temp.txt", "演示参数2")
            // 命令ID：文件_追加文本
            // [5] 文件_追加文本(路径, 内容)
            // 功能：按 UTF-8 向文件末尾追加文本。
            // 参数：路径:wideString，内容:wideString；返回：bool；可见性：default
            文件_追加文本("module-demo-temp.txt", "演示参数2")
            // 命令ID：文件_复制
            // [6] 文件_复制(来源, 目标, 允许覆盖)
            // 功能：复制文件并可选择是否覆盖。
            // 参数：来源:wideString，目标:wideString，允许覆盖:bool；返回：bool；可见性：default
            文件_复制("演示参数1", "演示参数2", 真)
            // 命令ID：文件_移动
            // [7] 文件_移动(来源, 目标, 允许覆盖)
            // 功能：移动或重命名文件。
            // 参数：来源:wideString，目标:wideString，允许覆盖:bool；返回：bool；可见性：default
            文件_移动("演示参数1", "演示参数2", 真)
            // 命令ID：文件_删除
            // [8] 文件_删除(路径)
            // 功能：删除一个普通文件。
            // 参数：路径:wideString；返回：bool；可见性：default
            文件_删除("module-demo-temp.txt")
            // 命令ID：文件_取大小
            // [9] 文件_取大小(路径)
            // 功能：返回文件字节数，失败返回 -1。
            // 参数：路径:wideString；返回：longLong；可见性：default
            文件_取大小("module-demo-temp.txt")
            // 命令ID：目录_创建
            // [10] 目录_创建(路径)
            // 功能：递归创建目录；目录已经存在也返回真。
            // 参数：路径:wideString；返回：bool；可见性：default
            目录_创建("module-demo-temp.txt")
            // 命令ID：目录_删除空目录
            // [11] 目录_删除空目录(路径)
            // 功能：只删除空目录，不执行递归删除。
            // 参数：路径:wideString；返回：bool；可见性：default
            目录_删除空目录("module-demo-temp.txt")
        如果结束
        调试输出("已查看 命令 1–11：文件_是否存在 至 目录_删除空目录；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
