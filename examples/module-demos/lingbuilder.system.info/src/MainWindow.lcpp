包 系统信息模块完整演示
使用 Win32窗口基础模块
使用 系统信息模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "系统信息模块：7 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.system.info")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–7：系统_取用户名 至 系统_取环境变量；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：系统_取用户名
            // [1] 系统_取用户名()
            // 功能：返回当前登录用户名。
            // 参数：无；返回：wideString；可见性：default
            系统_取用户名()
            // 命令ID：系统_取计算机名
            // [2] 系统_取计算机名()
            // 功能：返回本机计算机名。
            // 参数：无；返回：wideString；可见性：default
            系统_取计算机名()
            // 命令ID：系统_取Windows版本
            // [3] 系统_取Windows版本()
            // 功能：返回真实 Windows 主版本、次版本和内部版本号。
            // 参数：无；返回：wideString；可见性：default
            系统_取Windows版本()
            // 命令ID：系统_取处理器数量
            // [4] 系统_取处理器数量()
            // 功能：返回系统逻辑处理器数量。
            // 参数：无；返回：int；可见性：default
            系统_取处理器数量()
            // 命令ID：系统_取内存总量MB
            // [5] 系统_取内存总量MB()
            // 功能：返回物理内存总量，单位 MB。
            // 参数：无；返回：longLong；可见性：default
            系统_取内存总量MB()
            // 命令ID：系统_取内存可用MB
            // [6] 系统_取内存可用MB()
            // 功能：返回当前可用物理内存，单位 MB。
            // 参数：无；返回：longLong；可见性：default
            系统_取内存可用MB()
            // 命令ID：系统_取环境变量
            // [7] 系统_取环境变量(名称)
            // 功能：读取当前进程可见的环境变量。
            // 参数：名称:wideString；返回：wideString；可见性：default
            系统_取环境变量("演示参数1")
        如果结束
        调试输出("已查看 命令 1–7：系统_取用户名 至 系统_取环境变量；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
