包 磁盘信息模块完整演示
使用 Win32窗口基础模块
使用 磁盘信息模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "磁盘信息模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.system.disk")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–5：磁盘_总容量MB 至 磁盘_取驱动器类型；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：磁盘_总容量MB
            // [1] 磁盘_总容量MB(路径)
            // 功能：返回路径所在卷的总容量 MB，失败返回 -1。
            // 参数：路径:wideString；返回：longLong；可见性：default
            磁盘_总容量MB("module-demo-temp.txt")
            // 命令ID：磁盘_可用容量MB
            // [2] 磁盘_可用容量MB(路径)
            // 功能：返回当前用户可用容量 MB，失败返回 -1。
            // 参数：路径:wideString；返回：longLong；可见性：default
            磁盘_可用容量MB("module-demo-temp.txt")
            // 命令ID：磁盘_取卷标
            // [3] 磁盘_取卷标(根路径)
            // 功能：返回卷标，例如传入 C:\。
            // 参数：根路径:wideString；返回：wideString；可见性：default
            磁盘_取卷标("module-demo-temp.txt")
            // 命令ID：磁盘_取文件系统
            // [4] 磁盘_取文件系统(根路径)
            // 功能：返回 NTFS、FAT32 等文件系统名称。
            // 参数：根路径:wideString；返回：wideString；可见性：default
            磁盘_取文件系统("module-demo-temp.txt")
            // 命令ID：磁盘_取驱动器类型
            // [5] 磁盘_取驱动器类型(根路径)
            // 功能：返回 Win32 DRIVE_* 类型编号。
            // 参数：根路径:wideString；返回：int；可见性：default
            磁盘_取驱动器类型("module-demo-temp.txt")
        如果结束
        调试输出("已查看 命令 1–5：磁盘_总容量MB 至 磁盘_取驱动器类型；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
