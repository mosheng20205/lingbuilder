包 进程内存模块完整演示
使用 Win32窗口基础模块
使用 进程内存模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "进程内存模块：4 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.advanced.process-memory")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–4：进程内存_打开 至 进程内存_关闭；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：进程内存_打开
            // [1] 进程内存_打开(进程ID, 允许写入)
            // 功能：打开目标进程句柄。
            // 参数：进程ID:int，允许写入:bool；返回：handle；可见性：default
            进程内存_打开(1, 真)
            // 命令ID：进程内存_读整数
            // [2] 进程内存_读整数(进程句柄, 地址, 默认值)
            // 功能：读取目标地址 32 位整数。
            // 参数：进程句柄:handle，地址:longLong，默认值:int；返回：int；可见性：default
            进程内存_读整数(0, 1, 1)
            // 命令ID：进程内存_写整数
            // [3] 进程内存_写整数(进程句柄, 地址, 数值)
            // 功能：写入目标地址 32 位整数。
            // 参数：进程句柄:handle，地址:longLong，数值:int；返回：bool；可见性：default
            进程内存_写整数(0, 1, 1)
            // 命令ID：进程内存_关闭
            // [4] 进程内存_关闭(进程句柄)
            // 功能：关闭目标进程句柄。
            // 参数：进程句柄:handle；返回：bool；可见性：default
            进程内存_关闭(0)
        如果结束
        调试输出("已查看 命令 1–4：进程内存_打开 至 进程内存_关闭；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
