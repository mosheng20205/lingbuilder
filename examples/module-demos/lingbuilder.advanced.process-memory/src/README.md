# 进程内存模块完整演示

- 模块 ID：`lingbuilder.advanced.process-memory`
- 版本：`1.0.0`
- 类型：LingBuilder 内置模块
- 命令数：4
- 设计器控件数：0
- 分组数：1

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

显式打开目标进程并读写 32 位整数；属于高风险模块，不默认启用。

## 命令清单

| # | 命令 | 签名 | 返回 | 说明 |
|---:|---|---|---|---|
| 1 | `进程内存_打开` | `进程内存_打开(进程ID, 允许写入)` | handle | 打开目标进程句柄。 |
| 2 | `进程内存_读整数` | `进程内存_读整数(进程句柄, 地址, 默认值)` | int | 读取目标地址 32 位整数。 |
| 3 | `进程内存_写整数` | `进程内存_写整数(进程句柄, 地址, 数值)` | bool | 写入目标地址 32 位整数。 |
| 4 | `进程内存_关闭` | `进程内存_关闭(进程句柄)` | bool | 关闭目标进程句柄。 |
