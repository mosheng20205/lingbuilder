包 日期时间模块完整演示
使用 Win32窗口基础模块
使用 日期时间模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "日期时间模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.std.datetime")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–5：时间_当前时间戳 至 时间_格式化时间戳；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：时间_当前时间戳
            // [1] 时间_当前时间戳()
            // 功能：返回当前 Unix 秒级时间戳。
            // 参数：无；返回：longLong；可见性：default
            时间_当前时间戳()
            // 命令ID：时间_当前毫秒
            // [2] 时间_当前毫秒()
            // 功能：返回当前 Unix 毫秒级时间戳。
            // 参数：无；返回：longLong；可见性：default
            时间_当前毫秒()
            // 命令ID：时间_单调毫秒
            // [3] 时间_单调毫秒()
            // 功能：返回适合计算耗时的单调时钟毫秒值。
            // 参数：无；返回：longLong；可见性：default
            时间_单调毫秒()
            // 命令ID：时间_格式化当前
            // [4] 时间_格式化当前(格式)
            // 功能：使用 wcsftime 格式格式化当前本地时间。
            // 参数：格式:wideString；返回：wideString；可见性：default
            时间_格式化当前("演示参数1")
            // 命令ID：时间_格式化时间戳
            // [5] 时间_格式化时间戳(时间戳, 格式)
            // 功能：格式化指定 Unix 秒级时间戳。
            // 参数：时间戳:longLong，格式:wideString；返回：wideString；可见性：default
            时间_格式化时间戳(1, "演示参数2")
        如果结束
        调试输出("已查看 命令 1–5：时间_当前时间戳 至 时间_格式化时间戳；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
