包 Win32菜单模块完整演示
使用 Win32窗口基础模块
使用 Win32菜单模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "Win32菜单模块：8 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.win32.menu")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–8：菜单_创建 至 菜单_销毁；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：菜单_创建
            // [1] 菜单_创建()
            // 功能：创建菜单栏。
            // 参数：无；返回：handle；可见性：default
            菜单_创建()
            // 命令ID：菜单_创建弹出菜单
            // [2] 菜单_创建弹出菜单()
            // 功能：创建弹出菜单。
            // 参数：无；返回：handle；可见性：default
            菜单_创建弹出菜单()
            // 命令ID：菜单_添加项目
            // [3] 菜单_添加项目(菜单句柄, 命令ID, 标题)
            // 功能：添加普通菜单项。
            // 参数：菜单句柄:handle，命令ID:int，标题:wideString；返回：bool；可见性：default
            菜单_添加项目(0, 1, "演示参数3")
            // 命令ID：菜单_添加子菜单
            // [4] 菜单_添加子菜单(菜单句柄, 子菜单句柄, 标题)
            // 功能：添加子菜单。
            // 参数：菜单句柄:handle，子菜单句柄:handle，标题:wideString；返回：bool；可见性：default
            菜单_添加子菜单(0, 0, "演示参数3")
            // 命令ID：菜单_添加分隔线
            // [5] 菜单_添加分隔线(菜单句柄)
            // 功能：添加分隔线。
            // 参数：菜单句柄:handle；返回：bool；可见性：default
            菜单_添加分隔线(0)
            // 命令ID：菜单_设置到窗口
            // [6] 菜单_设置到窗口(窗口句柄, 菜单句柄)
            // 功能：把菜单设置为窗口菜单栏。
            // 参数：窗口句柄:handle，菜单句柄:handle；返回：bool；可见性：default
            菜单_设置到窗口(0, 0)
            // 命令ID：菜单_弹出并取命令
            // [7] 菜单_弹出并取命令(菜单句柄, 窗口句柄, 横坐标, 纵坐标)
            // 功能：显示弹出菜单并返回选择的命令 ID。
            // 参数：菜单句柄:handle，窗口句柄:handle，横坐标:int，纵坐标:int；返回：int；可见性：default
            菜单_弹出并取命令(0, 0, 1, 1)
            // 命令ID：菜单_销毁
            // [8] 菜单_销毁(菜单句柄)
            // 功能：销毁未附加或不再使用的菜单。
            // 参数：菜单句柄:handle；返回：bool；可见性：default
            菜单_销毁(0)
        如果结束
        调试输出("已查看 命令 1–8：菜单_创建 至 菜单_销毁；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
