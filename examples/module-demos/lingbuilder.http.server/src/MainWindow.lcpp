包 HTTP服务端模块完整演示
使用 Win32窗口基础模块
使用 HTTP服务端模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "HTTP 服务端模块：51 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.http.server")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–17：HTTP_创建服务 至 HTTP_取活动连接数；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：HTTP_创建服务
            // [1] HTTP_创建服务()
            // 功能：创建尚未启动的受管 HTTP/1.1 服务端并返回稳定句柄。
            // 参数：无；返回：HTTP服务端；可见性：default
            HTTP_创建服务()
            // 命令ID：HTTP_配置服务
            // [2] HTTP_配置服务(服务端, 监听地址, 端口, 工作线程数, 等待队列上限)
            // 功能：配置监听地址、端口、有界工作线程和连接等待队列；只能在停止状态修改。
            // 参数：服务端:HTTP服务端，监听地址:wideString，端口:int，工作线程数:int，等待队列上限:int；返回：bool；可见性：default
            HTTP_配置服务(0, "https://example.com", 1, 1, 1)
            // 命令ID：HTTP_设置请求限制
            // [3] HTTP_设置请求限制(服务端, 请求头上限KB, 请求体上限MB, 请求超时毫秒)
            // 功能：在停止状态设置请求头、请求体和单次读写超时，阻止无限请求与慢速连接耗尽资源。
            // 参数：服务端:HTTP服务端，请求头上限KB:int，请求体上限MB:int，请求超时毫秒:int；返回：bool；可见性：default
            HTTP_设置请求限制(0, 1, 1, 1)
            // 命令ID：HTTP_允许外部监听
            // [4] HTTP_允许外部监听(服务端, 允许)
            // 功能：显式允许非回环地址监听；默认关闭，避免无意把开发服务暴露到局域网。
            // 参数：服务端:HTTP服务端，允许:bool；返回：bool；可见性：default
            HTTP_允许外部监听(0, 真)
            // 命令ID：HTTP_绑定请求处理器
            // [5] HTTP_绑定请求处理器(服务端, &处理器)
            // 功能：绑定未命中路由时在窗口 UI 线程执行的无参数处理器；处理器用 HTTP_取当前请求() 取得请求。
            // 参数：服务端:HTTP服务端，处理器:handler；返回：bool；可见性：default
            HTTP_绑定请求处理器(0, &模块演示回调)
            // 命令ID：HTTP_添加路由
            // [6] HTTP_添加路由(服务端, 方法, 路径模式, &处理器)
            // 功能：添加方法和路径路由；支持精确路径、末尾 / * 前缀匹配及方法 *；HEAD 请求未显式登记 HEAD 路由时自动回落匹配 GET 路由并返回无正文响应。
            // 参数：服务端:HTTP服务端，方法:wideString，路径模式:wideString，处理器:handler；返回：bool；可见性：default
            HTTP_添加路由(0, "演示参数2", "module-demo-temp.txt", &模块演示回调)
            // 命令ID：HTTP_添加静态路由
            // [7] HTTP_添加静态路由(服务端, 方法, 路径模式, 响应内容, 内容类型)
            // 功能：注册工作线程直接回应的固定内容路由（JSON/文本/HTML）；命中后不进入请求处理器、不占用 UI 线程，是高吞吐固定响应的首选；与 HTTP_添加路由 同方法同路径时后注册者整体覆盖。
            // 参数：服务端:HTTP服务端，方法:wideString，路径模式:wideString，响应内容:wideString，内容类型:wideString；返回：bool；可见性：default
            HTTP_添加静态路由(0, "演示参数2", "module-demo-temp.txt", "演示参数4", "演示参数5")
            // 命令ID：HTTP_添加静态文件路由
            // [8] HTTP_添加静态文件路由(服务端, 方法, 路径模式, 文件路径, 下载名称, 内容类型)
            // 功能：注册工作线程直接分块发送磁盘文件的路由（页面、图片、附件下载）；不占用 UI 线程；每次请求读取当前文件内容，文件不存在时该请求返回 404。
            // 参数：服务端:HTTP服务端，方法:wideString，路径模式:wideString，文件路径:wideString，下载名称:wideString，内容类型:wideString；返回：bool；可见性：default
            HTTP_添加静态文件路由(0, "演示参数2", "module-demo-temp.txt", "module-demo-temp.txt", "演示参数5", "演示参数6")
            // 命令ID：HTTP_设置连接轮转
            // [9] HTTP_设置连接轮转(服务端, 请求数)
            // 功能：设置单个 Keep-Alive 连接被强制关闭前最多处理的请求数，默认 100；运行中可修改，对后续请求立即生效。
            // 参数：服务端:HTTP服务端，请求数:int；返回：bool；可见性：default
            HTTP_设置连接轮转(0, 1)
            // 命令ID：HTTP_清空路由
            // [10] HTTP_清空路由(服务端)
            // 功能：清空服务端的全部显式路由，不影响默认请求处理器。
            // 参数：服务端:HTTP服务端；返回：bool；可见性：default
            HTTP_清空路由(0)
            // 命令ID：HTTP_启动
            // [11] HTTP_启动(服务端)
            // 功能：启动后台监听、连接队列和工作线程；成功返回真。
            // 参数：服务端:HTTP服务端；返回：bool；可见性：default
            HTTP_启动(0)
            // 命令ID：HTTP_停止
            // [12] HTTP_停止(服务端)
            // 功能：停止监听、取消等待请求并回收全部连接和工作线程。
            // 参数：服务端:HTTP服务端；返回：bool；可见性：default
            HTTP_停止(0)
            // 命令ID：HTTP_销毁服务
            // [13] HTTP_销毁服务(服务端)
            // 功能：停止并释放服务端句柄；重复销毁会返回假。
            // 参数：服务端:HTTP服务端；返回：bool；可见性：default
            HTTP_销毁服务(0)
            // 命令ID：HTTP_是否运行
            // [14] HTTP_是否运行(服务端)
            // 功能：判断服务端是否正在接受连接。
            // 参数：服务端:HTTP服务端；返回：bool；可见性：default
            HTTP_是否运行(0)
            // 命令ID：HTTP_取监听地址
            // [15] HTTP_取监听地址(服务端)
            // 功能：返回服务端当前配置的监听地址。
            // 参数：服务端:HTTP服务端；返回：wideString；可见性：default
            HTTP_取监听地址(0)
            // 命令ID：HTTP_取监听端口
            // [16] HTTP_取监听端口(服务端)
            // 功能：返回实际监听端口；配置端口为 0 时可取得系统分配的临时端口。
            // 参数：服务端:HTTP服务端；返回：int；可见性：default
            HTTP_取监听端口(0)
            // 命令ID：HTTP_取活动连接数
            // [17] HTTP_取活动连接数(服务端)
            // 功能：返回当前正在处理的客户端连接数量。
            // 参数：服务端:HTTP服务端；返回：int；可见性：default
            HTTP_取活动连接数(0)
        如果结束
        调试输出("已查看 命令 1–17：HTTP_创建服务 至 HTTP_取活动连接数；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 _运行第02组_被单击()
        控件_设置文本(演示状态, "命令 18–34：HTTP_取累计请求数 至 HTTP_取协议版本；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：HTTP_取累计请求数
            // [18] HTTP_取累计请求数(服务端)
            // 功能：返回服务端成功解析并分发的累计请求数量。
            // 参数：服务端:HTTP服务端；返回：longLong；可见性：default
            HTTP_取累计请求数(0)
            // 命令ID：HTTP_取服务错误
            // [19] HTTP_取服务错误(服务端)
            // 功能：返回该服务端最近一次中文错误；句柄无效时返回管理器错误。
            // 参数：服务端:HTTP服务端；返回：wideString；可见性：default
            HTTP_取服务错误(0)
            // 命令ID：HTTP_取当前请求
            // [20] HTTP_取当前请求()
            // 功能：在请求处理器内取得当前受管请求句柄；其它上下文返回 0。
            // 参数：无；返回：HTTP请求；可见性：default
            HTTP_取当前请求()
            // 命令ID：HTTP_取请求服务
            // [21] HTTP_取请求服务(请求)
            // 功能：返回接收该请求的服务端句柄。
            // 参数：请求:HTTP请求；返回：HTTP服务端；可见性：default
            HTTP_取请求服务(0)
            // 命令ID：HTTP_取请求方法
            // [22] HTTP_取请求方法(请求)
            // 功能：返回大写 HTTP 方法，例如 GET、POST。
            // 参数：请求:HTTP请求；返回：wideString；可见性：default
            HTTP_取请求方法(0)
            // 命令ID：HTTP_取请求目标
            // [23] HTTP_取请求目标(请求)
            // 功能：返回请求行中的原始目标，包含原始查询字符串。
            // 参数：请求:HTTP请求；返回：wideString；可见性：default
            HTTP_取请求目标(0)
            // 命令ID：HTTP_取请求路径
            // [24] HTTP_取请求路径(请求)
            // 功能：返回经过安全百分号解码的 URL 路径。
            // 参数：请求:HTTP请求；返回：wideString；可见性：default
            HTTP_取请求路径(0)
            // 命令ID：HTTP_取查询字符串
            // [25] HTTP_取查询字符串(请求)
            // 功能：返回未解码的查询字符串，不含问号。
            // 参数：请求:HTTP请求；返回：wideString；可见性：default
            HTTP_取查询字符串(0)
            // 命令ID：HTTP_取查询参数
            // [26] HTTP_取查询参数(请求, 名称)
            // 功能：按 UTF-8 URL 编码读取首个查询参数值；不存在时返回空文本。
            // 参数：请求:HTTP请求，名称:wideString；返回：wideString；可见性：default
            HTTP_取查询参数(0, "演示参数2")
            // 命令ID：HTTP_取请求头
            // [27] HTTP_取请求头(请求, 名称)
            // 功能：不区分大小写读取首个请求头值。
            // 参数：请求:HTTP请求，名称:wideString；返回：wideString；可见性：default
            HTTP_取请求头(0, "演示参数2")
            // 命令ID：HTTP_取全部请求头
            // [28] HTTP_取全部请求头(请求)
            // 功能：以 UTF-16 JSON 对象返回全部请求头；重复头以逗号合并。
            // 参数：请求:HTTP请求；返回：wideString；可见性：default
            HTTP_取全部请求头(0)
            // 命令ID：HTTP_取请求正文
            // [29] HTTP_取请求正文(请求)
            // 功能：把 UTF-8 请求正文转换为文本；二进制数据请使用十六进制接口。
            // 参数：请求:HTTP请求；返回：wideString；可见性：default
            HTTP_取请求正文(0)
            // 命令ID：HTTP_取请求正文十六进制
            // [30] HTTP_取请求正文十六进制(请求)
            // 功能：以小写十六进制返回原始请求正文字节，适用于二进制上传。
            // 参数：请求:HTTP请求；返回：wideString；可见性：default
            HTTP_取请求正文十六进制(0)
            // 命令ID：HTTP_取请求正文大小
            // [31] HTTP_取请求正文大小(请求)
            // 功能：返回原始请求正文的字节数。
            // 参数：请求:HTTP请求；返回：longLong；可见性：default
            HTTP_取请求正文大小(0)
            // 命令ID：HTTP_取客户端地址
            // [32] HTTP_取客户端地址(请求)
            // 功能：返回 TCP 对端的数字 IP 地址。
            // 参数：请求:HTTP请求；返回：wideString；可见性：default
            HTTP_取客户端地址(0)
            // 命令ID：HTTP_取客户端端口
            // [33] HTTP_取客户端端口(请求)
            // 功能：返回 TCP 对端端口。
            // 参数：请求:HTTP请求；返回：int；可见性：default
            HTTP_取客户端端口(0)
            // 命令ID：HTTP_取协议版本
            // [34] HTTP_取协议版本(请求)
            // 功能：返回 HTTP/1.0 或 HTTP/1.1。
            // 参数：请求:HTTP请求；返回：wideString；可见性：default
            HTTP_取协议版本(0)
        如果结束
        调试输出("已查看 命令 18–34：HTTP_取累计请求数 至 HTTP_取协议版本；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 _运行第03组_被单击()
        控件_设置文本(演示状态, "命令 35–51：HTTP_设置状态码 至 HTTP_关闭服务；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：HTTP_设置状态码
            // [35] HTTP_设置状态码(请求, 状态码)
            // 功能：设置响应状态码，范围为 100 到 599。
            // 参数：请求:HTTP请求，状态码:int；返回：bool；可见性：default
            HTTP_设置状态码(0, 1)
            // 命令ID：HTTP_设置响应头
            // [36] HTTP_设置响应头(请求, 名称, 值)
            // 功能：设置或替换响应头；拒绝非法名称和 CR/LF 注入。
            // 参数：请求:HTTP请求，名称:wideString，值:wideString；返回：bool；可见性：default
            HTTP_设置响应头(0, "演示参数2", "演示参数3")
            // 命令ID：HTTP_添加响应头
            // [37] HTTP_添加响应头(请求, 名称, 值)
            // 功能：追加响应头，适用于多个 Set-Cookie；拒绝 CR/LF 注入。
            // 参数：请求:HTTP请求，名称:wideString，值:wideString；返回：bool；可见性：default
            HTTP_添加响应头(0, "演示参数2", "演示参数3")
            // 命令ID：HTTP_设置Cookie
            // [38] HTTP_设置Cookie(请求, 名称, 值, 路径, 最大秒数, HttpOnly, Secure, SameSite)
            // 功能：追加安全编码的 Set-Cookie；SameSite 支持 Lax、Strict、None，None 必须同时启用 Secure。
            // 参数：请求:HTTP请求，名称:wideString，值:wideString，路径:wideString，最大秒数:int，HttpOnly:bool，Secure:bool，SameSite:wideString；返回：bool；可见性：default
            HTTP_设置Cookie(0, "演示参数2", "演示参数3", "module-demo-temp.txt", 1, 真, 真, "演示参数8")
            // 命令ID：HTTP_发送文本
            // [39] HTTP_发送文本(请求, 内容, 内容类型, 状态码)
            // 功能：发送 UTF-8 文本并完成响应；内容类型为空时使用 text/plain。
            // 参数：请求:HTTP请求，内容:wideString，内容类型:wideString，状态码:int；返回：bool；可见性：default
            HTTP_发送文本(0, "演示参数2", "演示参数3", 1)
            // 命令ID：HTTP_发送JSON
            // [40] HTTP_发送JSON(请求, JSON, 状态码)
            // 功能：发送 application/json; charset=utf-8 响应并完成请求。
            // 参数：请求:HTTP请求，JSON:wideString，状态码:int；返回：bool；可见性：default
            HTTP_发送JSON(0, "{}", 1)
            // 命令ID：HTTP_发送十六进制
            // [41] HTTP_发送十六进制(请求, 十六进制, 内容类型, 状态码)
            // 功能：校验并解码偶数长度十六进制文本，发送原始二进制响应。
            // 参数：请求:HTTP请求，十六进制:wideString，内容类型:wideString，状态码:int；返回：bool；可见性：default
            HTTP_发送十六进制(0, "演示参数2", "演示参数3", 1)
            // 命令ID：HTTP_发送文件
            // [42] HTTP_发送文件(请求, 文件路径, 下载名称, 内容类型, 状态码)
            // 功能：以分块读取方式发送文件，不把整个文件载入内存；自动设置长度和可选下载名称。
            // 参数：请求:HTTP请求，文件路径:wideString，下载名称:wideString，内容类型:wideString，状态码:int；返回：bool；可见性：default
            HTTP_发送文件(0, "module-demo-temp.txt", "演示参数3", "演示参数4", 1)
            // 命令ID：HTTP_重定向
            // [43] HTTP_重定向(请求, 地址, 状态码)
            // 功能：发送 301、302、303、307 或 308 重定向响应。
            // 参数：请求:HTTP请求，地址:wideString，状态码:int；返回：bool；可见性：default
            HTTP_重定向(0, "https://example.com", 1)
            // 命令ID：HTTP_发送空响应
            // [44] HTTP_发送空响应(请求, 状态码)
            // 功能：发送无正文响应并完成请求，适用于 204、304 等状态。
            // 参数：请求:HTTP请求，状态码:int；返回：bool；可见性：default
            HTTP_发送空响应(0, 1)
            // 命令ID：HTTP_是否已响应
            // [45] HTTP_是否已响应(请求)
            // 功能：判断请求是否已经提交响应或被中止。
            // 参数：请求:HTTP请求；返回：bool；可见性：default
            HTTP_是否已响应(0)
            // 命令ID：HTTP_中止请求
            // [46] HTTP_中止请求(请求)
            // 功能：中止请求并关闭对应连接，不发送应用层响应。
            // 参数：请求:HTTP请求；返回：bool；可见性：default
            HTTP_中止请求(0)
            // 命令ID：HTTP_启动服务
            // [47] HTTP_启动服务(端口)
            // 功能：旧版单服务兼容入口；在 127.0.0.1 启动后台服务，建议新代码使用受管服务 API。
            // 参数：端口:int；返回：int；可见性：advanced
            HTTP_启动服务(1)
            // 命令ID：HTTP_等待请求
            // [48] HTTP_等待请求()
            // 功能：旧版阻塞兼容入口；等待一个请求并返回原始请求文本，新代码应使用请求处理器。
            // 参数：无；返回：wideString；可见性：advanced
            HTTP_等待请求()
            // 命令ID：HTTP_等待请求到调试输出
            // [49] HTTP_等待请求到调试输出()
            // 功能：旧版阻塞兼容入口；等待请求并输出原始内容。
            // 参数：无；返回：int；可见性：advanced
            HTTP_等待请求到调试输出()
            // 命令ID：HTTP_回复文本
            // [50] HTTP_回复文本(内容)
            // 功能：旧版兼容入口；向最近等待或当前回调请求发送 200 文本响应。
            // 参数：内容:wideString；返回：int；可见性：advanced
            HTTP_回复文本("演示参数1")
            // 命令ID：HTTP_关闭服务
            // [51] HTTP_关闭服务()
            // 功能：旧版兼容入口；停止并销毁旧版默认服务。
            // 参数：无；返回：void；可见性：advanced
            HTTP_关闭服务()
        如果结束
        调试输出("已查看 命令 35–51：HTTP_设置状态码 至 HTTP_关闭服务；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 模块演示回调()
        调试输出("模块演示回调已触发。")
    结束

结束类
