# HTTP 服务端模块完整演示

- 模块 ID：`lingbuilder.http.server`
- 版本：`2.0.0`
- 类型：LingBuilder 内置模块
- 命令数：48
- 设计器控件数：0
- 分组数：3

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

提供受管 HTTP/1.1 服务端、后台多连接处理、路由、完整请求读取、可配置响应、资源限制和运行状态。

## 命令清单

| # | 命令 | 签名 | 返回 | 说明 |
|---:|---|---|---|---|
| 1 | `HTTP_创建服务` | `HTTP_创建服务()` | HTTP服务端 | 创建尚未启动的受管 HTTP/1.1 服务端并返回稳定句柄。 |
| 2 | `HTTP_配置服务` | `HTTP_配置服务(服务端, 监听地址, 端口, 工作线程数, 等待队列上限)` | bool | 配置监听地址、端口、有界工作线程和连接等待队列；只能在停止状态修改。 |
| 3 | `HTTP_设置请求限制` | `HTTP_设置请求限制(服务端, 请求头上限KB, 请求体上限MB, 请求超时毫秒)` | bool | 在停止状态设置请求头、请求体和单次读写超时，阻止无限请求与慢速连接耗尽资源。 |
| 4 | `HTTP_允许外部监听` | `HTTP_允许外部监听(服务端, 允许)` | bool | 显式允许非回环地址监听；默认关闭，避免无意把开发服务暴露到局域网。 |
| 5 | `HTTP_绑定请求处理器` | `HTTP_绑定请求处理器(服务端, &处理器)` | bool | 绑定未命中路由时在窗口 UI 线程执行的无参数处理器；处理器用 HTTP_取当前请求() 取得请求。 |
| 6 | `HTTP_添加路由` | `HTTP_添加路由(服务端, 方法, 路径模式, &处理器)` | bool | 添加方法和路径路由；支持精确路径、末尾 /* 前缀匹配及方法 *。 |
| 7 | `HTTP_清空路由` | `HTTP_清空路由(服务端)` | bool | 清空服务端的全部显式路由，不影响默认请求处理器。 |
| 8 | `HTTP_启动` | `HTTP_启动(服务端)` | bool | 启动后台监听、连接队列和工作线程；成功返回真。 |
| 9 | `HTTP_停止` | `HTTP_停止(服务端)` | bool | 停止监听、取消等待请求并回收全部连接和工作线程。 |
| 10 | `HTTP_销毁服务` | `HTTP_销毁服务(服务端)` | bool | 停止并释放服务端句柄；重复销毁会返回假。 |
| 11 | `HTTP_是否运行` | `HTTP_是否运行(服务端)` | bool | 判断服务端是否正在接受连接。 |
| 12 | `HTTP_取监听地址` | `HTTP_取监听地址(服务端)` | wideString | 返回服务端当前配置的监听地址。 |
| 13 | `HTTP_取监听端口` | `HTTP_取监听端口(服务端)` | int | 返回实际监听端口；配置端口为 0 时可取得系统分配的临时端口。 |
| 14 | `HTTP_取活动连接数` | `HTTP_取活动连接数(服务端)` | int | 返回当前正在处理的客户端连接数量。 |
| 15 | `HTTP_取累计请求数` | `HTTP_取累计请求数(服务端)` | longLong | 返回服务端成功解析并分发的累计请求数量。 |
| 16 | `HTTP_取服务错误` | `HTTP_取服务错误(服务端)` | wideString | 返回该服务端最近一次中文错误；句柄无效时返回管理器错误。 |
| 17 | `HTTP_取当前请求` | `HTTP_取当前请求()` | HTTP请求 | 在请求处理器内取得当前受管请求句柄；其它上下文返回 0。 |
| 18 | `HTTP_取请求服务` | `HTTP_取请求服务(请求)` | HTTP服务端 | 返回接收该请求的服务端句柄。 |
| 19 | `HTTP_取请求方法` | `HTTP_取请求方法(请求)` | wideString | 返回大写 HTTP 方法，例如 GET、POST。 |
| 20 | `HTTP_取请求目标` | `HTTP_取请求目标(请求)` | wideString | 返回请求行中的原始目标，包含原始查询字符串。 |
| 21 | `HTTP_取请求路径` | `HTTP_取请求路径(请求)` | wideString | 返回经过安全百分号解码的 URL 路径。 |
| 22 | `HTTP_取查询字符串` | `HTTP_取查询字符串(请求)` | wideString | 返回未解码的查询字符串，不含问号。 |
| 23 | `HTTP_取查询参数` | `HTTP_取查询参数(请求, 名称)` | wideString | 按 UTF-8 URL 编码读取首个查询参数值；不存在时返回空文本。 |
| 24 | `HTTP_取请求头` | `HTTP_取请求头(请求, 名称)` | wideString | 不区分大小写读取首个请求头值。 |
| 25 | `HTTP_取全部请求头` | `HTTP_取全部请求头(请求)` | wideString | 以 UTF-16 JSON 对象返回全部请求头；重复头以逗号合并。 |
| 26 | `HTTP_取请求正文` | `HTTP_取请求正文(请求)` | wideString | 把 UTF-8 请求正文转换为文本；二进制数据请使用十六进制接口。 |
| 27 | `HTTP_取请求正文十六进制` | `HTTP_取请求正文十六进制(请求)` | wideString | 以小写十六进制返回原始请求正文字节，适用于二进制上传。 |
| 28 | `HTTP_取请求正文大小` | `HTTP_取请求正文大小(请求)` | longLong | 返回原始请求正文的字节数。 |
| 29 | `HTTP_取客户端地址` | `HTTP_取客户端地址(请求)` | wideString | 返回 TCP 对端的数字 IP 地址。 |
| 30 | `HTTP_取客户端端口` | `HTTP_取客户端端口(请求)` | int | 返回 TCP 对端端口。 |
| 31 | `HTTP_取协议版本` | `HTTP_取协议版本(请求)` | wideString | 返回 HTTP/1.0 或 HTTP/1.1。 |
| 32 | `HTTP_设置状态码` | `HTTP_设置状态码(请求, 状态码)` | bool | 设置响应状态码，范围为 100 到 599。 |
| 33 | `HTTP_设置响应头` | `HTTP_设置响应头(请求, 名称, 值)` | bool | 设置或替换响应头；拒绝非法名称和 CR/LF 注入。 |
| 34 | `HTTP_添加响应头` | `HTTP_添加响应头(请求, 名称, 值)` | bool | 追加响应头，适用于多个 Set-Cookie；拒绝 CR/LF 注入。 |
| 35 | `HTTP_设置Cookie` | `HTTP_设置Cookie(请求, 名称, 值, 路径, 最大秒数, HttpOnly, Secure, SameSite)` | bool | 追加安全编码的 Set-Cookie；SameSite 支持 Lax、Strict、None，None 必须同时启用 Secure。 |
| 36 | `HTTP_发送文本` | `HTTP_发送文本(请求, 内容, 内容类型, 状态码)` | bool | 发送 UTF-8 文本并完成响应；内容类型为空时使用 text/plain。 |
| 37 | `HTTP_发送JSON` | `HTTP_发送JSON(请求, JSON, 状态码)` | bool | 发送 application/json; charset=utf-8 响应并完成请求。 |
| 38 | `HTTP_发送十六进制` | `HTTP_发送十六进制(请求, 十六进制, 内容类型, 状态码)` | bool | 校验并解码偶数长度十六进制文本，发送原始二进制响应。 |
| 39 | `HTTP_发送文件` | `HTTP_发送文件(请求, 文件路径, 下载名称, 内容类型, 状态码)` | bool | 以分块读取方式发送文件，不把整个文件载入内存；自动设置长度和可选下载名称。 |
| 40 | `HTTP_重定向` | `HTTP_重定向(请求, 地址, 状态码)` | bool | 发送 301、302、303、307 或 308 重定向响应。 |
| 41 | `HTTP_发送空响应` | `HTTP_发送空响应(请求, 状态码)` | bool | 发送无正文响应并完成请求，适用于 204、304 等状态。 |
| 42 | `HTTP_是否已响应` | `HTTP_是否已响应(请求)` | bool | 判断请求是否已经提交响应或被中止。 |
| 43 | `HTTP_中止请求` | `HTTP_中止请求(请求)` | bool | 中止请求并关闭对应连接，不发送应用层响应。 |
| 44 | `HTTP_启动服务` | `HTTP_启动服务(端口)` | int | 旧版单服务兼容入口；在 127.0.0.1 启动后台服务，建议新代码使用受管服务 API。 |
| 45 | `HTTP_等待请求` | `HTTP_等待请求()` | wideString | 旧版阻塞兼容入口；等待一个请求并返回原始请求文本，新代码应使用请求处理器。 |
| 46 | `HTTP_等待请求到调试输出` | `HTTP_等待请求到调试输出()` | int | 旧版阻塞兼容入口；等待请求并输出原始内容。 |
| 47 | `HTTP_回复文本` | `HTTP_回复文本(内容)` | int | 旧版兼容入口；向最近等待或当前回调请求发送 200 文本响应。 |
| 48 | `HTTP_关闭服务` | `HTTP_关闭服务()` | void | 旧版兼容入口；停止并销毁旧版默认服务。 |
