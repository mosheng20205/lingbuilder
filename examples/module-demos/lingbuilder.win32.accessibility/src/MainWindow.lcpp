包 辅助功能模块完整演示
使用 Win32窗口基础模块
使用 辅助功能模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "辅助功能模块：4 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.win32.accessibility")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–4：辅助_取名称 至 辅助_取状态值；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：辅助_取名称
            // [1] 辅助_取名称(窗口句柄)
            // 功能：读取窗口可访问对象名称。
            // 参数：窗口句柄:handle；返回：wideString；可见性：default
            辅助_取名称(0)
            // 命令ID：辅助_取值
            // [2] 辅助_取值(窗口句柄)
            // 功能：读取窗口可访问对象值。
            // 参数：窗口句柄:handle；返回：wideString；可见性：default
            辅助_取值(0)
            // 命令ID：辅助_取角色
            // [3] 辅助_取角色(窗口句柄)
            // 功能：返回本地化角色文本。
            // 参数：窗口句柄:handle；返回：wideString；可见性：default
            辅助_取角色(0)
            // 命令ID：辅助_取状态值
            // [4] 辅助_取状态值(窗口句柄)
            // 功能：返回 MSAA 状态位。
            // 参数：窗口句柄:handle；返回：longLong；可见性：default
            辅助_取状态值(0)
        如果结束
        调试输出("已查看 命令 1–4：辅助_取名称 至 辅助_取状态值；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
