包 受控内存模块完整演示
使用 Win32窗口基础模块
使用 受控内存模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "受控内存模块：6 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.advanced.memory")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–6：内存_申请 至 内存_释放；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：内存_申请
            // [1] 内存_申请(字节数)
            // 功能：申请清零内存并返回受控句柄。
            // 参数：字节数:int；返回：handle；可见性：default
            内存_申请(1)
            // 命令ID：内存_写整数
            // [2] 内存_写整数(内存句柄, 偏移, 数值)
            // 功能：在边界内写入 32 位整数。
            // 参数：内存句柄:handle，偏移:int，数值:int；返回：bool；可见性：default
            内存_写整数(0, 1, 1)
            // 命令ID：内存_读整数
            // [3] 内存_读整数(内存句柄, 偏移)
            // 功能：在边界内读取 32 位整数。
            // 参数：内存句柄:handle，偏移:int；返回：int；可见性：default
            内存_读整数(0, 1)
            // 命令ID：内存_写UTF8文本
            // [4] 内存_写UTF8文本(内存句柄, 偏移, 文本)
            // 功能：在边界内写入带零结尾 UTF-8 文本。
            // 参数：内存句柄:handle，偏移:int，文本:wideString；返回：bool；可见性：default
            内存_写UTF8文本(0, 1, "演示参数3")
            // 命令ID：内存_读UTF8文本
            // [5] 内存_读UTF8文本(内存句柄, 偏移, 最大字节数)
            // 功能：在边界内读取 UTF-8 文本。
            // 参数：内存句柄:handle，偏移:int，最大字节数:int；返回：wideString；可见性：default
            内存_读UTF8文本(0, 1, 1)
            // 命令ID：内存_释放
            // [6] 内存_释放(内存句柄)
            // 功能：释放受控内存块。
            // 参数：内存句柄:handle；返回：bool；可见性：default
            内存_释放(0)
        如果结束
        调试输出("已查看 命令 1–6：内存_申请 至 内存_释放；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
