包 Win32窗口操作模块完整演示
使用 Win32窗口基础模块
使用 Win32窗口操作模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "Win32窗口操作模块：7 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.win32.window-utils")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–7：窗口_按标题查找 至 窗口_置前台；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：窗口_按标题查找
            // [1] 窗口_按标题查找(标题)
            // 功能：按完整窗口标题查找顶层窗口。
            // 参数：标题:wideString；返回：handle；可见性：default
            窗口_按标题查找("演示参数1")
            // 命令ID：窗口_句柄是否有效
            // [2] 窗口_句柄是否有效(窗口句柄)
            // 功能：判断 HWND 是否仍然有效。
            // 参数：窗口句柄:handle；返回：bool；可见性：default
            窗口_句柄是否有效(0)
            // 命令ID：窗口_取标题
            // [3] 窗口_取标题(窗口句柄)
            // 功能：读取窗口标题。
            // 参数：窗口句柄:handle；返回：wideString；可见性：default
            窗口_取标题(0)
            // 命令ID：窗口_设置标题
            // [4] 窗口_设置标题(窗口句柄, 标题)
            // 功能：设置窗口标题。
            // 参数：窗口句柄:handle，标题:wideString；返回：bool；可见性：default
            窗口_设置标题(0, "演示参数2")
            // 命令ID：窗口_显示
            // [5] 窗口_显示(窗口句柄, 显示方式)
            // 功能：按 Win32 SW_* 编号显示、隐藏、最小化或最大化窗口。
            // 参数：窗口句柄:handle，显示方式:int；返回：bool；可见性：default
            窗口_显示(0, 1)
            // 命令ID：窗口_移动
            // [6] 窗口_移动(窗口句柄, 横坐标, 纵坐标, 宽度, 高度)
            // 功能：移动窗口并设置尺寸。
            // 参数：窗口句柄:handle，横坐标:int，纵坐标:int，宽度:int，高度:int；返回：bool；可见性：default
            窗口_移动(0, 1, 1, 1, 1)
            // 命令ID：窗口_置前台
            // [7] 窗口_置前台(窗口句柄)
            // 功能：请求将窗口切换到前台。
            // 参数：窗口句柄:handle；返回：bool；可见性：default
            窗口_置前台(0)
        如果结束
        调试输出("已查看 命令 1–7：窗口_按标题查找 至 窗口_置前台；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
