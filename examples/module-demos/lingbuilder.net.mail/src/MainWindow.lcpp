包 SMTP邮件模块完整演示
使用 Win32窗口基础模块
使用 SMTP邮件模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "SMTP邮件模块：2 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.net.mail")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–2：SMTP_发送普通邮件 至 SMTP_取错误；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：SMTP_发送普通邮件
            // [1] SMTP_发送普通邮件(主机, 端口, 用户名, 密码, 发件人, 收件人, 主题, 正文)
            // 功能：通过不加密 SMTP 发送 UTF-8 文本邮件。
            // 参数：主机:wideString，端口:int，用户名:wideString，密码:wideString，发件人:wideString，收件人:wideString，主题:wideString，正文:wideString；返回：bool；可见性：default
            SMTP_发送普通邮件("演示参数1", 1, "演示参数3", "演示参数4", "演示参数5", "演示参数6", "演示参数7", "演示参数8")
            // 命令ID：SMTP_取错误
            // [2] SMTP_取错误()
            // 功能：返回最近 SMTP 错误或服务端响应。
            // 参数：无；返回：wideString；可见性：default
            SMTP_取错误()
        如果结束
        调试输出("已查看 命令 1–2：SMTP_发送普通邮件 至 SMTP_取错误；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
