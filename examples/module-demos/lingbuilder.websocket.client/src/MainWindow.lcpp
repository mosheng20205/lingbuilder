包 WebSocket客户端模块完整演示
使用 Win32窗口基础模块
使用 WebSocket客户端模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "WebSocket 客户端模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.websocket.client")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–5：WS_连接 至 WS_关闭；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：WS_连接
            // [1] WS_连接(地址)
            // 功能：连接到 ws:// 或 wss:// WebSocket 服务端，成功返回 1，失败返回 0 并输出中文调试信息。
            // 参数：地址:wideString；返回：int；可见性：default
            WS_连接("https://example.com")
            // 命令ID：WS_发送文本
            // [2] WS_发送文本(内容)
            // 功能：向当前 WebSocket 连接发送一条文本消息，成功返回 1。
            // 参数：内容:wideString；返回：int；可见性：default
            WS_发送文本("演示参数1")
            // 命令ID：WS_接收到调试输出
            // [3] WS_接收到调试输出()
            // 功能：从当前 WebSocket 连接接收一条文本消息，并写入调试输出。
            // 参数：无；返回：int；可见性：default
            WS_接收到调试输出()
            // 命令ID：WS_接收文本
            // [4] WS_接收文本()
            // 功能：接收一条文本消息并返回最近接收内容；可在原生 C++ 语句中读取返回值。
            // 参数：无；返回：wideString；可见性：default
            WS_接收文本()
            // 命令ID：WS_关闭
            // [5] WS_关闭()
            // 功能：关闭当前 WebSocket 连接并释放网络句柄。
            // 参数：无；返回：void；可见性：default
            WS_关闭()
        如果结束
        调试输出("已查看 命令 1–5：WS_连接 至 WS_关闭；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
