# WebSocket 客户端模块完整演示

- 模块 ID：`lingbuilder.websocket.client`
- 版本：`2.0.0`
- 类型：LingBuilder 内置模块
- 命令数：51
- 设计器控件数：0
- 分组数：3

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

提供受管 WinHTTP WebSocket 客户端、多连接、后台接收、文本与二进制消息、wss://、代理、身份验证、证书固定、自动重连、资源限制和运行统计。

## 命令清单

| # | 命令 | 签名 | 返回 | 说明 |
|---:|---|---|---|---|
| 1 | `WS_创建连接` | `WS_创建连接()` | WebSocket连接 | 创建尚未启动的受管 WebSocket 客户端并返回稳定连接 ID。 |
| 2 | `WS_配置连接` | `WS_配置连接(连接, 地址)` | bool | 配置 ws:// 或 wss:// 地址；只能在连接停止时修改。 |
| 3 | `WS_设置资源限制` | `WS_设置资源限制(连接, 连接超时毫秒, 接收超时毫秒, 消息上限MB, 单次发送上限MB)` | bool | 设置连接/接收超时和消息硬限制，防止无限阻塞或超大消息耗尽内存。 |
| 4 | `WS_设置UserAgent` | `WS_设置UserAgent(连接, UserAgent)` | bool | 设置握手使用的 User-Agent；只能在连接停止时修改。 |
| 5 | `WS_设置请求头` | `WS_设置请求头(连接, 请求头)` | bool | 设置 UTF-16 原始请求头，每行一个“名称: 值”；禁止覆盖 WebSocket 升级关键头。 |
| 6 | `WS_设置Origin` | `WS_设置Origin(连接, 来源)` | bool | 设置 WebSocket 握手 Origin；空文本表示不发送。 |
| 7 | `WS_设置子协议` | `WS_设置子协议(连接, 子协议列表)` | bool | 设置逗号分隔的客户端子协议优先表，并校验服务端选择结果。 |
| 8 | `WS_设置代理` | `WS_设置代理(连接, 模式, 代理地址, 绕过列表)` | bool | 设置代理模式：0=系统自动，1=直连，2=固定代理；固定代理格式遵循 WinHTTP。 |
| 9 | `WS_设置服务器凭据` | `WS_设置服务器凭据(连接, 用户名, 密码)` | bool | 设置握手服务器 HTTP Basic 身份验证凭据；仅保存在连接对象内存中。 |
| 10 | `WS_设置代理凭据` | `WS_设置代理凭据(连接, 用户名, 密码)` | bool | 设置代理 HTTP Basic 身份验证凭据；仅保存在连接对象内存中。 |
| 11 | `WS_设置TLS验证` | `WS_设置TLS验证(连接, 验证证书, 允许自签名, 证书SHA256)` | bool | 配置 wss:// 证书验证和可选 SHA-256 证书固定；关闭验证属于高风险调试能力。 |
| 12 | `WS_设置自动重连` | `WS_设置自动重连(连接, 启用, 最大次数, 初始延迟毫秒, 最大延迟毫秒)` | bool | 设置意外断开后的指数退避重连；主动关闭不会触发重连。 |
| 13 | `WS_绑定已连接处理器` | `WS_绑定已连接处理器(连接, &处理器)` | bool | 绑定已连接事件处理器；处理器通过窗口消息在创建连接的 UI 线程执行，并使用 WS_取当前事件系列命令读取快照。 |
| 14 | `WS_绑定消息处理器` | `WS_绑定消息处理器(连接, &处理器)` | bool | 绑定消息事件处理器；处理器通过窗口消息在创建连接的 UI 线程执行，并使用 WS_取当前事件系列命令读取快照。 |
| 15 | `WS_绑定已断开处理器` | `WS_绑定已断开处理器(连接, &处理器)` | bool | 绑定已断开事件处理器；处理器通过窗口消息在创建连接的 UI 线程执行，并使用 WS_取当前事件系列命令读取快照。 |
| 16 | `WS_绑定错误处理器` | `WS_绑定错误处理器(连接, &处理器)` | bool | 绑定错误事件处理器；处理器通过窗口消息在创建连接的 UI 线程执行，并使用 WS_取当前事件系列命令读取快照。 |
| 17 | `WS_绑定重连处理器` | `WS_绑定重连处理器(连接, &处理器)` | bool | 绑定重连事件处理器；处理器通过窗口消息在创建连接的 UI 线程执行，并使用 WS_取当前事件系列命令读取快照。 |
| 18 | `WS_开始连接` | `WS_开始连接(连接)` | bool | 在后台启动连接、握手和接收循环，不阻塞 UI 线程。 |
| 19 | `WS_等待连接` | `WS_等待连接(连接, 超时毫秒)` | bool | 等待连接进入“已连接”或失败状态；主要用于命令行和测试，UI 代码应使用已连接处理器。 |
| 20 | `WS_关闭连接` | `WS_关闭连接(连接, 状态码, 原因)` | bool | 发送关闭帧并停止后台接收；状态码和 UTF-8 原因按 RFC 6455 校验。 |
| 21 | `WS_强制断开` | `WS_强制断开(连接)` | bool | 立即关闭底层 WinHTTP 句柄并停止自动重连，仅用于超时或故障恢复。 |
| 22 | `WS_销毁连接` | `WS_销毁连接(连接)` | bool | 停止并释放受管连接；重复销毁返回假。 |
| 23 | `WS_是否已连接` | `WS_是否已连接(连接)` | bool | 判断连接是否已完成握手且未进入关闭状态。 |
| 24 | `WS_取连接状态` | `WS_取连接状态(连接)` | wideString | 返回连接状态：未配置、已停止、连接中、已连接、重连等待、关闭中或错误。 |
| 25 | `WS_取连接地址` | `WS_取连接地址(连接)` | wideString | 返回当前配置的 ws:// 或 wss:// 地址。 |
| 26 | `WS_取协商子协议` | `WS_取协商子协议(连接)` | wideString | 返回服务端握手选择的子协议。 |
| 27 | `WS_取握手响应头` | `WS_取握手响应头(连接)` | wideString | 返回最近一次 WebSocket 握手的完整响应头。 |
| 28 | `WS_取握手状态码` | `WS_取握手状态码(连接)` | int | 返回最近一次握手 HTTP 状态码。 |
| 29 | `WS_取连接错误` | `WS_取连接错误(连接)` | wideString | 返回连接最近一次中文错误信息。 |
| 30 | `WS_取连接时长` | `WS_取连接时长(连接)` | longLong | 返回当前连接完成握手后的在线毫秒数。 |
| 31 | `WS_取发送字节数` | `WS_取发送字节数(连接)` | longLong | 返回累计成功发送的应用负载字节数。 |
| 32 | `WS_取接收字节数` | `WS_取接收字节数(连接)` | longLong | 返回累计接收的应用负载字节数。 |
| 33 | `WS_取发送消息数` | `WS_取发送消息数(连接)` | longLong | 返回累计成功发送的文本和二进制消息数。 |
| 34 | `WS_取接收消息数` | `WS_取接收消息数(连接)` | longLong | 返回累计接收的完整文本和二进制消息数。 |
| 35 | `WS_取重连次数` | `WS_取重连次数(连接)` | int | 返回当前启动周期已经执行的自动重连次数。 |
| 36 | `WS_发送文本到连接` | `WS_发送文本到连接(连接, 内容)` | bool | 向指定已连接客户端发送一条完整 UTF-8 文本消息，并执行 UTF-8 与发送大小校验。 |
| 37 | `WS_发送二进制到连接` | `WS_发送二进制到连接(连接, 数据)` | bool | 向指定已连接客户端发送一条完整二进制消息。 |
| 38 | `WS_取当前事件类型` | `WS_取当前事件类型()` | wideString | 返回当前回调事件类型：已连接、文本、二进制、已断开、错误或重连。 |
| 39 | `WS_取当前连接` | `WS_取当前连接()` | WebSocket连接 | 返回当前事件对应的受管连接 ID。 |
| 40 | `WS_取当前消息类型` | `WS_取当前消息类型()` | wideString | 返回当前消息类型：文本或二进制；其它事件返回空文本。 |
| 41 | `WS_取当前文本` | `WS_取当前文本()` | wideString | 返回当前文本消息的不可变快照。 |
| 42 | `WS_取当前二进制` | `WS_取当前二进制()` | bytes | 返回当前二进制消息的独立字节集副本。 |
| 43 | `WS_取当前关闭代码` | `WS_取当前关闭代码()` | int | 返回当前断开事件的 WebSocket 关闭状态码。 |
| 44 | `WS_取当前关闭原因` | `WS_取当前关闭原因()` | wideString | 返回当前断开事件的 UTF-8 关闭原因。 |
| 45 | `WS_取当前错误` | `WS_取当前错误()` | wideString | 返回当前错误事件的中文说明。 |
| 46 | `WS_取当前重连次数` | `WS_取当前重连次数()` | int | 返回当前重连事件对应的重试序号。 |
| 47 | `WS_连接` | `WS_连接(地址)` | int | 旧版单连接兼容入口；同步等待最多 30 秒，新代码应使用受管连接和事件处理器。 |
| 48 | `WS_发送文本` | `WS_发送文本(内容)` | int | 旧版兼容入口；向默认受管连接发送文本。 |
| 49 | `WS_接收到调试输出` | `WS_接收到调试输出()` | int | 旧版阻塞兼容入口；等待一条默认连接文本消息并写入调试输出。 |
| 50 | `WS_接收文本` | `WS_接收文本()` | wideString | 旧版阻塞兼容入口；等待并返回默认连接的下一条文本消息。 |
| 51 | `WS_关闭` | `WS_关闭()` | void | 旧版兼容入口；关闭并销毁默认受管连接。 |
