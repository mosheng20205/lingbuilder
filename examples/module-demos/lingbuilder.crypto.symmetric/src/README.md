# 对称加密模块完整演示

- 模块 ID：`lingbuilder.crypto.symmetric`
- 版本：`1.0.0`
- 类型：LingBuilder 内置模块
- 命令数：26
- 设计器控件数：0
- 分组数：2

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

提供认证加密、传统分组加密和兼容流加密；密钥统一使用严格十六进制文本，密文使用带算法和随机数的自描述封装。

## 命令清单

| # | 命令 | 签名 | 返回 | 说明 |
|---:|---|---|---|---|
| 1 | `对称_生成密钥` | `对称_生成密钥(字节数)` | wideString | 使用系统密码学随机源生成大写十六进制密钥。 |
| 2 | `对称_AES256GCM加密` | `对称_AES256GCM加密(明文, 密钥十六进制, 附加数据)` | wideString | 使用 AES-256/GCM 加密并返回自描述密文。加密时自动生成随机 nonce，并验证认证标签。 |
| 3 | `对称_AES256GCM解密` | `对称_AES256GCM解密(自描述密文, 密钥十六进制, 附加数据)` | wideString | 解密 AES-256/GCM 自描述密文。认证或格式失败时返回空文本并记录错误。加密时自动生成随机 nonce，并验证认证标签。 |
| 4 | `对称_ChaCha20Poly1305加密` | `对称_ChaCha20Poly1305加密(明文, 密钥十六进制, 附加数据)` | wideString | 使用 ChaCha20-Poly1305 加密并返回自描述密文。加密时自动生成随机 nonce，并验证认证标签。 |
| 5 | `对称_ChaCha20Poly1305解密` | `对称_ChaCha20Poly1305解密(自描述密文, 密钥十六进制, 附加数据)` | wideString | 解密 ChaCha20-Poly1305 自描述密文。认证或格式失败时返回空文本并记录错误。加密时自动生成随机 nonce，并验证认证标签。 |
| 6 | `对称_SM4GCM加密` | `对称_SM4GCM加密(明文, 密钥十六进制, 附加数据)` | wideString | 使用 SM4/GCM 加密并返回自描述密文。加密时自动生成随机 nonce，并验证认证标签。 |
| 7 | `对称_SM4GCM解密` | `对称_SM4GCM解密(自描述密文, 密钥十六进制, 附加数据)` | wideString | 解密 SM4/GCM 自描述密文。认证或格式失败时返回空文本并记录错误。加密时自动生成随机 nonce，并验证认证标签。 |
| 8 | `对称_Camellia256GCM加密` | `对称_Camellia256GCM加密(明文, 密钥十六进制, 附加数据)` | wideString | 使用 Camellia-256/GCM 加密并返回自描述密文。加密时自动生成随机 nonce，并验证认证标签。 |
| 9 | `对称_Camellia256GCM解密` | `对称_Camellia256GCM解密(自描述密文, 密钥十六进制, 附加数据)` | wideString | 解密 Camellia-256/GCM 自描述密文。认证或格式失败时返回空文本并记录错误。加密时自动生成随机 nonce，并验证认证标签。 |
| 10 | `对称_TwofishGCM加密` | `对称_TwofishGCM加密(明文, 密钥十六进制, 附加数据)` | wideString | 使用 Twofish/GCM 加密并返回自描述密文。加密时自动生成随机 nonce，并验证认证标签。 |
| 11 | `对称_TwofishGCM解密` | `对称_TwofishGCM解密(自描述密文, 密钥十六进制, 附加数据)` | wideString | 解密 Twofish/GCM 自描述密文。认证或格式失败时返回空文本并记录错误。加密时自动生成随机 nonce，并验证认证标签。 |
| 12 | `对称_SerpentGCM加密` | `对称_SerpentGCM加密(明文, 密钥十六进制, 附加数据)` | wideString | 使用 Serpent/GCM 加密并返回自描述密文。加密时自动生成随机 nonce，并验证认证标签。 |
| 13 | `对称_SerpentGCM解密` | `对称_SerpentGCM解密(自描述密文, 密钥十六进制, 附加数据)` | wideString | 解密 Serpent/GCM 自描述密文。认证或格式失败时返回空文本并记录错误。加密时自动生成随机 nonce，并验证认证标签。 |
| 14 | `对称_AES256CBC加密` | `对称_AES256CBC加密(明文, 密钥十六进制)` | wideString | 使用 AES-256/CBC 加密并返回自描述密文。该算法不提供完整性认证，仅用于兼容旧数据；新项目优先使用 AES-256/GCM 或 ChaCha20-Poly1305。 |
| 15 | `对称_AES256CBC解密` | `对称_AES256CBC解密(自描述密文, 密钥十六进制)` | wideString | 解密 AES-256/CBC 自描述密文。认证或格式失败时返回空文本并记录错误。该算法不提供完整性认证，仅用于兼容旧数据；新项目优先使用 AES-256/GCM 或 ChaCha20-Poly1305。 |
| 16 | `对称_BlowfishCBC加密` | `对称_BlowfishCBC加密(明文, 密钥十六进制)` | wideString | 使用 Blowfish/CBC 加密并返回自描述密文。该算法不提供完整性认证，仅用于兼容旧数据；新项目优先使用 AES-256/GCM 或 ChaCha20-Poly1305。 |
| 17 | `对称_BlowfishCBC解密` | `对称_BlowfishCBC解密(自描述密文, 密钥十六进制)` | wideString | 解密 Blowfish/CBC 自描述密文。认证或格式失败时返回空文本并记录错误。该算法不提供完整性认证，仅用于兼容旧数据；新项目优先使用 AES-256/GCM 或 ChaCha20-Poly1305。 |
| 18 | `对称_DESCBC加密` | `对称_DESCBC加密(明文, 密钥十六进制)` | wideString | 使用 DES/CBC 加密并返回自描述密文。该算法不提供完整性认证，仅用于兼容旧数据；新项目优先使用 AES-256/GCM 或 ChaCha20-Poly1305。 |
| 19 | `对称_DESCBC解密` | `对称_DESCBC解密(自描述密文, 密钥十六进制)` | wideString | 解密 DES/CBC 自描述密文。认证或格式失败时返回空文本并记录错误。该算法不提供完整性认证，仅用于兼容旧数据；新项目优先使用 AES-256/GCM 或 ChaCha20-Poly1305。 |
| 20 | `对称_三DES_CBC加密` | `对称_三DES_CBC加密(明文, 密钥十六进制)` | wideString | 使用 3DES/CBC 加密并返回自描述密文。该算法不提供完整性认证，仅用于兼容旧数据；新项目优先使用 AES-256/GCM 或 ChaCha20-Poly1305。 |
| 21 | `对称_三DES_CBC解密` | `对称_三DES_CBC解密(自描述密文, 密钥十六进制)` | wideString | 解密 3DES/CBC 自描述密文。认证或格式失败时返回空文本并记录错误。该算法不提供完整性认证，仅用于兼容旧数据；新项目优先使用 AES-256/GCM 或 ChaCha20-Poly1305。 |
| 22 | `对称_RC4加密` | `对称_RC4加密(明文, 密钥十六进制)` | wideString | 使用 RC4 加密并返回自描述密文。该算法不提供完整性认证，仅用于兼容旧数据；新项目优先使用 AES-256/GCM 或 ChaCha20-Poly1305。 |
| 23 | `对称_RC4解密` | `对称_RC4解密(自描述密文, 密钥十六进制)` | wideString | 解密 RC4 自描述密文。认证或格式失败时返回空文本并记录错误。该算法不提供完整性认证，仅用于兼容旧数据；新项目优先使用 AES-256/GCM 或 ChaCha20-Poly1305。 |
| 24 | `对称_RC2CBC加密` | `对称_RC2CBC加密(明文, 密钥十六进制)` | wideString | 使用 Windows CryptoAPI RC2-128/CBC 加密并返回自描述密文。该算法仅用于兼容旧数据，新项目不要使用。 |
| 25 | `对称_RC2CBC解密` | `对称_RC2CBC解密(自描述密文, 密钥十六进制)` | wideString | 解密 RC2-128/CBC 自描述密文。该算法仅用于兼容旧数据，新项目不要使用。 |
| 26 | `对称_取错误` | `对称_取错误()` | wideString | 读取最近一次对称加密或解密失败的中文错误。 |
