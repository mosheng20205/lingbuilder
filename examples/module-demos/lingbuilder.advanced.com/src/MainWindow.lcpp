包 COM自动化模块完整演示
使用 Win32窗口基础模块
使用 COM自动化模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "COM自动化模块：6 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.advanced.com")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–6：COM_创建对象 至 COM_关闭；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：COM_创建对象
            // [1] COM_创建对象(ProgID)
            // 功能：创建一个 COM 自动化对象。
            // 参数：ProgID:wideString；返回：bool；可见性：default
            COM_创建对象("演示参数1")
            // 命令ID：COM_取文本属性
            // [2] COM_取文本属性(属性名)
            // 功能：读取文本或可转文本属性。
            // 参数：属性名:wideString；返回：wideString；可见性：default
            COM_取文本属性("演示参数1")
            // 命令ID：COM_置文本属性
            // [3] COM_置文本属性(属性名, 值)
            // 功能：设置 BSTR 文本属性。
            // 参数：属性名:wideString，值:wideString；返回：bool；可见性：default
            COM_置文本属性("演示参数1", "演示参数2")
            // 命令ID：COM_调用无参方法
            // [4] COM_调用无参方法(方法名)
            // 功能：调用无参数方法。
            // 参数：方法名:wideString；返回：bool；可见性：default
            COM_调用无参方法("演示参数1")
            // 命令ID：COM_取错误
            // [5] COM_取错误()
            // 功能：返回最近 COM 错误。
            // 参数：无；返回：wideString；可见性：default
            COM_取错误()
            // 命令ID：COM_关闭
            // [6] COM_关闭()
            // 功能：释放当前自动化对象。
            // 参数：无；返回：void；可见性：default
            COM_关闭()
        如果结束
        调试输出("已查看 命令 1–6：COM_创建对象 至 COM_关闭；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
