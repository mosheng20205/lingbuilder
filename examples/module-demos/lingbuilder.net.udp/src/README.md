# UDP通信模块完整演示

- 模块 ID：`lingbuilder.net.udp`
- 版本：`1.0.0`
- 类型：LingBuilder 内置模块
- 命令数：7
- 设计器控件数：0
- 分组数：1

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

提供 UDP 绑定、UTF-8 数据报发送接收和关闭能力。

## 命令清单

| # | 命令 | 签名 | 返回 | 说明 |
|---:|---|---|---|---|
| 1 | `UDP_绑定` | `UDP_绑定(端口)` | bool | 在本机全部接口绑定 UDP 端口。 |
| 2 | `UDP_发送文本` | `UDP_发送文本(主机, 端口, 内容)` | int | 发送一条 UTF-8 UDP 数据报。 |
| 3 | `UDP_接收文本` | `UDP_接收文本(最大字节数, 超时毫秒)` | wideString | 接收一条 UDP 数据报，超时返回空文本。 |
| 4 | `UDP_取来源地址` | `UDP_取来源地址()` | wideString | 返回最近数据报来源 IP。 |
| 5 | `UDP_取来源端口` | `UDP_取来源端口()` | int | 返回最近数据报来源端口。 |
| 6 | `UDP_取错误` | `UDP_取错误()` | wideString | 返回最近 UDP 中文错误。 |
| 7 | `UDP_关闭` | `UDP_关闭()` | void | 关闭当前 UDP 套接字。 |
