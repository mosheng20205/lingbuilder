包 UDP通信模块完整演示
使用 Win32窗口基础模块
使用 UDP通信模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "UDP通信模块：7 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.net.udp")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–7：UDP_绑定 至 UDP_关闭；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：UDP_绑定
            // [1] UDP_绑定(端口)
            // 功能：在本机全部接口绑定 UDP 端口。
            // 参数：端口:int；返回：bool；可见性：default
            UDP_绑定(1)
            // 命令ID：UDP_发送文本
            // [2] UDP_发送文本(主机, 端口, 内容)
            // 功能：发送一条 UTF-8 UDP 数据报。
            // 参数：主机:wideString，端口:int，内容:wideString；返回：int；可见性：default
            UDP_发送文本("演示参数1", 1, "演示参数3")
            // 命令ID：UDP_接收文本
            // [3] UDP_接收文本(最大字节数, 超时毫秒)
            // 功能：接收一条 UDP 数据报，超时返回空文本。
            // 参数：最大字节数:int，超时毫秒:int；返回：wideString；可见性：default
            UDP_接收文本(1, 1)
            // 命令ID：UDP_取来源地址
            // [4] UDP_取来源地址()
            // 功能：返回最近数据报来源 IP。
            // 参数：无；返回：wideString；可见性：default
            UDP_取来源地址()
            // 命令ID：UDP_取来源端口
            // [5] UDP_取来源端口()
            // 功能：返回最近数据报来源端口。
            // 参数：无；返回：int；可见性：default
            UDP_取来源端口()
            // 命令ID：UDP_取错误
            // [6] UDP_取错误()
            // 功能：返回最近 UDP 中文错误。
            // 参数：无；返回：wideString；可见性：default
            UDP_取错误()
            // 命令ID：UDP_关闭
            // [7] UDP_关闭()
            // 功能：关闭当前 UDP 套接字。
            // 参数：无；返回：void；可见性：default
            UDP_关闭()
        如果结束
        调试输出("已查看 命令 1–7：UDP_绑定 至 UDP_关闭；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
