包 CEF3传输模块完整演示
使用 Win32窗口基础模块
使用 CEF3传输模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "CEF3传输模块：2 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.cef3.transfer")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–2：CEF3传输_开始下载 至 CEF3传输_打印；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：CEF3传输_开始下载
            // [1] CEF3传输_开始下载(控件名, 地址)
            // 功能：使用当前实例会话开始下载。
            // 参数：控件名:controlRef，地址:wideString；返回：int；可见性：default
            CEF3传输_开始下载(演示CefBrowser控件, "https://example.com")
            // 命令ID：CEF3传输_打印
            // [2] CEF3传输_打印(控件名)
            // 功能：打开当前页面的原生打印流程。
            // 参数：控件名:controlRef；返回：int；可见性：default
            CEF3传输_打印(演示CefBrowser控件)
        如果结束
        调试输出("已查看 命令 1–2：CEF3传输_开始下载 至 CEF3传输_打印；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
