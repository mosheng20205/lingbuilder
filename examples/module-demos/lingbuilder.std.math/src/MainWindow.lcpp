包 数学与随机模块完整演示
使用 Win32窗口基础模块
使用 数学与随机模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "数学与随机模块：7 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.std.math")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–7：数学_绝对值 至 数学_随机整数；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：数学_绝对值
            // [1] 数学_绝对值(数值)
            // 功能：返回数值的绝对值。
            // 参数：数值:double；返回：double；可见性：default
            数学_绝对值(1.0)
            // 命令ID：数学_最小值
            // [2] 数学_最小值(数值一, 数值二)
            // 功能：返回两个数值中的较小值。
            // 参数：数值一:double，数值二:double；返回：double；可见性：default
            数学_最小值(1.0, 1.0)
            // 命令ID：数学_最大值
            // [3] 数学_最大值(数值一, 数值二)
            // 功能：返回两个数值中的较大值。
            // 参数：数值一:double，数值二:double；返回：double；可见性：default
            数学_最大值(1.0, 1.0)
            // 命令ID：数学_限制范围
            // [4] 数学_限制范围(数值, 最小值, 最大值)
            // 功能：将数值限制在指定闭区间内。
            // 参数：数值:double，最小值:double，最大值:double；返回：double；可见性：default
            数学_限制范围(1.0, 1.0, 1.0)
            // 命令ID：数学_平方根
            // [5] 数学_平方根(数值)
            // 功能：返回非负数值的平方根，负数返回 0。
            // 参数：数值:double；返回：double；可见性：default
            数学_平方根(1.0)
            // 命令ID：数学_乘方
            // [6] 数学_乘方(底数, 指数)
            // 功能：返回底数的指定次方。
            // 参数：底数:double，指数:double；返回：double；可见性：default
            数学_乘方(1.0, 1.0)
            // 命令ID：数学_随机整数
            // [7] 数学_随机整数(最小值, 最大值)
            // 功能：返回指定闭区间内的随机整数，参数顺序可交换。
            // 参数：最小值:int，最大值:int；返回：int；可见性：default
            数学_随机整数(1, 1)
        如果结束
        调试输出("已查看 命令 1–7：数学_绝对值 至 数学_随机整数；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
