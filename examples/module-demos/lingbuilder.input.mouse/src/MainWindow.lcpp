包 鼠标输入模块完整演示
使用 Win32窗口基础模块
使用 鼠标输入模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "鼠标输入模块：6 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.input.mouse")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–6：鼠标_取横坐标 至 鼠标_滚轮；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：鼠标_取横坐标
            // [1] 鼠标_取横坐标()
            // 功能：返回鼠标当前屏幕横坐标。
            // 参数：无；返回：int；可见性：default
            鼠标_取横坐标()
            // 命令ID：鼠标_取纵坐标
            // [2] 鼠标_取纵坐标()
            // 功能：返回鼠标当前屏幕纵坐标。
            // 参数：无；返回：int；可见性：default
            鼠标_取纵坐标()
            // 命令ID：鼠标_移动
            // [3] 鼠标_移动(横坐标, 纵坐标)
            // 功能：把鼠标移动到指定屏幕坐标。
            // 参数：横坐标:int，纵坐标:int；返回：bool；可见性：default
            鼠标_移动(1, 1)
            // 命令ID：鼠标_左键单击
            // [4] 鼠标_左键单击()
            // 功能：模拟鼠标左键按下并释放。
            // 参数：无；返回：bool；可见性：default
            鼠标_左键单击()
            // 命令ID：鼠标_右键单击
            // [5] 鼠标_右键单击()
            // 功能：模拟鼠标右键按下并释放。
            // 参数：无；返回：bool；可见性：default
            鼠标_右键单击()
            // 命令ID：鼠标_滚轮
            // [6] 鼠标_滚轮(滚动量)
            // 功能：模拟垂直滚轮，常用单位为 120。
            // 参数：滚动量:int；返回：bool；可见性：default
            鼠标_滚轮(1)
        如果结束
        调试输出("已查看 命令 1–6：鼠标_取横坐标 至 鼠标_滚轮；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
