包 鼠标输入模块完整演示
使用 Win32窗口基础模块
使用 鼠标输入模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "鼠标输入模块：29 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.input.mouse")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–15：鼠标_取横坐标 至 鼠标_水平滚轮；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：鼠标_取横坐标
            // [1] 鼠标_取横坐标()
            // 功能：[前台全局｜不占用系统鼠标] 读取真实系统光标当前屏幕横坐标；只读，不移动光标。
            // 参数：无；返回：int；可见性：default
            鼠标_取横坐标()
            // 命令ID：鼠标_取纵坐标
            // [2] 鼠标_取纵坐标()
            // 功能：[前台全局｜不占用系统鼠标] 读取真实系统光标当前屏幕纵坐标；只读，不移动光标。
            // 参数：无；返回：int；可见性：default
            鼠标_取纵坐标()
            // 命令ID：鼠标_移动
            // [3] 鼠标_移动(横坐标, 纵坐标)
            // 功能：[前台全局｜占用系统鼠标] 调用 SetCursorPos 移动真实系统光标；会改变用户当前光标位置。
            // 参数：横坐标:int，纵坐标:int；返回：bool；可见性：default
            鼠标_移动(1, 1)
            // 命令ID：鼠标_相对移动
            // [4] 鼠标_相对移动(横向增量, 纵向增量)
            // 功能：[前台全局｜占用系统鼠标] 通过 SendInput 发送相对鼠标输入；会改变用户当前光标位置，实际位移受 Windows 鼠标速度和加速度设置影响。
            // 参数：横向增量:int，纵向增量:int；返回：bool；可见性：default
            鼠标_相对移动(1, 1)
            // 命令ID：鼠标_左键按下
            // [5] 鼠标_左键按下()
            // 功能：[前台全局｜占用系统鼠标] 向当前系统输入流注入左键按下；会占用真实鼠标输入，必须与放开成对使用。
            // 参数：无；返回：bool；可见性：default
            鼠标_左键按下()
            // 命令ID：鼠标_左键放开
            // [6] 鼠标_左键放开()
            // 功能：[前台全局｜占用系统鼠标] 向当前系统输入流注入左键放开；会占用真实鼠标输入。
            // 参数：无；返回：bool；可见性：default
            鼠标_左键放开()
            // 命令ID：鼠标_左键单击
            // [7] 鼠标_左键单击()
            // 功能：[前台全局｜占用系统鼠标] 向当前前台窗口注入左键按下和放开；不主动切换前台，但会占用真实鼠标输入流。
            // 参数：无；返回：bool；可见性：default
            鼠标_左键单击()
            // 命令ID：鼠标_右键按下
            // [8] 鼠标_右键按下()
            // 功能：[前台全局｜占用系统鼠标] 向当前系统输入流注入右键按下；会占用真实鼠标输入，必须与放开成对使用。
            // 参数：无；返回：bool；可见性：default
            鼠标_右键按下()
            // 命令ID：鼠标_右键放开
            // [9] 鼠标_右键放开()
            // 功能：[前台全局｜占用系统鼠标] 向当前系统输入流注入右键放开；会占用真实鼠标输入。
            // 参数：无；返回：bool；可见性：default
            鼠标_右键放开()
            // 命令ID：鼠标_右键单击
            // [10] 鼠标_右键单击()
            // 功能：[前台全局｜占用系统鼠标] 向当前前台窗口注入右键按下和放开；不主动切换前台，但会占用真实鼠标输入流。
            // 参数：无；返回：bool；可见性：default
            鼠标_右键单击()
            // 命令ID：鼠标_中键按下
            // [11] 鼠标_中键按下()
            // 功能：[前台全局｜占用系统鼠标] 向当前系统输入流注入中键按下；会占用真实鼠标输入，必须与放开成对使用。
            // 参数：无；返回：bool；可见性：default
            鼠标_中键按下()
            // 命令ID：鼠标_中键放开
            // [12] 鼠标_中键放开()
            // 功能：[前台全局｜占用系统鼠标] 向当前系统输入流注入中键放开；会占用真实鼠标输入。
            // 参数：无；返回：bool；可见性：default
            鼠标_中键放开()
            // 命令ID：鼠标_中键单击
            // [13] 鼠标_中键单击()
            // 功能：[前台全局｜占用系统鼠标] 向当前前台窗口注入中键按下和放开；不主动切换前台，但会占用真实鼠标输入流。
            // 参数：无；返回：bool；可见性：default
            鼠标_中键单击()
            // 命令ID：鼠标_滚轮
            // [14] 鼠标_滚轮(滚动量)
            // 功能：[前台全局｜占用系统鼠标] 向当前前台窗口注入垂直滚轮；通常不移动光标，但会占用真实鼠标输入流。
            // 参数：滚动量:int；返回：bool；可见性：default
            鼠标_滚轮(1)
            // 命令ID：鼠标_水平滚轮
            // [15] 鼠标_水平滚轮(滚动量)
            // 功能：[前台全局｜占用系统鼠标] 向当前前台窗口注入水平滚轮；通常不移动光标，但会占用真实鼠标输入流。
            // 参数：滚动量:int；返回：bool；可见性：default
            鼠标_水平滚轮(1)
        如果结束
        调试输出("已查看 命令 1–15：鼠标_取横坐标 至 鼠标_水平滚轮；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 _运行第02组_被单击()
        控件_设置文本(演示状态, "命令 16–29：鼠标_窗口消息移动 至 鼠标_UIA_释放；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：鼠标_窗口消息移动
            // [16] 鼠标_窗口消息移动(窗口句柄, 客户区横坐标, 客户区纵坐标)
            // 功能：[后台窗口消息｜不占用系统鼠标] 向指定 HWND 投递 WM_MOUSEMOVE；坐标为客户区坐标，不移动真实光标，目标程序可能忽略消息。
            // 参数：窗口句柄:handle，客户区横坐标:int，客户区纵坐标:int；返回：bool；可见性：default
            鼠标_窗口消息移动(0, 1, 1)
            // 命令ID：鼠标_窗口消息左键单击
            // [17] 鼠标_窗口消息左键单击(窗口句柄, 客户区横坐标, 客户区纵坐标)
            // 功能：[后台窗口消息｜不占用系统鼠标] 向指定 HWND 投递 WM_LBUTTONDOWN/UP；坐标为客户区坐标，不移动真实光标、不抢前台。
            // 参数：窗口句柄:handle，客户区横坐标:int，客户区纵坐标:int；返回：bool；可见性：default
            鼠标_窗口消息左键单击(0, 1, 1)
            // 命令ID：鼠标_窗口消息右键单击
            // [18] 鼠标_窗口消息右键单击(窗口句柄, 客户区横坐标, 客户区纵坐标)
            // 功能：[后台窗口消息｜不占用系统鼠标] 向指定 HWND 投递 WM_RBUTTONDOWN/UP；坐标为客户区坐标，不移动真实光标、不抢前台。
            // 参数：窗口句柄:handle，客户区横坐标:int，客户区纵坐标:int；返回：bool；可见性：default
            鼠标_窗口消息右键单击(0, 1, 1)
            // 命令ID：鼠标_窗口消息中键单击
            // [19] 鼠标_窗口消息中键单击(窗口句柄, 客户区横坐标, 客户区纵坐标)
            // 功能：[后台窗口消息｜不占用系统鼠标] 向指定 HWND 投递 WM_MBUTTONDOWN/UP；坐标为客户区坐标，不移动真实光标、不抢前台。
            // 参数：窗口句柄:handle，客户区横坐标:int，客户区纵坐标:int；返回：bool；可见性：default
            鼠标_窗口消息中键单击(0, 1, 1)
            // 命令ID：鼠标_窗口消息滚轮
            // [20] 鼠标_窗口消息滚轮(窗口句柄, 滚动量, 屏幕横坐标, 屏幕纵坐标)
            // 功能：[后台窗口消息｜不占用系统鼠标] 向指定 HWND 投递 WM_MOUSEWHEEL；滚轮消息坐标为屏幕坐标，不移动真实光标。
            // 参数：窗口句柄:handle，滚动量:int，屏幕横坐标:int，屏幕纵坐标:int；返回：bool；可见性：default
            鼠标_窗口消息滚轮(0, 1, 1, 1)
            // 命令ID：鼠标_窗口消息水平滚轮
            // [21] 鼠标_窗口消息水平滚轮(窗口句柄, 滚动量, 屏幕横坐标, 屏幕纵坐标)
            // 功能：[后台窗口消息｜不占用系统鼠标] 向指定 HWND 投递 WM_MOUSEHWHEEL；滚轮消息坐标为屏幕坐标，不移动真实光标。
            // 参数：窗口句柄:handle，滚动量:int，屏幕横坐标:int，屏幕纵坐标:int；返回：bool；可见性：default
            鼠标_窗口消息水平滚轮(0, 1, 1, 1)
            // 命令ID：鼠标_UIA_按名称查找
            // [22] 鼠标_UIA_按名称查找(窗口句柄, 名称)
            // 功能：[后台 UI Automation｜不占用系统鼠标] 在指定 HWND 的 UI Automation 子树中按 Name 查找元素，返回受管元素句柄；不移动真实光标。
            // 参数：窗口句柄:handle，名称:wideString；返回：handle；可见性：default
            鼠标_UIA_按名称查找(0, "演示参数2")
            // 命令ID：鼠标_UIA_按自动化ID查找
            // [23] 鼠标_UIA_按自动化ID查找(窗口句柄, 自动化ID)
            // 功能：[后台 UI Automation｜不占用系统鼠标] 在指定 HWND 的 UI Automation 子树中按 AutomationId 查找元素，返回受管元素句柄；不移动真实光标。
            // 参数：窗口句柄:handle，自动化ID:wideString；返回：handle；可见性：default
            鼠标_UIA_按自动化ID查找(0, "演示参数2")
            // 命令ID：鼠标_UIA_调用
            // [24] 鼠标_UIA_调用(元素句柄)
            // 功能：[后台 UI Automation｜不占用系统鼠标] 调用元素的 InvokePattern；按控件语义执行，不模拟鼠标、不移动光标。
            // 参数：元素句柄:handle；返回：bool；可见性：default
            鼠标_UIA_调用(0)
            // 命令ID：鼠标_UIA_设置文本
            // [25] 鼠标_UIA_设置文本(元素句柄, 文本)
            // 功能：[后台 UI Automation｜不占用系统鼠标] 通过 ValuePattern 设置元素文本；按控件语义执行，不模拟鼠标。
            // 参数：元素句柄:handle，文本:wideString；返回：bool；可见性：default
            鼠标_UIA_设置文本(0, "演示参数2")
            // 命令ID：鼠标_UIA_取文本
            // [26] 鼠标_UIA_取文本(元素句柄)
            // 功能：[后台 UI Automation｜不占用系统鼠标] 读取元素的 ValuePattern 文本，缺少 ValuePattern 时回退到 Name；不模拟鼠标。
            // 参数：元素句柄:handle；返回：wideString；可见性：default
            鼠标_UIA_取文本(0)
            // 命令ID：鼠标_UIA_设置勾选
            // [27] 鼠标_UIA_设置勾选(元素句柄, 是否勾选)
            // 功能：[后台 UI Automation｜不占用系统鼠标] 通过 TogglePattern 调整元素勾选状态；不模拟鼠标、不改变真实光标。
            // 参数：元素句柄:handle，是否勾选:bool；返回：bool；可见性：default
            鼠标_UIA_设置勾选(0, 真)
            // 命令ID：鼠标_UIA_设置焦点
            // [28] 鼠标_UIA_设置焦点(元素句柄)
            // 功能：[后台 UI Automation｜不占用系统鼠标] 调用 UI Automation SetFocus；不移动真实光标，但可能改变键盘焦点。
            // 参数：元素句柄:handle；返回：bool；可见性：default
            鼠标_UIA_设置焦点(0)
            // 命令ID：鼠标_UIA_释放
            // [29] 鼠标_UIA_释放(元素句柄)
            // 功能：[后台 UI Automation｜不占用系统鼠标] 释放本模块持有的 UI Automation 元素句柄；不操作真实鼠标。
            // 参数：元素句柄:handle；返回：bool；可见性：default
            鼠标_UIA_释放(0)
        如果结束
        调试输出("已查看 命令 16–29：鼠标_窗口消息移动 至 鼠标_UIA_释放；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
