包 TCP通信模块完整演示
使用 Win32窗口基础模块
使用 TCP通信模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "TCP通信模块：6 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.net.tcp")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–6：TCP_连接 至 TCP_关闭；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：TCP_连接
            // [1] TCP_连接(主机, 端口, 超时毫秒)
            // 功能：连接 TCP 服务端。
            // 参数：主机:wideString，端口:int，超时毫秒:int；返回：bool；可见性：default
            TCP_连接("演示参数1", 1, 1)
            // 命令ID：TCP_发送文本
            // [2] TCP_发送文本(内容)
            // 功能：发送完整 UTF-8 文本，返回字节数，失败返回 -1。
            // 参数：内容:wideString；返回：int；可见性：default
            TCP_发送文本("演示参数1")
            // 命令ID：TCP_接收文本
            // [3] TCP_接收文本(最大字节数)
            // 功能：接收一批 UTF-8 文本。
            // 参数：最大字节数:int；返回：wideString；可见性：default
            TCP_接收文本(1)
            // 命令ID：TCP_是否已连接
            // [4] TCP_是否已连接()
            // 功能：判断当前 TCP 句柄是否处于连接状态。
            // 参数：无；返回：bool；可见性：default
            TCP_是否已连接()
            // 命令ID：TCP_取错误
            // [5] TCP_取错误()
            // 功能：返回最近 TCP 中文错误。
            // 参数：无；返回：wideString；可见性：default
            TCP_取错误()
            // 命令ID：TCP_关闭
            // [6] TCP_关闭()
            // 功能：关闭当前 TCP 连接。
            // 参数：无；返回：void；可见性：default
            TCP_关闭()
        如果结束
        调试输出("已查看 命令 1–6：TCP_连接 至 TCP_关闭；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
