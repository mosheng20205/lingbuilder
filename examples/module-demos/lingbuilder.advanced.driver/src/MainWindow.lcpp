包 设备驱动通信模块完整演示
使用 Win32窗口基础模块
使用 设备驱动通信模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "设备驱动通信模块：4 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.advanced.driver")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–4：设备_打开 至 设备_取错误码；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：设备_打开
            // [1] 设备_打开(设备路径, 允许写入)
            // 功能：打开 \.\DeviceName 形式设备。
            // 参数：设备路径:wideString，允许写入:bool；返回：handle；可见性：default
            设备_打开("module-demo-temp.txt", 真)
            // 命令ID：设备_无参数控制
            // [2] 设备_无参数控制(设备句柄, 控制码)
            // 功能：执行无输入输出缓冲区 DeviceIoControl。
            // 参数：设备句柄:handle，控制码:int；返回：bool；可见性：default
            设备_无参数控制(0, 1)
            // 命令ID：设备_关闭
            // [3] 设备_关闭(设备句柄)
            // 功能：关闭设备句柄。
            // 参数：设备句柄:handle；返回：bool；可见性：default
            设备_关闭(0)
            // 命令ID：设备_取错误码
            // [4] 设备_取错误码()
            // 功能：返回最近设备操作 Win32 错误码。
            // 参数：无；返回：int；可见性：default
            设备_取错误码()
        如果结束
        调试输出("已查看 命令 1–4：设备_打开 至 设备_取错误码；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
