# FBro 135 内置 SDK（x64）完整演示

- 模块 ID：`lingbuilder.fbro.sdk`
- 版本：`135.0.21.2.0.0`
- 类型：外置/资产模块
- 命令数：0
- 设计器控件数：0
- 分组数：1

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

FBro CEF 135 x64 只读资产模块，包含固定头文件、库、C ABI 桥接层、运行时与 SHA-256 清单；项目无需单独启用。

## 命令清单

该模块不公开 LCPP 命令，是由其它模块自动消费的 SDK/二进制资产载体。
