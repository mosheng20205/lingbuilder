包 WebSocket服务端模块完整演示
使用 Win32窗口基础模块
使用 WebSocket服务端模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "WebSocket 服务端模块：6 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.websocket.server")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–6：WSS_启动服务 至 WSS_关闭服务；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：WSS_启动服务
            // [1] WSS_启动服务(端口)
            // 功能：在 127.0.0.1 指定端口启动单连接 WebSocket 服务端，成功返回 1。
            // 参数：端口:int；返回：int；可见性：default
            WSS_启动服务(1)
            // 命令ID：WSS_等待连接
            // [2] WSS_等待连接()
            // 功能：等待一个 WebSocket 客户端连接并完成标准握手。
            // 参数：无；返回：int；可见性：default
            WSS_等待连接()
            // 命令ID：WSS_接收文本
            // [3] WSS_接收文本()
            // 功能：从当前 WebSocket 客户端接收一条 UTF-8 文本消息。
            // 参数：无；返回：wideString；可见性：default
            WSS_接收文本()
            // 命令ID：WSS_接收到调试输出
            // [4] WSS_接收到调试输出()
            // 功能：接收一条 WebSocket 文本消息并写入调试输出。
            // 参数：无；返回：int；可见性：default
            WSS_接收到调试输出()
            // 命令ID：WSS_发送文本
            // [5] WSS_发送文本(内容)
            // 功能：向当前 WebSocket 客户端发送一条文本消息。
            // 参数：内容:wideString；返回：int；可见性：default
            WSS_发送文本("演示参数1")
            // 命令ID：WSS_关闭服务
            // [6] WSS_关闭服务()
            // 功能：关闭当前 WebSocket 服务端监听和客户端连接。
            // 参数：无；返回：void；可见性：default
            WSS_关闭服务()
        如果结束
        调试输出("已查看 命令 1–6：WSS_启动服务 至 WSS_关闭服务；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
