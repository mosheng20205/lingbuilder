包 WebSocket服务端模块完整演示
使用 Win32窗口基础模块
使用 WebSocket服务端模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "WebSocket 服务端模块：50 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.websocket.server")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–17：WSS_创建服务 至 WSS_取监听地址；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：WSS_创建服务
            // [1] WSS_创建服务()
            // 功能：创建尚未启动的受管 WebSocket 服务端并返回稳定句柄。
            // 参数：无；返回：WebSocket服务端；可见性：default
            WSS_创建服务()
            // 命令ID：WSS_配置服务
            // [2] WSS_配置服务(服务端, 监听地址, 端口, 最大客户端数)
            // 功能：配置监听地址、端口和最大并发客户端数；只能在停止状态修改。
            // 参数：服务端:WebSocket服务端，监听地址:wideString，端口:int，最大客户端数:int；返回：bool；可见性：default
            WSS_配置服务(0, "https://example.com", 1, 1)
            // 命令ID：WSS_设置资源限制
            // [3] WSS_设置资源限制(服务端, 握手头上限KB, 消息上限MB, 单客户端发送队列上限MB, 超时毫秒)
            // 功能：设置握手、消息、发送背压和空闲连接硬限制，防止慢连接或大消息耗尽资源。
            // 参数：服务端:WebSocket服务端，握手头上限KB:int，消息上限MB:int，单客户端发送队列上限MB:int，超时毫秒:int；返回：bool；可见性：default
            WSS_设置资源限制(0, 1, 1, 1, 1)
            // 命令ID：WSS_设置心跳
            // [4] WSS_设置心跳(服务端, 间隔毫秒, Pong超时毫秒)
            // 功能：设置自动 Ping/Pong 心跳；间隔为 0 时关闭自动心跳。
            // 参数：服务端:WebSocket服务端，间隔毫秒:int，Pong超时毫秒:int；返回：bool；可见性：default
            WSS_设置心跳(0, 1, 1)
            // 命令ID：WSS_允许外部监听
            // [5] WSS_允许外部监听(服务端, 允许)
            // 功能：显式允许非回环地址监听；默认关闭，避免开发服务意外暴露到局域网。
            // 参数：服务端:WebSocket服务端，允许:bool；返回：bool；可见性：default
            WSS_允许外部监听(0, 真)
            // 命令ID：WSS_设置访问路径
            // [6] WSS_设置访问路径(服务端, 路径)
            // 功能：限制握手请求路径；空文本接受任意路径，非空路径必须以 / 开头。
            // 参数：服务端:WebSocket服务端，路径:wideString；返回：bool；可见性：default
            WSS_设置访问路径(0, "module-demo-temp.txt")
            // 命令ID：WSS_设置允许来源
            // [7] WSS_设置允许来源(服务端, 来源列表)
            // 功能：设置逗号分隔的 Origin 白名单；空文本不限制来源，条目按完整值匹配。
            // 参数：服务端:WebSocket服务端，来源列表:wideString；返回：bool；可见性：default
            WSS_设置允许来源(0, "演示参数2")
            // 命令ID：WSS_设置子协议
            // [8] WSS_设置子协议(服务端, 子协议列表)
            // 功能：设置逗号分隔的服务端子协议优先表；握手时选择客户端同时支持的第一项。
            // 参数：服务端:WebSocket服务端，子协议列表:wideString；返回：bool；可见性：default
            WSS_设置子协议(0, "演示参数2")
            // 命令ID：WSS_绑定连接处理器
            // [9] WSS_绑定连接处理器(服务端, &处理器)
            // 功能：绑定连接事件处理器；运行时通过窗口消息回到创建服务的 UI 线程，处理器内使用 WSS_取当前事件系列命令读取快照。
            // 参数：服务端:WebSocket服务端，处理器:handler；返回：bool；可见性：default
            WSS_绑定连接处理器(0, &模块演示回调)
            // 命令ID：WSS_绑定消息处理器
            // [10] WSS_绑定消息处理器(服务端, &处理器)
            // 功能：绑定消息事件处理器；运行时通过窗口消息回到创建服务的 UI 线程，处理器内使用 WSS_取当前事件系列命令读取快照。
            // 参数：服务端:WebSocket服务端，处理器:handler；返回：bool；可见性：default
            WSS_绑定消息处理器(0, &模块演示回调)
            // 命令ID：WSS_绑定断开处理器
            // [11] WSS_绑定断开处理器(服务端, &处理器)
            // 功能：绑定断开事件处理器；运行时通过窗口消息回到创建服务的 UI 线程，处理器内使用 WSS_取当前事件系列命令读取快照。
            // 参数：服务端:WebSocket服务端，处理器:handler；返回：bool；可见性：default
            WSS_绑定断开处理器(0, &模块演示回调)
            // 命令ID：WSS_绑定错误处理器
            // [12] WSS_绑定错误处理器(服务端, &处理器)
            // 功能：绑定错误事件处理器；运行时通过窗口消息回到创建服务的 UI 线程，处理器内使用 WSS_取当前事件系列命令读取快照。
            // 参数：服务端:WebSocket服务端，处理器:handler；返回：bool；可见性：default
            WSS_绑定错误处理器(0, &模块演示回调)
            // 命令ID：WSS_启动
            // [13] WSS_启动(服务端)
            // 功能：启动后台非阻塞监听和多客户端 WebSocket reactor；成功返回真。
            // 参数：服务端:WebSocket服务端；返回：bool；可见性：default
            WSS_启动(0)
            // 命令ID：WSS_停止
            // [14] WSS_停止(服务端)
            // 功能：停止监听，向在线客户端发送 1001 关闭帧并回收后台线程。
            // 参数：服务端:WebSocket服务端；返回：bool；可见性：default
            WSS_停止(0)
            // 命令ID：WSS_销毁服务
            // [15] WSS_销毁服务(服务端)
            // 功能：停止并释放受管服务端句柄；重复销毁返回假。
            // 参数：服务端:WebSocket服务端；返回：bool；可见性：default
            WSS_销毁服务(0)
            // 命令ID：WSS_是否运行
            // [16] WSS_是否运行(服务端)
            // 功能：判断服务端是否正在接受连接。
            // 参数：服务端:WebSocket服务端；返回：bool；可见性：default
            WSS_是否运行(0)
            // 命令ID：WSS_取监听地址
            // [17] WSS_取监听地址(服务端)
            // 功能：返回服务端当前配置的监听地址。
            // 参数：服务端:WebSocket服务端；返回：wideString；可见性：default
            WSS_取监听地址(0)
        如果结束
        调试输出("已查看 命令 1–17：WSS_创建服务 至 WSS_取监听地址；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 _运行第02组_被单击()
        控件_设置文本(演示状态, "命令 18–34：WSS_取监听端口 至 WSS_取客户端来源；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：WSS_取监听端口
            // [18] WSS_取监听端口(服务端)
            // 功能：返回实际监听端口；配置端口为 0 时可取得系统分配端口。
            // 参数：服务端:WebSocket服务端；返回：int；可见性：default
            WSS_取监听端口(0)
            // 命令ID：WSS_取在线客户端数
            // [19] WSS_取在线客户端数(服务端)
            // 功能：返回当前已完成握手且在线的客户端数量。
            // 参数：服务端:WebSocket服务端；返回：int；可见性：default
            WSS_取在线客户端数(0)
            // 命令ID：WSS_取累计连接数
            // [20] WSS_取累计连接数(服务端)
            // 功能：返回成功完成握手的累计连接数量。
            // 参数：服务端:WebSocket服务端；返回：longLong；可见性：default
            WSS_取累计连接数(0)
            // 命令ID：WSS_取累计消息数
            // [21] WSS_取累计消息数(服务端)
            // 功能：返回收到的完整文本和二进制消息总数。
            // 参数：服务端:WebSocket服务端；返回：longLong；可见性：default
            WSS_取累计消息数(0)
            // 命令ID：WSS_取服务错误
            // [22] WSS_取服务错误(服务端)
            // 功能：返回服务端最近一次中文错误；句柄无效时返回管理器错误。
            // 参数：服务端:WebSocket服务端；返回：wideString；可见性：default
            WSS_取服务错误(0)
            // 命令ID：WSS_取当前事件类型
            // [23] WSS_取当前事件类型()
            // 功能：返回当前回调事件类型：连接、文本、二进制、断开或错误。
            // 参数：无；返回：wideString；可见性：default
            WSS_取当前事件类型()
            // 命令ID：WSS_取当前客户端
            // [24] WSS_取当前客户端()
            // 功能：返回当前事件对应的受管客户端句柄。
            // 参数：无；返回：WebSocket客户端；可见性：default
            WSS_取当前客户端()
            // 命令ID：WSS_取当前消息类型
            // [25] WSS_取当前消息类型()
            // 功能：返回当前消息类型：文本或二进制；其它事件返回空文本。
            // 参数：无；返回：wideString；可见性：default
            WSS_取当前消息类型()
            // 命令ID：WSS_取当前文本
            // [26] WSS_取当前文本()
            // 功能：返回当前文本消息或错误说明快照。
            // 参数：无；返回：wideString；可见性：default
            WSS_取当前文本()
            // 命令ID：WSS_取当前二进制
            // [27] WSS_取当前二进制()
            // 功能：返回当前二进制消息的独立字节集副本。
            // 参数：无；返回：bytes；可见性：default
            WSS_取当前二进制()
            // 命令ID：WSS_取当前关闭代码
            // [28] WSS_取当前关闭代码()
            // 功能：返回当前断开事件的 WebSocket 关闭状态码。
            // 参数：无；返回：int；可见性：default
            WSS_取当前关闭代码()
            // 命令ID：WSS_取当前关闭原因
            // [29] WSS_取当前关闭原因()
            // 功能：返回当前断开事件的 UTF-8 关闭原因。
            // 参数：无；返回：wideString；可见性：default
            WSS_取当前关闭原因()
            // 命令ID：WSS_客户端是否在线
            // [30] WSS_客户端是否在线(客户端)
            // 功能：判断客户端是否仍在线且未进入关闭状态。
            // 参数：客户端:WebSocket客户端；返回：bool；可见性：default
            WSS_客户端是否在线(0)
            // 命令ID：WSS_取客户端地址
            // [31] WSS_取客户端地址(客户端)
            // 功能：返回客户端数字 IP 地址。
            // 参数：客户端:WebSocket客户端；返回：wideString；可见性：default
            WSS_取客户端地址(0)
            // 命令ID：WSS_取客户端端口
            // [32] WSS_取客户端端口(客户端)
            // 功能：返回客户端 TCP 端口。
            // 参数：客户端:WebSocket客户端；返回：int；可见性：default
            WSS_取客户端端口(0)
            // 命令ID：WSS_取客户端路径
            // [33] WSS_取客户端路径(客户端)
            // 功能：返回客户端握手请求路径。
            // 参数：客户端:WebSocket客户端；返回：wideString；可见性：default
            WSS_取客户端路径(0)
            // 命令ID：WSS_取客户端来源
            // [34] WSS_取客户端来源(客户端)
            // 功能：返回客户端握手 Origin。
            // 参数：客户端:WebSocket客户端；返回：wideString；可见性：default
            WSS_取客户端来源(0)
        如果结束
        调试输出("已查看 命令 18–34：WSS_取监听端口 至 WSS_取客户端来源；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 _运行第03组_被单击()
        控件_设置文本(演示状态, "命令 35–50：WSS_取客户端子协议 至 WSS_关闭服务；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：WSS_取客户端子协议
            // [35] WSS_取客户端子协议(客户端)
            // 功能：返回握手协商出的子协议。
            // 参数：客户端:WebSocket客户端；返回：wideString；可见性：default
            WSS_取客户端子协议(0)
            // 命令ID：WSS_取客户端连接时长
            // [36] WSS_取客户端连接时长(客户端)
            // 功能：返回客户端完成握手后的在线毫秒数。
            // 参数：客户端:WebSocket客户端；返回：longLong；可见性：default
            WSS_取客户端连接时长(0)
            // 命令ID：WSS_取客户端列表JSON
            // [37] WSS_取客户端列表JSON(服务端)
            // 功能：返回在线客户端的 UTF-16 JSON 数组，包含句柄、地址、端口、路径、来源和子协议。
            // 参数：服务端:WebSocket服务端；返回：wideString；可见性：default
            WSS_取客户端列表JSON(0)
            // 命令ID：WSS_发送文本给客户端
            // [38] WSS_发送文本给客户端(客户端, 内容)
            // 功能：把一条完整 UTF-8 文本消息加入指定客户端的有界发送队列。
            // 参数：客户端:WebSocket客户端，内容:wideString；返回：bool；可见性：default
            WSS_发送文本给客户端(0, "演示参数2")
            // 命令ID：WSS_发送二进制给客户端
            // [39] WSS_发送二进制给客户端(客户端, 数据)
            // 功能：把字节集作为一条完整二进制消息加入指定客户端的有界发送队列。
            // 参数：客户端:WebSocket客户端，数据:bytes；返回：bool；可见性：default
            WSS_发送二进制给客户端(0, {})
            // 命令ID：WSS_广播文本
            // [40] WSS_广播文本(服务端, 内容)
            // 功能：向服务端全部在线客户端广播文本，返回成功入队的客户端数量。
            // 参数：服务端:WebSocket服务端，内容:wideString；返回：int；可见性：default
            WSS_广播文本(0, "演示参数2")
            // 命令ID：WSS_广播二进制
            // [41] WSS_广播二进制(服务端, 数据)
            // 功能：向服务端全部在线客户端广播二进制消息，返回成功入队的客户端数量。
            // 参数：服务端:WebSocket服务端，数据:bytes；返回：int；可见性：default
            WSS_广播二进制(0, {})
            // 命令ID：WSS_发送Ping
            // [42] WSS_发送Ping(客户端, 数据)
            // 功能：发送最多 125 字节的 Ping 控制帧；数据使用 UTF-8 编码。
            // 参数：客户端:WebSocket客户端，数据:wideString；返回：bool；可见性：default
            WSS_发送Ping(0, "演示参数2")
            // 命令ID：WSS_关闭客户端
            // [43] WSS_关闭客户端(客户端, 状态码, 原因)
            // 功能：执行 WebSocket 优雅关闭握手；状态码和 UTF-8 原因会经过 RFC 6455 校验。
            // 参数：客户端:WebSocket客户端，状态码:int，原因:wideString；返回：bool；可见性：default
            WSS_关闭客户端(0, 1, "演示参数3")
            // 命令ID：WSS_强制断开客户端
            // [44] WSS_强制断开客户端(客户端)
            // 功能：立即关闭客户端 TCP 连接，仅用于协议错误、超时或管理操作。
            // 参数：客户端:WebSocket客户端；返回：bool；可见性：default
            WSS_强制断开客户端(0)
            // 命令ID：WSS_启动服务
            // [45] WSS_启动服务(端口)
            // 功能：旧版默认服务兼容入口；启动回环服务，新代码应使用受管服务 API。
            // 参数：端口:int；返回：int；可见性：advanced
            WSS_启动服务(1)
            // 命令ID：WSS_等待连接
            // [46] WSS_等待连接()
            // 功能：旧版阻塞兼容入口；最多等待 30 秒，新代码应使用连接处理器。
            // 参数：无；返回：int；可见性：advanced
            WSS_等待连接()
            // 命令ID：WSS_接收文本
            // [47] WSS_接收文本()
            // 功能：旧版阻塞兼容入口；最多等待 30 秒返回下一条文本消息。
            // 参数：无；返回：wideString；可见性：advanced
            WSS_接收文本()
            // 命令ID：WSS_接收到调试输出
            // [48] WSS_接收到调试输出()
            // 功能：旧版兼容入口；接收一条文本消息并写入调试输出。
            // 参数：无；返回：int；可见性：advanced
            WSS_接收到调试输出()
            // 命令ID：WSS_发送文本
            // [49] WSS_发送文本(内容)
            // 功能：旧版兼容入口；向默认服务最近连接的客户端发送文本。
            // 参数：内容:wideString；返回：int；可见性：advanced
            WSS_发送文本("演示参数1")
            // 命令ID：WSS_关闭服务
            // [50] WSS_关闭服务()
            // 功能：旧版兼容入口；停止并销毁默认受管服务。
            // 参数：无；返回：void；可见性：advanced
            WSS_关闭服务()
        如果结束
        调试输出("已查看 命令 35–50：WSS_取客户端子协议 至 WSS_关闭服务；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 模块演示回调()
        调试输出("模块演示回调已触发。")
    结束

结束类
