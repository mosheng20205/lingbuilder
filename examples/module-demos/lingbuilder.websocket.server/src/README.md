# WebSocket 服务端模块完整演示

- 模块 ID：`lingbuilder.websocket.server`
- 版本：`2.0.0`
- 最低 LingBuilder：`0.2.8`
- 类型：LingBuilder 内置模块
- 命令数：50
- 设计器控件数：0
- 分组数：3

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

提供受管 RFC 6455 WebSocket 服务端、后台多客户端处理、文本与二进制消息、心跳、关闭握手、访问控制、资源限制和运行统计。

## 命令清单

| # | 命令 | 签名 | 返回 | 说明 |
|---:|---|---|---|---|
| 1 | `WSS_创建服务` | `WSS_创建服务()` | WebSocket服务端 | 创建尚未启动的受管 WebSocket 服务端并返回稳定句柄。 |
| 2 | `WSS_配置服务` | `WSS_配置服务(服务端, 监听地址, 端口, 最大客户端数)` | bool | 配置监听地址、端口和最大并发客户端数；只能在停止状态修改。 |
| 3 | `WSS_设置资源限制` | `WSS_设置资源限制(服务端, 握手头上限KB, 消息上限MB, 单客户端发送队列上限MB, 超时毫秒)` | bool | 设置握手、消息、发送背压和空闲连接硬限制，防止慢连接或大消息耗尽资源。 |
| 4 | `WSS_设置心跳` | `WSS_设置心跳(服务端, 间隔毫秒, Pong超时毫秒)` | bool | 设置自动 Ping/Pong 心跳；间隔为 0 时关闭自动心跳。 |
| 5 | `WSS_允许外部监听` | `WSS_允许外部监听(服务端, 允许)` | bool | 显式允许非回环地址监听；默认关闭，避免开发服务意外暴露到局域网。 |
| 6 | `WSS_设置访问路径` | `WSS_设置访问路径(服务端, 路径)` | bool | 限制握手请求路径；空文本接受任意路径，非空路径必须以 / 开头。 |
| 7 | `WSS_设置允许来源` | `WSS_设置允许来源(服务端, 来源列表)` | bool | 设置逗号分隔的 Origin 白名单；空文本不限制来源，条目按完整值匹配。 |
| 8 | `WSS_设置子协议` | `WSS_设置子协议(服务端, 子协议列表)` | bool | 设置逗号分隔的服务端子协议优先表；握手时选择客户端同时支持的第一项。 |
| 9 | `WSS_绑定连接处理器` | `WSS_绑定连接处理器(服务端, &处理器)` | bool | 绑定连接事件处理器；运行时通过窗口消息回到创建服务的 UI 线程，处理器内使用 WSS_取当前事件系列命令读取快照。 |
| 10 | `WSS_绑定消息处理器` | `WSS_绑定消息处理器(服务端, &处理器)` | bool | 绑定消息事件处理器；运行时通过窗口消息回到创建服务的 UI 线程，处理器内使用 WSS_取当前事件系列命令读取快照。 |
| 11 | `WSS_绑定断开处理器` | `WSS_绑定断开处理器(服务端, &处理器)` | bool | 绑定断开事件处理器；运行时通过窗口消息回到创建服务的 UI 线程，处理器内使用 WSS_取当前事件系列命令读取快照。 |
| 12 | `WSS_绑定错误处理器` | `WSS_绑定错误处理器(服务端, &处理器)` | bool | 绑定错误事件处理器；运行时通过窗口消息回到创建服务的 UI 线程，处理器内使用 WSS_取当前事件系列命令读取快照。 |
| 13 | `WSS_启动` | `WSS_启动(服务端)` | bool | 启动后台非阻塞监听和多客户端 WebSocket reactor；成功返回真。 |
| 14 | `WSS_停止` | `WSS_停止(服务端)` | bool | 停止监听，向在线客户端发送 1001 关闭帧并回收后台线程。 |
| 15 | `WSS_销毁服务` | `WSS_销毁服务(服务端)` | bool | 停止并释放受管服务端句柄；重复销毁返回假。 |
| 16 | `WSS_是否运行` | `WSS_是否运行(服务端)` | bool | 判断服务端是否正在接受连接。 |
| 17 | `WSS_取监听地址` | `WSS_取监听地址(服务端)` | wideString | 返回服务端当前配置的监听地址。 |
| 18 | `WSS_取监听端口` | `WSS_取监听端口(服务端)` | int | 返回实际监听端口；配置端口为 0 时可取得系统分配端口。 |
| 19 | `WSS_取在线客户端数` | `WSS_取在线客户端数(服务端)` | int | 返回当前已完成握手且在线的客户端数量。 |
| 20 | `WSS_取累计连接数` | `WSS_取累计连接数(服务端)` | longLong | 返回成功完成握手的累计连接数量。 |
| 21 | `WSS_取累计消息数` | `WSS_取累计消息数(服务端)` | longLong | 返回收到的完整文本和二进制消息总数。 |
| 22 | `WSS_取服务错误` | `WSS_取服务错误(服务端)` | wideString | 返回服务端最近一次中文错误；句柄无效时返回管理器错误。 |
| 23 | `WSS_取当前事件类型` | `WSS_取当前事件类型()` | wideString | 返回当前回调事件类型：连接、文本、二进制、断开或错误。 |
| 24 | `WSS_取当前客户端` | `WSS_取当前客户端()` | WebSocket客户端 | 返回当前事件对应的受管客户端句柄。 |
| 25 | `WSS_取当前消息类型` | `WSS_取当前消息类型()` | wideString | 返回当前消息类型：文本或二进制；其它事件返回空文本。 |
| 26 | `WSS_取当前文本` | `WSS_取当前文本()` | wideString | 返回当前文本消息或错误说明快照。 |
| 27 | `WSS_取当前二进制` | `WSS_取当前二进制()` | bytes | 返回当前二进制消息的独立字节集副本。 |
| 28 | `WSS_取当前关闭代码` | `WSS_取当前关闭代码()` | int | 返回当前断开事件的 WebSocket 关闭状态码。 |
| 29 | `WSS_取当前关闭原因` | `WSS_取当前关闭原因()` | wideString | 返回当前断开事件的 UTF-8 关闭原因。 |
| 30 | `WSS_客户端是否在线` | `WSS_客户端是否在线(客户端)` | bool | 判断客户端是否仍在线且未进入关闭状态。 |
| 31 | `WSS_取客户端地址` | `WSS_取客户端地址(客户端)` | wideString | 返回客户端数字 IP 地址。 |
| 32 | `WSS_取客户端端口` | `WSS_取客户端端口(客户端)` | int | 返回客户端 TCP 端口。 |
| 33 | `WSS_取客户端路径` | `WSS_取客户端路径(客户端)` | wideString | 返回客户端握手请求路径。 |
| 34 | `WSS_取客户端来源` | `WSS_取客户端来源(客户端)` | wideString | 返回客户端握手 Origin。 |
| 35 | `WSS_取客户端子协议` | `WSS_取客户端子协议(客户端)` | wideString | 返回握手协商出的子协议。 |
| 36 | `WSS_取客户端连接时长` | `WSS_取客户端连接时长(客户端)` | longLong | 返回客户端完成握手后的在线毫秒数。 |
| 37 | `WSS_取客户端列表JSON` | `WSS_取客户端列表JSON(服务端)` | wideString | 返回在线客户端的 UTF-16 JSON 数组，包含句柄、地址、端口、路径、来源和子协议。 |
| 38 | `WSS_发送文本给客户端` | `WSS_发送文本给客户端(客户端, 内容)` | bool | 把一条完整 UTF-8 文本消息加入指定客户端的有界发送队列。 |
| 39 | `WSS_发送二进制给客户端` | `WSS_发送二进制给客户端(客户端, 数据)` | bool | 把字节集作为一条完整二进制消息加入指定客户端的有界发送队列。 |
| 40 | `WSS_广播文本` | `WSS_广播文本(服务端, 内容)` | int | 向服务端全部在线客户端广播文本，返回成功入队的客户端数量。 |
| 41 | `WSS_广播二进制` | `WSS_广播二进制(服务端, 数据)` | int | 向服务端全部在线客户端广播二进制消息，返回成功入队的客户端数量。 |
| 42 | `WSS_发送Ping` | `WSS_发送Ping(客户端, 数据)` | bool | 发送最多 125 字节的 Ping 控制帧；数据使用 UTF-8 编码。 |
| 43 | `WSS_关闭客户端` | `WSS_关闭客户端(客户端, 状态码, 原因)` | bool | 执行 WebSocket 优雅关闭握手；状态码和 UTF-8 原因会经过 RFC 6455 校验。 |
| 44 | `WSS_强制断开客户端` | `WSS_强制断开客户端(客户端)` | bool | 立即关闭客户端 TCP 连接，仅用于协议错误、超时或管理操作。 |
| 45 | `WSS_启动服务` | `WSS_启动服务(端口)` | int | 旧版默认服务兼容入口；启动回环服务，新代码应使用受管服务 API。 |
| 46 | `WSS_等待连接` | `WSS_等待连接()` | int | 旧版阻塞兼容入口；最多等待 30 秒，新代码应使用连接处理器。 |
| 47 | `WSS_接收文本` | `WSS_接收文本()` | wideString | 旧版阻塞兼容入口；最多等待 30 秒返回下一条文本消息。 |
| 48 | `WSS_接收到调试输出` | `WSS_接收到调试输出()` | int | 旧版兼容入口；接收一条文本消息并写入调试输出。 |
| 49 | `WSS_发送文本` | `WSS_发送文本(内容)` | int | 旧版兼容入口；向默认服务最近连接的客户端发送文本。 |
| 50 | `WSS_关闭服务` | `WSS_关闭服务()` | void | 旧版兼容入口；停止并销毁默认受管服务。 |
