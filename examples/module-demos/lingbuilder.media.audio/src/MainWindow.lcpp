包 基础音频模块完整演示
使用 Win32窗口基础模块
使用 基础音频模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "基础音频模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.media.audio")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–5：音频_播放WAV 至 音频_设置主音量；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：音频_播放WAV
            // [1] 音频_播放WAV(路径, 循环)
            // 功能：异步播放 WAV 文件。
            // 参数：路径:wideString，循环:bool；返回：bool；可见性：default
            音频_播放WAV("module-demo-temp.txt", 真)
            // 命令ID：音频_停止
            // [2] 音频_停止()
            // 功能：停止当前 PlaySound 音频。
            // 参数：无；返回：void；可见性：default
            音频_停止()
            // 命令ID：音频_播放系统提示
            // [3] 音频_播放系统提示()
            // 功能：播放系统通知声音。
            // 参数：无；返回：bool；可见性：default
            音频_播放系统提示()
            // 命令ID：音频_取主音量
            // [4] 音频_取主音量()
            // 功能：返回 waveOut 主音量 0 到 100。
            // 参数：无；返回：int；可见性：default
            音频_取主音量()
            // 命令ID：音频_设置主音量
            // [5] 音频_设置主音量(音量)
            // 功能：设置 waveOut 主音量 0 到 100。
            // 参数：音量:int；返回：bool；可见性：default
            音频_设置主音量(1)
        如果结束
        调试输出("已查看 命令 1–5：音频_播放WAV 至 音频_设置主音量；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
