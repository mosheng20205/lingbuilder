包 位图像素模块完整演示
使用 Win32窗口基础模块
使用 位图像素模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "位图像素模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.image.bitmap")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–5：位图_取像素ARGB 至 位图_取像素蓝色；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：位图_取像素ARGB
            // [1] 位图_取像素ARGB(路径, 横坐标, 纵坐标)
            // 功能：返回 ARGB 颜色值，失败返回 -1。
            // 参数：路径:wideString，横坐标:int，纵坐标:int；返回：longLong；可见性：default
            位图_取像素ARGB("module-demo-temp.txt", 1, 1)
            // 命令ID：位图_置像素ARGB
            // [2] 位图_置像素ARGB(来源, 目标, 横坐标, 纵坐标, ARGB)
            // 功能：修改一个像素并保存到目标文件。
            // 参数：来源:wideString，目标:wideString，横坐标:int，纵坐标:int，ARGB:longLong；返回：bool；可见性：default
            位图_置像素ARGB("演示参数1", "演示参数2", 1, 1, 1)
            // 命令ID：位图_取像素红色
            // [3] 位图_取像素红色(ARGB)
            // 功能：从 ARGB 值读取红色分量。
            // 参数：ARGB:longLong；返回：int；可见性：default
            位图_取像素红色(1)
            // 命令ID：位图_取像素绿色
            // [4] 位图_取像素绿色(ARGB)
            // 功能：从 ARGB 值读取绿色分量。
            // 参数：ARGB:longLong；返回：int；可见性：default
            位图_取像素绿色(1)
            // 命令ID：位图_取像素蓝色
            // [5] 位图_取像素蓝色(ARGB)
            // 功能：从 ARGB 值读取蓝色分量。
            // 参数：ARGB:longLong；返回：int；可见性：default
            位图_取像素蓝色(1)
        如果结束
        调试输出("已查看 命令 1–5：位图_取像素ARGB 至 位图_取像素蓝色；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
