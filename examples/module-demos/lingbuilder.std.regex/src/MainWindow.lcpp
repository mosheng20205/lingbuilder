包 正则表达式模块完整演示
使用 Win32窗口基础模块
使用 正则表达式模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "正则表达式模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.std.regex")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–5：正则_完全匹配 至 正则_匹配数量；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：正则_完全匹配
            // [1] 正则_完全匹配(文本, 表达式)
            // 功能：判断整个文本是否匹配表达式。
            // 参数：文本:wideString，表达式:wideString；返回：bool；可见性：default
            正则_完全匹配("演示参数1", "演示参数2")
            // 命令ID：正则_是否包含
            // [2] 正则_是否包含(文本, 表达式)
            // 功能：判断文本中是否存在匹配内容。
            // 参数：文本:wideString，表达式:wideString；返回：bool；可见性：default
            正则_是否包含("演示参数1", "演示参数2")
            // 命令ID：正则_取首个
            // [3] 正则_取首个(文本, 表达式)
            // 功能：返回首个匹配文本，没有匹配或表达式非法时返回空文本。
            // 参数：文本:wideString，表达式:wideString；返回：wideString；可见性：default
            正则_取首个("演示参数1", "演示参数2")
            // 命令ID：正则_替换
            // [4] 正则_替换(文本, 表达式, 替换内容)
            // 功能：替换全部正则匹配。
            // 参数：文本:wideString，表达式:wideString，替换内容:wideString；返回：wideString；可见性：default
            正则_替换("演示参数1", "演示参数2", "演示参数3")
            // 命令ID：正则_匹配数量
            // [5] 正则_匹配数量(文本, 表达式)
            // 功能：返回不重叠匹配数量。
            // 参数：文本:wideString，表达式:wideString；返回：int；可见性：default
            正则_匹配数量("演示参数1", "演示参数2")
        如果结束
        调试输出("已查看 命令 1–5：正则_完全匹配 至 正则_匹配数量；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
