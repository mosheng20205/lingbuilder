# HTTP 客户端模块完整演示

- 模块 ID：`lingbuilder.net.http-client`
- 版本：`2.0.0`
- 类型：LingBuilder 内置模块
- 命令数：74
- 设计器控件数：0
- 分组数：4

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

提供受管 WinHTTP HTTP/HTTPS 客户端、多请求并发、后台完成事件、代理与身份验证、TLS 证书策略、重定向、Cookie、压缩、文本/二进制/文件上传下载、资源限制和运行统计。

## 命令清单

| # | 命令 | 签名 | 返回 | 说明 |
|---:|---|---|---|---|
| 1 | `HTTP客户端_创建客户端` | `HTTP客户端_创建客户端()` | HTTP客户端 | 创建独立的受管 WinHTTP 客户端并返回稳定 ID。 |
| 2 | `HTTP客户端_销毁客户端` | `HTTP客户端_销毁客户端(客户端)` | bool | 取消并回收该客户端的全部请求、Cookie 会话和 WinHTTP 资源。 |
| 3 | `HTTP客户端_设置UserAgent` | `HTTP客户端_设置UserAgent(客户端, UserAgent)` | bool | 设置该客户端后续请求使用的 User-Agent；仅在无活动请求时修改。 |
| 4 | `HTTP客户端_设置超时` | `HTTP客户端_设置超时(客户端, 解析毫秒, 连接毫秒, 发送毫秒, 接收毫秒)` | bool | 分别设置 DNS 解析、连接、发送和接收超时，范围 100-3600000 毫秒。 |
| 5 | `HTTP客户端_设置资源限制` | `HTTP客户端_设置资源限制(客户端, 响应头上限KB, 响应体上限MB, 上传上限MB, 最大重定向次数)` | bool | 限制响应头、内存或文件响应、上传和自动重定向，阻止不受控资源占用。 |
| 6 | `HTTP客户端_设置重定向策略` | `HTTP客户端_设置重定向策略(客户端, 允许重定向, 允许HTTPS降级HTTP)` | bool | 设置自动重定向；默认允许同等或更安全协议，禁止 HTTPS 降级到 HTTP。 |
| 7 | `HTTP客户端_设置代理` | `HTTP客户端_设置代理(客户端, 模式, 代理地址, 绕过列表)` | bool | 配置代理模式：0 系统默认、1 直连、2 固定代理；固定代理必须提供地址。 |
| 8 | `HTTP客户端_设置服务器凭据` | `HTTP客户端_设置服务器凭据(客户端, 用户名, 密码)` | bool | 设置内存中的 HTTP Basic 服务器凭据；不会写入模块日志或持久化配置。 |
| 9 | `HTTP客户端_设置代理凭据` | `HTTP客户端_设置代理凭据(客户端, 用户名, 密码)` | bool | 设置内存中的 HTTP Basic 代理凭据。 |
| 10 | `HTTP客户端_设置TLS策略` | `HTTP客户端_设置TLS策略(客户端, 验证证书, 允许自签名)` | bool | 设置 HTTPS 证书策略；默认完整验证系统信任链、主机名、用途和有效期。 |
| 11 | `HTTP客户端_设置证书固定` | `HTTP客户端_设置证书固定(客户端, SHA256指纹)` | bool | 设置最终 HTTPS 服务端证书的 SHA-256 指纹；空文本清除固定，可包含冒号或空格。 |
| 12 | `HTTP客户端_设置自动解压` | `HTTP客户端_设置自动解压(客户端, 启用)` | bool | 启用或关闭 WinHTTP gzip/deflate 自动解压；默认启用。 |
| 13 | `HTTP客户端_设置Cookie` | `HTTP客户端_设置Cookie(客户端, 启用)` | bool | 启用或关闭该受管客户端会话内的 Cookie 接收和回送；默认启用且不与浏览器共享。 |
| 14 | `HTTP客户端_设置默认请求头` | `HTTP客户端_设置默认请求头(客户端, 名称, 值)` | bool | 设置或替换客户端默认请求头，拒绝非法名称、CR/LF 注入和受运行时管理的头。 |
| 15 | `HTTP客户端_添加默认请求头` | `HTTP客户端_添加默认请求头(客户端, 名称, 值)` | bool | 追加客户端默认请求头；适合 Accept、Cache-Control 等可重复头。 |
| 16 | `HTTP客户端_删除默认请求头` | `HTTP客户端_删除默认请求头(客户端, 名称)` | bool | 删除客户端中全部同名默认请求头。 |
| 17 | `HTTP客户端_清空默认请求头` | `HTTP客户端_清空默认请求头(客户端)` | bool | 清空客户端默认请求头。 |
| 18 | `HTTP客户端_取消全部请求` | `HTTP客户端_取消全部请求(客户端)` | int | 取消客户端全部运行中请求并关闭对应 WinHTTP 请求句柄。 |
| 19 | `HTTP客户端_取活动请求数` | `HTTP客户端_取活动请求数(客户端)` | int | 返回当前正在发送或接收的请求数量。 |
| 20 | `HTTP客户端_取累计请求数` | `HTTP客户端_取累计请求数(客户端)` | longLong | 返回客户端已开始执行的累计请求数量。 |
| 21 | `HTTP客户端_取客户端错误` | `HTTP客户端_取客户端错误(客户端)` | wideString | 返回客户端配置、会话创建或句柄操作的最近中文错误。 |
| 22 | `HTTP客户端_创建请求` | `HTTP客户端_创建请求(客户端, 方法, 地址)` | HTTP客户端请求 | 创建尚未执行的受管请求；仅接受 http:// 或 https:// 和合法 HTTP 方法。 |
| 23 | `HTTP客户端_设置请求头` | `HTTP客户端_设置请求头(请求, 名称, 值)` | bool | 在请求启动前设置或替换请求头。 |
| 24 | `HTTP客户端_添加请求头` | `HTTP客户端_添加请求头(请求, 名称, 值)` | bool | 在请求启动前追加请求头。 |
| 25 | `HTTP客户端_删除请求头` | `HTTP客户端_删除请求头(请求, 名称)` | bool | 在请求启动前删除全部同名请求头。 |
| 26 | `HTTP客户端_清空请求头` | `HTTP客户端_清空请求头(请求)` | bool | 在请求启动前清空请求级请求头，不影响客户端默认头。 |
| 27 | `HTTP客户端_设置文本正文` | `HTTP客户端_设置文本正文(请求, 正文, 内容类型)` | bool | 把文本编码为 UTF-8 请求正文并设置 Content-Type。 |
| 28 | `HTTP客户端_设置JSON正文` | `HTTP客户端_设置JSON正文(请求, JSON)` | bool | 把 UTF-8 JSON 文本设为请求正文并使用 application/json。 |
| 29 | `HTTP客户端_设置二进制正文` | `HTTP客户端_设置二进制正文(请求, 数据, 内容类型)` | bool | 设置独立复制的字节集请求正文。 |
| 30 | `HTTP客户端_设置十六进制正文` | `HTTP客户端_设置十六进制正文(请求, 十六进制, 内容类型)` | bool | 校验并解码偶数长度十六进制文本作为请求正文。 |
| 31 | `HTTP客户端_设置文件正文` | `HTTP客户端_设置文件正文(请求, 文件路径, 内容类型)` | bool | 以 64KB 分块上传本机文件，不把整个文件载入内存。 |
| 32 | `HTTP客户端_设置响应文件` | `HTTP客户端_设置响应文件(请求, 文件路径, 允许覆盖)` | bool | 把响应流式写入临时文件并原子替换目标文件，避免大响应驻留内存。 |
| 33 | `HTTP客户端_绑定完成处理器` | `HTTP客户端_绑定完成处理器(请求, &处理器)` | bool | 绑定请求完成后在创建窗口 UI 线程调用的无参数处理器。 |
| 34 | `HTTP客户端_开始请求` | `HTTP客户端_开始请求(请求)` | bool | 在独立后台线程执行请求，完成后投递 UI 线程处理器。 |
| 35 | `HTTP客户端_执行同步` | `HTTP客户端_执行同步(请求)` | bool | 在当前线程同步执行请求；主要用于命令行、测试或受控后台任务，UI 代码应使用异步。 |
| 36 | `HTTP客户端_等待请求` | `HTTP客户端_等待请求(请求, 超时毫秒)` | bool | 等待异步请求完成；主要用于测试和后台任务，UI 线程不应长时间等待。 |
| 37 | `HTTP客户端_取消请求` | `HTTP客户端_取消请求(请求)` | bool | 请求取消并关闭活动 WinHTTP 句柄；已完成请求返回假。 |
| 38 | `HTTP客户端_销毁请求` | `HTTP客户端_销毁请求(请求)` | bool | 取消、等待后台线程结束并永久释放请求 ID。 |
| 39 | `HTTP客户端_取请求客户端` | `HTTP客户端_取请求客户端(请求)` | HTTP客户端 | 返回请求所属的受管客户端 ID。 |
| 40 | `HTTP客户端_取请求方法` | `HTTP客户端_取请求方法(请求)` | wideString | 返回请求的大写 HTTP 方法。 |
| 41 | `HTTP客户端_取请求地址` | `HTTP客户端_取请求地址(请求)` | wideString | 返回请求创建时的原始 URL。 |
| 42 | `HTTP客户端_取请求状态` | `HTTP客户端_取请求状态(请求)` | wideString | 返回未开始、运行中、已完成、已取消或错误。 |
| 43 | `HTTP客户端_请求是否完成` | `HTTP客户端_请求是否完成(请求)` | bool | 判断请求是否已结束，包括成功、HTTP 非 2xx、错误或取消。 |
| 44 | `HTTP客户端_请求是否成功` | `HTTP客户端_请求是否成功(请求)` | bool | 判断传输成功且 HTTP 状态码位于 200-299。 |
| 45 | `HTTP客户端_取请求错误` | `HTTP客户端_取请求错误(请求)` | wideString | 返回请求最近中文错误，不包含凭据或请求正文。 |
| 46 | `HTTP客户端_取系统错误码` | `HTTP客户端_取系统错误码(请求)` | int | 返回最近 WinHTTP 或 Win32 错误码；协议状态失败通常为 0。 |
| 47 | `HTTP客户端_取请求耗时` | `HTTP客户端_取请求耗时(请求)` | longLong | 返回请求开始到结束的毫秒数。 |
| 48 | `HTTP客户端_取上传字节数` | `HTTP客户端_取上传字节数(请求)` | longLong | 返回已经成功写入 WinHTTP 的请求正文字节数。 |
| 49 | `HTTP客户端_取下载字节数` | `HTTP客户端_取下载字节数(请求)` | longLong | 返回已经读取并校验的响应正文字节数。 |
| 50 | `HTTP客户端_取响应状态码` | `HTTP客户端_取响应状态码(请求)` | int | 返回 HTTP 响应状态码；传输失败且无响应时为 0。 |
| 51 | `HTTP客户端_取响应状态文本` | `HTTP客户端_取响应状态文本(请求)` | wideString | 返回服务端状态文本，例如 OK 或 Not Found。 |
| 52 | `HTTP客户端_取响应协议` | `HTTP客户端_取响应协议(请求)` | wideString | 返回响应协议版本，例如 HTTP/1.1 或 HTTP/2。 |
| 53 | `HTTP客户端_取最终地址` | `HTTP客户端_取最终地址(请求)` | wideString | 返回自动重定向后的最终 URL。 |
| 54 | `HTTP客户端_取全部响应头` | `HTTP客户端_取全部响应头(请求)` | wideString | 返回 WinHTTP 提供的完整 CRLF 响应头文本。 |
| 55 | `HTTP客户端_取响应头JSON` | `HTTP客户端_取响应头JSON(请求)` | wideString | 以 UTF-16 JSON 对象返回响应头；重复头合并为逗号分隔文本。 |
| 56 | `HTTP客户端_取响应十六进制` | `HTTP客户端_取响应十六进制(请求)` | wideString | 以小写十六进制返回内存响应正文。 |
| 57 | `HTTP客户端_取响应大小` | `HTTP客户端_取响应大小(请求)` | longLong | 返回响应正文总字节数，包括流式写入文件的响应。 |
| 58 | `HTTP客户端_取响应文件` | `HTTP客户端_取响应文件(请求)` | wideString | 返回成功流式保存后的目标文件路径。 |
| 59 | `HTTP客户端_取内容类型` | `HTTP客户端_取内容类型(请求)` | wideString | 返回响应 Content-Type。 |
| 60 | `HTTP客户端_取服务器证书SHA256` | `HTTP客户端_取服务器证书SHA256(请求)` | wideString | 返回最终 HTTPS 服务端证书 SHA-256 指纹；HTTP 请求为空。 |
| 61 | `HTTP客户端_取响应头` | `HTTP客户端_取响应头(请求, 名称)` | wideString | 不区分大小写返回合并后的指定响应头。 |
| 62 | `HTTP客户端_取响应字节集` | `HTTP客户端_取响应字节集(请求)` | bytes | 返回内存响应正文的独立字节集副本；流式文件响应返回空字节集。 |
| 63 | `HTTP客户端_取响应文本编码` | `HTTP客户端_取响应文本编码(请求, 编码)` | wideString | 按 auto、UTF-8、UTF-16LE、UTF-16BE、GBK、GB18030 或 ANSI 解码内存响应。 |
| 64 | `HTTP客户端_保存响应文件` | `HTTP客户端_保存响应文件(请求, 文件路径, 允许覆盖)` | bool | 把已缓冲的响应正文原子保存为文件；流式响应应使用设置响应文件。 |
| 65 | `HTTP客户端_取当前请求` | `HTTP客户端_取当前请求()` | HTTP客户端请求 | 在完成处理器中返回当前请求 ID；其它上下文返回 0。 |
| 66 | `HTTP客户端_GET异步` | `HTTP客户端_GET异步(客户端, 地址, &处理器)` | HTTP客户端请求 | 创建并开始 GET 请求，返回请求 ID；完成后在 UI 线程调用处理器。 |
| 67 | `HTTP客户端_POSTJSON异步` | `HTTP客户端_POSTJSON异步(客户端, 地址, JSON, &处理器)` | HTTP客户端请求 | 创建带 UTF-8 JSON 正文的 POST 请求并异步开始。 |
| 68 | `HTTP客户端_请求` | `HTTP客户端_请求(方法, 地址, 正文, 超时毫秒)` | bool | 旧版同步兼容入口；使用默认客户端发送 UTF-8 请求，新代码应使用受管客户端和请求。 |
| 69 | `HTTP客户端_GET` | `HTTP客户端_GET(地址)` | bool | 旧版同步 GET 兼容入口。 |
| 70 | `HTTP客户端_POST` | `HTTP客户端_POST(地址, 正文)` | bool | 旧版同步 UTF-8 JSON POST 兼容入口。 |
| 71 | `HTTP客户端_取状态码` | `HTTP客户端_取状态码()` | int | 旧版兼容入口；返回最近默认请求状态码。 |
| 72 | `HTTP客户端_取响应文本` | `HTTP客户端_取响应文本()` | wideString | 旧版兼容入口；按 UTF-8 返回最近默认请求正文。 |
| 73 | `HTTP客户端_取错误` | `HTTP客户端_取错误()` | wideString | 旧版兼容入口；返回最近默认请求中文错误。 |
| 74 | `HTTP客户端_清空状态` | `HTTP客户端_清空状态()` | void | 旧版兼容入口；清空最近默认请求状态。 |
