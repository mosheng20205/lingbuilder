包 ODBC数据库模块完整演示
使用 Win32窗口基础模块
使用 ODBC数据库模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "ODBC数据库模块：8 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.database.odbc")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–8：ODBC_连接 至 ODBC_关闭；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：ODBC_连接
            // [1] ODBC_连接(连接字符串)
            // 功能：使用 ODBC 连接字符串打开数据库。
            // 参数：连接字符串:wideString；返回：bool；可见性：default
            ODBC_连接("演示参数1")
            // 命令ID：ODBC_执行
            // [2] ODBC_执行(SQL语句)
            // 功能：执行 SQL 并返回受影响行数，失败返回 -1。
            // 参数：SQL语句:wideString；返回：int；可见性：default
            ODBC_执行("演示参数1")
            // 命令ID：ODBC_查询首值
            // [3] ODBC_查询首值(SQL语句)
            // 功能：执行查询并返回首行首列文本。
            // 参数：SQL语句:wideString；返回：wideString；可见性：default
            ODBC_查询首值("演示参数1")
            // 命令ID：ODBC_设置自动提交
            // [4] ODBC_设置自动提交(启用)
            // 功能：启用或禁用自动提交。
            // 参数：启用:bool；返回：bool；可见性：default
            ODBC_设置自动提交(真)
            // 命令ID：ODBC_提交
            // [5] ODBC_提交()
            // 功能：提交当前事务。
            // 参数：无；返回：bool；可见性：default
            ODBC_提交()
            // 命令ID：ODBC_回滚
            // [6] ODBC_回滚()
            // 功能：回滚当前事务。
            // 参数：无；返回：bool；可见性：default
            ODBC_回滚()
            // 命令ID：ODBC_取错误
            // [7] ODBC_取错误()
            // 功能：返回最近 ODBC 错误。
            // 参数：无；返回：wideString；可见性：default
            ODBC_取错误()
            // 命令ID：ODBC_关闭
            // [8] ODBC_关闭()
            // 功能：关闭 ODBC 连接和环境。
            // 参数：无；返回：void；可见性：default
            ODBC_关闭()
        如果结束
        调试输出("已查看 命令 1–8：ODBC_连接 至 ODBC_关闭；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
