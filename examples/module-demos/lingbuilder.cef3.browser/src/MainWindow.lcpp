包 CEF3浏览器模块完整演示
使用 Win32窗口基础模块
使用 CEF3浏览器模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "CEF3浏览器模块：22 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.cef3.browser")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–11：CEF3_导航 至 CEF3_设置代理；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：CEF3_导航
            // [1] CEF3_导航(控件名, 地址)
            // 功能：让指定 CEF3 浏览器控件导航到 HTTP/HTTPS 地址或本地文件地址。
            // 参数：控件名:controlRef，地址:wideString；返回：int；可见性：default
            CEF3_导航(演示CefBrowser控件, "https://example.com")
            // 命令ID：CEF3_打开原生UI浏览器
            // [2] CEF3_打开原生UI浏览器(控件名, 地址)
            // 功能：使用 CEF Chrome Runtime 创建带原生地址栏和浏览器界面的独立顶层窗口，并纳入指定内嵌控件的 popup 生命周期管理。
            // 参数：控件名:controlRef，地址:wideString；返回：int；可见性：default
            CEF3_打开原生UI浏览器(演示CefBrowser控件, "https://example.com")
            // 命令ID：CEF3_执行JS
            // [3] CEF3_执行JS(控件名, 脚本)
            // 功能：通过 DevTools Runtime.evaluate 执行 JavaScript，最多等待 5 秒并返回 JSON 结果；新代码优先使用异步任务接口。
            // 参数：控件名:controlRef，脚本:wideString；返回：wideString；可见性：default
            CEF3_执行JS(演示CefBrowser控件, "演示参数2")
            // 命令ID：CEF3_后退
            // [4] CEF3_后退(控件名)
            // 功能：指定 CEF3 浏览器控件可以后退时返回上一页，成功返回 1。
            // 参数：控件名:controlRef；返回：int；可见性：default
            CEF3_后退(演示CefBrowser控件)
            // 命令ID：CEF3_前进
            // [5] CEF3_前进(控件名)
            // 功能：指定 CEF3 浏览器控件可以前进时进入下一页，成功返回 1。
            // 参数：控件名:controlRef；返回：int；可见性：default
            CEF3_前进(演示CefBrowser控件)
            // 命令ID：CEF3_刷新
            // [6] CEF3_刷新(控件名)
            // 功能：刷新指定 CEF3 浏览器控件的当前网页。
            // 参数：控件名:controlRef；返回：void；可见性：default
            CEF3_刷新(演示CefBrowser控件)
            // 命令ID：CEF3_停止
            // [7] CEF3_停止(控件名)
            // 功能：停止指定 CEF3 浏览器控件的当前导航。
            // 参数：控件名:controlRef；返回：void；可见性：default
            CEF3_停止(演示CefBrowser控件)
            // 命令ID：CEF3_取标题
            // [8] CEF3_取标题(控件名)
            // 功能：返回指定 CEF3 浏览器控件当前网页标题。
            // 参数：控件名:controlRef；返回：wideString；可见性：default
            CEF3_取标题(演示CefBrowser控件)
            // 命令ID：CEF3_取地址
            // [9] CEF3_取地址(控件名)
            // 功能：返回指定 CEF3 浏览器控件当前网页地址。
            // 参数：控件名:controlRef；返回：wideString；可见性：default
            CEF3_取地址(演示CefBrowser控件)
            // 命令ID：CEF3_设置缓存目录
            // [10] CEF3_设置缓存目录(控件名, 目录)
            // 功能：设置实例独立 RequestContext 的缓存目录标识；实际目录被安全映射到全局 root_cache_path 的直接子目录。需在创建前设置。
            // 参数：控件名:controlRef，目录:wideString；返回：int；可见性：default
            CEF3_设置缓存目录(演示CefBrowser控件, ".")
            // 命令ID：CEF3_设置代理
            // [11] CEF3_设置代理(控件名, 代理地址)
            // 功能：为实例独立 RequestContext 设置 HTTP/HTTPS/SOCKS5 代理；空文本使用直连。需在创建前设置。
            // 参数：控件名:controlRef，代理地址:wideString；返回：int；可见性：default
            CEF3_设置代理(演示CefBrowser控件, "https://example.com")
        如果结束
        调试输出("已查看 命令 1–11：CEF3_导航 至 CEF3_设置代理；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 _运行第02组_被单击()
        控件_设置文本(演示状态, "命令 12–22：CEF3_创建 至 CEF3_是否加载中；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：CEF3_创建
            // [12] CEF3_创建(控件名)
            // 功能：使用属性面板配置的地址、缓存目录和代理参数初始化指定 CEF3 浏览器控件；传空控件名时初始化当前窗口全部 CEF3 控件。成功返回 1。
            // 参数：控件名:controlRef；返回：int；可见性：default
            CEF3_创建(演示CefBrowser控件)
            // 命令ID：CEF3_关闭
            // [13] CEF3_关闭(控件名)
            // 功能：关闭指定 CEF3 浏览器控件并释放 Chromium 资源。
            // 参数：控件名:controlRef；返回：void；可见性：default
            CEF3_关闭(演示CefBrowser控件)
            // 命令ID：CEF3_取最近事件
            // [14] CEF3_取最近事件(控件名)
            // 功能：返回最近 CEF3 事件名；当前目录包含 92 个浏览器回调。
            // 参数：控件名:controlRef；返回：wideString；可见性：default
            CEF3_取最近事件(演示CefBrowser控件)
            // 命令ID：CEF3_取事件数据
            // [15] CEF3_取事件数据(控件名)
            // 功能：返回最近事件的主要文本数据。
            // 参数：控件名:controlRef；返回：wideString；可见性：default
            CEF3_取事件数据(演示CefBrowser控件)
            // 命令ID：CEF3_取事件字段
            // [16] CEF3_取事件字段(控件名, 字段名)
            // 功能：读取最近事件的命名字段，例如 url、frameId、statusCode、progress、commandId。
            // 参数：控件名:controlRef，字段名:wideString；返回：wideString；可见性：default
            CEF3_取事件字段(演示CefBrowser控件, "演示参数2")
            // 命令ID：CEF3_设置事件结果
            // [17] CEF3_设置事件结果(控件名, 结果)
            // 功能：设置当前同步事件结果：0=默认、1=允许/继续、2=拒绝/取消、3=已处理。
            // 参数：控件名:controlRef，结果:int；返回：int；可见性：default
            CEF3_设置事件结果(演示CefBrowser控件, 1)
            // 命令ID：CEF3_设置事件返回文本
            // [18] CEF3_设置事件返回文本(控件名, 文本)
            // 功能：设置当前事件的返回文本，例如修改后的 URL、下载路径、对话框输入或身份验证信息。
            // 参数：控件名:controlRef，文本:wideString；返回：int；可见性：default
            CEF3_设置事件返回文本(演示CefBrowser控件, "演示参数2")
            // 命令ID：CEF3_绑定事件
            // [19] CEF3_绑定事件(控件名, 事件名, 处理器)
            // 功能：绑定 CEF3 浏览器事件目录（92 项）到当前窗口无参数中文事件或方法；处理器必须使用 &处理器名。
            // 参数：控件名:controlRef，事件名:wideString，处理器:handler；返回：int；可见性：default
            CEF3_绑定事件(演示CefBrowser控件, "演示事件", &模块演示回调)
            // 命令ID：CEF3_是否可后退
            // [20] CEF3_是否可后退(控件名)
            // 功能：指定 CEF3 浏览器控件可以后退时返回 1。
            // 参数：控件名:controlRef；返回：int；可见性：default
            CEF3_是否可后退(演示CefBrowser控件)
            // 命令ID：CEF3_是否可前进
            // [21] CEF3_是否可前进(控件名)
            // 功能：指定 CEF3 浏览器控件可以前进时返回 1。
            // 参数：控件名:controlRef；返回：int；可见性：default
            CEF3_是否可前进(演示CefBrowser控件)
            // 命令ID：CEF3_是否加载中
            // [22] CEF3_是否加载中(控件名)
            // 功能：指定 CEF3 浏览器控件正在加载网页时返回 1。
            // 参数：控件名:controlRef；返回：int；可见性：default
            CEF3_是否加载中(演示CefBrowser控件)
        如果结束
        调试输出("已查看 命令 12–22：CEF3_创建 至 CEF3_是否加载中；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 模块演示回调()
        调试输出("模块演示回调已触发。")
    结束

结束类
