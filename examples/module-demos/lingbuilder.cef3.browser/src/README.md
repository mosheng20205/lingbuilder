# CEF3浏览器模块完整演示

- 模块 ID：`lingbuilder.cef3.browser`
- 版本：`3.0.0-alpha.2`
- 类型：LingBuilder 内置模块
- 命令数：22
- 设计器控件数：1
- 分组数：2

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

基于 Chromium Embedded Framework 3，提供设计器浏览器控件、中文命令和集中式浏览器事件目录。

## 命令清单

| # | 命令 | 签名 | 返回 | 说明 |
|---:|---|---|---|---|
| 1 | `CEF3_导航` | `CEF3_导航(控件名, 地址)` | int | 让指定 CEF3 浏览器控件导航到 HTTP/HTTPS 地址或本地文件地址。 |
| 2 | `CEF3_打开原生UI浏览器` | `CEF3_打开原生UI浏览器(控件名, 地址)` | int | 使用 CEF Chrome Runtime 创建带原生地址栏和浏览器界面的独立顶层窗口，并纳入指定内嵌控件的 popup 生命周期管理。 |
| 3 | `CEF3_执行JS` | `CEF3_执行JS(控件名, 脚本)` | wideString | 通过 DevTools Runtime.evaluate 执行 JavaScript，最多等待 5 秒并返回 JSON 结果；新代码优先使用异步任务接口。 |
| 4 | `CEF3_后退` | `CEF3_后退(控件名)` | int | 指定 CEF3 浏览器控件可以后退时返回上一页，成功返回 1。 |
| 5 | `CEF3_前进` | `CEF3_前进(控件名)` | int | 指定 CEF3 浏览器控件可以前进时进入下一页，成功返回 1。 |
| 6 | `CEF3_刷新` | `CEF3_刷新(控件名)` | void | 刷新指定 CEF3 浏览器控件的当前网页。 |
| 7 | `CEF3_停止` | `CEF3_停止(控件名)` | void | 停止指定 CEF3 浏览器控件的当前导航。 |
| 8 | `CEF3_取标题` | `CEF3_取标题(控件名)` | wideString | 返回指定 CEF3 浏览器控件当前网页标题。 |
| 9 | `CEF3_取地址` | `CEF3_取地址(控件名)` | wideString | 返回指定 CEF3 浏览器控件当前网页地址。 |
| 10 | `CEF3_设置缓存目录` | `CEF3_设置缓存目录(控件名, 目录)` | int | 设置实例独立 RequestContext 的缓存目录标识；实际目录被安全映射到全局 root_cache_path 的直接子目录。需在创建前设置。 |
| 11 | `CEF3_设置代理` | `CEF3_设置代理(控件名, 代理地址)` | int | 为实例独立 RequestContext 设置 HTTP/HTTPS/SOCKS5 代理；空文本使用直连。需在创建前设置。 |
| 12 | `CEF3_创建` | `CEF3_创建(控件名)` | int | 使用属性面板配置的地址、缓存目录和代理参数初始化指定 CEF3 浏览器控件；传空控件名时初始化当前窗口全部 CEF3 控件。成功返回 1。 |
| 13 | `CEF3_关闭` | `CEF3_关闭(控件名)` | void | 关闭指定 CEF3 浏览器控件并释放 Chromium 资源。 |
| 14 | `CEF3_取最近事件` | `CEF3_取最近事件(控件名)` | wideString | 返回最近 CEF3 事件名；当前目录包含 92 个浏览器回调。 |
| 15 | `CEF3_取事件数据` | `CEF3_取事件数据(控件名)` | wideString | 返回最近事件的主要文本数据。 |
| 16 | `CEF3_取事件字段` | `CEF3_取事件字段(控件名, 字段名)` | wideString | 读取最近事件的命名字段，例如 url、frameId、statusCode、progress、commandId。 |
| 17 | `CEF3_设置事件结果` | `CEF3_设置事件结果(控件名, 结果)` | int | 设置当前同步事件结果：0=默认、1=允许/继续、2=拒绝/取消、3=已处理。 |
| 18 | `CEF3_设置事件返回文本` | `CEF3_设置事件返回文本(控件名, 文本)` | int | 设置当前事件的返回文本，例如修改后的 URL、下载路径、对话框输入或身份验证信息。 |
| 19 | `CEF3_绑定事件` | `CEF3_绑定事件(控件名, 事件名, 处理器)` | int | 绑定 CEF3 浏览器事件目录（92 项）到当前窗口无参数中文事件或方法；处理器必须使用 &处理器名。 |
| 20 | `CEF3_是否可后退` | `CEF3_是否可后退(控件名)` | int | 指定 CEF3 浏览器控件可以后退时返回 1。 |
| 21 | `CEF3_是否可前进` | `CEF3_是否可前进(控件名)` | int | 指定 CEF3 浏览器控件可以前进时返回 1。 |
| 22 | `CEF3_是否加载中` | `CEF3_是否加载中(控件名)` | int | 指定 CEF3 浏览器控件正在加载网页时返回 1。 |
