包 Windows数据保护模块完整演示
使用 Win32窗口基础模块
使用 Windows数据保护模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "Windows数据保护模块：4 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.crypto.windows")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–4：数据保护_加密文本 至 数据保护_取错误；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：数据保护_加密文本
            // [1] 数据保护_加密文本(文本)
            // 功能：使用当前用户 DPAPI 加密 UTF-8 文本并返回 Base64。
            // 参数：文本:wideString；返回：wideString；可见性：default
            数据保护_加密文本("演示参数1")
            // 命令ID：数据保护_解密文本
            // [2] 数据保护_解密文本(Base64密文)
            // 功能：解密当前用户 DPAPI Base64 密文。
            // 参数：Base64密文:wideString；返回：wideString；可见性：default
            数据保护_解密文本("演示参数1")
            // 命令ID：数据保护_机器级加密文本
            // [3] 数据保护_机器级加密文本(文本)
            // 功能：使用本机范围 DPAPI 加密文本。
            // 参数：文本:wideString；返回：wideString；可见性：default
            数据保护_机器级加密文本("演示参数1")
            // 命令ID：数据保护_取错误
            // [4] 数据保护_取错误()
            // 功能：返回最近数据保护错误。
            // 参数：无；返回：wideString；可见性：default
            数据保护_取错误()
        如果结束
        调试输出("已查看 命令 1–4：数据保护_加密文本 至 数据保护_取错误；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
