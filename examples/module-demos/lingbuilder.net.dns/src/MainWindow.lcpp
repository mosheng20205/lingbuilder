包 DNS与IP模块完整演示
使用 Win32窗口基础模块
使用 DNS与IP模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "DNS与IP模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.net.dns")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–5：DNS_解析首个地址 至 网络_是否IPv6；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：DNS_解析首个地址
            // [1] DNS_解析首个地址(主机名)
            // 功能：解析并返回首个 IPv4 或 IPv6 地址。
            // 参数：主机名:wideString；返回：wideString；可见性：default
            DNS_解析首个地址("演示参数1")
            // 命令ID：DNS_反向查询
            // [2] DNS_反向查询(IP地址)
            // 功能：对 IP 地址执行反向名称查询。
            // 参数：IP地址:wideString；返回：wideString；可见性：default
            DNS_反向查询("https://example.com")
            // 命令ID：网络_取本机名
            // [3] 网络_取本机名()
            // 功能：返回 Winsock 本机主机名。
            // 参数：无；返回：wideString；可见性：default
            网络_取本机名()
            // 命令ID：网络_是否IPv4
            // [4] 网络_是否IPv4(地址)
            // 功能：检查是否为合法 IPv4 文本。
            // 参数：地址:wideString；返回：bool；可见性：default
            网络_是否IPv4("https://example.com")
            // 命令ID：网络_是否IPv6
            // [5] 网络_是否IPv6(地址)
            // 功能：检查是否为合法 IPv6 文本。
            // 参数：地址:wideString；返回：bool；可见性：default
            网络_是否IPv6("https://example.com")
        如果结束
        调试输出("已查看 命令 1–5：DNS_解析首个地址 至 网络_是否IPv6；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
