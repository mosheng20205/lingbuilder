包 CSV数据模块完整演示
使用 Win32窗口基础模块
使用 CSV数据模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "CSV数据模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.data.csv")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–5：CSV_转义字段 至 CSV_取字段；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：CSV_转义字段
            // [1] CSV_转义字段(字段)
            // 功能：按需添加双引号并转义字段。
            // 参数：字段:wideString；返回：wideString；可见性：default
            CSV_转义字段("演示参数1")
            // 命令ID：CSV_生成两列
            // [2] CSV_生成两列(第一列, 第二列)
            // 功能：生成包含两个字段的一行 CSV。
            // 参数：第一列:wideString，第二列:wideString；返回：wideString；可见性：default
            CSV_生成两列("演示参数1", "演示参数2")
            // 命令ID：CSV_生成三列
            // [3] CSV_生成三列(第一列, 第二列, 第三列)
            // 功能：生成包含三个字段的一行 CSV。
            // 参数：第一列:wideString，第二列:wideString，第三列:wideString；返回：wideString；可见性：default
            CSV_生成三列("演示参数1", "演示参数2", "演示参数3")
            // 命令ID：CSV_字段数量
            // [4] CSV_字段数量(行文本)
            // 功能：解析一行 CSV 并返回字段数。
            // 参数：行文本:wideString；返回：int；可见性：default
            CSV_字段数量("演示参数1")
            // 命令ID：CSV_取字段
            // [5] CSV_取字段(行文本, 索引)
            // 功能：按从 0 开始索引读取字段。
            // 参数：行文本:wideString，索引:int；返回：wideString；可见性：default
            CSV_取字段("演示参数1", 1)
        如果结束
        调试输出("已查看 命令 1–5：CSV_转义字段 至 CSV_取字段；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
