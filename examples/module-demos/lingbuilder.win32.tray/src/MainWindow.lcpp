包 托盘图标模块完整演示
使用 Win32窗口基础模块
使用 托盘图标模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "托盘图标模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.win32.tray")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–5：托盘_添加 至 托盘_取回调消息号；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：托盘_添加
            // [1] 托盘_添加(窗口句柄, 编号, 提示文本)
            // 功能：使用应用默认图标添加托盘图标。
            // 参数：窗口句柄:handle，编号:int，提示文本:wideString；返回：bool；可见性：default
            托盘_添加(0, 1, "演示参数3")
            // 命令ID：托盘_修改提示
            // [2] 托盘_修改提示(窗口句柄, 编号, 提示文本)
            // 功能：修改托盘提示文本。
            // 参数：窗口句柄:handle，编号:int，提示文本:wideString；返回：bool；可见性：default
            托盘_修改提示(0, 1, "演示参数3")
            // 命令ID：托盘_显示通知
            // [3] 托盘_显示通知(窗口句柄, 编号, 标题, 内容)
            // 功能：显示托盘气泡通知。
            // 参数：窗口句柄:handle，编号:int，标题:wideString，内容:wideString；返回：bool；可见性：default
            托盘_显示通知(0, 1, "演示参数3", "演示参数4")
            // 命令ID：托盘_删除
            // [4] 托盘_删除(窗口句柄, 编号)
            // 功能：删除托盘图标。
            // 参数：窗口句柄:handle，编号:int；返回：bool；可见性：default
            托盘_删除(0, 1)
            // 命令ID：托盘_取回调消息号
            // [5] 托盘_取回调消息号()
            // 功能：返回固定回调消息 WM_APP+77。
            // 参数：无；返回：int；可见性：default
            托盘_取回调消息号()
        如果结束
        调试输出("已查看 命令 1–5：托盘_添加 至 托盘_取回调消息号；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
