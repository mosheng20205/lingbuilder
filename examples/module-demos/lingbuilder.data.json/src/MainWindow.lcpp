包 JSON数据模块完整演示
使用 Win32窗口基础模块
使用 JSON数据模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "JSON 数据模块：5 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.data.json")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–5：JSON_是否有效 至 JSON_取逻辑；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：JSON_是否有效
            // [1] JSON_是否有效(JSON文本)
            // 功能：严格检查 JSON 文本语法是否完整有效。
            // 参数：JSON文本:wideString；返回：bool；可见性：default
            JSON_是否有效("{}")
            // 命令ID：JSON_转义文本
            // [2] JSON_转义文本(文本)
            // 功能：把普通文本转义为可放入 JSON 字符串的内容，不包含外层引号。
            // 参数：文本:wideString；返回：wideString；可见性：default
            JSON_转义文本("演示参数1")
            // 命令ID：JSON_取文本
            // [3] JSON_取文本(JSON文本, 字段名)
            // 功能：读取顶层对象中的字符串字段，缺失或类型不符返回空文本。
            // 参数：JSON文本:wideString，字段名:wideString；返回：wideString；可见性：default
            JSON_取文本("{}", "演示参数2")
            // 命令ID：JSON_取整数
            // [4] JSON_取整数(JSON文本, 字段名, 默认值)
            // 功能：读取顶层对象中的整数，缺失或类型不符返回默认值。
            // 参数：JSON文本:wideString，字段名:wideString，默认值:int；返回：int；可见性：default
            JSON_取整数("{}", "演示参数2", 1)
            // 命令ID：JSON_取逻辑
            // [5] JSON_取逻辑(JSON文本, 字段名, 默认值)
            // 功能：读取顶层对象中的逻辑值，缺失或类型不符返回默认值。
            // 参数：JSON文本:wideString，字段名:wideString，默认值:bool；返回：bool；可见性：default
            JSON_取逻辑("{}", "演示参数2", 真)
        如果结束
        调试输出("已查看 命令 1–5：JSON_是否有效 至 JSON_取逻辑；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
