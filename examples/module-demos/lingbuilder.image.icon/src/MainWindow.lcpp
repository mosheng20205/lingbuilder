包 图标处理模块完整演示
使用 Win32窗口基础模块
使用 图标处理模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "图标处理模块：3 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.image.icon")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–3：图标_取数量 至 图标_提取小图标到PNG；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：图标_取数量
            // [1] 图标_取数量(文件路径)
            // 功能：返回 EXE、DLL 或 ICO 中可提取图标数量。
            // 参数：文件路径:wideString；返回：int；可见性：default
            图标_取数量("module-demo-temp.txt")
            // 命令ID：图标_提取大图标到PNG
            // [2] 图标_提取大图标到PNG(文件路径, 索引, 目标路径)
            // 功能：提取大图标并保存 PNG。
            // 参数：文件路径:wideString，索引:int，目标路径:wideString；返回：bool；可见性：default
            图标_提取大图标到PNG("module-demo-temp.txt", 1, "module-demo-temp.txt")
            // 命令ID：图标_提取小图标到PNG
            // [3] 图标_提取小图标到PNG(文件路径, 索引, 目标路径)
            // 功能：提取小图标并保存 PNG。
            // 参数：文件路径:wideString，索引:int，目标路径:wideString；返回：bool；可见性：default
            图标_提取小图标到PNG("module-demo-temp.txt", 1, "module-demo-temp.txt")
        如果结束
        调试输出("已查看 命令 1–3：图标_取数量 至 图标_提取小图标到PNG；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
