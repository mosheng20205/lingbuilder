包 进程通信模块完整演示
使用 Win32窗口基础模块
使用 进程通信模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "进程通信模块：8 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.ipc")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–8：IPC_创建管道服务端 至 IPC_关闭；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：IPC_创建管道服务端
            // [1] IPC_创建管道服务端(名称)
            // 功能：创建本机命名管道服务端。
            // 参数：名称:wideString；返回：bool；可见性：default
            IPC_创建管道服务端("演示参数1")
            // 命令ID：IPC_等待管道客户端
            // [2] IPC_等待管道客户端()
            // 功能：等待一个管道客户端连接。
            // 参数：无；返回：bool；可见性：default
            IPC_等待管道客户端()
            // 命令ID：IPC_连接管道
            // [3] IPC_连接管道(名称, 超时毫秒)
            // 功能：连接本机命名管道。
            // 参数：名称:wideString，超时毫秒:int；返回：bool；可见性：default
            IPC_连接管道("演示参数1", 1)
            // 命令ID：IPC_发送文本
            // [4] IPC_发送文本(文本)
            // 功能：发送一条长度前缀 UTF-8 文本。
            // 参数：文本:wideString；返回：bool；可见性：default
            IPC_发送文本("演示参数1")
            // 命令ID：IPC_接收文本
            // [5] IPC_接收文本()
            // 功能：接收一条长度前缀 UTF-8 文本。
            // 参数：无；返回：wideString；可见性：default
            IPC_接收文本()
            // 命令ID：IPC_创建单实例锁
            // [6] IPC_创建单实例锁(名称)
            // 功能：创建命名互斥体；首次创建返回真。
            // 参数：名称:wideString；返回：bool；可见性：default
            IPC_创建单实例锁("演示参数1")
            // 命令ID：IPC_取错误
            // [7] IPC_取错误()
            // 功能：返回最近 IPC 错误。
            // 参数：无；返回：wideString；可见性：default
            IPC_取错误()
            // 命令ID：IPC_关闭
            // [8] IPC_关闭()
            // 功能：关闭管道和单实例锁。
            // 参数：无；返回：void；可见性：default
            IPC_关闭()
        如果结束
        调试输出("已查看 命令 1–8：IPC_创建管道服务端 至 IPC_关闭；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
