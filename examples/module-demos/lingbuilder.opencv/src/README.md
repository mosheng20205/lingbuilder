# OpenCV 图像处理模块完整演示

- 模块 ID：`lingbuilder.opencv`
- 版本：`1.0.0`
- 类型：LingBuilder 内置模块
- 命令数：33
- 设计器控件数：0
- 分组数：2

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

基于 OpenCV 4.14.0 的 x64 本地图像处理、模板匹配、轮廓分析和单/双缺口候选分析模块。

## 命令清单

| # | 命令 | 签名 | 返回 | 说明 |
|---:|---|---|---|---|
| 1 | `OpenCV_取版本` | `OpenCV_取版本()` | wideString | 返回当前 Bridge 使用的 OpenCV 与 Bridge ABI 版本。 |
| 2 | `OpenCV_加载图像` | `OpenCV_加载图像(路径, 读取模式)` | handle | 加载图像并返回受管图像句柄。 |
| 3 | `OpenCV_保存图像` | `OpenCV_保存图像(图像, 路径, 质量)` | bool | 按目标扩展名保存图像。 |
| 4 | `OpenCV_克隆图像` | `OpenCV_克隆图像(图像)` | handle | 深复制图像并返回新句柄。 |
| 5 | `OpenCV_释放图像` | `OpenCV_释放图像(图像)` | bool | 释放一个受管图像句柄。 |
| 6 | `OpenCV_释放全部` | `OpenCV_释放全部()` | void | 释放当前进程中全部 OpenCV 图像与结果句柄。 |
| 7 | `OpenCV_取宽度` | `OpenCV_取宽度(图像)` | int | 返回图像像素宽度。 |
| 8 | `OpenCV_取高度` | `OpenCV_取高度(图像)` | int | 返回图像像素高度。 |
| 9 | `OpenCV_取通道数` | `OpenCV_取通道数(图像)` | int | 返回图像通道数。 |
| 10 | `OpenCV_取错误` | `OpenCV_取错误()` | wideString | 读取当前线程最近一次 OpenCV 操作的中文错误。 |
| 11 | `OpenCV_灰度化` | `OpenCV_灰度化(图像)` | handle | 转换为灰度图并返回新句柄。 |
| 12 | `OpenCV_缩放` | `OpenCV_缩放(图像, 宽度, 高度)` | handle | 使用高质量插值缩放图像并返回新句柄。 |
| 13 | `OpenCV_裁剪` | `OpenCV_裁剪(图像, 横坐标, 纵坐标, 宽度, 高度)` | handle | 裁剪图像区域并返回新句柄。 |
| 14 | `OpenCV_高斯模糊` | `OpenCV_高斯模糊(图像, 卷积核, Sigma)` | handle | 执行高斯模糊并返回新句柄。 |
| 15 | `OpenCV_二值化` | `OpenCV_二值化(图像, 阈值, 模式)` | handle | 灰度化后执行固定或大津二值化。 |
| 16 | `OpenCV_自适应二值化` | `OpenCV_自适应二值化(图像, 块大小, 常数, 反向)` | handle | 执行高斯自适应二值化。 |
| 17 | `OpenCV_Canny边缘` | `OpenCV_Canny边缘(图像, 低阈值, 高阈值)` | handle | 灰度化后执行 Canny 边缘检测。 |
| 18 | `OpenCV_形态学` | `OpenCV_形态学(图像, 操作, 卷积核, 次数)` | handle | 执行形态学操作并返回新句柄。 |
| 19 | `OpenCV_模板匹配` | `OpenCV_模板匹配(图像, 模板, 方法, 最低分数, 最大结果数)` | handle | 执行模板匹配和重叠抑制，返回受管分析结果。 |
| 20 | `OpenCV_查找轮廓` | `OpenCV_查找轮廓(图像, 最小面积, 最大面积, 最大结果数)` | handle | 查找外部轮廓并按面积排序返回矩形候选。 |
| 21 | `OpenCV_分析缺口` | `OpenCV_分析缺口(背景图像, 滑块图像, 候选数量, 配置JSON)` | handle | 使用模板边缘与轮廓的确定性规则分析单缺口或双缺口候选。 |
| 22 | `OpenCV结果_取类型` | `OpenCV结果_取类型(结果)` | wideString | 返回模板匹配、轮廓或缺口。 |
| 23 | `OpenCV结果_取数量` | `OpenCV结果_取数量(结果)` | int | 返回候选项数量。 |
| 24 | `OpenCV结果_取横坐标` | `OpenCV结果_取横坐标(结果, 索引)` | int | 返回候选矩形左上角横坐标。 |
| 25 | `OpenCV结果_取纵坐标` | `OpenCV结果_取纵坐标(结果, 索引)` | int | 返回候选矩形左上角纵坐标。 |
| 26 | `OpenCV结果_取宽度` | `OpenCV结果_取宽度(结果, 索引)` | int | 返回候选矩形宽度。 |
| 27 | `OpenCV结果_取高度` | `OpenCV结果_取高度(结果, 索引)` | int | 返回候选矩形高度。 |
| 28 | `OpenCV结果_取中心横坐标` | `OpenCV结果_取中心横坐标(结果, 索引)` | int | 返回候选中心横坐标。 |
| 29 | `OpenCV结果_取中心纵坐标` | `OpenCV结果_取中心纵坐标(结果, 索引)` | int | 返回候选中心纵坐标。 |
| 30 | `OpenCV结果_取置信度` | `OpenCV结果_取置信度(结果, 索引)` | double | 返回 0～1 的候选置信度。 |
| 31 | `OpenCV结果_取JSON` | `OpenCV结果_取JSON(结果)` | wideString | 返回 schemaVersion 1 的 UTF-16 JSON 结果。 |
| 32 | `OpenCV结果_保存标注图` | `OpenCV结果_保存标注图(结果, 路径, 质量)` | bool | 保存带候选矩形和置信度标注的调试图。 |
| 33 | `OpenCV结果_释放` | `OpenCV结果_释放(结果)` | bool | 释放一个受管分析结果句柄。 |
