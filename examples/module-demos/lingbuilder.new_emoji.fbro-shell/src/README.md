# new_emoji FBro 浏览器外壳完整演示

- 模块 ID：`lingbuilder.new_emoji.fbro-shell`
- 版本：`1.1.0`
- 类型：LingBuilder 内置模块
- 命令数：38
- 设计器控件数：0
- 分组数：2

## 使用方式

打开项目后按标签页查看命令分组。源码默认只展示并记录，不执行有副作用的调用；确认演示参数和运行环境后，勾选“允许实际执行”再点击对应分组按钮。

每条命令在 `MainWindow.lcpp` 中包含签名、功能、参数、返回类型和真实调用；结构化清单位于 `模块命令清单.json`。

## 模块说明

在 new_emoji 浏览器框架窗口中管理 FBro x64 多标签独立 HWND、导航状态和 BrowserViewport 占位区。

## 命令清单

| # | 命令 | 签名 | 返回 | 说明 |
|---:|---|---|---|---|
| 1 | `浏览器外壳_创建` | `浏览器外壳_创建(标签页控件, 页面占位控件, 状态处理器)` | bool | 绑定 Tabs 和 BrowserViewport，占位区仅用于加载/错误提示，真实网页由独立 FBro HWND 渲染。 |
| 2 | `浏览器外壳_绑定实例列表` | `浏览器外壳_绑定实例列表(实例列表控件)` | bool | 把动态标签集合、进程状态、PID 和选中项同步到 RichList。 |
| 3 | `浏览器外壳_选择列表键` | `浏览器外壳_选择列表键(选中键JSON)` | bool | 使用 RichList SelectionChanged 返回的选中键 JSON 切换动态实例。 |
| 4 | `浏览器外壳_销毁` | `浏览器外壳_销毁()` | void | 关闭全部受管标签页和 FBro 子宿主。 |
| 5 | `浏览器外壳_新建标签页` | `浏览器外壳_新建标签页(稳定ID, 地址, 标题)` | bool | 创建稳定 ID 对应的独立 FBro 句柄和宿主 HWND。 |
| 6 | `浏览器外壳_新建独立实例` | `浏览器外壳_新建独立实例(稳定ID, 地址, 标题, 缓存目录)` | bool | 动态创建一个独立 Host 进程、独立 WebSocket 会话、独立 Profile 和伴随 HWND；没有固定数量上限。 |
| 7 | `浏览器外壳_新建空白标签页` | `浏览器外壳_新建空白标签页()` | bool | 使用运行时生成的稳定 ID 新建并选择空白标签页。 |
| 8 | `浏览器外壳_关闭标签页` | `浏览器外壳_关闭标签页(稳定ID)` | bool | 关闭并销毁指定稳定 ID 的标签页。 |
| 9 | `浏览器外壳_关闭当前标签页` | `浏览器外壳_关闭当前标签页()` | bool | 关闭当前选中的标签页。 |
| 10 | `浏览器外壳_关闭其他标签页` | `浏览器外壳_关闭其他标签页()` | bool | 保留当前标签页并关闭、销毁其余标签页宿主。 |
| 11 | `浏览器外壳_取标签页数量` | `浏览器外壳_取标签页数量()` | int | 返回浏览器外壳当前受管标签页数量。 |
| 12 | `浏览器外壳_选择标签页` | `浏览器外壳_选择标签页(稳定ID)` | bool | 只显示指定稳定 ID 的页面宿主。 |
| 13 | `浏览器外壳_重排标签页` | `浏览器外壳_重排标签页(稳定ID, 新索引)` | bool | 按稳定 ID 重排标签页和运行时映射。 |
| 14 | `浏览器外壳_导航` | `浏览器外壳_导航(地址)` | bool | 让当前标签页导航到地址。 |
| 15 | `浏览器外壳_后退` | `浏览器外壳_后退()` | bool | 当前标签页后退。 |
| 16 | `浏览器外壳_前进` | `浏览器外壳_前进()` | bool | 当前标签页前进。 |
| 17 | `浏览器外壳_刷新` | `浏览器外壳_刷新()` | void | 刷新当前标签页。 |
| 18 | `浏览器外壳_停止` | `浏览器外壳_停止()` | void | 停止当前标签页加载。 |
| 19 | `浏览器外壳_取地址` | `浏览器外壳_取地址()` | wideString | 读取当前标签页地址。 |
| 20 | `浏览器外壳_取标题` | `浏览器外壳_取标题()` | wideString | 读取当前标签页标题。 |
| 21 | `浏览器外壳_取当前稳定ID` | `浏览器外壳_取当前稳定ID()` | wideString | 返回当前实例的稳定 ID。 |
| 22 | `浏览器外壳_取当前进程状态` | `浏览器外壳_取当前进程状态()` | wideString | 返回当前独立 Host 的启动状态。 |
| 23 | `浏览器外壳_取当前进程ID` | `浏览器外壳_取当前进程ID()` | int | 返回当前独立 Host 的 PID。 |
| 24 | `浏览器外壳_取当前调试端口` | `浏览器外壳_取当前调试端口()` | int | 返回当前独立 Host 的随机 CDP 端口。 |
| 25 | `浏览器外壳_取当前错误` | `浏览器外壳_取当前错误()` | wideString | 返回当前实例最近的进程或 FBro 错误。 |
| 26 | `浏览器外壳_强制刷新` | `浏览器外壳_强制刷新()` | bool | 忽略缓存刷新当前实例。 |
| 27 | `浏览器外壳_执行JS` | `浏览器外壳_执行JS(脚本)` | wideString | 通过 WebSocket 在当前独立 Host 中同步执行 JavaScript。 |
| 28 | `浏览器外壳_取Cookie` | `浏览器外壳_取Cookie(地址)` | wideString | 通过 WebSocket 读取当前独立 Host 指定地址的 Cookie。 |
| 29 | `浏览器外壳_截图到文件` | `浏览器外壳_截图到文件(路径, 格式, 质量)` | bool | 让当前独立 Host 截图并保存到文件。 |
| 30 | `浏览器外壳_隐藏当前` | `浏览器外壳_隐藏当前()` | bool | 隐藏当前 Host，但保持进程和页面状态。 |
| 31 | `浏览器外壳_显示当前` | `浏览器外壳_显示当前()` | bool | 恢复显示当前 Host。 |
| 32 | `浏览器外壳_取当前代理` | `浏览器外壳_取当前代理()` | wideString | 读取当前实例保存的代理地址。 |
| 33 | `浏览器外壳_取当前UserAgent` | `浏览器外壳_取当前UserAgent()` | wideString | 读取当前实例保存的 User-Agent。 |
| 34 | `浏览器外壳_取当前指纹配置` | `浏览器外壳_取当前指纹配置()` | wideString | 读取当前实例保存的指纹 JSON。 |
| 35 | `浏览器外壳_取当前视口宽度` | `浏览器外壳_取当前视口宽度()` | int | 读取当前实例视口宽度。 |
| 36 | `浏览器外壳_取当前视口高度` | `浏览器外壳_取当前视口高度()` | int | 读取当前实例视口高度。 |
| 37 | `浏览器外壳_按配置重建当前` | `浏览器外壳_按配置重建当前(代理, UserAgent, 指纹JSON, 宽度, 高度)` | bool | 保留稳定 ID 与 Profile，关闭并按新配置重启当前独立 Host。 |
| 38 | `浏览器外壳_聚焦地址栏` | `浏览器外壳_聚焦地址栏(地址栏控件)` | bool | 把键盘焦点切换到指定 new_emoji Omnibox。 |
