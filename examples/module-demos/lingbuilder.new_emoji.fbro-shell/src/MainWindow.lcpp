包 new_emojiFBro浏览器外壳完整演示
使用 Win32窗口基础模块
使用 new_emojiFBro浏览器外壳

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "new_emoji FBro 浏览器外壳：38 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.new_emoji.fbro-shell")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–19：浏览器外壳_创建 至 浏览器外壳_取地址；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：浏览器外壳_创建
            // [1] 浏览器外壳_创建(标签页控件, 页面占位控件, 状态处理器)
            // 功能：绑定 Tabs 和 BrowserViewport，占位区仅用于加载/错误提示，真实网页由独立 FBro HWND 渲染。
            // 参数：标签页控件:controlRef，页面占位控件:controlRef，状态处理器:handler；返回：bool；可见性：default
            浏览器外壳_创建(演示lingbuildernew_emojiuiTabs控件, 演示lingbuildernew_emojiuiBrowserViewport控件, &模块演示回调_整数型_文本型_文本型_逻辑型_空)
            // 命令ID：浏览器外壳_绑定实例列表
            // [2] 浏览器外壳_绑定实例列表(实例列表控件)
            // 功能：把动态标签集合、进程状态、PID 和选中项同步到 RichList。
            // 参数：实例列表控件:controlRef；返回：bool；可见性：default
            浏览器外壳_绑定实例列表(演示lingbuildernew_emojiuiRichList控件)
            // 命令ID：浏览器外壳_选择列表键
            // [3] 浏览器外壳_选择列表键(选中键JSON)
            // 功能：使用 RichList SelectionChanged 返回的选中键 JSON 切换动态实例。
            // 参数：选中键JSON:wideString；返回：bool；可见性：default
            浏览器外壳_选择列表键("{}")
            // 命令ID：浏览器外壳_销毁
            // [4] 浏览器外壳_销毁()
            // 功能：关闭全部受管标签页和 FBro 子宿主。
            // 参数：无；返回：void；可见性：default
            浏览器外壳_销毁()
            // 命令ID：浏览器外壳_新建标签页
            // [5] 浏览器外壳_新建标签页(稳定ID, 地址, 标题)
            // 功能：创建稳定 ID 对应的独立 FBro 句柄和宿主 HWND。
            // 参数：稳定ID:wideString，地址:wideString，标题:wideString；返回：bool；可见性：default
            浏览器外壳_新建标签页("演示参数1", "https://example.com", "演示参数3")
            // 命令ID：浏览器外壳_新建独立实例
            // [6] 浏览器外壳_新建独立实例(稳定ID, 地址, 标题, 缓存目录)
            // 功能：动态创建一个独立 Host 进程、独立 WebSocket 会话、独立 Profile 和伴随 HWND；没有固定数量上限。
            // 参数：稳定ID:wideString，地址:wideString，标题:wideString，缓存目录:wideString；返回：bool；可见性：default
            浏览器外壳_新建独立实例("演示参数1", "https://example.com", "演示参数3", ".")
            // 命令ID：浏览器外壳_新建空白标签页
            // [7] 浏览器外壳_新建空白标签页()
            // 功能：使用运行时生成的稳定 ID 新建并选择空白标签页。
            // 参数：无；返回：bool；可见性：default
            浏览器外壳_新建空白标签页()
            // 命令ID：浏览器外壳_关闭标签页
            // [8] 浏览器外壳_关闭标签页(稳定ID)
            // 功能：关闭并销毁指定稳定 ID 的标签页。
            // 参数：稳定ID:wideString；返回：bool；可见性：default
            浏览器外壳_关闭标签页("演示参数1")
            // 命令ID：浏览器外壳_关闭当前标签页
            // [9] 浏览器外壳_关闭当前标签页()
            // 功能：关闭当前选中的标签页。
            // 参数：无；返回：bool；可见性：default
            浏览器外壳_关闭当前标签页()
            // 命令ID：浏览器外壳_关闭其他标签页
            // [10] 浏览器外壳_关闭其他标签页()
            // 功能：保留当前标签页并关闭、销毁其余标签页宿主。
            // 参数：无；返回：bool；可见性：default
            浏览器外壳_关闭其他标签页()
            // 命令ID：浏览器外壳_取标签页数量
            // [11] 浏览器外壳_取标签页数量()
            // 功能：返回浏览器外壳当前受管标签页数量。
            // 参数：无；返回：int；可见性：default
            浏览器外壳_取标签页数量()
            // 命令ID：浏览器外壳_选择标签页
            // [12] 浏览器外壳_选择标签页(稳定ID)
            // 功能：只显示指定稳定 ID 的页面宿主。
            // 参数：稳定ID:wideString；返回：bool；可见性：default
            浏览器外壳_选择标签页("演示参数1")
            // 命令ID：浏览器外壳_重排标签页
            // [13] 浏览器外壳_重排标签页(稳定ID, 新索引)
            // 功能：按稳定 ID 重排标签页和运行时映射。
            // 参数：稳定ID:wideString，新索引:int；返回：bool；可见性：default
            浏览器外壳_重排标签页("演示参数1", 1)
            // 命令ID：浏览器外壳_导航
            // [14] 浏览器外壳_导航(地址)
            // 功能：让当前标签页导航到地址。
            // 参数：地址:wideString；返回：bool；可见性：default
            浏览器外壳_导航("https://example.com")
            // 命令ID：浏览器外壳_后退
            // [15] 浏览器外壳_后退()
            // 功能：当前标签页后退。
            // 参数：无；返回：bool；可见性：default
            浏览器外壳_后退()
            // 命令ID：浏览器外壳_前进
            // [16] 浏览器外壳_前进()
            // 功能：当前标签页前进。
            // 参数：无；返回：bool；可见性：default
            浏览器外壳_前进()
            // 命令ID：浏览器外壳_刷新
            // [17] 浏览器外壳_刷新()
            // 功能：刷新当前标签页。
            // 参数：无；返回：void；可见性：default
            浏览器外壳_刷新()
            // 命令ID：浏览器外壳_停止
            // [18] 浏览器外壳_停止()
            // 功能：停止当前标签页加载。
            // 参数：无；返回：void；可见性：default
            浏览器外壳_停止()
            // 命令ID：浏览器外壳_取地址
            // [19] 浏览器外壳_取地址()
            // 功能：读取当前标签页地址。
            // 参数：无；返回：wideString；可见性：default
            浏览器外壳_取地址()
        如果结束
        调试输出("已查看 命令 1–19：浏览器外壳_创建 至 浏览器外壳_取地址；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 _运行第02组_被单击()
        控件_设置文本(演示状态, "命令 20–38：浏览器外壳_取标题 至 浏览器外壳_聚焦地址栏；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：浏览器外壳_取标题
            // [20] 浏览器外壳_取标题()
            // 功能：读取当前标签页标题。
            // 参数：无；返回：wideString；可见性：default
            浏览器外壳_取标题()
            // 命令ID：浏览器外壳_取当前稳定ID
            // [21] 浏览器外壳_取当前稳定ID()
            // 功能：返回当前实例的稳定 ID。
            // 参数：无；返回：wideString；可见性：default
            浏览器外壳_取当前稳定ID()
            // 命令ID：浏览器外壳_取当前进程状态
            // [22] 浏览器外壳_取当前进程状态()
            // 功能：返回当前独立 Host 的启动状态。
            // 参数：无；返回：wideString；可见性：default
            浏览器外壳_取当前进程状态()
            // 命令ID：浏览器外壳_取当前进程ID
            // [23] 浏览器外壳_取当前进程ID()
            // 功能：返回当前独立 Host 的 PID。
            // 参数：无；返回：int；可见性：default
            浏览器外壳_取当前进程ID()
            // 命令ID：浏览器外壳_取当前调试端口
            // [24] 浏览器外壳_取当前调试端口()
            // 功能：返回当前独立 Host 的随机 CDP 端口。
            // 参数：无；返回：int；可见性：default
            浏览器外壳_取当前调试端口()
            // 命令ID：浏览器外壳_取当前错误
            // [25] 浏览器外壳_取当前错误()
            // 功能：返回当前实例最近的进程或 FBro 错误。
            // 参数：无；返回：wideString；可见性：default
            浏览器外壳_取当前错误()
            // 命令ID：浏览器外壳_强制刷新
            // [26] 浏览器外壳_强制刷新()
            // 功能：忽略缓存刷新当前实例。
            // 参数：无；返回：bool；可见性：default
            浏览器外壳_强制刷新()
            // 命令ID：浏览器外壳_执行JS
            // [27] 浏览器外壳_执行JS(脚本)
            // 功能：通过 WebSocket 在当前独立 Host 中同步执行 JavaScript。
            // 参数：脚本:wideString；返回：wideString；可见性：default
            浏览器外壳_执行JS("演示参数1")
            // 命令ID：浏览器外壳_取Cookie
            // [28] 浏览器外壳_取Cookie(地址)
            // 功能：通过 WebSocket 读取当前独立 Host 指定地址的 Cookie。
            // 参数：地址:wideString；返回：wideString；可见性：default
            浏览器外壳_取Cookie("https://example.com")
            // 命令ID：浏览器外壳_截图到文件
            // [29] 浏览器外壳_截图到文件(路径, 格式, 质量)
            // 功能：让当前独立 Host 截图并保存到文件。
            // 参数：路径:wideString，格式:wideString，质量:int；返回：bool；可见性：default
            浏览器外壳_截图到文件("module-demo-temp.txt", "演示参数2", 1)
            // 命令ID：浏览器外壳_隐藏当前
            // [30] 浏览器外壳_隐藏当前()
            // 功能：隐藏当前 Host，但保持进程和页面状态。
            // 参数：无；返回：bool；可见性：default
            浏览器外壳_隐藏当前()
            // 命令ID：浏览器外壳_显示当前
            // [31] 浏览器外壳_显示当前()
            // 功能：恢复显示当前 Host。
            // 参数：无；返回：bool；可见性：default
            浏览器外壳_显示当前()
            // 命令ID：浏览器外壳_取当前代理
            // [32] 浏览器外壳_取当前代理()
            // 功能：读取当前实例保存的代理地址。
            // 参数：无；返回：wideString；可见性：default
            浏览器外壳_取当前代理()
            // 命令ID：浏览器外壳_取当前UserAgent
            // [33] 浏览器外壳_取当前UserAgent()
            // 功能：读取当前实例保存的 User-Agent。
            // 参数：无；返回：wideString；可见性：default
            浏览器外壳_取当前UserAgent()
            // 命令ID：浏览器外壳_取当前指纹配置
            // [34] 浏览器外壳_取当前指纹配置()
            // 功能：读取当前实例保存的指纹 JSON。
            // 参数：无；返回：wideString；可见性：default
            浏览器外壳_取当前指纹配置()
            // 命令ID：浏览器外壳_取当前视口宽度
            // [35] 浏览器外壳_取当前视口宽度()
            // 功能：读取当前实例视口宽度。
            // 参数：无；返回：int；可见性：default
            浏览器外壳_取当前视口宽度()
            // 命令ID：浏览器外壳_取当前视口高度
            // [36] 浏览器外壳_取当前视口高度()
            // 功能：读取当前实例视口高度。
            // 参数：无；返回：int；可见性：default
            浏览器外壳_取当前视口高度()
            // 命令ID：浏览器外壳_按配置重建当前
            // [37] 浏览器外壳_按配置重建当前(代理, UserAgent, 指纹JSON, 宽度, 高度)
            // 功能：保留稳定 ID 与 Profile，关闭并按新配置重启当前独立 Host。
            // 参数：代理:wideString，UserAgent:wideString，指纹JSON:wideString，宽度:int，高度:int；返回：bool；可见性：default
            浏览器外壳_按配置重建当前("演示参数1", "演示参数2", "{}", 1, 1)
            // 命令ID：浏览器外壳_聚焦地址栏
            // [38] 浏览器外壳_聚焦地址栏(地址栏控件)
            // 功能：把键盘焦点切换到指定 new_emoji Omnibox。
            // 参数：地址栏控件:controlRef；返回：bool；可见性：default
            浏览器外壳_聚焦地址栏(演示lingbuildernew_emojiuiOmnibox控件)
        如果结束
        调试输出("已查看 命令 20–38：浏览器外壳_取标题 至 浏览器外壳_聚焦地址栏；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 模块演示回调_整数型_文本型_文本型_逻辑型_空(整数型 参数1, 文本型 参数2, 文本型 参数3, 逻辑型 参数4)
        调试输出("模块演示回调已触发。")
    结束

结束类
