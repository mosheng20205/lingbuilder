包 显示器与DPI模块完整演示
使用 Win32窗口基础模块
使用 显示器与DPI模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "显示器与DPI模块：6 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.win32.monitor")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–6：显示器_数量 至 显示器_系统DPI；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：显示器_数量
            // [1] 显示器_数量()
            // 功能：返回当前桌面连接的显示器数量。
            // 参数：无；返回：int；可见性：default
            显示器_数量()
            // 命令ID：显示器_主屏宽度
            // [2] 显示器_主屏宽度()
            // 功能：返回主显示器像素宽度。
            // 参数：无；返回：int；可见性：default
            显示器_主屏宽度()
            // 命令ID：显示器_主屏高度
            // [3] 显示器_主屏高度()
            // 功能：返回主显示器像素高度。
            // 参数：无；返回：int；可见性：default
            显示器_主屏高度()
            // 命令ID：显示器_工作区宽度
            // [4] 显示器_工作区宽度()
            // 功能：返回主显示器排除任务栏后的工作区宽度。
            // 参数：无；返回：int；可见性：default
            显示器_工作区宽度()
            // 命令ID：显示器_工作区高度
            // [5] 显示器_工作区高度()
            // 功能：返回主显示器排除任务栏后的工作区高度。
            // 参数：无；返回：int；可见性：default
            显示器_工作区高度()
            // 命令ID：显示器_系统DPI
            // [6] 显示器_系统DPI()
            // 功能：返回系统 DPI，无法读取时返回 96。
            // 参数：无；返回：int；可见性：default
            显示器_系统DPI()
        如果结束
        调试输出("已查看 命令 1–6：显示器_数量 至 显示器_系统DPI；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
