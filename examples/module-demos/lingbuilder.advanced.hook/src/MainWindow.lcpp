包 键盘Hook模块完整演示
使用 Win32窗口基础模块
使用 键盘Hook模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "键盘Hook模块：4 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.advanced.hook")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–4：键盘钩子_启动 至 键盘钩子_停止；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：键盘钩子_启动
            // [1] 键盘钩子_启动()
            // 功能：启动 WH_KEYBOARD_LL 钩子。
            // 参数：无；返回：bool；可见性：default
            键盘钩子_启动()
            // 命令ID：键盘钩子_取最后键码
            // [2] 键盘钩子_取最后键码()
            // 功能：读取最近键盘消息虚拟键码。
            // 参数：无；返回：int；可见性：default
            键盘钩子_取最后键码()
            // 命令ID：键盘钩子_取消息数量
            // [3] 键盘钩子_取消息数量()
            // 功能：读取钩子收到的消息数量。
            // 参数：无；返回：longLong；可见性：default
            键盘钩子_取消息数量()
            // 命令ID：键盘钩子_停止
            // [4] 键盘钩子_停止()
            // 功能：卸载键盘钩子。
            // 参数：无；返回：void；可见性：default
            键盘钩子_停止()
        如果结束
        调试输出("已查看 命令 1–4：键盘钩子_启动 至 键盘钩子_停止；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
