包 剪贴板模块完整演示
使用 Win32窗口基础模块
使用 剪贴板模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "剪贴板模块：4 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.system.clipboard")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–4：剪贴板_置文本 至 剪贴板_清空；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：剪贴板_置文本
            // [1] 剪贴板_置文本(文本)
            // 功能：把 Unicode 文本写入系统剪贴板。
            // 参数：文本:wideString；返回：bool；可见性：default
            剪贴板_置文本("演示参数1")
            // 命令ID：剪贴板_取文本
            // [2] 剪贴板_取文本()
            // 功能：读取系统剪贴板中的 Unicode 文本。
            // 参数：无；返回：wideString；可见性：default
            剪贴板_取文本()
            // 命令ID：剪贴板_是否有文本
            // [3] 剪贴板_是否有文本()
            // 功能：判断剪贴板是否包含 Unicode 文本。
            // 参数：无；返回：bool；可见性：default
            剪贴板_是否有文本()
            // 命令ID：剪贴板_清空
            // [4] 剪贴板_清空()
            // 功能：清空系统剪贴板。
            // 参数：无；返回：bool；可见性：default
            剪贴板_清空()
        如果结束
        调试输出("已查看 命令 1–4：剪贴板_置文本 至 剪贴板_清空；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
