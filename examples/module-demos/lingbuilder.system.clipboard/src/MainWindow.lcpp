包 剪贴板模块完整演示
使用 Win32窗口基础模块
使用 剪贴板模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项(命令分组选项卡, 0)
        控件_设置文本(演示状态, "剪贴板模块：10 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.system.clipboard")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本(演示状态, "命令 1–10：剪贴板_置文本 至 剪贴板_清空；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选(允许实际执行))
            // 命令ID：剪贴板_置文本
            // [1] 剪贴板_置文本(文本)
            // 功能：把 Unicode 文本写入系统剪贴板。
            // 参数：文本:wideString；返回：bool；可见性：default
            剪贴板_置文本("演示参数1")
            // 命令ID：剪贴板_取文本
            // [2] 剪贴板_取文本()
            // 功能：读取系统剪贴板中的 Unicode 文本。
            // 参数：无；返回：wideString；可见性：default
            剪贴板_取文本()
            // 命令ID：剪贴板_是否有文本
            // [3] 剪贴板_是否有文本()
            // 功能：判断剪贴板是否包含 Unicode 文本。
            // 参数：无；返回：bool；可见性：default
            剪贴板_是否有文本()
            // 命令ID：剪贴板_置图片字节集
            // [4] 剪贴板_置图片字节集(图片数据)
            // 功能：自动识别 GIF87a/GIF89a、Windows DIB/DIBV5 或带 BMP 文件头的图片字节集；GIF 会保留全部动画帧。
            // 参数：图片数据:bytes；返回：bool；可见性：default
            剪贴板_置图片字节集({})
            // 命令ID：剪贴板_取图片字节集
            // [5] 剪贴板_取图片字节集()
            // 功能：优先读取原始 GIF 动图字节集，否则读取 DIB/DIBV5 图片字节集；没有可读图片时返回空字节集。
            // 参数：无；返回：bytes；可见性：default
            剪贴板_取图片字节集()
            // 命令ID：剪贴板_是否有图片
            // [6] 剪贴板_是否有图片()
            // 功能：判断系统剪贴板是否包含 GIF、DIB、DIBV5 或位图图片。
            // 参数：无；返回：bool；可见性：default
            剪贴板_是否有图片()
            // 命令ID：剪贴板_取图片格式
            // [7] 剪贴板_取图片格式()
            // 功能：返回当前图片格式名：GIF、CF_DIBV5、CF_DIB 或 CF_BITMAP；没有图片时返回空文本。
            // 参数：无；返回：wideString；可见性：default
            剪贴板_取图片格式()
            // 命令ID：剪贴板_置GIF字节集
            // [8] 剪贴板_置GIF字节集(GIF数据)
            // 功能：把原始 GIF87a/GIF89a 字节集以 GIF、image/gif 和 HTML Format 格式写入剪贴板，保留动图帧。
            // 参数：GIF数据:bytes；返回：bool；可见性：default
            剪贴板_置GIF字节集({})
            // 命令ID：剪贴板_取GIF字节集
            // [9] 剪贴板_取GIF字节集()
            // 功能：读取剪贴板 GIF 注册格式中的原始 GIF 字节集；没有 GIF 时返回空字节集。
            // 参数：无；返回：bytes；可见性：default
            剪贴板_取GIF字节集()
            // 命令ID：剪贴板_清空
            // [10] 剪贴板_清空()
            // 功能：清空系统剪贴板。
            // 参数：无；返回：bool；可见性：default
            剪贴板_清空()
        如果结束
        调试输出("已查看 命令 1–10：剪贴板_置文本 至 剪贴板_清空；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
