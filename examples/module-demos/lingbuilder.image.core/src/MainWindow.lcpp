包 图像基础模块完整演示
使用 Win32窗口基础模块
使用 图像基础模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "图像基础模块：7 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.image.core")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–7：图像_取宽度 至 图像_取错误；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：图像_取宽度
            // [1] 图像_取宽度(路径)
            // 功能：返回图片像素宽度，失败返回 0。
            // 参数：路径:wideString；返回：int；可见性：default
            图像_取宽度("module-demo-temp.txt")
            // 命令ID：图像_取高度
            // [2] 图像_取高度(路径)
            // 功能：返回图片像素高度，失败返回 0。
            // 参数：路径:wideString；返回：int；可见性：default
            图像_取高度("module-demo-temp.txt")
            // 命令ID：图像_转换格式
            // [3] 图像_转换格式(来源, 目标, 格式)
            // 功能：转换为 png、jpg、bmp、gif 或 tif。
            // 参数：来源:wideString，目标:wideString，格式:wideString；返回：bool；可见性：default
            图像_转换格式("演示参数1", "演示参数2", "演示参数3")
            // 命令ID：图像_缩放
            // [4] 图像_缩放(来源, 目标, 宽度, 高度)
            // 功能：高质量缩放并按目标扩展名保存。
            // 参数：来源:wideString，目标:wideString，宽度:int，高度:int；返回：bool；可见性：default
            图像_缩放("演示参数1", "演示参数2", 1, 1)
            // 命令ID：图像_裁剪
            // [5] 图像_裁剪(来源, 目标, 横坐标, 纵坐标, 宽度, 高度)
            // 功能：裁剪图像区域。
            // 参数：来源:wideString，目标:wideString，横坐标:int，纵坐标:int，宽度:int，高度:int；返回：bool；可见性：default
            图像_裁剪("演示参数1", "演示参数2", 1, 1, 1, 1)
            // 命令ID：图像_旋转90度
            // [6] 图像_旋转90度(来源, 目标, 顺时针)
            // 功能：顺时针或逆时针旋转 90 度。
            // 参数：来源:wideString，目标:wideString，顺时针:bool；返回：bool；可见性：default
            图像_旋转90度("演示参数1", "演示参数2", 真)
            // 命令ID：图像_取错误
            // [7] 图像_取错误()
            // 功能：返回最近图像错误。
            // 参数：无；返回：wideString；可见性：default
            图像_取错误()
        如果结束
        调试输出("已查看 命令 1–7：图像_取宽度 至 图像_取错误；勾选允许实际执行后会使用演示参数调用。")
    结束

结束类
