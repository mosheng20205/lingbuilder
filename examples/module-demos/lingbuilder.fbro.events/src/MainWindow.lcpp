包 FBro事件模块完整演示
使用 Win32窗口基础模块
使用 FBro事件模块

// 本项目由模块清单自动生成；每条 binding 命令都在下方具有真实调用语句。
// 为避免误删文件、启动进程、访问网络或操作设备，必须先勾选“允许实际执行”才会进入命令调用区。
类 MainWindow : 公开 窗口
    事件 _MainWindow_创建完毕()
        控件_设置选择项("命令分组选项卡", 0)
        控件_设置文本("演示状态", "FBro事件模块：6 条命令，默认处于安全预览模式。")
        调试输出("已加载模块演示：lingbuilder.fbro.events")
    结束

    事件 _运行第01组_被单击()
        控件_设置文本("演示状态", "命令 1–6：FBro_取事件数据 至 FBro_绑定事件；源码内含完整签名、参数、返回值与调用。")
        如果 (控件_取勾选("允许实际执行"))
            // 命令ID：FBro_取事件数据
            // [1] FBro_取事件数据(控件名)
            // 功能：取得当前处理中的事件主数据。
            // 参数：控件名:wideString；返回：wideString；可见性：default
            FBro_取事件数据("演示输出")
            // 命令ID：FBro_取事件字段
            // [2] FBro_取事件字段(控件名, 字段名)
            // 功能：从 UTF-16 JSON 事件包读取结构化字段。
            // 参数：控件名:wideString，字段名:wideString；返回：wideString；可见性：default
            FBro_取事件字段("演示输出", "演示参数2")
            // 命令ID：FBro_设置事件结果
            // [3] FBro_设置事件结果(控件名, 动作)
            // 功能：设置同步事件动作；未设置时使用事件目录默认动作。
            // 参数：控件名:wideString，动作:int；返回：int；可见性：default
            FBro_设置事件结果("演示输出", 1)
            // 命令ID：FBro_设置事件返回文本
            // [4] FBro_设置事件返回文本(控件名, 文本)
            // 功能：设置同步事件返回文本。
            // 参数：控件名:wideString，文本:wideString；返回：int；可见性：default
            FBro_设置事件返回文本("演示输出", "演示参数2")
            // 命令ID：FBro_取事件对象
            // [5] FBro_取事件对象(控件名)
            // 功能：取得 CertificateError 或 DragEnter 事件携带的受管对象句柄。
            // 参数：控件名:wideString；返回：longLong；可见性：advanced
            FBro_取事件对象("演示输出")
            // 命令ID：FBro_绑定事件
            // [6] FBro_绑定事件(控件名, 事件名, 处理器)
            // 功能：动态绑定 FBro 事件处理器。
            // 参数：控件名:wideString，事件名:wideString，处理器:handler；返回：int；可见性：default
            FBro_绑定事件("演示输出", "演示事件", &模块演示回调)
        如果结束
        调试输出("已查看 命令 1–6：FBro_取事件数据 至 FBro_绑定事件；勾选允许实际执行后会使用演示参数调用。")
    结束

    事件 模块演示回调()
        调试输出("模块演示回调已触发。")
    结束

结束类
