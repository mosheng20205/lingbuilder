包 FBro多浏览器示例
使用 Win32窗口基础模块
使用 FBro指纹浏览器模块
使用 FBro事件模块

类 FBro多浏览器窗体 : 窗口
  事件 _FBro多浏览器窗体_创建完毕()
    FBro_绑定事件(浏览器1, "加载状态改变", &浏览器1_加载状态改变)
    FBro_绑定事件(浏览器2, "加载状态改变", &浏览器2_加载状态改变)
    FBro_绑定事件(浏览器3, "加载状态改变", &浏览器3_加载状态改变)
    FBro_导航(浏览器1, "https://example.com")
    FBro_导航(浏览器2, "https://www.baidu.com")
    FBro_导航(浏览器3, "https://www.bing.com")
    调试输出("三个 FBro 浏览器已开始导航")
  结束

  事件 浏览器1_加载状态改变()
    如果 (FBro_是否加载中(浏览器1) == 0)
      调试输出("浏览器1 标题：", FBro_取标题(浏览器1), " 地址：", FBro_取地址(浏览器1))
    如果结束
  结束

  事件 浏览器2_加载状态改变()
    如果 (FBro_是否加载中(浏览器2) == 0)
      调试输出("浏览器2 标题：", FBro_取标题(浏览器2), " 地址：", FBro_取地址(浏览器2))
    如果结束
  结束

  事件 浏览器3_加载状态改变()
    如果 (FBro_是否加载中(浏览器3) == 0)
      调试输出("浏览器3 标题：", FBro_取标题(浏览器3), " 地址：", FBro_取地址(浏览器3))
    如果结束
  结束
结束类
