包 上传拖拽测试
使用 Win32窗口基础模块
使用 Win32高级控件模块

类 上传拖拽测试窗体 : 窗口
公开
  构造()
    控件_设置文本(状态栏1, "就绪")
  结束

  事件 普通上传_文件已选择()
    调试输出("普通上传组件：文件选择回调已触发")
    调试输出(上传_取文件("普通上传", 0))
    控件_设置图片(预览图片框, 上传_取文件("普通上传", 0))
    控件_设置文本(状态栏1, "普通上传：已选择文件")
  结束

  事件 普通上传_上传操作()
    调试输出("普通上传组件：上传操作回调已触发")
    调试输出("普通上传组件：由原生 Win32 上传事件执行")
    控件_设置文本(状态栏1, "普通上传：上传操作已触发")
  结束

  事件 拖拽上传_文件已选择()
    调试输出("拖拽上传组件：文件拖入或选择回调已触发")
    调试输出(上传_取文件("拖拽上传", 0))
    控件_设置图片(预览图片框, 上传_取文件("拖拽上传", 0))
    控件_设置文本(状态栏1, "拖拽上传：已选择文件")
  结束

  事件 拖拽上传_上传操作()
    调试输出("拖拽上传组件：上传操作回调已触发")
    调试输出("拖拽上传组件：由原生 Win32 上传事件执行")
    控件_设置文本(状态栏1, "拖拽上传：上传操作已触发")
  结束

  事件 选择文件按钮_被单击()
    调试输出("普通上传：点击按钮触发文件选择")
    上传_打开("普通上传")
  结束

  事件 浏览文件按钮_被单击()
    调试输出("拖拽上传：点击按钮触发文件选择")
    上传_打开("拖拽上传")
  结束
结束类
