包 CEF3浏览器简单示例
使用 Win32窗口基础模块
使用 CEF3浏览器模块

类 CEF3浏览器简单窗体 : 窗口
公开
  构造()
  结束

  事件 _CEF3浏览器简单窗体_创建完毕()
    CEF3_绑定事件(浏览器1, "加载完成", &浏览器1_加载完成)
    CEF3_绑定事件(浏览器1, "加载状态改变", &浏览器1_加载状态改变)
    // CEF3_导航(浏览器1, "https://example.com")
    // 调试输出("CEF3 浏览器已开始导航")
  结束

  事件 浏览器1_加载完成()
    调试输出("CEF3 页面加载完成：", CEF3_取地址(浏览器1))
  结束

  事件 浏览器1_加载状态改变()
    如果 (CEF3_是否加载中(浏览器1) == 0)
      调试输出("CEF3 页面标题：", CEF3_执行JS(浏览器1, "document.title"))
      调试输出("CEF3 当前地址：", CEF3_取地址(浏览器1))
    如果结束
  结束
结束类
