# 灵码 LingBuilder：为中文开发者打造的 C++ 集成开发环境

大家好，向大家介绍一款正在持续开发中的中文编程工具——**灵码 LingBuilder**。

灵码是一款面向中文用户的 C++ / `.lcpp` 集成开发环境，**既支持直接编写和管理原生 C++ 代码，也支持把中文代码确定性生成真实 C++**。它希望降低桌面软件开发和 C++ 工程配置的入门门槛，让用户可以使用中文界面、中文命令、可视化窗口设计器、Git 源代码管理和 AI 辅助完成项目开发，同时得到真实、可阅读、可复制的 C++ 源码及 Visual Studio 工程。

它并不是只能在软件内部运行的解释器，也不是套着编辑器外壳的演示程序。灵码的核心方向，是把中文源码和设计器模型通过本地确定性规则转换为 C++，再使用标准工具链编译成真正的 Windows 可执行程序。

## 主要功能

### 1. 全中文 IDE 工作台

- 中文菜单、命令、设置、诊断、提示和任务输出。
- 类似现代专业 IDE 的工作台布局，包含解决方案资源管理器、编辑器、多标签页、活动栏、底部面板和状态栏。
- 支持命令面板、可配置快捷键、深色/浅色主题和用户/工作区两级设置。
- 使用本地 Monaco Editor，支持行号、缩进、查找替换、撤销重做、代码高亮、诊断标记和 Diff 对比。
- 支持整个工作区的文本或正则搜索与替换，替换前可预览，误操作可撤销。
- 支持 UTF-8、UTF-8 BOM、UTF-16 和 LF/CRLF，尽量减少中文源码与资源文件的乱码问题。

### 2. 中文 `.lcpp` 编程并生成真实 C++

- 使用中文事件、中文命令和接近自然语言的代码编写 Windows 程序。
- 支持窗口事件、控件事件、信息框、调试输出、程序结束等基础能力，并持续扩充语法和模块命令。
- 中文源码通过本地规则确定性生成 C++，核心生成过程不依赖 AI，也可以离线使用。
- 可导出可阅读、可迁移的 C++ 源码、`.sln`、`.vcxproj` 和工程筛选文件，方便在 Visual Studio 中继续查看、编译和二次开发。
- IDE 内 F5 运行与导出工程共用同一套生成规则，尽量保证两边行为一致。

### 3. 原生 C++ 开发支持

灵码并不局限于中文 `.lcpp`。熟悉 C++ 的开发者也可以直接使用原生 C++，中文编程和原生编程可以在同一个解决方案中协作。

- 可直接创建、打开、编辑和管理 `.cpp`、`.h`、`.rc`、`.ini` 等原生工程文件。
- 提供本地 Monaco 代码编辑器、C++ 基础语法高亮、多文件标签、工作区搜索、Diff 对比和编码管理。
- 可检测 MSVC、Windows SDK、CMake、g++ 和 clang++ 等本机开发环境，并用中文显示检测结果和修复建议。
- 支持 CMake/CTest 测试发现、C++ 调试配置、编译诊断、静态分析报告以及 ASan/UBSan 报告导入。
- 支持 vcpkg、Conan 2 和 NuGet 依赖清单，能够检查版本冲突并执行受控的在线恢复或离线缓存恢复。
- 可以把已有 C++ 源码、头文件、静态库和 DLL 封装成 `.lbmod` 模块，再通过中文命令提供给其他项目调用。
- 中文项目导出的 C++ 不是隐藏的中间文件：用户可以复制源码、用 Visual Studio 打开解决方案、继续手工修改和二次开发。

当前已具备 C++ 基础编辑、工程生成、环境检查、依赖、测试和调试链路；更完整的 clangd/LSP 语义补全、重构和跳转能力会在后续版本继续增强。

### 4. 可视化 Win32 窗口设计器

- 通过拖放方式设计窗口和控件，并在属性面板中调整位置、尺寸、文字、颜色和事件。
- 支持按钮、编辑框、标签、复选框、列表、树、选项卡、工具栏、状态栏、图片框、日期控件、RichEdit、菜单、文件对话框、颜色选择器等控件或非可视资源。
- 支持多选、对齐、等宽等高、水平/垂直分布、方向键微调以及撤销/重做。
- 单击事件即可自动创建并跳转到对应的中文代码，不需要手工维护处理器名称。
- 图片会作为项目资源统一管理，导出工程时自动携带，避免只能在开发者电脑上运行。
- 支持生成真正的 Win32 窗口和控件，不使用网页界面冒充最终程序。

### 5. 构建、运行与原生调试

- 支持环境检查，可检测 MSVC、Windows SDK、CMake、g++、clang++、WebView2 等开发组件，并提供中文修复提示。
- 支持 F5 构建运行、Shift+F5 停止，以及编译错误、警告和原始输出查看。
- 集成真实终端，可使用 PowerShell 或 CMD 多会话工作。
- 支持断点、局部变量、监视表达式、调用栈，以及寄存器、内存、反汇编等高级调试能力。
- 支持附加进程、打开 core/minidump 和连接 gdb-remote；部分高级能力需要安装 LLDB/相关调试工具。

### 6. 模块与扩展生态

- 模块采用 `.lbmod` 包格式，可安装、卸载、启用、禁用，并按项目选择所需模块。
- 提供模块开发者中心和 CLI，可创建、检查、打包、查看和迁移原生 C++ 模块。
- 模块可以同时贡献中文命令、代码补全、诊断、C++ 映射、头文件、源码、静态库和运行时 DLL。
- 已有 Win32 基础控件、高级公共控件、EdgeView（WebView2）、多线程和 `new_emoji` 等模块方向。
- 独立 Extension Host 支持命令、菜单、视图、语言和主题等贡献点；扩展默认在受限进程中运行，与原生 C++ 模块分开管理。

### 7. AI 编程助手

- 支持解释代码和编译错误、生成代码、创建项目、提出修改方案及辅助修复问题。
- 提供“系统 AI”和“自定义 API（BYOK）”两种独立模式，用户可以选择平台模型，也可以使用自己的兼容接口。
- AI 修改源码时先生成可审查的 Diff 草稿，经过路径、语法和模块上下文检查后再应用，不会默认静默覆盖整个项目。
- 本地 AI Bridge 支持只读、预览确认和受控自动执行三种权限模式，并限制在当前工作区内。
- AI 不是核心编译依赖；即使没有网络或不使用 AI，编辑、设计、诊断、生成和基础构建能力仍可使用。

### 8. 完整的 Git 源代码管理

灵码内置了面向日常开发的 Git 管理界面，不需要为了常用版本控制操作频繁切换到其他软件。

- 在解决方案资源管理器中直接查看当前分支、已修改文件和暂存状态。
- 支持初始化新仓库；工作区重新获得焦点时自动刷新 Git 状态，新建但尚未首次提交的仓库也能正确显示默认分支。
- 支持暂存、取消暂存和提交，并可填写中文提交说明。
- 修改文件和已暂存文件都可直接查看统一 Diff；未暂存修改可在二次确认后恢复，未跟踪文件会明确提示删除风险。
- 支持创建、切换和删除已经合并的本地分支。
- 支持查看提交历史，以及使用 Blame 查看指定代码行的作者和来源提交。
- 支持远程仓库获取、快进拉取和推送。
- 支持添加、修改和删除远程仓库配置。
- 支持 merge 与 rebase，并可继续或中止正在进行的合并操作。
- 冲突处理界面可同时查看基础版本、当前版本和传入版本，用户可以选择一侧，也可以手工编辑最终合并内容。
- 支持通过 GitHub CLI 创建 Pull Request，明确填写标题、说明、源分支和目标分支。
- Git 操作限制在当前工作区内，避免意外修改工作区之外的文件。
- Git 写操作统一经过工作台命令系统和服务层，界面不会直接拼接或执行任意 shell 命令。

### 9. 项目管理与其他开发工具

- 使用 `.lbsln` 作为灵码解决方案入口，支持多项目组织、最近项目和窗口状态恢复。
- 可识别 vcpkg、Conan 2 和 NuGet 依赖清单，并提供在线恢复或离线缓存。
- 内置测试资源管理器，支持 Node 测试和 CMake/CTest；可导入覆盖率、SARIF、ASan、UBSan 等质量报告。
- 提供 CPU、内存和 I/O 性能采样，并支持本地发布、文件哈希清单和 Windows 签名验证。

## 灵码适合哪些人？

- 希望用中文方式学习编程、桌面软件开发或 C++ 的初学者。
- 熟悉易语言一类中文编程体验，但希望最终得到真实 C++ 工程的用户。
- 想快速制作 Windows 小工具、管理软件、原型程序或 Win32 桌面应用的开发者。
- 希望把现有 C++ 库封装成中文模块，提供给其他用户简单安装和调用的模块作者。
- 需要中文诊断、可视化设计、AI 辅助与传统 C++ 工具链结合的开发者。

## 后续版本更新计划

### 近期：稳定性与基础体验

- 继续修复中文编码、项目持久化、安装版升级和复杂路径下的兼容问题。
- 优化窗口设计器的操作手感、控件属性、事件绑定和原生运行效果一致性。
- 完善构建日志、错误定位、环境安装引导和崩溃恢复。
- 增加更多示例项目、中文模板、入门教程和可直接复用的代码片段。

### 中期：语言服务与模块市场

- 接入更完整的 clangd/LSP 能力，增强 C/C++ 语义补全、跳转定义、重构和实时诊断。
- 继续完善 `.lcpp` 的 AST、格式化、符号导航、重命名和强类型事件参数。
- 扩充 Win32 控件、菜单、资源编辑和 WebView2 能力，提高复杂桌面项目的开发效率。
- 完善模块市场、模块签名、来源信任、版本升级、依赖检查和一键安装体验。
- 为扩展系统补充更完整的树视图、Webview、主题令牌、API 版本协商和性能分析。

### 长期：完整的中文开发生态

- 建立更成熟的中文标准库、模块 SDK、离线模块仓库和第三方开发者生态。
- 增强测试、静态分析、C++ 覆盖率、性能火焰图和发布流水线。
- 继续加强 AI 的工作区理解、中文诊断解释和可审查批量重构，同时坚持本地规则优先和用户确认机制。
- 探索更完善的远程构建、跨设备设置同步和跨平台桌面宿主；首要运行目标仍将以 Windows 原生 C++/Win32 为主。
- 逐步让“中文写代码—可视化设计—生成 C++—编译调试—打包发布”成为一条完整、可靠的开发链路。

## 当前阶段说明

灵码目前仍处于持续开发和测试阶段，功能更新较快，部分高级能力依赖 Visual Studio Build Tools、Windows SDK、LLDB、WebView2 或 GitHub CLI 等外部工具。首发版本将优先保证 Windows 平台、中文 `.lcpp`、Win32 设计器和真实 C++ 生成链路的可用性。

项目不会复制 VS Code 的源码、商标、图标、文案或扩展市场服务，但会参考现代 IDE 的工作台布局、命令系统、服务边界和扩展思路，独立实现适合中文开发者的使用体验。

如果你对中文编程、C++、Win32 桌面开发、模块封装或 AI IDE 感兴趣，欢迎下载体验，也欢迎反馈问题、提出建议或参与模块开发。大家的每一条意见，都会帮助灵码变得更实用。

**软件名称：** 灵码 LingBuilder  
**主要平台：** Windows  
**主要方向：** 中文编程、原生 C++、Win32 可视化开发、Git 管理、模块生态、AI 辅助  
**当前状态：** 持续开发与测试中

## 交流QQ群：

1083244094



![image-20260725161458265](https://img.msblog.cc/img/image-20260725161458265.png)

## 截图

![ff8125cf-bd41-44a0-a280-43e19d03cc14](https://img.msblog.cc/img/ff8125cf-bd41-44a0-a280-43e19d03cc14.png)

![image-20260725161751129](https://img.msblog.cc/img/image-20260725161751129.png)

![image-20260725161919191](https://img.msblog.cc/img/image-20260725161919191.png)

## 视频

https://www.bilibili.com/video/BV1g63M6MEMM



## 用灵码编译的成品

https://mscloak.lanzouv.com/ibK6j3ykuhxi



## 灵码IDE下载

123网盘：https://1855765585.share.123pan.cn/123pan/jgROvd-lPcW

天翼网盘：https://cloud.189.cn/web/share?code=7rEZniR7r2u2（访问码：xi65）

百度网盘：https://pan.baidu.com/s/13ApwWnvg7ypC8RkALtnwqg?pwd=z25w 提取码: z25w

迅雷网盘：https://pan.xunlei.com/s/VOyNJe3jycYK-6GzlUFxXApqA1?pwd=emuk
