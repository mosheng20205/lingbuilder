包 单精度浮点验证
使用 Win32窗口基础模块
使用 文件目录模块

类 主窗口 : 窗口
  事件 _主窗口_创建完毕()
    局部 单精度小数型 输入值 = 1.0
    局部 单精度小数型 结果 = 半值(输入值)
    局部 小数型 双精度结果 = 半值(输入值)
    局部 文件号 报告
    报告 = 文件_打开("float-report.txt", 4, 1)
    文件_写文本行(报告, 格式化文本("单精度结果={}", 结果))
    文件_写文本行(报告, 格式化文本("存到双精度={}", 双精度结果))
    文件_写文本行(报告, "DONE=1")
    文件_关闭(报告)
  结束
结束类
