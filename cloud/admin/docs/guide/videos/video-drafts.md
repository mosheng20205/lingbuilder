按竖版 9:16、成片 65 秒来录。不要一镜到底，分段录制，后期拼接会更稳。

录制前准备：

- 启动：`cd electron; npm run dev`
- 打开默认“空窗口”项目，代码文件是 `src/lingbuilder-project-2/MainWindow.lcpp`
- 先按一次 `F5` 预热，确认 EXE 能打开、MSVC 编译正常。
- 资源管理器提前打开 `generated\cpp\lingbuilder-project-2`，并开启“文件扩展名”显示。
- 关闭通知、隐藏任务栏；代码字号调到约 22–24 px。
- 只用绿色播放按钮或 `F5` 录真实构建。顶部 CPU“生成 C++”按钮目前不执行真实编译，别拍成构建证据。

| 成片时间 | 录什么画面 | 具体操作 | 旁白/字幕 |
|---|---|---|---|
| 0:00–0:03 | 已运行的 `LingBuilderPreview.exe` | 点击一次“增加进度”，让进度条明显变化 | `中文写 C++，F5 真能生成 EXE？` |
| 0:03–0:06 | EXE 保持不动 | 鼠标移开，停 1 秒让观众看清窗口 | `先看结果：这是正在运行的 Windows 程序。` |
| 0:06–0:09 | 切回 LingBuilder | 展示项目树、编辑器和工作台全景 | `这个窗口，代码部分可以直接用中文写。` |
| 0:09–0:13 | `MainWindow.lcpp` | 定位到 `按钮增加_被单击` | 字幕：`中文 .lcpp 事件代码` |
| 0:13–0:17 | 同一段代码 | 鼠标停在 `控件_取数值(进度条1)` | `点击按钮后，先读取进度条当前数值。` |
| 0:17–0:21 | 同一段代码 | 停在 `控件_设置数值(进度条1, 当前进度 + 10)` | `小于一百，就让进度增加十。` |
| 0:21–0:25 | 中文代码全貌 | 不要快速滚动，停住 | `事件名、控件名和逻辑都能用中文表达。` |
| 0:25–0:29 | 窗口设计器 | 切到设计器，完整展示按钮、进度条、编辑框 | 字幕：`可视化窗口设计器` |
| 0:29–0:33 | 设计器属性/事件 | 选中“按钮增加”或“进度条1”，展示属性和事件绑定 | `设计器负责界面，代码负责行为。` |
| 0:33–0:36 | 设计器或代码页 | 按 `F5`，鼠标停在绿色播放按钮附近 | 大字幕：`F5：生成并运行` |
| 0:36–0:40 | 底部输出面板 | 录真实生成日志 | `现在根据中文源码和窗口模型生成 C++。` |
| 0:40–0:44 | 输出面板 | 保留包含 MSVC、编译过程的真实日志 | `随后交给真实的 MSVC 编译。` |
| 0:44–0:47 | 输出面板 | 保留“编译成功”和 EXE 启动路径 | 字幕：`编译成功，已启动 EXE` |
| 0:47–0:51 | 新启动的 EXE | 连点两次“增加进度”，再点一次“减少进度” | `中文事件代码，已经变成可运行的 Windows 程序。` |
| 0:51–0:54 | `generated/cpp/.../main.cpp` | 打开生成的 C++，停在 `#include <windows.h>`、`wWinMain` 等真实代码处 | `生成的是可阅读的 Win32 C++ 源码。` |
| 0:54–0:58 | 同一份 C++ | 停在控件创建或事件分发相关代码 | `不是 IDE 内部模拟，而是实际生成的程序代码。` |
| 0:58–1:02 | 文件资源管理器 | 打开 `generated\cpp\lingbuilder-project-2` | 字幕：`生成结果可复制、可迁移` |
| 1:02–1:05 | 同一目录 | 依次指向 `main.cpp`、`lingbuilder-project-2.sln`、`.vcxproj` | `C++ 源码和 Visual Studio 工程都会自动生成。` |
| 1:05–1:08 | 回到 EXE 或 IDE 全景 | 定格 2 秒 | `你觉得中文编程只是教学，还是能成为工程入口？` |

实际录屏时，每段前后多留 2 至 3 秒。尤其是 F5 构建，完整录下来，后期只保留“开始生成、MSVC、编译成功、启动 EXE”这几段，不需要假装编译只花 11 秒。

推荐旁白总稿：

> 中文写 C++，F5 真能生成 EXE？这里的按钮逻辑来自中文 `.lcpp`。设计器负责窗口和控件，代码负责事件。按下 F5 后，LingBuilder 生成 Win32 C++，再由 MSVC 编译并启动 EXE。生成目录里还有真实的 `main.cpp`、Visual Studio 解决方案和工程文件。你觉得中文编程只能教学，还是可以成为工程入口？

不要说“点击导出 Visual Studio 工程”，当前流程是 F5 后自动生成工程，再从 `generated\cpp\lingbuilder-project-2` 目录展示证据。