# 图像格式转换支持库 2.0 支持库手册

> 来源：[esdn.ijingyi.com 精易论坛·易语言知识库](https://esdn.ijingyi.com/title-9178.html)（《Windows平台支持库手册5.3版》）。易语言支持库文档版权归大连大有吴涛易语言软件开发有限公司所有，本手册仅为本地研发参考整理，未经授权不得对外分发。
> 采集时间：2026-09-15；版本校验：本手册采用目标网址当前版本（2.0）；开源平台未检索到版本更高的同名支持库文档（详见《易语言支持库采集与IDE封装分析汇报.md》）。

## 支持库基本介绍

**图像格式转换支持库2.0版**   [返回首页](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/index.htm)

本支持库用来转换图像格式，目前支持读取的源的格式有：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅），目前支持写入的目标格式有：标准JPEG（全部格式）、BMP（24位）、TIFF（灰度和真彩）、PNG（24位）、PPM（文本和二进制模式）、PGM（文本和二进制模式）、PBM（文本和二进制模式）、PCX（24位）、PICS（24位）、SGI（24位）。

操作系统支持： Windows

**命令类别：**

| 图像转换 |  |  |  |
| --- | --- | --- | --- |

## 命令分类：命令类别（10 条）

### 转换到JPG

- 原文链接：https://esdn.ijingyi.com/title-35328.html
- 操作系统支持：Windows
- 所属类别：图像转换
- 返回值类型：整数型
- 语法：整数型 转换到JPG （ 源文件名 ， 目标文件名 ， 灰度还是真彩 ， 图像品质 ， 是否使用优化编码 ， 是否柔化 ）
- 功能说明：将本库支持读取的图像文件转换为JPEG文件，支持大部分JPEG压缩选项。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到JPEG格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

| 参数名 | 是否必填 | 参数类型 | 参数说明 |
| --- | --- | --- | --- |
| 源文件名 | 必填 | 文本型 | 可以是本支持库支持的任意文件格式，用文件全名(包含路径)。 |
| 目标文件名 | 必填 | 文本型 | 参数值指定转换后保存到的目标文件名。 |
| 灰度还是真彩 | 必填 | 逻辑型，初始值为“假” | 参数值等于真为8位灰度图，等于假为24位真彩图，默认值为假。 |
| 图像品质 | 必填 | 整数型，初始值为“75” | 参数值在 1 到 100 之间，否则为错！默认为 75 。 |
| 是否使用优化编码 | 必填 | 逻辑型，初始值为“真” | 同等质量的JPEG图像文件，优化编码是压缩比最好的，默认值为真。 |
| 是否柔化 | 必填 | 整数型，初始值为“0” | 参数值在 0 到 100 之间，否则为错！0 为无柔化。在压缩比很大的JPEG文件中，适当的柔化可以改善图像质量。默认值为 0 。 |

- 参见：语法的描述规则、基本数据类型

**例程**

![例程截图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.files/image001.gif)

> 注：例程在源站点为截图图片，可点击原文链接查看。

**注意事项/说明**

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“． jpg ”，则通过“转换到 JPG ”命令将 bmp 等图片格式转换为 jpg 格式。 参考： 例程1

**完整正文（站点原文转换）**

**转换到JPG 命令**   操作系统支持：Windows    所属类别：[图像转换](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/eImgConverter/ct0.htm)

将本库支持读取的图像文件转换为JPEG文件，支持大部分JPEG压缩选项。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到JPEG格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

*语法：*  整数型  转换到JPG （源文件名， 目标文件名， 灰度还是真彩， 图像品质， 是否使用优化编码， 是否柔化）

| 参数名 | 描 述 |
| --- | --- |
| 源文件名 | 必需的 ； 文本型。可以是本支持库支持的任意文件格式，用文件全名(包含路径)。 |
| 目标文件名 | 必需的 ； 文本型。参数值指定转换后保存到的目标文件名。 |
| 灰度还是真彩 | 必需的 ； 逻辑型，初始值为“假”。参数值等于真为8位灰度图，等于假为24位真彩图，默认值为假。 |
| 图像品质 | 必需的 ； 整数型，初始值为“75”。参数值在 1 到 100 之间，否则为错！默认为 75 。 |
| 是否使用优化编码 | 必需的 ； 逻辑型，初始值为“真”。同等质量的JPEG图像文件，优化编码是压缩比最好的，默认值为真。 |
| 是否柔化 | 必需的 ； 整数型，初始值为“0”。参数值在 0 到 100 之间，否则为错！0 为无柔化。在压缩比很大的JPEG文件中，适当的柔化可以改善图像质量。默认值为 0 。 |

　

*参见：*[语法的描述规则](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/rule.htm)、[基本数据类型](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/datatype.htm)

例程

![站点例程/配图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.files/image001.gif)

说明

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“．jpg”，则通过“转换到JPG”命令将bmp等图片格式转换为jpg格式。

参考：[例程1](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.e)

---

### 转换到BMP

- 原文链接：https://esdn.ijingyi.com/title-35329.html
- 操作系统支持：Windows
- 所属类别：图像转换
- 返回值类型：整数型
- 语法：整数型 转换到BMP （ 源文件名 ， 目标文件名 ）
- 功能说明：将本库支持读取的图像文件转换为24位的BMP文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到BMP格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

| 参数名 | 是否必填 | 参数类型 | 参数说明 |
| --- | --- | --- | --- |
| 源文件名 | 必填 | 文本型 | 可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必填 | 文本型 | 参数值指定转换后保存到的目标文件名。 |

- 参见：语法的描述规则、基本数据类型

**例程**

![例程截图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd1.files/image001.gif)

> 注：例程在源站点为截图图片，可点击原文链接查看。

**注意事项/说明**

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“． bmp ”，则通过“转换到 BMP ”命令将 jpg 等图片格式转换为 bmp 图片格式。 参考： 例程1

**完整正文（站点原文转换）**

**转换到BMP 命令**   操作系统支持：Windows    所属类别：[图像转换](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/eImgConverter/ct0.htm)

将本库支持读取的图像文件转换为24位的BMP文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到BMP格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

*语法：*  整数型  转换到BMP （源文件名， 目标文件名）

| 参数名 | 描 述 |
| --- | --- |
| 源文件名 | 必需的 ； 文本型。可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必需的 ； 文本型。参数值指定转换后保存到的目标文件名。 |

　

*参见：*[语法的描述规则](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/rule.htm)、[基本数据类型](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/datatype.htm)

例程

![站点例程/配图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd1.files/image001.gif)

说明

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“．bmp”，则通过“转换到BMP”命令将jpg等图片格式转换为bmp图片格式。

参考：[例程1](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.e)

---

### 转换到TIF

- 原文链接：https://esdn.ijingyi.com/title-35330.html
- 操作系统支持：Windows
- 所属类别：图像转换
- 返回值类型：整数型
- 语法：整数型 转换到TIF （ 源文件名 ， 目标文件名 ， 灰度还是真彩 ）
- 功能说明：将本库支持读取的图像文件转换为TIFF文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到TIFF格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

| 参数名 | 是否必填 | 参数类型 | 参数说明 |
| --- | --- | --- | --- |
| 源文件名 | 必填 | 文本型 | 可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必填 | 文本型 | 参数值指定转换后保存到的目标文件名。 |
| 灰度还是真彩 | 必填 | 逻辑型，初始值为“假” | 参数值等于真为8位灰度图，等于假为24位真彩图，默认值为假。 |

- 参见：语法的描述规则、基本数据类型

**例程**

![例程截图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd2.files/image001.gif)

> 注：例程在源站点为截图图片，可点击原文链接查看。

**注意事项/说明**

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“． tif ”，通过“转换到 TIF ”命令，将 bmp 等图片格式转换为 tif 图片格式。 参考： 例程1

**完整正文（站点原文转换）**

**转换到TIF 命令**   操作系统支持：Windows    所属类别：[图像转换](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/eImgConverter/ct0.htm)

将本库支持读取的图像文件转换为TIFF文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到TIFF格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

*语法：*  整数型  转换到TIF （源文件名， 目标文件名， 灰度还是真彩）

| 参数名 | 描 述 |
| --- | --- |
| 源文件名 | 必需的 ； 文本型。可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必需的 ； 文本型。参数值指定转换后保存到的目标文件名。 |
| 灰度还是真彩 | 必需的 ； 逻辑型，初始值为“假”。参数值等于真为8位灰度图，等于假为24位真彩图，默认值为假。 |

　

*参见：*[语法的描述规则](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/rule.htm)、[基本数据类型](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/datatype.htm)

例程

![站点例程/配图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd2.files/image001.gif)

说明

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“．tif”，通过“转换到TIF”命令，将bmp等图片格式转换为tif图片格式。

参考：[例程1](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.e)

---

### 转换到PNG

- 原文链接：https://esdn.ijingyi.com/title-35331.html
- 操作系统支持：Windows
- 所属类别：图像转换
- 返回值类型：整数型
- 语法：整数型 转换到PNG （ 源文件名 ， 目标文件名 ， 目标文件的颜色位数 ）
- 功能说明：将本库支持读取的图像文件转换为24位的PNG文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。目前可以读取如下源格式的图像并转换到PNG格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

| 参数名 | 是否必填 | 参数类型 | 参数说明 |
| --- | --- | --- | --- |
| 源文件名 | 必填 | 文本型 | 可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必填 | 文本型 | 参数值指定转换后保存到的目标文件名。 |
| 目标文件的颜色位数 | 必填 | 整数型，初始值为“24” | 该参数目前只能为数值 24 (目前仅支持 24 位颜色)。 |

- 参见：语法的描述规则、基本数据类型

**例程**

![例程截图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd3.files/image001.gif)

> 注：例程在源站点为截图图片，可点击原文链接查看。

**注意事项/说明**

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“． png ”，则通过“转换到 PNG ”命令将 bmp 等图片格式转换为 png 图片格式。 参考： 例程1

**完整正文（站点原文转换）**

**转换到PNG 命令**   操作系统支持：Windows    所属类别：[图像转换](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/eImgConverter/ct0.htm)

将本库支持读取的图像文件转换为24位的PNG文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。目前可以读取如下源格式的图像并转换到PNG格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

*语法：*  整数型  转换到PNG （源文件名， 目标文件名， 目标文件的颜色位数）

| 参数名 | 描 述 |
| --- | --- |
| 源文件名 | 必需的 ； 文本型。可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必需的 ； 文本型。参数值指定转换后保存到的目标文件名。 |
| 目标文件的颜色位数 | 必需的 ； 整数型，初始值为“24”。该参数目前只能为数值 24 (目前仅支持 24 位颜色)。 |

　

*参见：*[语法的描述规则](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/rule.htm)、[基本数据类型](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/datatype.htm)

例程

![站点例程/配图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd3.files/image001.gif)

说明

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“．png”，则通过“转换到PNG”命令将bmp等图片格式转换为png图片格式。

参考：[例程1](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.e)

---

### 转换到PPM

- 原文链接：https://esdn.ijingyi.com/title-35332.html
- 操作系统支持：Windows
- 所属类别：图像转换
- 返回值类型：整数型
- 语法：整数型 转换到PPM （ 源文件名 ， 目标文件名 ， 保存模式 ）
- 功能说明：将本库支持读取的图像文件转换为PPM文件，目前支持文本模式和二进制模式。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到PPM格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

| 参数名 | 是否必填 | 参数类型 | 参数说明 |
| --- | --- | --- | --- |
| 源文件名 | 必填 | 文本型 | 可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必填 | 文本型 | 参数值指定转换后保存到的目标文件名。 |
| 保存模式 | 必填 | 逻辑型，初始值为“真” | 参数值为真则保存为二进制模式，为假则保存为文本模式，默认参数值为真。 |

- 参见：语法的描述规则、基本数据类型

**例程**

![例程截图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd4.files/image002.gif)

> 注：例程在源站点为截图图片，可点击原文链接查看。

**注意事项/说明**

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“． ppm ”，则通过“转换到 PPM ”命令将 bmp 等图片格式转换为 ppm 图片格式。 参考： 例程1

**完整正文（站点原文转换）**

**转换到PPM 命令**   操作系统支持：Windows    所属类别：[图像转换](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/eImgConverter/ct0.htm)

将本库支持读取的图像文件转换为PPM文件，目前支持文本模式和二进制模式。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到PPM格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

*语法：*  整数型  转换到PPM （源文件名， 目标文件名， 保存模式）

| 参数名 | 描 述 |
| --- | --- |
| 源文件名 | 必需的 ； 文本型。可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必需的 ； 文本型。参数值指定转换后保存到的目标文件名。 |
| 保存模式 | 必需的 ； 逻辑型，初始值为“真”。参数值为真则保存为二进制模式，为假则保存为文本模式，默认参数值为真。 |

　

*参见：*[语法的描述规则](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/rule.htm)、[基本数据类型](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/datatype.htm)

例程

![站点例程/配图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd4.files/image002.gif)

说明

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“．ppm”，则通过“转换到PPM”命令将bmp等图片格式转换为ppm图片格式。

参考：[例程1](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.e)

---

### 转换到PGM

- 原文链接：https://esdn.ijingyi.com/title-35333.html
- 操作系统支持：Windows
- 所属类别：图像转换
- 返回值类型：整数型
- 语法：整数型 转换到PGM （ 源文件名 ， 目标文件名 ， 保存模式 ）
- 功能说明：将本库支持读取的图像文件转换为PGM文件，目前支持文本模式和二进制模式。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误 目前可以读取如下源格式的图像并转换到PGM格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

| 参数名 | 是否必填 | 参数类型 | 参数说明 |
| --- | --- | --- | --- |
| 源文件名 | 必填 | 文本型 | 可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必填 | 文本型 | 参数值指定转换后保存到的目标文件名。 |
| 保存模式 | 必填 | 逻辑型，初始值为“真” | 参数值为真则保存为二进制模式，为假则保存为文本模式，默认参数值为真。 |

- 参见：语法的描述规则、基本数据类型

**例程**

![例程截图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd5.files/image001.gif)

> 注：例程在源站点为截图图片，可点击原文链接查看。

**注意事项/说明**

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“． pgm ”，则通过“转换到 PGM ”命令将 bmp 等图片格式转换为 pgm 格式。 参考： 例程1

**完整正文（站点原文转换）**

**转换到PGM 命令**   操作系统支持：Windows    所属类别：[图像转换](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/eImgConverter/ct0.htm)

将本库支持读取的图像文件转换为PGM文件，目前支持文本模式和二进制模式。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误 目前可以读取如下源格式的图像并转换到PGM格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

*语法：*  整数型  转换到PGM （源文件名， 目标文件名， 保存模式）

| 参数名 | 描 述 |
| --- | --- |
| 源文件名 | 必需的 ； 文本型。可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必需的 ； 文本型。参数值指定转换后保存到的目标文件名。 |
| 保存模式 | 必需的 ； 逻辑型，初始值为“真”。参数值为真则保存为二进制模式，为假则保存为文本模式，默认参数值为真。 |

　

*参见：*[语法的描述规则](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/rule.htm)、[基本数据类型](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/datatype.htm)

例程

![站点例程/配图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd5.files/image001.gif)

说明

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“．pgm”，则通过“转换到PGM”命令将bmp等图片格式转换为pgm格式。

参考：[例程1](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.e)

---

### 转换到PBM

- 原文链接：https://esdn.ijingyi.com/title-35334.html
- 操作系统支持：Windows
- 所属类别：图像转换
- 返回值类型：整数型
- 语法：整数型 转换到PBM （ 源文件名 ， 目标文件名 ）
- 功能说明：将本库支持读取的图像文件转换为PBM文件，目前支持文本模式。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到PBM格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

| 参数名 | 是否必填 | 参数类型 | 参数说明 |
| --- | --- | --- | --- |
| 源文件名 | 必填 | 文本型 | 可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必填 | 文本型 | 参数值指定转换后保存到的目标文件名。 |

- 参见：语法的描述规则、基本数据类型

**例程**

![例程截图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd6.files/image001.gif)

> 注：例程在源站点为截图图片，可点击原文链接查看。

**注意事项/说明**

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“． pbm ”，则通过“转换为 PBM ”命令，将 bmp 等图片格式转换为 pbm 格式。 参考： 例程1

**完整正文（站点原文转换）**

**转换到PBM 命令**   操作系统支持：Windows    所属类别：[图像转换](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/eImgConverter/ct0.htm)

将本库支持读取的图像文件转换为PBM文件，目前支持文本模式。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到PBM格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

*语法：*  整数型  转换到PBM （源文件名， 目标文件名）

| 参数名 | 描 述 |
| --- | --- |
| 源文件名 | 必需的 ； 文本型。可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必需的 ； 文本型。参数值指定转换后保存到的目标文件名。 |

　

*参见：*[语法的描述规则](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/rule.htm)、[基本数据类型](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/datatype.htm)

例程

![站点例程/配图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd6.files/image001.gif)

说明

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“．pbm”，则通过“转换为PBM”命令，将bmp等图片格式转换为pbm格式。

参考：[例程1](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.e)

---

### 转换到PCX

- 原文链接：https://esdn.ijingyi.com/title-35335.html
- 操作系统支持：Windows
- 所属类别：图像转换
- 返回值类型：整数型
- 语法：整数型 转换到PCX （ 源文件名 ， 目标文件名 ）
- 功能说明：将本库支持读取的图像文件转换为24位的PCX文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误 目前可以读取如下源格式的图像并转换到PCX格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

| 参数名 | 是否必填 | 参数类型 | 参数说明 |
| --- | --- | --- | --- |
| 源文件名 | 必填 | 文本型 | 可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必填 | 文本型 | 参数值指定转换后保存到的目标文件名。 |

- 参见：语法的描述规则、基本数据类型

**例程**

![例程截图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd7.files/image002.gif)

> 注：例程在源站点为截图图片，可点击原文链接查看。

**注意事项/说明**

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“． pcx ”，则通过“转换为 PCX ”命令，将 bmp 等图片格式转换为 pcx 格式。 参考： 例程1

**完整正文（站点原文转换）**

**转换到PCX 命令**   操作系统支持：Windows    所属类别：[图像转换](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/eImgConverter/ct0.htm)

将本库支持读取的图像文件转换为24位的PCX文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误 目前可以读取如下源格式的图像并转换到PCX格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

*语法：*  整数型  转换到PCX （源文件名， 目标文件名）

| 参数名 | 描 述 |
| --- | --- |
| 源文件名 | 必需的 ； 文本型。可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必需的 ； 文本型。参数值指定转换后保存到的目标文件名。 |

　

*参见：*[语法的描述规则](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/rule.htm)、[基本数据类型](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/datatype.htm)

例程

![站点例程/配图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd7.files/image002.gif)

说明

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“．pcx”，则通过“转换为PCX”命令，将bmp等图片格式转换为pcx格式。

参考：[例程1](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.e)

---

### 转换到PIC

- 原文链接：https://esdn.ijingyi.com/title-35336.html
- 操作系统支持：Windows
- 所属类别：图像转换
- 返回值类型：整数型
- 语法：整数型 转换到PIC （ 源文件名 ， 目标文件名 ）
- 功能说明：将本库支持读取的图像文件转换为24位的PICS文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到PICS格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

| 参数名 | 是否必填 | 参数类型 | 参数说明 |
| --- | --- | --- | --- |
| 源文件名 | 必填 | 文本型 | 可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必填 | 文本型 | 参数值指定转换后保存到的目标文件名。 |

- 参见：语法的描述规则、基本数据类型

**例程**

![例程截图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd8.files/image001.gif)

> 注：例程在源站点为截图图片，可点击原文链接查看。

**注意事项/说明**

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“． pics ”，则通过“转换为 PICS ”命令，将 bmp 等图片格式转换为 pics 格式。 参考： 例程1

**完整正文（站点原文转换）**

**转换到PIC 命令**   操作系统支持：Windows    所属类别：[图像转换](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/eImgConverter/ct0.htm)

将本库支持读取的图像文件转换为24位的PICS文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。 目前可以读取如下源格式的图像并转换到PICS格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

*语法：*  整数型  转换到PIC （源文件名， 目标文件名）

| 参数名 | 描 述 |
| --- | --- |
| 源文件名 | 必需的 ； 文本型。可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必需的 ； 文本型。参数值指定转换后保存到的目标文件名。 |

　

*参见：*[语法的描述规则](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/rule.htm)、[基本数据类型](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/datatype.htm)

例程

![站点例程/配图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd8.files/image001.gif)

说明

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“．pics”，则通过“转换为PICS”命令，将bmp等图片格式转换为pics格式。

参考：[例程1](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.e)

---

### 转换到SGI

- 原文链接：https://esdn.ijingyi.com/title-35337.html
- 操作系统支持：Windows
- 所属类别：图像转换
- 返回值类型：整数型
- 语法：整数型 转换到SGI （ 源文件名 ， 目标文件名 ）
- 功能说明：将本库支持读取的图像文件转换为24位的SGI文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。目前可以读取如下源格式的图像并转换到SGI格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

| 参数名 | 是否必填 | 参数类型 | 参数说明 |
| --- | --- | --- | --- |
| 源文件名 | 必填 | 文本型 | 可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必填 | 文本型 | 参数值指定转换后保存到的目标文件名。 |

- 参见：语法的描述规则、基本数据类型

**例程**

![例程截图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd9.files/image001.gif)

> 注：例程在源站点为截图图片，可点击原文链接查看。

**注意事项/说明**

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“． sgi ”，则通过“转换到 SGI ”命令，将 bmp 等图片格式转换为 sgi 格式。 参考： 例程1

**完整正文（站点原文转换）**

**转换到SGI 命令**   操作系统支持：Windows    所属类别：[图像转换](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/eImgConverter/ct0.htm)

将本库支持读取的图像文件转换为24位的SGI文件。成功返回 1 ，失败返回一个小于等于零的数值，具体为： 0: 未知错误； -1: 文件不存在； -2: 系统资源不足； -3: 该文件格式不被支持； -4: 编码时出错； -6: 编码时系统资源不足； -7: 编码参数错误。目前可以读取如下源格式的图像并转换到SGI格式：标准JPEG（全部格式）、BMP（目前仅支持24位BMP）、TIFF（单页图像所有格式）、PNG（32位及32位以下）、PCX（8位和24位，RLE压缩）、PCD（取第三幅）。

*语法：*  整数型  转换到SGI （源文件名， 目标文件名）

| 参数名 | 描 述 |
| --- | --- |
| 源文件名 | 必需的 ； 文本型。可以是本支持库支持的任意文件格式，请提供文件全名(包含路径)。 |
| 目标文件名 | 必需的 ； 文本型。参数值指定转换后保存到的目标文件名。 |

　

*参见：*[语法的描述规则](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/rule.htm)、[基本数据类型](https://esdn.ijingyi.com/source/plugin/eknow/resource5.3/datatype.htm)

例程

![站点例程/配图](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd9.files/image001.gif)

说明

使用文本操作命令将欲保存文件的扩展名取出，如果扩展名为“．sgi”，则通过“转换到SGI”命令，将bmp等图片格式转换为sgi格式。

参考：[例程1](https://esdn.ijingyi.com/source/plugin/eknow/resource/eimg//eImgConverter/cmd0.e)

---
